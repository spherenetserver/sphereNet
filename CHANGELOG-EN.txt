================================================================================
 SphereNet Changelog (English)
 All notable changes are documented in this file.
 Turkish: CHANGELOG-TR.txt  |  Archive (pre 2026-05-29, bilingual): CHANGELOG_OLD.txt
================================================================================

[2026-09-12] - A per-spell coverage matrix instead of a blanket label
----------------------------------------

DOC - Which spells actually work is now measured, not asserted. The status report
        carried one line - "Bushido/Ninjitsu/Mysticism/Spellweaving enum-only" -
        that the measurement contradicts four ways: Spellweaving is 14 of 16
        castable and Mysticism 11 of 16, Bard Masteries are not named at all but
        are entirely dead (0 of 6), and Chivalry was counted as working while two
        of its spells are refused. The two halves that were right: Bushido (0 of
        6) and Ninjitsu (0 of 8) really are dead.

        docs/BUYU_MATRISI_TR.md holds the per-school counts and names all 30
        refused spells. The live pack defines 168 spells; 138 are castable.

        Skill Masteries are listed separately: the enum carries 39 of them and
        the pack defines none, so they are not a gap but an unreachable id space.

        The classification is the engine's own (IsInertSchoolSpell), so the
        document describes the code rather than an opinion about it, and a
        guardrail test pins the counts against the live pack so the two cannot
        drift apart. It skips cleanly where that pack is absent.


[2026-09-12] - The chat window's privacy buttons do something now
----------------------------------------

NEW - Private messages can be switched off, and /whois can be refused. Source-X
        keeps both switches on a chat member and both default ON
        (CChatChanMember.cpp:8): one gates incoming private messages
        (CChatChannel.cpp:110), the other decides whether /whois gives your
        character name away (CChatChannel.cpp:53). The client sends seven actions
        for them - +receive, -receive, /receive, +showname, -showname, /showname
        and /whois - and all seven were dropped on the floor, so the buttons did
        nothing at all: every player was permanently reachable and permanently
        identifiable.

        Refusing private messages is not the same as an ignore. An ignore is the
        recipient dropping one sender at delivery; this turns the whole message
        away at the source and tells the sender so, as the reference does.

        Both switches are session state, like the ignore list, and are forgotten
        when the character's chat session ends.


[2026-09-12] - Who a guild stone will take on
----------------------------------------

FIX - A stone refuses an NPC, and refuses anyone already sworn to another stone
        of the same kind. Source-X gates both in CItemStone::AddRecruit
        (CItemStone.cpp:1074-1087): only a player can be a member, and someone who
        already belongs elsewhere must resign that first. There was no gate at all
        here, so a script could enrol an NPC, or put one player in two guilds at
        once - and the APPLYTOJOIN path then stamped them with a second stone
        memory, which is what affiliation and dedup checks read.

        A town and a guild stay independent, as they are in the reference
        (MEMORY_TOWN vs MEMORY_GUILD): a guild member may still be a citizen. Only
        a stone of the SAME kind blocks. A pending candidacy elsewhere does not
        lock anybody out either - the clash is about membership.

        The refusal is silent here. The reference speaks it from the stone, but
        this engine has no item-speech channel and adding one for a single line
        is not worth a new hook; the refusal itself is what matters.


[2026-09-12] - A ship that says why it stopped
----------------------------------------

FIX - The tiller tells the crew why the ship halted. Source-X tracks the reason
        and speaks it (CCMultiMovable.cpp:852-860): the edge of the world is
        "turbulent water", anything else is "something is blocking". Both lines
        already existed here as message keys nothing ever spoke - a ship simply
        went quiet and the crew was left guessing whether they had run aground,
        hit another hull, or reached the end of the map. The map-edge test was
        folded into the obstacle test, so the two reasons could not be told
        apart; they are separate now.

        Unchanged, and checked: a blocked order keeps the tiles it DID cover
        (the reference advances its delta per tile and applies what it got,
        :841), and @Ship_Move still fires only for a run that completed.


[2026-09-12] - A house design commit a script can price and refuse
----------------------------------------

FIX - @HouseDesignCommit fires BEFORE the commit and can refuse it. Source-X
        hands it ARGN1 = the committed design's tile count, ARGN2 = the working
        design's, ARGN3 = the new revision, ARGO = the multi, and
        LOCAL.FIXTURES.OLD / FIXTURES.NEW / MAXZ; RETURN 1 abandons the commit
        (CItemMultiCustom.cpp:314-327).

        This fired AFTER the commit with ARGN1 = the revision and nothing else.
        The reference pack prices a build from those two counts -
        (new - old) * 500 gold - and RETURNs 1 when the owner cannot afford it
        (Scripts-X house_typedefs.scp:603-615). Against that pack every commit
        read a revision as an "old tile count", charged from garbage (usually
        PAYING the owner, since the computed cost came out negative), and its
        refusal did nothing at all: the design committed anyway. A refused commit
        now leaves the working design untouched and the session open, so the
        owner can change it and try again.

FIX - Committing an unchanged design is not a commit. The reference returns
        early rather than bump a revision for nothing (:277), so pressing the
        button twice on the same design does not charge anybody a second time.

        @HouseDesignCommitItem - the per-tile veto - was left unwritten: the
        reference pack carries it only as a commented-out probe line, so it has
        no live consumer.


[2026-09-12] - What a house owes the world when it stops existing
----------------------------------------

FIX - Deleting a house lets go of what it had locked down and secured. A
        locked-down item is not movable here (ObjAttributes.LockedDown fails
        Item.IsMovableType), so one left behind by a deleted house was stuck in
        the world for good, still linked to a multi that no longer existed and
        still carrying its house marker event. The reference lets go of them
        (UnlockAllItems, CItemMulti.cpp:1804); nothing here did, because only the
        REDEED path ever cleared them - a GM .remove, a script REMOVE or any other
        teardown stranded them.

FIX - A deleted house hands its moving crate over instead of leaving it buried.
        The crate lives at house Z - 20, so one left behind sat under a house that
        no longer existed with its contents inside it. It goes to the owner's bank,
        or to the ground when there is no bank to reach.

FIX - An empty moving crate is deleted rather than delivered. The reference will
        not post an empty box to someone's bank (TransferMovingCrateToBank,
        CItemMulti.cpp:1539). Applies to both routes - the teardown above and a
        redeed.

        Measured first: the MOVEALLTOCRATE / MOVELOCKSTOCRATE / MOVECRATETOBANK /
        REDEEDADDONS verbs and the @Redeed ARGN2/ARGN3 read-back have no use in
        either script pack - Scripts-X drives a redeed with the bare REDEED verb
        and does its own crate work in script - so they were left alone and the
        work went to the invariants that the existing paths actually break.


[2026-09-12] - The permission gate a housing pack is built on
----------------------------------------

NEW - ISOWNER and the GET...POS family on a house multi. Scripts-X housing asks
        the multi directly, over and over:
            if (<isowner <src>>) || (<GetCoownerPos <src>> >= 0) || ...
        ISOWNER answers 1/0 and every GET...POS answers the position in that list
        or -1 (Source-X GetCoownerIndex and its siblings, CItemMulti.cpp:811/888/
        962/1037/1648/1818/1887). None of them existed, so every such test in that
        pack read as nothing at all. Added: ISOWNER, GETCOOWNERPOS, GETFRIENDPOS,
        GETACCESSPOS, GETBANPOS, GETLOCKEDITEMPOS, GETSECUREDCONTAINERPOS,
        GETCOMPPOS, GETHOUSEVENDORPOS. A non-member is -1, not 0, because that is
        what the packs test against.

FIX - A locked-down or secured item carries its marker event. Source-X puts
        "EVENTS +ei_house_lockdown" / "+ei_house_secure" on the item
        (CItemMulti.cpp:1779/1856). These are not scripts a pack defines - they
        are how it RECOGNISES such an item: the housing pack clears a house by
        asking each item <isevent.ei_house_lockdown> and then calling unlockitem
        on it (house_functions.scp:297/881/885/927/930). Without the marker
        nothing there could tell a locked item from a loose one, so the whole
        clear-the-house flow silently did nothing. Releasing removes it again.

NEW - GETSECUREDCONTAINERS and GETSECUREDITEMS. The reference keeps them apart
        (CItemMulti.cpp:1904/1919): one counts the secured containers, the other
        counts the items inside them, and the housing dialog spends the storage
        budget with the second.

        Measured in the pack first: ISOWNER 45 sites, GETCOOWNERPOS 32,
        GETFRIENDPOS 29, GETSECUREDITEMS 3, GETACCESSPOS 2, GETBANPOS 2,
        GETSECUREDCONTAINERPOS 1. The rest of the multi key table that is still
        missing (ADDADDON, ADDKEY, ADDVENDOR, REMOVEKEYS, GETADDONPOS) has no use
        in either pack and was left alone.


[2026-09-12] - A house answers its own keys, and keeps its crate
----------------------------------------

FIX - A house multi reads its housing keys BARE. That is where the reference
        keeps them (CItemMulti::r_WriteVal) and how the housing packs read them.
        Only the SETTER took the bare form here, so BASESTORAGE=4688 reached a
        live house and <BASESTORAGE> then read back nothing at all - write-only
        properties. The HOUSE. prefix still works and means the same thing. Keys
        the item and the house share (TYPE, OWNER) still mean the ITEM's: the
        housing lookup runs last, and on anything that is not a house it does not
        answer at all.

NEW - MOVINGCRATE. Source-X keeps a moving crate ON the house, not just during a
        redeed: <MOVINGCRATE> reads the crate uid or 0, <MOVINGCRATE 1> makes one
        first, <MOVINGCRATE.key> reads through to the crate, and the uid is saved
        when the house has one (CItemMulti.cpp:1329/2666/2858). The crate is an
        ITEMID_CRATE1 at the house's own spot but 20 below it, linked back to the
        house. Replacing a crate that holds something moves the contents across
        and deletes the old one, so a swap never strands goods (:1317).

FIX - Redeeding a house uses the crate it already had instead of abandoning it
        and minting a fresh one. A crate holding goods is taken off the house
        whether or not there is anything else to pack, so it is never left buried
        under a house that no longer exists.


[2026-09-11] - What death ends, and who sees you loot
----------------------------------------

FIX - Death ends every spell on you. Source-X CChar::Death runs
        Spell_Dispel(100) before the corpse is made (CCharAct.cpp:4397) -
        "get rid of all spell effects". Nothing did that here, so a player who
        died buffed rose from the dead still buffed and kept it until the timers
        ran out, and a curse outlived the death that ended it. The dispel is
        indiscriminate in the reference and is here too: what helped you and what
        hurt you both end.

FIX - A corpse holds no more than its owner could carry. The reference sets the
        bound and says why: "set corpse maxweight to prevent weird exploits like
        when someone place many items on an player corpse just to make this player
        get stuck on resurrect" (CItemCorpse.cpp:194). The drop path already
        enforces a container's MODMAXWEIGHT - a corpse simply had none, so it was
        an unbounded public container anyone could fill.

FIX - Looting or carving an innocent's corpse is reported to the people watching.
        CheckCorpseCrime does two things when the act is criminal
        (CItemCorpse.cpp:132-133): it runs the witness pass with the corpse's
        owner as the mark - an overt crime, so line of sight is the whole test -
        and only then flags the criminal. Only the flag was raised here: the
        witnesses recorded no SAWCRIME, so the looter did not even show grey to
        them, a guarded town's NPCs never called the guards, and @SeeCrime never
        fired for a script to react to.


[2026-09-11] - A ghost, a berserk summon, and a boat left sailing
----------------------------------------

FIX - A ghost is not stopped by a door. Source-X gives every DEAD char
        CAN_C_GHOST (GetCanMoveFlags, CCharStatus.cpp:739) and that clears
        CAN_I_BLOCK off a door tile (CChar.cpp:760). Nothing here read the dead
        flag as movement geometry, so a player who died in a room whose door had
        swung shut stayed shut in - and this server closes doors on its own after
        20 seconds. Only a DOOR: a wall still needs CAN_C_PASSWALLS, which the
        dead flag does not grant. Door items answer to it by TYPE as well as by
        tile flag.

FIX - A berserk creature never deserts its owner. The reference guards
        NPC_PetDesert itself (CCharNPCPet.cpp:906) and says why: a berserk summon
        counts against CURFOLLOWER, so if attacking one made it desert, a player
        could attack his own to free the slot and summon another at no cost. Both
        desert paths here - the owner's own blow and the starvation tick - went
        through unguarded. The live pack's Energy Vortex (c_vortex) and Blade
        Spirit (c_blade_spirit) are both brain_berserk, the exact pair named.

FIX - A ship stops when the char aboard it logs off (CChar::ClientDetach,
        CChar.cpp:497-502). Nothing did that, so logging off mid-voyage left the
        boat sailing on its own until it hit something.

FIX - How many pets a stablemaster holds. MAXPLAYERPETS is a tag on the
        STABLEMASTER, not on the player: the reference reads its own tags
        (CCharNPCAct_Vendor.cpp:124) and the reference script pack documents it
        that way. Reading it off the owner meant a shard that configured its
        stablemaster got nothing. Without the tag the count is a tier on the
        combined Taming + Animal Lore + Veterinary skills (2 / 3 / 4 / 5), plus
        (skill - 90.0) slots for each of the three at 100.0 or above - not a flat
        base of 5 plus one slot per 60.0. An untrained owner now gets 2. The
        container item limit caps the stable first, as it does in the reference.


[2026-09-11] - What a dismissed vendor gives back
----------------------------------------

FIX - Releasing a player vendor hands its holdings back. The reference empties it
        the moment it loses its owner: the purse and every vendor-layer container's
        contents go into the OWNER'S BANK and the vendor stops being invulnerable
        (NPC_PetClearOwners, CCharNPCPet.cpp:562-584). Releasing one here only
        cleared the ownership flags, so a shopkeeper's takings and everything it
        had bought from players went ownerless with it. With no bank to put them
        in they land at the owner's feet rather than nowhere.

        Only what really exists comes back: this engine's SELL stock is a template
        rebuilt on demand and never persisted, so returning that would mint items
        rather than return them. The extra container - what the vendor bought from
        players - is the real one.


[2026-09-11] - Who the follower cap binds
----------------------------------------

FIX - OF_PETSLOTS switches the follower cap on. The reference keeps the whole
        slot system behind that flag - the summon path, the taming path and the
        pet-memory bookkeeping all ask first (CCharSpell.cpp:2660,
        CChar.cpp:1257). It was in the OptionFlags enum with nothing reading it,
        so the cap bound a shard that had switched the system off, and the live
        one has: its OPTIONFLAGS does not carry the 0x10 bit.

FIX - A GM is not stopped by the maximum (fIgnoreMax, CCharUse.cpp:1238). Both
        the ownership path and the stable claim answer to the same gate now,
        instead of each carrying their own copy of the check.


[2026-09-11] - A spot that answered nothing stays that way
----------------------------------------

FIX - A barren draw is written down instead of thrown away. The reference creates
        the resource node even when the group answered mr_nothing and arms its
        decay from THAT definition's REGEN (CWorldMap.cpp:119/131/148); only then
        does the gather refuse, because the definition reaps nothing
        (CCharSkill.cpp:1012). This engine discarded the barren draw and rolled the
        group again on the next swing, so a fisherman could stand on one tile and
        re-roll until it paid out. The live pack counts on the other behaviour: its
        mr_nothing carries REGEN=60*60*10 - an hour - with the comment "Nothing can
        be found at this location for this many seconds", and weights that draw at
        60% for water.


[2026-09-11] - How a finished piece is announced
----------------------------------------

FIX - A crafted piece says what it turned out like. The reference names the
        quality band as it rolls it and tells the crafter - shoddy, poor, below
        average, above average, excellent, superior, with the average band alone
        staying quiet (DEFMSG_MAKESUCCESS_1..6, CCharSkill.cpp:758-788). All six
        messages were in this engine's table and nothing ever sent one, so a
        shoddy dagger and a superior one read the same.

FIX - OF_NOITEMNAMING switches the maker's mark off. The reference asks for a
        grandmaster, a quality above 175 AND that flag to be clear (:799); the
        flag was in the OptionFlags enum with nothing reading it, so a shard could
        turn the marks away and keep getting them.

FIX - A smelting test no longer decides itself on a dice roll. It was seen red
        once in thirteen suite runs: Mining at 100.0 still draws a bell curve, and
        the produce - not the roll - is what the test is about. It now decides the
        roll the way every other test in that file does.


[2026-09-11] - What a failed craft still costs
----------------------------------------

FIX - ACTIONEFFECT exists. It is the reference's one-shot per-attempt override a
        script writes inside a skill trigger (m_Act_Effect, CChar.cpp:3084) - the
        amount healed, the mana drained, the radius of an area skill, the share of
        a failed craft's resources lost. Nothing answered it here, although the
        live pack prints it in its player-info dialog and writes it back through an
        INPDLG. A negative write normalises to -1, and every skill start and
        cleanup restores that, so one attempt cannot steer the next.

FIX - A failed craft works its cost out the way the reference does
        (Skill_MakeItem SKTRIG_FAIL, CCharSkill.cpp:928-943): ACTIONEFFECT when a
        script set one, otherwise the crafting skill's own EFFECT curve, and only
        with neither a flat 0-49% roll. Only the flat roll existed, so a pack that
        tunes the loss was ignored - the live one gives Inscription EFFECT=50.


[2026-09-11] - Where a spell is refused
----------------------------------------

FIX - The caster's region gets the reference's whole anti-magic question, not two
        bits of it (CheckAntiMagic, CRegion.cpp:720). RECALL_IN refuses Mark and
        Gate Travel, RECALL_OUT refuses Recall, Gate Travel AND Mark, GATE refuses
        Gate Travel, TELEPORT refuses Teleport - and a SHIP region answers the
        recall-in question too, which is how upstream stops a rune being marked on
        the deck of a boat. Mark had no region gate here at all, so it worked
        everywhere. The refusal now says so instead of failing silently.

FIX - MAGICF_SUMMONWALKCHECK does something. A summon has to be able to stand
        where it is called (CCharSpell.cpp:2646); the flag was in the enum and in
        sphere.ini and nothing read it, so a creature could be called into a wall.

FIX - MAGICF_OVERRIDEFIELDS does something. A new field replaces the spell items
        already on the tile rather than stacking on them (CCharSpell.cpp:2295).
        Also declared and never read.


[2026-09-11] - What a spell costs, and walking while you cast it
----------------------------------------

FIX - Walking no longer cancels a spell. The reference gives one of two answers
        (OnFreezeCheck, CCharAct.cpp:4539): the cast ROOTS the caster
        (MAGICF_FREEZEONCAST, or a spell carrying SPELLFLAG_FREEZEONCAST while the
        global flag is off), or the caster walks and keeps casting. It never
        interrupts on a step. This engine cancelled on any movement and enforced
        neither flag, so on a shard with MAGICFLAGS=0 - the live one - a caster
        lost the spell to a single footfall where upstream would have let them walk.
        The rooting case now refuses the STEP, not the spell.

FIX - LOWERMANACOST is read. It is a percent off the mana bill and may be NEGATIVE
        to raise it, which is how the reference expresses a penalty
        (Calc_SpellManaCost, CResourceCalc.cpp:545). Summed off the character and
        everything worn, like the other spell properties.

FIX - LOWERREAGENTCOST is read. It is a percent CHANCE that a cast spends no
        reagents at all rather than a discount on how many (:570), and the roll
        covers the availability check too, so a free cast cannot be refused for
        lacking what it was not going to spend. The reference script pack sets both
        properties on its artifacts; neither did anything here.


[2026-09-11] - Casting with your hands full
----------------------------------------

FIX - With EQUIPPEDCAST off a held weapon goes to the pack and the cast proceeds.
        The reference empties the hands rather than refusing the spell
        (Spell_Unequip, CCharSpell.cpp:2827/3544); this engine fizzled instead, and
        that is the live shard's own setting - its EQUIPPEDCAST is 0, so a player
        holding a weapon simply could not cast. A spellbook, a wand and a shield
        stay where they are; an item that cannot be moved, or has nowhere to go,
        still stops the cast.

FIX - CAN_I_EQUIPONCAST is read. It is how a pack says a held item survives a cast
        with EQUIPPEDCAST off (CBase.h:61), and nothing consulted it.

FIX - MAGICF_CASTPARALYZED does something. A frozen caster was refused outright
        before the flag was ever consulted, which made it inert. The reference has
        no blanket refusal there at all: freezing is weighed while the hands are
        freed, where NOCASTFROZENHANDS and CASTPARALYZED decide.

FIX - The test suite resets the combat and magic knobs it had been leaving behind
        (weapon damage lookup, durability band, combat flags and eras, the reagent
        and equipped-cast switches). A value left by one test could steer a later
        one's swing or cast; it showed up as a failure about once every dozen runs.


[2026-09-10] - What a parry is allowed to decide
----------------------------------------

FIX - @HitParry fires BEFORE the parry roll, which is the only moment its
        arguments mean anything. It used to fire after a SUCCESSFUL roll, so
        LOCAL.ParryChance - the chance the roll uses - was unreachable: a script
        could neither parry where the engine would not nor refuse one where it
        would.

FIX - ARGN1 is the PERCENT the parry takes off, not the damage let through. The
        two are inverse quantities, so a script raising ARGN1 to parry harder took
        LESS off the blow. 100 (the default) is a full block, and the Parrying
        skill's own EFFECT curve sets it when a pack defines one - the pack decides
        how much a parry is worth, not the engine.

FIX - The rest of the contract arrived with it: ARGN2 the damage type, ARGO the
        parrying item, LOCAL.ParrySkillID (which skill rolls and trains),
        LOCAL.ItemParryDamageChance and LOCAL.Damage (the raw blow, rewritable).
        RETURN 1 drops the blow whole.

FIX - A parry wears the item that made it. The reference damages the shield or
        weapon for 1 on every successful parry; here it parried forever for free.


[2026-09-10] - What @HitCheck's answer means
----------------------------------------

FIX - A trigger's RETURN can be a number again, and the engine reads it. A
        true/false result carries a veto but not the reference's NUMERIC returns,
        and @HitCheck's whole contract is numeric. The raw value now travels back
        with the trigger arguments.

FIX - @HitCheck steers the swing instead of always missing. The reference reads
        the return as a war swing state (CCharFight.cpp:1770-1779): RETURN 1 means
        "the state is in ARGN1", RETURN -1 means the target is invalid, RETURN -2
        means take my edits and run the hardcoded path anyway. This engine read any
        true return as a forced miss - the exact inverse of what a script says with
        `argn1 SWING_READY / return 1`, which is "hold, not yet" and is how the
        reference pack's own combat override paces every swing. Holding no longer
        renders a swing and a miss; -1 drops the target; -2 and a plain
        fall-through take the script's ARGN1 and LOCAL.Recoil_NoRange and carry on.
        The NPC path answers the same contract, so a script sees one meaning of
        RETURN 1 whether it swings or its pet does.

FIX - <SWING> resolves. It is the reference's own name for the war swing state
        (CChar.cpp:2998) and the live pack reads it in its player-info dialog;
        only the SphereNet-invented SWINGSTATE answered. SWING= writes it too, and
        refuses anything outside -1..2 the way the reference does.


[2026-09-10] - Who an NPC turns on next
----------------------------------------

FIX - The ATTACKER.* script surface answers what a pack actually asks it. Measured
        against the live pack rather than the enum: its own player-info dialog reads
        <ATTACKER.0>, <ATTACKER.LAST.DAM> and <ATTACKER.MAX.DAM>, and one of its
        functions writes ATTACKER.CLEAR. None of the four resolved - the engine
        answered LAST and MAX as bare uids with no field after them, refused a bare
        index, and accepted only n.IGNORE on the way in. The reference resolves a
        SELECTOR to a row and then reads or writes a field on it, and now so does
        this: ID, TARGET, MAX, LAST, a number; DAM, ELAPSED, THREAT, IGNORE, UID;
        and the CLEAR / DELETE / ADD / TARGET verbs.

FIX - The attacker list keeps its order. An entry that took a hit was moved to the
        end so ATTACKER.LAST would point at it, which renumbered every row under it
        - and ATTACKER.n is the handle a script holds between two lines. The
        reference only ever appends; LAST is resolved from the last-hit stamp.

FIX - An NPC picks its next opponent off its own attacker list. The list is written
        from both directions upstream - engaging a target records it before it has
        hit back - so when the current one drops, the rest of the fight is already
        written down. This engine only filled the list from damage taken and swept
        the whole sight range again instead, which is how a creature that had just
        killed one of three attackers could stand idle among the other two.

FIX - THREAT is a real stored value, and the invented one is gone. It decides which
        target comes off that list (highest threat with NPC_AI_THREAT, else nearest)
        and it is what a master's order rides on: a pet told to attack carries 1000
        plus the highest threat already on its list, so nothing outbids the order.
        What was here instead was a damage-derived bonus blended into the
        motivation score - a formula the reference does not have, and one no script
        could reach. Threat is saved with the attacker row.

FIX - @Attack and @CombatAdd carry the threat (ARGN1) and the ignore flag (ARGN2)
        and read both back, and RETURN 1 stops the engagement or the add. @Attack
        used to fire with no arguments at all, so a script could refuse a fight but
        never weight one.

FIX - Kill credit requires damage. The reference credits an attacker row only while
        it has dealt some (CCharAct.cpp:4361); without that gate, now that the list
        also holds characters who merely engaged, swinging once and missing would
        earn a share of the kill.


[2026-09-10] - What a restart does to goods in flight
----------------------------------------

FIX - An item's own TYPE now survives a save. The reference writes the TYPE line
        whenever an instance's type differs from its definition's (CItem.cpp:2461);
        here it was written for three structure types only, so every other retype -
        a script's TYPE=t_door, an engine-set type - was reverted to whatever the
        ITEMDEF said on the next restart. Measured on the 76,359-item shard dump:
        2,383 items carry a type of their own, exactly the ones whose record already
        had a TYPE line, so the save does not grow.

FIX - A save taken while two players had a trade window open no longer eats what was
        in it. A trade window may only exist on a player with a live client, so every
        one a save contains belongs to a session that died with the process; the
        reference bounces its contents into the owner's pack and deletes the window at
        load (FixWeirdness, CItem.cpp:1005). Without that the offered goods came back
        sealed in a Special-layer container that no inventory view reaches - present in
        the world, gone for the player.

FIX - @TradeAccepted now names the goods, not just how many. The reference fills the
        trigger's object list with the items the receiving side is about to get
        (CItemContainer.cpp:196) and scripts read them as REF1..REFn; only the count in
        ARGN1 was passed. A script written against the reference could not tell what
        changed hands - the housing pack's transfer walks that list to find the deed,
        so a traded house never moved. REF1..REFn are shared with the whole trigger
        chain, so a script's own REF writes survive it.

[2026-09-09] - A container can say how much it holds
----------------------------------------

FIX - MODMAXWEIGHT now works on an item, not only on a character. The reference keeps
        it on the shared object base (CObjBase.cpp:1104), so a chest answers it exactly
        as a character does, and for an ordinary container it is the WHOLE weight rule -
        the check is skipped while it is not positive (CItemContainer.cpp:906). Here it
        existed on characters only, so a script could say how much a chest holds and
        nothing listened. It is read, written and saved now; losing it on a restart
        would quietly un-bound every chest a script had set up.

        This completes the same day's removal of the invented flat 400-stone cap: with
        no global limit left, the container's own value is what bounds it, and the
        configured setting is only consulted when the container says nothing.

[2026-09-09] - Two invented economies removed
----------------------------------------

CHANGE - Mining, fishing and lumberjacking no longer invent a resource when the world
        has nothing defined for the spot. There was a whole second economy behind the
        real one: a hardcoded iron ore, fish and log, a marker pool of its own and its
        own difficulty numbers - a resource table the reference does not have and no
        script could reach or change. Upstream simply fails the attempt when its
        resource setup finds nothing (CCharSkill.cpp:1456), and that is what happens
        now.

        Measured before removing rather than after: the live script pack defines
        REGIONTYPE t_rock, t_water and t_tree with RESOURCES, so the real gathering path
        answers every one of these and the invented branch never ran with a pack
        loaded. No test depended on it either.

CHANGE - An ordinary container no longer stops accepting items at 400 stones. The
        reference has no global cap for chests and bags: a container's limit is its own
        MODMAXWEIGHT, which starts at zero, and the check is skipped while that is not
        positive (CItemContainer.cpp:906). Only the player's backpack has a real limit,
        from its owner's carry weight plus BACKPACKOVERLOAD - which is already how it
        works here. The setting remains for a shard that wants a cap; it is simply no
        longer imposed. The BANK limit is untouched: that one IS in the reference
        (m_iBankWMax).

[2026-09-09] - The verbs a real script pack issues, measured
----------------------------------------

NOTE - The trigger sweep asked which @Hooks a pack writes that nothing fires. This is
        the same question for the other half of the script surface: 1,881 files, 14,223
        statements, 314 distinct verbs. Two of them are real reference verbs this engine
        did not implement. Everything else that came back unmatched is the shard's own
        [FUNCTION] blocks - F_SHADOW_CURSE, MAKE_TEKRAR and the like - which resolve
        through the function path and are not engine verbs at all.

FIX - MESSAGE and MSG now work on an object, which is where upstream puts them
        (CObjBase.cpp:2402). They existed here only as an admin console command, so a
        script line like `MESSAGE You feel a chill` did nothing at all - and the packs
        on hand issue it 330 times, by a wide margin the most-used verb that was
        missing. The audience follows the reference: private to the character that
        issued the verb when there is one, and over the object for everyone nearby when
        there is not (a delayed call, a console with nobody behind it). It is not
        SYSMESSAGE - that writes in the corner and is unchanged.

FIX - ADDCIRCLE <circle>[,<andLower>] fills a spellbook with the eight spells of that
        circle, and with every circle below it when the second argument is not zero
        (CItem.cpp:3213). Anything that is not a spellbook is refused, as upstream
        refuses it. The magery script in the pack calls it nine times.

[2026-09-09] - SKILLUSEQUICK, the query that is not one
----------------------------------------

FIX - SKILLUSEQUICK.<skill>,<difficulty>[,<noBellCurve>][,<force>] is now answered. It
        sits among the read-only queries and behaves like none of them: upstream hands
        it straight to Skill_UseQuick with gain allowed (CChar.cpp:2802), so reading the
        key rolls the dice, can raise the skill and runs @SkillUseQuick. That is why it
        was held back from the CANMAKE wave and given a pass of its own.

        Two details are the reference's own and are kept rather than tidied: the third
        argument is INVERTED - a non-zero value turns the bell curve OFF - and fewer
        than two arguments leaves the key unhandled rather than answering zero, so the
        caller sees its own text instead of a made-up result. The fourth argument rolls
        even for a SCRIPTED skill, which SkillEngine.UseQuick now accepts.

NOTE - SKILLTEST was left out on purpose. It is the neighbouring key in the reference
        and a genuine read-only query, but no script pack on hand uses it, and answering
        it means exposing the resource-list parser for runtime use. Recorded rather than
        guessed at.

[2026-09-09] - CANMAKE and CANMAKESKILL answer at last
----------------------------------------

FIX - A script asking CANMAKE.<itemdef> or CANMAKESKILL.<itemdef> got nothing back;
        both keys were simply absent, and the packs on hand use them eight times.
        Upstream answers both from Skill_MakeItem at its SELECT stage (CChar.cpp:2787):
        CANMAKE asks the whole question - the SKILLMAKE requirements and whether the
        materials are to hand - while CANMAKESKILL passes fSkillOnly and asks about the
        skill side alone (CCharSkill.cpp:913). The crafting engine already knew how to
        answer; it had no way to be asked, and no way to answer the skill half on its
        own.

NOTE - The work site is deliberately not part of the answer. Upstream checks the forge
        in the smithing stage rather than in Skill_MakeItem's SELECT, so a smith
        standing in a field still answers yes to "do I know how, and do I have the
        iron". Crafting itself still requires the forge.

[2026-09-09] - Items are heard again: drop and equip sounds
----------------------------------------

FIX - Wearing something made no sound at all. Upstream plays EQUIPSOUND - 0x057 unless
        the item says otherwise - on any layer that actually shows
        (CCharAct.cpp:3355), so armour and weapons went on in silence here.

FIX - Every drop sounded the same. Upstream picks the sound from the item's TYPE first
        (coins and gold by how many there are, gems by whether they are large, an ingot
        only when it lands on the ground), then lets the item's own DROPSOUND key
        override that, and only then falls back - and its fallback depends on WHERE the
        item landed: 0x057 onto something, 0x042 on the ground (CItem.cpp:1490-1552).
        SphereNet answered 0x042 for everything and never distinguished the two.

CHANGE - The gold drop sounds were invented. SphereNet banded them 1 / under 6 / more
        and played 0x2E4-0x2E6; the reference bands them 1 / 2 / 3-4 / more and plays
        0x035, 0x032, 0x036, 0x037. Gold now sounds like the reference says it does.

NOTE - DROPSOUND and EQUIPSOUND read from the instance first and the ITEMDEF second,
        which is the resolution upstream's GetDefKey performs. No script pack on hand
        sets either key, so this changes nothing for them today - it is the contract for
        the code path, not a guess about what a shard wants.

[2026-09-09] - Training that survives the night, and a modifier that means something
----------------------------------------

FIX - A character imported from a classic save lost every stat it gained, at every
        restart. OSTR/ODEX/OINT are not a second value beside the base stat - upstream
        reads them with Stat_GetBase and writes them with Stat_SetBase, the same calls
        STR/DEX/INT use (CChar.cpp:3139, :3641). SphereNet kept a separate field for
        them, stale from the moment the stat changed, and the record states STR before
        OSTR - so the shadow won on load and a trained 110 came back as the 100 it was
        imported with. They are now the base under another name, and the save states it
        once. A classic record, which carries only the O-names, still loads.

FIX - MODSTR/MODDEX/MODINT did nothing at all. The value was stored, saved by nobody,
        and read by nothing - the live script pack sets it 83 times, every one of them
        a no-op. It now joins the stat the character actually uses, which is upstream's
        Stat_GetAdjusted: base plus modifier plus the equipped suit
        (CCharStat.cpp:143). It is persisted too, written before the base the way the
        reference insists on ("saving the MOD first", CChar.cpp:4250).

CHANGE - Reading <STR> now answers with the adjusted stat and <OSTR> with the bare base,
        as upstream does (CChar.cpp:3146/3139). The stored base field is still never
        touched at equip time - deriving the suit share on read is unchanged - but the
        script surface no longer disagrees with the number the character fights,
        carries and equips with. One test asserted the old reading; it had tied the
        read contract to the storage decision, which are separate questions.

FIX - A map extracted from a UOP left its ~84 MB temp file behind whenever the reader
        was not disposed - including every test run that built a map. The file is now
        opened DeleteOnClose, so the OS reclaims it when the last handle goes, even if
        the process never gets to Dispose.

[2026-09-09] - A blow dealt from script bounces like any other
----------------------------------------

FIX - Reactive Armour, Blood Oath and REFLECTPHYSICALDAM now answer scripted damage,
        not only weapon swings. The reference has ONE damage entry - CChar::OnTakeDamage
        - and the DAMAGE verb calls it directly (CObjBase.cpp:2249), so the reflect
        family belongs to <SRC.DAMAGE 40> exactly as much as to a hit. SphereNet grew a
        second entry for scripted damage and the family stayed behind on the melee one,
        so a shard that deals its damage from script - the usual way a custom attack, an
        arena or a trap with a culprit is written - got none of it.

        The gates are the reference's own and are deliberately narrow: the family
        answers a PHYSICAL blow (blunt/pierce/slash) with somebody behind it. A spell,
        a burning field or a trap with no culprit still bounces off nothing, which is
        why a mage does not take his own fireball back.

[2026-09-09] - The four triggers a script pack asked for and never got
----------------------------------------

FIX - @RegionResourceFound and @ResourceFound now fire the moment a mining or
        lumberjacking swing turns up a vein, with the vein marker as the object
        argument (CWorldMap.cpp:151-166). They share one return value, the later
        overwriting the earlier and each guarded by whether a script actually hooks it,
        and RETURN 1 empties the vein rather than removing it - the spot was searched
        and holds nothing, which is not the same as never having looked.

FIX - @RegionResourceGather now fires on the gatherer alongside the resource
        definition's own @ResourceGather, sharing the same arguments and the same
        return value (CCharSkill.cpp:1029-1040). ARGN1 is the amount and
        LOCAL.ResourceID the item, both writable from either side.

FIX - @HitReactive now fires when Reactive Armour is about to bounce a blow, and the
        whole bounce is handed over in locals - Sound, EffectID, Damage,
        ReflectDamage, ReduceDamage, DamageType - and read back (CCharFight.cpp:965).

CHANGE - Reactive Armour was rebuilt on the reference's own terms and three defects
        went with it. The reflected share came from a flat quarter of the blow, a
        number the reference does not contain: it reads the percentage the spell
        definition's EFFECT curve gives at the caster's skill, stored when the spell is
        cast (CCharSpell.cpp:1411), so a shard's own EFFECT= now decides it. The blow
        was reflected but never REDUCED, so the spell hurt the attacker without ever
        sparing its wearer. And the two-tile reach the reference requires was not
        checked, so a bow shot from across the screen bounced back.

        The flag alone therefore no longer reflects anything - that is the reference's
        behaviour, since it reads the worn memory and a definition asking for no
        reflection gets none. There is no minimum of one: the reference has that clamp
        commented out, so a small blow at a small percentage bounces nothing.

NOTE - Reactive Armour, Blood Oath and REFLECTPHYSICALDAM all live on the melee path
        only. The reference puts them in OnTakeDamage, which the DAMAGE verb also goes
        through, so scripted damage bounces off none of them here. Recorded, not
        changed - it is one gap for the whole reflect family rather than a fourth
        trigger's business.

[2026-09-08] - Which triggers a real script pack asks for, and which answer
----------------------------------------

NOTE - The trigger long tail was measured from the PACK side and against the
        reference's own table (tables/triggers.tbl, 248 triggers) rather than against
        our enum - measuring coverage with your own enum only proves the enum agrees
        with itself. Across 910 script files, 10,166 hooks and 123 distinct trigger
        names, nine names reach nothing. Five of them (@npcmount, @NPCDisMount, @move,
        @skilluse, @statgain) plus @TameAbort, @SpellStart and @PartyJoin do not exist
        in the reference either - dead script written against triggers that never
        existed. The remaining FOUR are real and are now recorded gaps: @ResourceFound,
        @RegionResourceFound, @RegionResourceGather and @HitReactive.

        Several first-pass "gaps" were false alarms and were verified away: the skill
        stages (@PreStart, @Stroke, @Abort, @Fail, @Success, @Select, @UseQuick) fire
        under their short section names, the region stages (@Enter, @RegPeriodic,
        @CliPeriodic) and spell stages (@Effect, @Select) fire by name, and the
        @item<Name>/@char<Name> mirrors cross-fire through string composition - none of
        them pass through a trigger enum value.

CHANGE - The dispatcher publishes the set of names it can actually reach
        (DispatchableTriggerNames), and a test checks every hook in the script packs
        present on the machine against it. It skips cleanly when no pack is installed.
        A recorded gap that stops being a gap fails the test until it is struck from
        the list, so the list cannot quietly become a place where gaps hide.

[2026-09-08] - A character logs back in with the pool it logged out with
----------------------------------------

FIX - A player wearing a suit with BONUSHITSMAX logged out at 120 of 120 hits and came
        back at 100 of 120, every time, and the same for mana and stamina. Two separate
        causes, both in the load:

        1. Writing HITS was treated as a HEAL. Upstream keeps two operations apart -
        an assignment goes to Stat_SetVal, which stores what it is given and refuses
        only a negative (CChar.cpp:3901), while damage, healing and regeneration go to
        UpdateStatVal, which clamps to the adjusted maximum (CCharAct.cpp:753).
        SphereNet had one clamping setter doing both, so a record read BEFORE the
        equipment goes back on had its value trimmed to the bare base maximum. The
        assignment path (a save record and a script's HITS= alike) now stores the value
        as given; the gameplay path still stops at the maximum.

        2. The load put a piece of equipment on TWICE - once from the item's own
        record, once from the character's EQUIP link - and the second time takes it off
        first. For that instant the wearer's maximum drops by the suit's bonus and the
        current pool is trimmed to it; the suit goes back on, the maximum returns, the
        lost hits do not. The load no longer re-equips what is already on the layer.

CHANGE - A character record states MAXHITS/MAXMANA/MAXSTAM before HITS/MANA/STAM, the
        order the reference calls out as "VERY important" (CChar.cpp:4250) for exactly
        this reason: setting a maximum trims the value under it. Old records still load
        - the order only matters within one record.

[2026-09-08] - The raw type field, measured rather than assumed
----------------------------------------

NOTE - Roughly twenty places read an item's RAW type field instead of the resolving
        property (stacking, blocking, traps, ship parts, corpses, multi commands). Two
        of them were real bugs and were fixed when a classic record's map pins and book
        pages went missing; the rest were recorded as a deferred concern. They were
        measured this wave instead of changed: after a load every item's raw type is
        materialised from its definition, and the world invariant sweep over the real
        56T shard - 76,359 items - reports ZERO type divergences. So the remaining raw
        readers are correct, and the thing that makes them correct is now asserted on
        live data instead of assumed.

CHANGE - The real-data check now fails if the loaded world breaks ANY of the world
        invariants (container index, parent links, type materialisation, spawner counts,
        limbo items), naming the first few. It used to only load and count keys.

[2026-09-08] - A region's own script runs on the region
----------------------------------------

FIX - A region's @Enter, @Exit and @Step ran with the CHARACTER as the object, so every
        region-level read inside them answered from the walker: a house script asking
        for <TAG.owner> got the walker's tag rather than the structure's - and that tag
        is exactly what a house region carries. Upstream calls the script from the
        region itself and hands it the character's console (CRegion::OnRegionTrigger,
        CRegion.cpp:882), so the object is the region and the walker is SRC. Room events
        follow the same rule, and so do the server-wide EVENTSREGION handlers.

NOTE - This was recorded as a static observation when the structures got their regions
        back; it is now reproduced, fixed and locked by a test that reads a region tag,
        a region name and SRC from inside the trigger.

[2026-09-08] - A creature's shape brings its strength (MAXPOLYSTATS)
----------------------------------------

FIX - Polymorph changed only the BODY: a mage in a dragon's shape kept a mage's
        strength. Upstream moves the caster's STR and DEX to the form's own when
        MAGICF_POLYMORPHSTATS is on, by no more than MAXPOLYSTATS points in either
        direction, and remembers the change on the spell memory so it comes off with the
        form (CCharSpell.cpp:1082). A definition that declares no value for a stat
        leaves that stat alone, as upstream's own "if (pCharDef->m_Str)" does. That
        setting - listed last wave as having no consumer - now has one.

NEW - A character definition can be found by the BODY it is drawn as. The definitions
        are keyed by their own index, so asking "what is a dragon's strength?" had no
        answer; the index is built once at load.

[2026-09-08] - The shipped ini meant the old placeholders (IS-5)
----------------------------------------

FIX - config/sphere.ini carried placeholder values for the keys that had not been read
        yet, each marked "[UYGULANMADI]". The moment a key IS read, its placeholder
        becomes live - and several of them said the opposite of the reference: the
        teleport effects and sounds were all 0 (show nothing, play nothing),
        MAXHOUSESGUILD was 0 (a guild could own no house at all), BACKPACKOVERLOAD was 0
        with a comment describing a 0/1 switch rather than an allowance in stones,
        FLIPDROPPEDITEMS was 0 and NPCCANFIZZLEONHIT was 1, the opposite of upstream.
        They now carry the reference's own values, their comments say what the setting
        actually does, and their labels say they work. MAXITEMCOMPLEXITY, which had no
        line at all, is in the file.

NEW - A test loads the shipped ini and holds it to what the engine now reads, so a key
        wired later cannot quietly inherit a placeholder again.

NOTE - The two values this pack deliberately keeps away from the reference are marked as
        such in the file: NOWEATHER=0 (this shard wants weather; upstream's default is
        none) and NPCSKILLSAVE=100 (stricter than the reference's 10).

[2026-09-08] - Weather and an NPC's small skills, as the reference has them (IS-5)
----------------------------------------

CHANGE - NOWEATHER exists, and its default is the reference's: a bare server has NO
        weather. This engine ran rain and snow unconditionally, so a shard that asked
        for silence got weather anyway. What the flag stops is the ROLLING of new
        weather and the TELLING of clients (GetWeatherCalc, CSector.cpp:861; addWeather,
        CClientMsg.cpp:526) - weather a script sets on a region is still kept and still
        readable, exactly as upstream keeps a sector's own. A shard that wants weather
        says NOWEATHER=0, which the live shard's ini already does.

FIX - NPCSKILLSAVE is read, and it is not the save switch its name suggests: upstream
        DROPS an NPC's skills below it while reading the world ("don't keep track of
        unused skills", CChar.cpp:1027), which is why they never reach the next save. A
        creature's one-point leftovers were kept here forever and written out on every
        cycle. A player's skills are never touched, and zero keeps everything.

[2026-09-07] - A pack holds what its owner can carry (IS-5)
----------------------------------------

FIX - A player's own backpack was bounded by the flat container weight cap that every
        chest and pouch gets, so a strong character could carry no more in it than a
        weak one. Upstream bounds the PACK by what its owner can carry plus
        BACKPACKOVERLOAD (CItemContainer.cpp:907, default 40 stones), which is exactly
        what that setting is for - and it was not read at all. Below zero means the pack
        has no limit of its own, as upstream defines it.

FIX - A dropped item always turned to its flipped graphic. It is a setting
        (FLIPDROPPEDITEMS, default true) and it never applies to something that cannot
        be moved (CCharAct.cpp:3266). A shard turning it off still got the flip.

[2026-09-07] - Whether a creature's casting can be disturbed is a setting (IS-5)
----------------------------------------

FIX - "Only players are disturbed by a hit" was written into the interrupt path as a
        rule. It is the DEFAULT: upstream gates it on NPCCANFIZZLEONHIT
        (CCharFight.cpp:881), so a shard that wants its creatures interruptible asks for
        it and gets it. The setting was not read at all, so asking changed nothing. The
        default is unchanged - only players, as before.

NOTE - Two more of the unread keys were looked at and deliberately left alone this
        wave. NPCSKILLSAVE is not a save setting upstream: it is a cleanup rule that
        ZEROES an NPC's skills below the threshold (FixWeirdness, CChar.cpp:1030), so
        wiring it changes imported data and wants a decision first. MAXPOLYSTATS has no
        consumer here yet - polymorph does not apply the form's stats at all, so a cap
        on them would cap nothing.

[2026-09-07] - Settings that worked under the wrong name (IS-5)
----------------------------------------

FIX - NPC training was configurable and the setting worked - under this engine's own
        spelling only. The reference calls those keys NPCTRAINPERCENT and NPCTRAINMAX
        (CServerConfig.cpp:667/668), which is what a shard's ini writes, so the shard
        set them and nothing changed. Both names are accepted now; the reference's wins
        when a file carries both.

NEW - MAXHOUSESGUILD: how many houses a guild may own when its own stone does not say.
        Upstream gives a stone this value the moment it is built (CItemStone.cpp:24,
        default 1); here a guild simply had no limit of its own unless a tag set one.

NOTE - HITSUPDATERATE, which the live ini also sets, is ELEM_VOID upstream - the
        reference accepts the key and does nothing with it. It is recorded as such
        rather than given a behaviour this engine would have had to invent.

[2026-09-07] - What a teleport looks and sounds like is a setting (IS-5)
----------------------------------------

FIX - Every teleport played the SAME sound and showed no effect at all. Upstream picks
        both by who did it (CCharSpell.cpp:178): a visible staff member gets the
        flamestrike, a player the vanish, an NPC the heal effect, each with its own
        sound - six ini settings (TELEPORTEFFECT* / TELEPORTSOUND*, CServerConfig.cpp:88).
        The live shard's ini sets them and nothing read them. They exist now, default to
        the reference's own values, and a zero means "show nothing" / "play nothing".

NOTE - Found by comparing the live sphere.ini against the config: of the 184 keys that
        shard sets, 33 were not read anywhere - not misparsed, simply never looked at.
        The classified list is in the verification plan; this wave takes the six with a
        visible effect and a defined reference behaviour. MAXITEMCOMPLEXITY, added in
        the previous wave for the same reason, came from the same list.

[2026-09-07] - What a copy reports, and how much of it there may be (IS-4)
----------------------------------------

FIX - DUPE on an item told nobody what it had made. Upstream hands the copy to
        CreateDupeItem with the calling console's character and fSetNew set, so NEW and
        the caller's ACT both name the copy - the last one when the line made several
        (CItem.cpp:3641 -> :385). Only NEW was set here, and only as a side effect of
        creating an object, so a script that duplicated something and then worked on ACT
        worked on whatever it had been looking at before.

FIX - The number of copies one DUPE could make was capped at a thousand, which is this
        engine's own number and not a rule from anywhere. The reference bounds a
        TOP-LEVEL pile by MAXITEMCOMPLEXITY (CItem.cpp:3636), an ini setting that
        defaults to 25, and leaves a copy made inside a container unbounded. That
        setting exists now and the cap is it.

NOTE - The entry points were compared column by column for the port plan (IS-4): what
        fires @Create, what NEW and ACT end up naming, how the amount is read, where the
        object is put and what happens when it fails. A duplicate deliberately does NOT
        re-run @Create on any path - upstream builds it with CreateBase plus DupeCopy
        and never reaches the script that fires it, and re-running would re-roll a magic
        weapon's properties so the copy would not be a copy. DUPE on a CHARACTER
        deliberately sets neither NEW nor ACT, because upstream's CHV_DUPE calls DupeFrom
        directly and touches neither.

[2026-09-07] - Save, load, save again - and say the same thing (IS-3)
----------------------------------------

NEW - A round-trip check: a world is saved, loaded back and saved again, and the two
        saves are compared line by line. This is the check that catches the class of
        bug no single assertion looks for - a field written in one shape and read back
        in another drifts a little on every save cycle, and nothing notices until the
        numbers are wrong. The equipped-bonus pool that compounded on every duplicate
        was exactly that shape. Two worlds are compared: one built through the engine's
        own API, and one loaded from CLASSIC records - a stone with its roster, a ship
        with its hold and planks, a structure with its region lines, a character with
        an old shard's kill counters and a renamed skill - which has to reach a fixed
        point in one cycle. A save that lost everything would also be a fixed point, so
        the translated data is checked to BE there first.

FIX - The first thing it found: a character with no maximum hit points of its own was
        saved as MAXHITS=0, and a zero maximum is not a value the engine can hold - the
        setter clamps it to one, so the NEXT save said something different from the
        first. It is left out now; a character with no maximum of its own takes its
        definition's.

NOTE - A clock is not drift: the world time, a countdown timer and a character's age
        are expected to have moved on between two saves, so the check compares what
        those lines are rather than what they say.

[2026-09-07] - A structure's region belongs to the structure (IS-2c)
----------------------------------------

FIX - A house out of a classic save had no region at all. Realizing one was hung off
        the HOUSE record, and a house record needs an ownership tag this engine writes
        itself - which a classic save never does, because that shard keeps ownership in
        its own scripts. So 93 structures in the 56T dump loaded as decoration: their
        Safe and NoBuild flags did nothing, their @Enter and @Step scripts never fired,
        and nothing inside them could be told apart from the open field around them.
        Upstream realizes the region whenever the MULTI is put in the world, owner or
        not (MultiRealizeRegion, CItemMulti.cpp:191/571), so that is where it hangs
        now: a multi with no ownership record still gets its footprint, its name, its
        events, its tags and the REGION.FLAGS its own record carries.

NOTE - Rebuilding the world twice replaces those regions rather than layering a second
        one over the first. What is NOT handled yet is a structure being deleted while
        the server runs: a house record still takes its region with it, but a multi
        that has none leaks one until the next rebuild.

[2026-09-07] - A structure resolves to its own definition (İŞ-2b)
----------------------------------------

FIX - Every house and ship out of a classic save loaded as graphic 0 with type Normal.
        A structure's record heads with a [MULTIDEF] name - "[WORLDITEM m_small_ship_n]"
        - and writes neither an ID nor a TYPE line, because both belong to that block.
        The loader only ever asked for an ITEMDEF, so the header resolved to nothing.
        Upstream has no such split: a multi's definition IS its item base
        (CItemBaseMulti), which is where the id and the type come from. The header now
        resolves through the multi definition as well, and the hull comes back drawn as
        its multi and typed as a ship.

FIX - A structure's PARTS head with the item id itself - "[WORLDITEM 03eb2]" - and
        write no ID line either, because the header already is the id
        (ResourceGetID_EatStr, CResourceHolder.cpp:102). They loaded with graphic 0 and
        type Normal too, so a hull could not tell its own planks from anything else. A
        numeric header is read as the Sphere number it is.

FIX - The ship engine refused to rebuild a hull that names no owner, so every ownerless
        ship stayed out of it: no region, no helm, no plank - all eight in the 56T dump.
        An owner is not what makes it a ship: upstream builds a CItemShip from the type
        and leaves the owner empty when nobody owns it (a guard ship, a scripted
        decoration). Seven of those eight hulls now come back as ships with their holds
        and planks attached; the eighth carries an explicit TYPE=t_multi of its own, and
        an instance's type outranks its definition.

[2026-09-07] - The murder count an old shard wrote (PLAN-106)
----------------------------------------

FIX - A character imported from a 0.56 shard came back with no murder count. That
        server kept TWO kill counters and wrote them as KILLSPLAYER and KILLSNPC;
        neither name exists anywhere in the reference, which keeps ONE - KILLS, the
        murder count (CCharPlayer.h:49) - and whose own legacy importer translates an
        old key into the field it maps to and leaves the rest (CWorldImport.cpp:750).
        KILLSPLAYER is that field: it is the count notoriety reads, so 834 characters
        in the 56T dump were loading as innocent. It reaches KILLS now. KILLSNPC has no
        counterpart and is kept as script-readable data rather than invented into the
        engine as a second counter the reference does not have.

NOTE - With this the 56T dump loads with NO unmapped keys left: every key in that
        shard's world, character and multi records now reaches a field, a tag with a
        meaning, or a documented decision. The measurement runs with the script pack
        and the server's own loader wiring, which is the configuration a server runs
        in.

[2026-09-07] - A classic ship names its hold and its planks (PLAN-106)
----------------------------------------

FIX - A ship out of a classic save came back with no hold and no boarding plank. The
        record lists NO components at all: a ship names its hold and its planks and
        nothing else (HATCH / PLANK, CItemShip.cpp:118/208). Neither key had a case
        here, so both were parked in SAVE.* tags and the two uids - the only record
        those parts have - were lost to the ship layer. They are read now, stored where
        the ship layer looks for them, and registered as components on rebuild, which
        is what categorises them by type.

NEW - Serial.NamesAnObject, the reference's own uid rule (CUID::IsValidUID,
        CUID.cpp:32): zero names nothing, and neither does a value whose index bits are
        all set - which is how a classic save writes a field it has CLEARED. Seven of
        the eight 56T ships carry exactly that as their hatch, and taking it at face
        value would have pointed each one at an object that does not exist.

NOTE - The keys are not gated on the item being typed as a ship. A multi's TYPE comes
        from its [MULTIDEF] block, which this engine keeps in the multi registry rather
        than on the item, so an imported hull is still typed Normal while its record is
        being read. That gap is real and separate: it also keeps an imported ship out
        of the ship engine's own rebuild, which additionally wants an OWNER the classic
        record does not always carry.

[2026-09-07] - A classic stone brings its guild with it (PLAN-106)
----------------------------------------

FIX - Every guild in an imported shard was lost. A classic guild or town stone keeps
        its roster in its own record - ALIGN, ABBREV, CHARTER<n>, WEBPAGE and one
        MEMBER line per member, written as
        uid,title,priv,loyaluid,abbrev,wedeclaredwar,accountgold (CItemStone.cpp:145) -
        and none of those keys had a case here. They were parked in SAVE.* tags, the
        guild layer (which rebuilds from the stone's GUILD.* tags) saw no guild at all,
        and the next save wrote none of it back. The 56T dump carries 10 stones and 144
        such lines. A classic line is translated into the engine's own shape now, so
        those 10 guilds come back with their names, abbreviations, alignments and
        rosters - the largest with 13 members, its master included.

FIX - A classic stone writes no name of its own: the guild is called whatever the
        STONE is called (CItemStone::GetName). The rebuild keyed on a GUILD.NAME tag
        alone, so even a stone whose roster had been read would have come back
        nameless. A stone carrying a roster now takes its own name.

NOTE - Privilege 100 / 101 in that list are not members: they are the war and alliance
        records (STONEPRIV_ENEMY / _ALLY), and they become relations rather than
        members. The 56T dump carries none of them, so that branch rests on the
        reference's own format comment and its unit test, not on live data.

[2026-09-07] - A structure's region travels inside its own record (PLAN-106)
----------------------------------------

FIX - A house's region lines were refused on load. A multi carries its structure's
        region in its own record, and upstream strips the "REGION." prefix and hands
        the rest to that region (SHL_REGION, CItemMulti.cpp:3011): REGION.FLAGS and
        REGION.EVENTS had cases here, REGION.TAG.<name> did not, so a 56T house's owner
        - 93 of them - was parked in a SAVE.* tag and lost on the next save. The line is
        kept on the multi, written back out under its own name, and copied onto the
        region when the house or ship realizes it, which is where a script standing
        inside the structure reads it from.

NOTE - Reading REGION.<key> is deliberately NOT intercepted on the item: on any object
        it answers from the region that object stands in, and for a structure that is
        its own region. Intercepting it would have hidden the region from everything
        else standing there.

[2026-09-07] - A record's type comes from its definition (PLAN-106)
----------------------------------------

FIX - Every pin on every map and every page of every book out of a classic save was
        refused on load and parked in a SAVE.* tag. A classic record carries no TYPE
        line - the type belongs to the ITEMDEF, and upstream fixes the object's class
        from it at creation (CreateBase, CItem.cpp:314) before reading a single key.
        SphereNet resolves an item's type lazily, and the book, map and ship property
        branches tested the RAW instance field, which is still Normal while the record
        is being read. They ask for the effective type now: the instance's own when it
        has one, the definition's otherwise. In the 56T dump that is 59 map pins and 18
        book pages read instead of parked.

CHANGE - The real-data pass now installs the same loader resolvers the server does
        (Program.cs:806). Without them the loader cannot reach an ITEMDEF at all, so
        the measurement blamed the engine for keys a running server reads. With the
        pack loaded and the server's wiring, the 56T dump leaves 10 kinds of unmapped
        key rather than 17.

[2026-09-07] - A skill is called what the shard calls it (PLAN-106)
----------------------------------------

FIX - A character loaded from a classic save lost every skill the shard had renamed.
        A pack renames a slot with the KEY of its [SKILL n] block and Source-X looks
        skills up by that key (CSkillDef): 56T calls skill 54 Sailormanship and skill
        19 Farming, so 128 characters carried skill lines under names the engine had
        never heard of. The value was parked in a SAVE.* tag and the skill came back at
        zero. Skill names now resolve through the loaded definitions as well as the
        built-in table, on the world-load path and the script path alike.

FIX - The pack's name was resolved through the shared defname table, where anything
        may claim the same word - 56T has a NEWBIE resource called "Farming" that won
        the lookup and hid the skill of that name. The skill blocks are indexed by
        their own names now, and the shared table is only the fallback.

CHANGE - The three separate skill-name tables (one for scripts, one for the world
        loader, and the built-in enum names) are one table
        (SphereNet.Game.Definitions.SkillNames). The loader's copy knew neither the
        pack's names nor half the classic spellings the script side knew, so the same
        save line was understood in one place and not the other.

CHANGE - The real-data check has a second pass that runs WITH the script pack loaded.
        The existing one loads the save alone, which is not how a server runs: a key
        whose meaning comes from the pack shows up there as unmapped even when the
        running server reads it fine. With the pack loaded the 56T dump now leaves 15
        kinds of unmapped key rather than 17.

[2026-09-07] - Naming, timing and asking about delayed work (12W)
----------------------------------------

FIX - TIMERF STOP and ISTIMERF reduced a pattern to a prefix search on the function
        name alone, so "f_job" cancelled "f_job_extra" as well, "f_jo?" cancelled
        nothing, and a job selected by its arguments could not be cancelled at all.
        Upstream matches the pattern against the WHOLE queued command, arguments
        included, with Str_Match (CTimedFunctionHandler.cpp:19/34): a plain name is an
        exact match, and '*' / '?' / '[a-z]' are the wildcards. That pattern language
        is now a shared helper, ported from sstring.cpp:1750.

FIX - The delay in front of a delayed command was cut at the first comma or space and
        then read as a token-or-sum, so "2*3, f_done" and "(1+1), f_done" scheduled
        NOTHING and "1 + 1, f_done" queued a job called "+" one second out. Upstream
        reads the delay with Exp_Get64Val, which consumes the whole expression -
        parentheses, multiplication and the spaces around an operator included - and
        leaves the pointer on the command that follows (CObjBase.cpp:2777,
        CExpression.cpp:794/1256). The reader now reports where the expression ended
        and the command begins there.

FIX - ISTIMERF answered with the SMALLEST remaining time among the matching jobs
        rather than the first one, and used zero as its "nothing found yet" marker, so
        a job that was already due was overwritten by a later one and reported as
        pending. Upstream returns on the first match and appends new jobs at the end
        (CTimedFunctionHandler.cpp:19/111).

[2026-09-07] - One recipe walk for the spawner, and what a copy carries (13I/13J)
----------------------------------------

FIX - A spawner pointed at a TEMPLATE picked the recipe's first concrete itemdef and
        built that object by hand, so everything the recipe said about it was lost: the
        root row's amount arrived as one, a row switched off with ",0" was spawned
        anyway as an empty container, and the recipe's NAME line never reached the
        object at all. Upstream runs the whole recipe through CItem::CreateTemplate and
        spawns what comes back (GenerateItem, CCSpawn.cpp:323).

FIX - A recipe whose first create row named ANOTHER recipe spawned nothing: resolving
        the root to a concrete itemdef discarded the template type, and the spawner
        quietly produced zero objects each cycle. The shared walk follows a referenced
        recipe, so the spawner now gets what SERV.NEWITEM already got.

FIX - The recipe's contents and property lines were applied AFTER @Spawn, so a trigger
        that inspects what it was handed saw an empty container with its base name, and
        anything the trigger set afterwards was overwritten by the recipe. Upstream
        finishes the object first and only then fires @Spawn (:346).

FIX - A @PreSpawn that retargeted the spawner built the ROOT from the new recipe and its
        CONTENTS from the old one, because the fill step still read the spawner's stored
        target. The id the trigger chose is now the single source for the whole object.

CHANGE - Building a recipe has ONE implementation. The spawner's fill-an-existing-
        container entry point (TemplateEngine.FillTemplateContents) is gone with its
        caller; BuildTemplate is the only walk, which is what keeps the spawner and the
        script factories from drifting apart again.

FIX - Duplicating a character copied its EFFECTIVE pools into the copy's base fields:
        the Max* getter reports base plus the equipped BONUS*MAX and the setter writes
        the base, so a creature in a +20 hits suit came out with 120 base and 140
        effective - and copying the copy made it 160. Taking the suit off did not give
        the increase back. The base pools are what crosses now, and the current pools
        are applied after the equipment so a creature standing at its equipped maximum
        is not trimmed on the way. This is the same base-not-derived rule the world save
        follows.

FIX - An equipped item pointing at the character it was worn by kept pointing at the
        ORIGINAL after duplication, so a copy's memory, link or marker still worked on
        the source. Upstream re-points MORE1, MORE2 and LINK at the new character when
        they name the one being copied from (DupeFrom, CChar.cpp:1227); a link to
        anything else is left alone.

FIX - A character's own DUPE verb never reached the central duplication: it copied a
        hand-picked handful of fields, so the copy came out naked and packless with no
        fame, karma, resistances, status flags or EVENTS, while NEWDUPE on the same
        character carried them all. DUPE is DupeFrom upstream (CChar.cpp:4541), and its
        argument chooses the newbie contract - below one, an absent argument included,
        marks the copy's equipment and that equipment's contents ATTR_NEWBIE.

FIX - A copied container scattered its contents: each child copy was added without its
        place, and an item with no position is given a random one. Upstream hands
        ContentAdd the source child's contained point (CItemContainer::DupeCopy,
        CItemContainer.cpp:830), so an arranged reward or sample bag keeps its layout,
        nested containers included.

[2026-09-07] - Script factory placement and recipe boundaries (13G/13H)
----------------------------------------

FIX - The amount field of NEWITEM was clamped to at least one and read only up to the
        first operator, so "NEWITEM i_gift,1+1" delivered a single item and
        "NEWITEM i_gift,0" delivered one where the script had asked for none. Upstream
        evaluates the field as an expression and hands the result straight to SetAmount,
        which stores a zero as a zero (CScriptObj.cpp:1358, CItem.cpp:2207). It now
        follows the same Sphere number rule the rest of the factory arguments do.

FIX - A parent container that refused the new object was reported as a success: the
        placement result was discarded, so an item turned away by the 255-entry
        container limit was left parented to nothing at 0,0,0 while the script was
        handed a valid uid and NEW pointed at it. The refusal now reaches the caller -
        the object is removed and NEWITEM returns 0.

FIX - An object whose definition puts it on the PACK layer was excluded from equipping
        outright, so creating a scripted starting backpack onto a character who had
        none built a SECOND, empty backpack, equipped that, and hid the scripted one
        (contents and all) inside it. LoadSetContainer applies the definition's layer
        through LayerAdd (CItem.cpp:2516, CCharAct.cpp:251): an EMPTY pack layer takes
        the container, and an occupied one refuses it so it bounces into the pack that
        is already worn (CanEquipLayer, CCharStatus.cpp:455).

FIX - The fourth NEWITEM field was parsed off the line and then never read, so both
        forms behaved as flag 0. It chooses between two paths for a character parent
        (CScriptObj.cpp:1360): with the flag, the object goes through ItemEquip
        (CCharAct.cpp:3278), which derives the layer itself and therefore applies the
        strength requirement and lets @EquipTest refuse the item, firing @Equip once it
        is worn. Without it, the load-style path is used and no strength test is made,
        because CanEquipLayer only reaches CanEquipStr when it has to work the layer out
        for itself (CCharStatus.cpp:326). Anything a slot refuses bounces into the pack
        rather than being worn regardless.

FIX - A failed NEWITEM or NEWNPC left the NEW reference standing, so a script that
        checks NEW to see whether the creation worked read the PREVIOUS object - or, for
        NEWNPC, the uid of the half-built creature that had just been removed from the
        world. Both clear it now, as upstream does (CScriptObj.cpp:1348/1402); the same
        clearing NEWDUPE already had.

FIX - A recipe row that created NOTHING left the previous item as the target for the
        lines after it, so the NAME, COLOR or TAG following a row switched off with ",0"
        - or one naming an item that does not exist - was applied to the reward created
        BEFORE it. ITC_CONTAINER assigns the reference before testing it
        (CItem.cpp:626), so a failed row leaves no target at all; a failed row still
        leaves the destination container as it was.

FIX - A recipe row naming another recipe lost its own amount: the sub-recipe's result
        was returned as-is, so a shared reward recipe used with "ITEM=t_gift,7" handed
        out the inner recipe's own quantity. CreateHeader applies the row's amount to
        whatever it made, a template's result included, and only overwrites when that
        amount is not 1 (CItem.cpp:526).

FIX - Only two item types counted as a recipe destination, so a scripted corpse or a
        custom bank box came out EMPTY - the rows after it were skipped for want of
        anywhere to put them. The reference decides containment by the C++ class the
        type maps to (CreateBase, CItem.cpp:314; CItemCorpse derives from CItemContainer,
        CItemCorpse.h:14), which is now what Item.IsContainerType answers.

FIX - A FUNC= row inside a recipe was stored as an ordinary property line and assigned
        rather than CALLED, so a recipe's randomising or quest-marking function never
        ran. ITC_FUNC splits the function name from its arguments and calls it on the
        item the recipe created last, with the container's top-level object as the
        caller and the server when there is no container (CItem.cpp:649). A function
        that deletes what it was called on now ends the recipe's item reference too.

FIX - An object made immovable while it was being created - an @Create body setting
        ATTR_MOVE_NEVER, say - was still added to the recipe's container. Upstream
        deletes it and reports the row as having produced nothing, because nothing
        immovable may live inside another item (CItem.cpp:519 -> IsMovableType, :712).
        A free-standing immovable object is unaffected.

[2026-09-07] - TEMPLATE recipes are run, not skimmed (13F)
----------------------------------------

FIX - A recipe's item amounts were never applied and a zero amount still produced an
        item, so a seven-piece reward arrived as one piece and a row switched off with
        ",0" was created anyway. Every create row now goes through the CreateHeader
        contract (CItem.cpp:461): the R# chance is rolled, the amount expression is
        evaluated - "{lo hi}" dice included - and an amount of zero refuses the row
        outright. The root row's amount reaches the object the caller receives too.

FIX - The NAME, COLOR and TAG lines inside a recipe were dropped at load: the loader
        kept only the create commands, so a quest reward came out with the base
        definition's name, no hue and no tracking tag. Source-X hands any line it does
        not recognise to r_LoadVal on the item the recipe most recently created
        (ReadTemplate, CItem.cpp:686). TemplateDef now keeps every line IN SOURCE
        ORDER, and a property row applies to whatever was created before it - the
        container for the rows before the first ITEM, that item afterwards.

FIX - A second CONTAINER row did not become the destination for the rows after it, so
        goods meant for an inner bag were created beside it and the bag came out
        empty. ITC_CONTAINER makes the new container current (CItem.cpp:631) and the
        following ITEM rows use it (:642). A row that does not actually name a
        container now leaves no destination at all, as the reference's failed cast
        does, so the following rows stop rather than spilling.

FIX - An ITEM row naming another TEMPLATE built nothing: the resolver reduced every
        row to an itemdef index and a template failed that, so a referenced sub-recipe
        yielded an empty box, or zero objects when it was the only row. CreateHeader
        accepts RES_TEMPLATE and reads it in the current container (:515 -> :555); the
        resolver keeps the resource TYPE now and recurses, with a depth bound so a
        recipe cycle ends in a bounded walk.

FIX - A decimal number in a resource position was read as hexadecimal, so "ITEM=4096"
        created 0x4096 - a different, perfectly valid item - instead of decimal 4096.
        Upstream reads it with Exp_GetDWVal (ResourceGetID_EatStr,
        CResourceHolder.cpp:102), where only a leading zero means hex. This is the
        same Sphere number rule the delayed-call and NEWDUPE parsers already follow.

CHANGE - SERV.NEWITEM on a TEMPLATE header now RUNS the recipe and hands back its own
        result - the first container it opened, or its last item - instead of building
        the first entry by hand and filling it. The spawner shares the same walk, so a
        template spawner gets the amounts, property lines and nested containers too.

[2026-09-07] - Script call chain, object factories and duplication (12Z/13A-13E)
----------------------------------------

FIX - CALL prepared nothing. It built a fresh args object that copied the caller's
        ARGN1/2/3 while replacing the argument string, so "CALL f_child 37" handed the
        child ARGN1=17 from its caller with ARGS=37, and an argument-less "CALL f_child"
        blanked ARGS instead of passing it on. Upstream Execute_Call
        (CScriptObj.cpp:1505) uses the CALLER'S OWN args object: with an argument it
        saves the numbers, the object and the string, re-Inits from the new argument and
        restores them on return; without one it passes the object through untouched.
        Both forms are now that. A plain function verb line prepares ARGN from ITS OWN
        argument instead (CScriptTriggerArgs::Init, :112), which it also did not do.

FIX - CALL did not share the caller's LOCAL pool. In upstream the pool lives on the
        args object (m_VarsLocal) and Init does not clear it, so passing the same object
        is what makes CALL share locals both ways, while an ordinary function line -
        which builds a fresh args object (CObjBase.cpp:2138) - keeps its own. A helper
        called with CALL could not see the LOCAL its caller had just set, and what it
        wrote never came back.

FIX - CALL took its first word as a function name, so "CALL SRC.f_mark" and
        "CALL TOPOBJ.f_mark" looked for functions of that literal name, found none, and
        the line vanished. Upstream resolves the reference through r_GetRef, plus SRC
        via pSrc->GetChar, and calls the remaining name on the resolved object
        (CScriptObj.cpp:1515). The reference-head resolver is now shared with the item
        verb chain that already had one.

FIX - TRY had its own cut-down executor: the space form tried a native verb and the
        console bridge and stopped there, so "TRY TAG.FLAG 1" and "TRY f_mark" both did
        nothing while the same lines worked unwrapped. Upstream builds a CScript from
        the rest of the line and calls the target's full r_Verb, suppressing only the
        invalid-reference error (CObjBase.cpp:2899).

FIX - An ARGN assignment used a decimal-or-0x int parser, so "ARGN1=010" set ten
        instead of sixteen and "ARGN1=1+1" was rejected outright, leaving the previous
        value in place. Upstream assigns through GetArgVal, the full expression parser
        (CScriptTriggerArgs.cpp:313).

FIX - TRYSRC changed the target as well as the source. It sent only the source uid over
        the reference bridge, which treats a lone uid as the object to act ON, so
        "TRYSRC <player> TAG.TEST 1" tagged the player and left the item that ran the
        line untouched. Upstream resolves the uid to a source console and runs the verb
        on the current object: r_Verb(script, pNewSrc) (CObjBase.cpp:2917). The bridged
        console now also speaks for that character, so a verb needing its caller finds
        one.

FIX - SERV.NEWITEM refused a TEMPLATE header outright on a resource-type check, so a
        reward-bundle or multi-part item recipe returned 0 and created nothing.
        Upstream goes through CreateHeader, which accepts ITEMDEF and TEMPLATE alike
        and hands the latter to CreateTemplate (CItem.cpp:461/554): the first entry
        becomes the object the caller receives and, when it is a CONTAINER, the rows
        after it are created inside it (:628). Template expansion now has one
        implementation, shared with the spawner.

FIX - NEWITEM's character parent always went to the backpack, so an item created
        straight onto someone was never worn. Upstream's LoadSetContainer equips at the
        layer the definition declares and falls back to the pack only when it names none
        (CItem.cpp:2516).

FIX - NEWITEM split its argument into three fields, so a valid four-field call left the
        equip flag glued to the parent uid ("...,<uid>,1"), the uid failed to parse, and
        the item was created and then placed nowhere.

FIX - SERV.NEWNPC left the new creature at 0 hits and 0 mana: applying a definition
        sets the MAXIMA, and nothing filled the pools. Upstream reaches
        CreateNewCharCheck through NPC_LoadScript and fills hits, stamina and mana at
        creation (CCharNPC.cpp:30/265, CChar.cpp:1042). The GM add-NPC path already did.

FIX - NEW and NEW.<property> resolved through different fields - the first preferring
        the last ITEM, the second falling through to the character - so they could name
        different objects, and a script that had just created an NPC read back an
        earlier item. Worse, the emptiness test was "!= 0" while the empty value is
        0xFFFFFFFF, so NEW reported a uid before anything had been created. Both forms
        now read one reference, the port of g_World.m_uidNew (CScriptObj.cpp:1381);
        SERV.LASTNEWITEM / LASTNEWCHAR keep their per-type fields.

FIX - A factory wrote NEW as a side effect of object creation, so an @Create hook that
        made a second item left NEW pointing at that one instead of the item the caller
        asked for. NEWITEM now writes the reference at the END of its work, as upstream
        does.

FIX - Bare NEWITEM written on an object did not update that object's ACT; both it and
        the explicit SERV.NEWITEM went through the same server-only bridge. Upstream
        points the caller's ACT at what it created when the command was not addressed to
        the server (CScriptObj.cpp:1383).

FIX - NEW had a read path only: "NEW=<uid>" and "NEW.<property>=<value>" were silently
        ignored, so a script could not restore the reference it had saved or dress the
        object it had just made. Both are now wired, and an assignment naming nothing
        clears the reference (SSV_NEW, CScriptObj.cpp:1305).

FIX - A NEWDUPE whose source uid names nothing left the previous NEW standing, so a
        script checking NEW to see whether the copy worked read the old object and
        treated it as the new copy. Upstream clears m_uidNew and returns false (:1311).

FIX - NEWDUPE read its uid as hexadecimal unconditionally, so a decimal "10" addressed
        a different - and perfectly valid - object, and an item's decimal uid overflowed
        and copied nothing. It is a Sphere argument: only a leading zero means hex
        (GetArgVal -> CScript.cpp:154). DUPE's count had the mirror-image bug, read as a
        decimal int, so "1+1" silently fell back to one and "010" made ten copies
        instead of sixteen. Both now use one shared Sphere number parser, which also
        backs the delayed-call parser added in the previous wave.

FIX - A container copy came out empty. Duplication shared the stack-split helper, which
        deliberately copies only the object's own state, so a reward bag or stocked
        crate duplicated into a bare shell. Upstream's container override recreates
        every child as a copy of its own, sub-containers included
        (CItemContainer.cpp:830).

FIX - A duplicated spawner kept its TYPE and lost its behaviour: the type came across as
        a raw field, so no component was ever built and the copy sat inert. Upstream
        carries the spawn configuration - quota, interval, range, spawn id - and
        deliberately not the children already produced (CCSpawn::Copy, CCSpawn.cpp:1272).

FIX - A duplicated character came out naked and packless, and lost its fame, karma,
        resistances, status flags (concealment included) and EVENTS list; only a narrow
        field list and the tags survived. Upstream DupeFrom carries the status flags,
        the stat block, EVENTS and the entity properties, then duplicates every equipped
        layer onto the new character (CChar.cpp:1092/1194). The live wiring - account,
        client, uid - is deliberately not copied.

FIX - A duplicate was pushed into the source's container instead of being placed beside
        it. Upstream finishes DUPE with MoveNearObj(this,1), which walks up to the
        TOP-LEVEL object and uses its WORLD position (CItem.cpp:3631, CObjBase.cpp:498).
        Reading the source's own Position gave a container-local coordinate that became
        a wrong spot on the map, and a full container either swallowed the copy or made
        it disappear entirely.

FIX - A book's AUTHOR could be written but not read: the shared setter stored TAG.AUTHOR
        and returned before the book-specific setter could run, while the getter read
        TAG.BOOK_AUTHOR. Upstream keeps one m_sAuthor for both (CItemMessage.cpp:61/104).
        The setter now writes both spellings and the getter accepts either, so neither
        the bulletin-board path nor an existing save loses its data.

NOTE - The 12Z finding - work added from inside a callback joining the same tick
        depending on where its target sat in the active set - was already answered by
        the previous wave's due-order fix, which fixes the pass membership before any
        callback runs. It is now locked by tests across all four target states.

[2026-09-06] - Delayed work: due order, command splitting, verb ownership and 64-bit args (12X/12Y)
----------------------------------------

FIX - Due order was per-object. The tick walked the active set and drained each
        object's ready jobs before moving on, so a job due 10 seconds ago ran after one
        due 100ms ago purely because its owner came later in the set. Upstream keeps
        every timed object - each CTimedFunction included - in one time-sorted vector
        and ticks the selection in that order (CWorldTicker.cpp:1051 selection, :1129
        execution, :1214 the TIMEDFUNCTIONS branch). The ready jobs are now collected
        across all objects, sorted by due time with the collection order as the
        tie-break, and run from that single list. Each job is still removed immediately
        before it runs, so a save taken mid-callback still sees the ones that have not.

FIX - A delayed payload split on whitespace alone, so "=" and "," stayed glued to the
        function name and "TIMERF 0, f_capture=37" queued a function called
        "f_capture=37" that resolved to nothing. Upstream stores the raw payload and
        runs ParseKey over it when the timer fires (CTimedFunction.cpp:88,
        CScript.cpp:336), whose Str_Parse default separator set is "=, \t"
        (CExpression.cpp:144). Splitting now goes through one shared helper that honours
        those separators plus the quote and bracket rules, and only the first separator
        is structural so "f_a 1,2,3" keeps its argument string intact.

FIX - The same whitespace-only split existed on the classic load path, so a saved
        "TimerFCall=f_mark=37" was restored as a job that could never run. It now uses
        the same helper as the live path (CTimedFunctionHandler.cpp:185).

FIX - A delayed line that no verb and no script function owned did nothing. Upstream
        falls through to CScriptObj::r_Verb, whose default branch is r_LoadVal - a
        property assignment (CScriptObj.cpp:1481) - so "NAME after" and "TAG.FLAG after"
        now reach the field. The same step was missing on the TOPOBJ./CONT./LINK.
        reference chain.

FIX - A built-in verb that recognised the name and then declined fell through to a
        like-named script [FUNCTION], because a single bool conflated "no such verb"
        with "that verb refused". Upstream only reaches the function branch when the
        verb table lookup misses (CObjBase.cpp:2134), so a pack's [FUNCTION UNEQUIP]
        could pick up a line the engine had just refused. TryExecuteCommand now reports
        whether the NAME belongs to the verb table, cleared at the single fall-through
        every switch ends in, and an owned name's answer is final.

FIX - A TOPOBJ./CONT./LINK. chain resolved its target and then only tried a verb and a
        property there, so "TOPOBJ.f_mark 37" was swallowed - the reference resolved,
        the function never ran, and the line reported success. Upstream hands the
        remainder to the target's own r_Verb, function step included
        (CScriptObj.cpp:1217, CObjBase.cpp:2138); the chain now runs the full order on
        the resolved object.

FIX - A delayed verb needing the calling character refused whenever no client was
        attached, because UNEQUIP, SUMMONTO and CONTROL read the character off the
        client interface alone. Upstream runs a delayed call with the top-level
        CHARACTER as SRC and no connected-client requirement (CTimedFunction.cpp:43),
        reaching it through pSrc->GetChar() (CItem.cpp:3574). ITextConsole gained that
        accessor, the stand-in console for a clientless character now answers it, and
        the three verbs resolve through it - so an NPC's or an offline player's timed
        equipment work completes.

FIX - Script numeric arguments saturated at the 32-bit bounds, so 2147483648 reached a
        script as 2147483647. ARGN1/2/3 are int64 upstream (CScriptTriggerArgs.h:21);
        the transport is now 64-bit end to end and the engine's own int fields narrow
        at their own boundary, saturating rather than wrapping.

FIX - A hex argument containing a letter read as zero AND stranded the parse cursor, so
        "0A,2,3" delivered 0,0,0 and "01F,2,3" delivered 1,0,0. The scan advanced over
        ASCII digits only; upstream treats a leading '0' as the hex MARKER and then
        consumes [0-9 A-F a-f], with the significant-nibble width deciding the signed
        reinterpretation (CExpression.cpp:666). The decimal path also skips '.' grouping
        separators, as upstream does.

FIX - The 64-jobs-per-object cap dropped the 65th silently, and the classic loader
        counted the dropped job as restored, so a save holding more work than the cap
        lost it with no trace. AddTimerF now reports whether the job was accepted;
        upstream has no per-object limit (CTimedFunctionHandler.cpp:103), so restoring
        a save bypasses the cap - a save is authoritative - while the cap still guards
        live scheduling and a refusal is logged instead of ignored.

[2026-09-06] - Delayed work: classic saves, mid-callback saves and who it runs as (12V)
----------------------------------------

FIX - A classic [TIMERF] section was skipped on load, so every delayed job a
        Sphere/Source-X world save carried was lost while its objects loaded fine. The
        section is now read and each job re-attached to the object its UID names
        (CWorld.cpp:842, CTimedFunctionHandler.cpp:122); the leading number tells the
        dialect apart - 99 marks milliseconds, anything smaller is a 0.56 save counting
        tenths of a second (:185). SphereNet keeps its own jobs inside the object
        record, so this path only adds what that record could not carry.

FIX - The tick took every due job off the object before running the first, so a world
        save from inside the first callback recorded none of the ones still waiting and
        they could never be restored. Each job is now removed immediately before it
        runs, matching the reference, where a timer deletes itself inside its own tick
        (CTimedFunction.cpp:77) while the save walks whatever is still pending (:203).
        A job cancelled by an earlier callback in the same pass no longer runs.

FIX - SRC in a delayed function was the item itself. Upstream uses the target's
        top-level object when that is a character - the player carrying it - and the
        server otherwise (CTimedFunction.cpp:43). The line also ran on the server
        console regardless of owner, giving a player's item administrator privileges;
        it now runs as their client, or at their privilege level when they are offline.

FIX - A delayed function's argument only arrived as ARGS: ARGN1/2/3 stayed at zero, so
        "TIMERF 5, f_give 30" handed the script an amount of nothing. The argument goes
        through the normal preparation, which reads the leading numbers
        (CScriptTriggerArgs.cpp:112).

FIX - A script [FUNCTION] shadowed the engine verb of the same name in a delayed call,
        so a pack's own REMOVE ran instead of the engine's and the object survived. The
        built-in verb table is searched first and the function only answers for a name
        no verb owns (CObjBase::r_Verb, CObjBase.cpp:2134) - the same order a typed
        command follows.

[2026-09-06] - The decay contract, the drop event and delayed work (12S-12U)
----------------------------------------

FIX - A decay of under a second was rounded away to nothing by the save and the pending
        rot was forgotten on reload. The remaining time is now written in milliseconds
        (DECAYMS); DECAY is still written alongside it so a save read by an older build
        finds the field it expects.

FIX - Picking an item up left its rot running, so an item carried in a bag could vanish
        out of it. Upstream ends the decay the moment the object is lifted
        (CCharAct.cpp:3064).

FIX - Putting an item down armed a decay over the top of a script timer it was already
        running, destroying it well before the interval its script had chosen. The shared
        contract now stands aside for a live timer (SetDecayTime, CItem.cpp:1485).

FIX - @DropOn_Ground did not speak the reference's language: the drop point went out as
        ARGN1/2/3 and the interval as a local, where upstream passes the decay in TENTHS
        OF A SECOND as ARGN1 and the point as the string argument, and reads both back
        (CItem.cpp:1629). A Source-X script's choices reached nothing. LOCAL.DECAY is
        still read, in seconds, and wins when the script actually changed it, so packs
        written against the older reading keep working.
        BREAKING for a pack that read ARGN1 on this event as a coordinate.

FIX - A no-decay region vetoed whatever the script had chosen instead of shaping the
        NATURAL time before the script speaks (:1620).

FIX - An item the drop script deleted was still placed back on the ground, and a
        container the script had moved it into was overruled by the drop point
        (:1634/:1654).

FIX - RETURN 1 on @DropOn_Ground bounced the item back to the backpack. On this event it
        means the script took over after the item was placed - upstream updates it and
        returns success (:1660) - not the veto other events use.

FIX - A refused @Timer stopped the special item types from doing their own work, and an
        item running a millisecond timer was not destroyed when it ran out. Only the
        default path deletes, and it now sees both clocks (:6380/:6412).

FIX - TIMERF CLEAR and TIMERF STOP fell into the scheduler, which found no payload and
        returned quietly: cancelling delayed work was accepted and did nothing, and the
        jobs ran anyway. They are separated before anything is scheduled
        (CObjBase.cpp:2762), and STOP takes a name or a trailing-wildcard family.

NEW - ISTIMERF.<name> answers the milliseconds left before a delayed job runs, 0 when
        there is no such job (CObjBase.cpp:1499).

FIX - A TIMERF delay was read with a plain integer parse, so "2 f_a" (space-separated),
        "1+1" and the leading-zero hex form all became zero and ran at once. The delay
        now ends at the first comma OR space and is read as a Sphere number; one that
        cannot be read, or a negative one, is refused rather than scheduled for now.

FIX - Due delayed jobs ran in the order they were SCHEDULED, so after a stall a pair of
        ordered script steps came out backwards. They run in due order
        (CWorldTicker.cpp:1051).

FIX - A sector whose last communication crystal simply decayed went on reporting a
        listener for ever: the tick removed the item from the list directly and skipped
        the counter, and the later removal could not fix it because the object was no
        longer in the list.

[2026-09-06] - One item clock, one timer gate (12Q-12R)
----------------------------------------

FIX - The timer script had no say over anything the tick then did. Upstream runs @Timer
        FIRST and a RETURN 1 ends the tick before the components, the type behaviour and
        the decay deletion alike (CItem.cpp:6217); a RETURN 0 destroys the item (:6412).
        A bee hive could not be stopped from refilling and an ordinary item could not be
        asked to go.

FIX - An item whose timer ran out was destroyed even without ATTR_DECAY and without the
        script asking for it. Upstream keeps it in that case (:6412) - and TIMER grants
        no decay right of its own (CObjBase.cpp:1978), so giving a plain movable object a
        script timer no longer quietly makes it perishable.

FIX - TIMER=-1 turned off the script side and left the decay clock running, so the item
        rotted on schedule anyway. There is one timer upstream, not two, and the off
        switch turns off all of it.

FIX - A script that kept a decaying item with RETURN 1 had its own chosen interval
        overwritten with a fresh default decay window. Upstream assigns no replacement
        (:6222); the two clocks now agree on what the script asked for.

FIX - A corpse never reached the timer trigger at all - its decay branch ran first - so a
        script could not hold a body for a quest or an event. The common gate comes before
        the corpse handling (:6247).

FIX - A timer that had come due but had not yet been ticked was dropped by the save, so
        "expired and pending" and "no timer" became the same record and the pending script
        work was silently cancelled on restart. A set timer is written even at zero
        (CObjBase.cpp:2081 -> CTimedObject.cpp:123).

FIX - TIMERMS accepted -1 and 0 and then ignored them, leaving the old interval in place.
        Upstream passes the value straight to the one timeout setter, where a negative
        clears the timer and zero schedules it for now (:2040 -> CTimedObject.cpp:57).

FIX - TIMERD was not understood at all, so a Source-X script writing tenths of a second
        set nothing (:2033).

FIX - Splitting a stack left the remaining piece without the script timer the original
        carried; DupeCopy takes the remaining timeout with it (CItem.cpp:4099).

FIX - A timer armed while an item lay on the ground stopped running the moment it went
        into a bag, while the same timer set after the move worked - the tracking list is
        only joined when the timer is set. An armed timer now follows the item.

[2026-09-06] - Leaving a spawner: taming, deletion and the events a spawner fires (12N-12P)
----------------------------------------

FIX - Taming a creature left it on its spawner's books, so the old spawn point still
        counted the pet against its quota AND destroyed it on the next stop. Upstream
        clears the home and calls DelObj as ownership changes hands (NPC_PetSetOwner,
        CCharNPCPet.cpp:614). Re-assigning the same owner still changes nothing.

FIX - A spawner whose only creature died during a world save was left empty and parked:
        the save's own dead-member sweep removed it without re-opening the schedule, and
        no ordinary tick ever restarted it. Losing a member re-arms the timer wherever
        the loss is noticed (CCSpawn.cpp:509).

FIX - A spawned item kept the ATTR_OWNED and ATTR_MOVE_ALWAYS its definition or its
        create script had set. Upstream strips exactly those two, and nothing else,
        before the spawn trigger runs (:340).

FIX - A group member's weight of zero was raised to one, so "disabled" and "rare" were
        the same setting; a zero weight takes a member out of the draw
        (CRandGroupDef.cpp:229).

FIX - A group member written as a numeric chardef - ID=0200,9 - was mistaken for a
        weight, and the group then looked for a creature called "9" and spawned nothing.
        Which half is the weight is decided by the second one (:79), and a numeric member
        resolves as the resource index it is.

FIX - A template's CONTAINER row produced the box and dropped everything the recipe put
        in it. The ITEM rows that follow a CONTAINER go inside it (CItem.cpp:628/642).

FIX - A template-targeted item spawner came back from a restart as an itemdef spawner
        and produced nothing: the saved target was taken as a bare index, losing which
        KIND of resource it named. The target is resolved by name again on load
        (CCSpawn.cpp:943).

FIX - Enrolling an existing creature or item through a live ADDOBJ never fired @AddObj,
        so a script's own bookkeeping did not run for it (:648).

FIX - A creature handed to a new spawner kept the home and wander distance of the old
        one, so it behaved as though it still lived where it used to. AddObj sets both
        from the new spawn point (:631). A refused enrolment leaves them untouched.

FIX - After a creature was released, asking it which spawner owned it still named the
        old one: the membership was cleared but the link was not (:542, CObjBase.cpp:1608).

FIX - @DelObj was handed the child as its object and either nothing or a definition
        index as ARGN1, and whatever it wrote back was discarded. Upstream hands it the
        SPAWN POINT and the remaining timer in seconds, and re-arms the spawner from what
        the script leaves there (:568).

FIX - The general creature-create event ran before the creature was placed or attached,
        so a script reading its position or its spawner saw neither. Upstream runs the
        definition's own script early and the general chain after placement and
        attachment (CCharNPC.cpp:296, CCSpawn.cpp:466).

FIX - Filling a spawner's last slot did not park its timer when the member arrived
        through a live enrolment or an item generation, so a full spawner stayed
        scheduled. The pause happens before the add trigger, which may then choose its
        own interval (:643/:648).

[2026-09-06] - Spawn groups, membership rules and what a spawner remembers (12J-12M)
----------------------------------------

FIX - A spawn group's weights were read backwards. Source-X writes the member as
        ID=<resource>,<weight> (CRandGroupDef.cpp:79), so the weight on a normal entry
        was dropped and a 9:1 group became 1:1. The WEIGHT key, which restates the
        weight of the member above it (:95), was ignored altogether. The reversed
        "weight,resource" order a pack may have been written against is still accepted.

FIX - @PreSpawn chooses WHAT is spawned by rewriting ARGN1, and the component already
        reads that back - but the server's trigger bridge built its own arguments and
        dropped the answer, so a handler picking a creature or item by region or event
        was ignored (CCSpawn.cpp:310/387).

FIX - PILE set an amount on objects that do not stack, leaving a single item carrying a
        quantity every other rule denies it. Upstream gates it on the type being
        stackable (:329).

FIX - A numeric spawn id was masked to 16 bits, so the same definition resolved to two
        different targets depending on whether it was written as a name or a number - a
        chardef at 0x12345 became 0x2345. A resource index is 20 bits
        (CResourceID.h:112).

FIX - A spawner's PILE and its stopped state were never written to the save, so a
        staff-set pile size went back to one and a spawner that had been switched off
        came back running on the next restart. Upstream writes both (:1119/:1233).

FIX - AMOUNT on a spawner is its CAPACITY and upstream routes it to the component
        (:938). Writing only the item field meant raising a spawner's capacity looked
        accepted and produced nothing extra.

FIX - RESET reported success on every item type while only ever acting on a custom
        house, so a spawner's RESET left every creature it had standing. It is claimed
        by the house branch only when the item really is one.

FIX - A stopped item spawner still produced on an ordinary tick: STOP parks the timer at
        -1, which any "is it due" comparison reads as long overdue, so the stopped flag
        has to be tested as well.

FIX - Handing a creature to a second spawner left it in both member lists, and the
        FIRST spawner's teardown then destroyed what the script thought it had moved.
        Upstream releases it from the previous owner before linking it (:621).

FIX - A TEMPLATE target was accepted and then handed to the itemdef path, which found
        nothing, so a spawner aimed at a template produced no items at all. The kind of
        resource is remembered now and the template is expanded (CItem.cpp:555).

FIX - A @DelObj script that removed members while a teardown was walking the list threw
        out of the middle of it and left the internal guard stuck, after which nothing
        fired @DelObj again. The sweep walks a snapshot, always releases the guard, and
        a re-entrant DelObj stands aside as upstream's does (:512).

FIX - An item spawner near the map edge skipped whole cycles: a scatter that landed off
        the map deleted the item instead of falling back to the spawner's own square
        (:353).

FIX - A live ADDOBJ accepted anything: a player could be enrolled as a spawn child -
        whom the next STOP would then delete - and the member count could run past the
        configured capacity. The live path applies the checks upstream applies
        (:585); the unchecked form stays where it belongs, in load-time relinking.

FIX - The startup pass counted a save's ADDOBJ lines into the capacity, so stale or
        duplicated membership entries - which need not resolve to anything - permanently
        widened a spawner, and every restart could widen it again. AMOUNT is the
        capacity; ADDOBJ is the membership.

FIX - MORE2 on a char spawner is the CURRENT member count, read-only upstream - its
        setter refuses and points at ADDOBJ (:1042). Treating it as the capacity let an
        old save's population counter widen a spawner configured for one.

FIX - The separate TIMELO / TIMEHI / MAXDIST fields a save writes alongside MOREP were
        re-applied on exactly one path, a SPAWNID char spawner, so every other shape
        came up on engine defaults with the values plainly in the file (:1124).

[2026-09-06] - Spawners and champions: what a script may retarget, and what may be destroyed (12E-12I)
----------------------------------------

FIX - DELOBJ destroyed the creature instead of releasing it, and did it without ever
        checking that the creature belonged to the spawner. Releasing an NPC killed it,
        and a stale or mistyped uid killed somebody else's NPC or a player's character.
        Upstream checks membership first and then only unlinks (CCSpawn.cpp:509).

FIX - Writing SPAWNID on a running spawner stored a tag and nothing else, so a retarget
        looked accepted while the spawner went on producing the old creature or item.
        Writing ADDOBJ was the same: the link waited for the next re-initialisation, so
        the spawner did not count the NPC it had just been handed and spawned another
        one over its own quota (:585/:933/:943).

FIX - Naming a single CHARDEF left an earlier spawn group in place, and the group still
        won at spawn time. There is one effective target upstream (:943).

FIX - A spawner inside a container still produced objects into the world when something
        gave it a tick. Neither generator starts unless the spawner is top level
        (:299/:383).

FIX - An item placed by @Spawn was dragged straight back to the spawner, because the
        default position was applied unconditionally; upstream only picks a spot when
        the trigger left the object without a valid one (:353). The definition's name
        was likewise written over whatever the item's own @Create had chosen (:323).

FIX - Item spawners were second-class in five more places: their members were never
        written to the save (so every restart left the old items lying about and topped
        the quota up again), MOREP's interval and scatter range were ignored, a manual
        TIMER did not reach them, STOP, START, RESET and DELOBJ were refused outright,
        and deleting the spawner left its items in the world. Upstream has ONE component
        for both kinds, with no exclusions (:76/:673/:1064/:1094/:1233).

FIX - A spawner record carrying SPAWNID was converted to a char spawner at startup even
        when its type already said item: upstream writes SPAWNID for item spawners too
        (:1105), so a Source-X item-spawner record was rebuilt as the wrong thing.

FIX - Reading a char spawner's MOREP wrote the component's stale range back into the
        item mid-way through, so the item and the component disagreed and the next
        re-initialisation restored the old wander range.

FIX - @AddObj was handed a definition index where upstream hands it the spawner's
        remaining timer in seconds, and nothing was read back, so a script setting the
        next interval from it was ignored (:648). The trigger bridge now copies the
        modified numbers back out.

FIX - Clearing a champion's NPCGROUP override deleted the definition's own wave with it
        and the level stopped spawning; the override and the definition are separate
        lists, and clearing one falls back to the other (CCChampion.cpp:277/1014).

FIX - Loading a different CHAMPION definition kept the previous one's boss, so an event
        switched to a definition that declares no CHAMPIONID still summoned the old
        boss. A definition is a whole new configuration (:146/:1218).

FIX - A boss removed with .nuke or a script REMOVE left its uid behind: the event never
        produced another one and never completed. A deleted spawn member now reaches its
        spawner, and a champion treats a uid that resolves to nothing as no boss at all
        (CObjBase.cpp:147 -> CCChampion.cpp:730).

FIX - Asking to link a candle by uid quietly created a different candle when the uid
        could not be resolved, so the link the caller asked for was lost (:360).
        CHAMPIONSUMMONED could be read but never written, so a script could not mark or
        correct the event's boss (:1046).

FIX - Assigning LEVEL re-ran the whole level transition: @Level fired, the next-level
        threshold grew again, and assigning the final level wiped the wave and summoned
        the boss. Upstream's LEVEL key assigns the field (:1010); the deliberate advance
        is available as SETLEVEL.

FIX - A @Stop script written to stop staff closing an event also stopped the event
        finishing itself - @Complete ran while the champion stayed active forever.
        @Stop belongs to a REQUESTED stop; the automatic teardown after the boss dies
        passes no source (:185/:216).

FIX - The candle-removal veto ran for a candle that was no longer in the world, so a
        protective script could keep a dead uid in the list and the count stopped
        matching the ground (:657).

FIX - A champion's state fields out of a classic Source-X save were dropped: they arrive
        as plain properties before the component exists, so an event that was mid-run
        came back inactive at level one. They are parked on the item and applied when
        the component is built.

FIX - LASTACTIVATIONTIME was a UTC second count where upstream stamps the game clock in
        milliseconds, so a script comparing it against game time got a meaningless
        interval (:168).

[2026-09-06] - The world clock, the environment on arrival, and the champion event (12B-12D)
----------------------------------------

FIX - Logging in and resyncing sent the light and the season and nothing about the
        weather, so a player who connected after the rain started stood under clear
        skies until something else happened to correct it. Upstream sends the weather
        alongside the light on resync (addReSync, CClientMsg.cpp:2107).

FIX - Walking off the edge of every defined region published nothing at all, so the
        last region's rain stayed drawn over ground that has none. Upstream resolves
        weather from the sector, which exists everywhere on the map (:529); only the
        region-specific announcements are skipped now.

FIX - MAP.n.SECTOR.i resolved the linear index against a hardcoded 96 columns, which is
        only right for a 6144-wide map. On any other size it addressed the wrong row,
        and past the first row a sector that does not exist. It is resolved against that
        map's own grid (CWorldMap.cpp:229).

FIX - MAPLIST assumed map 0 was the only map and answered fixed 6144x4096 metadata, so
        a shard on a second facet or a custom-sized map read a world that is not there.
        It answers from the maps actually loaded and their real sizes and sector grids
        (CServerConfig.cpp:1710).

FIX - An ordinary weather change reached only characters with a client, so an NPC whose
        script watches the weather was never told it started raining. Upstream runs
        @EnvironChange for every active character and gates only the packet on having a
        client (CSector.cpp:1310).

FIX - Saving and loading reset the game clock to midnight, taking the day/night cycle
        and both moon phases with it: the header's TIME field is the real-world date the
        save was taken, and nothing carried the game clock. It is written and restored
        under its own field, the way upstream keeps TIMEHIRES (CWorld.cpp:1510/1625). A
        save without it still loads.

FIX - A late tick moved the world clock on by exactly one minute and then snapped its
        mark to now, throwing away every further whole minute and the leftover
        milliseconds - so a long save or a stalled main loop left the world running
        behind for good. It advances by the time that really passed and keeps the
        remainder (CWorldClock.cpp:31).

FIX - Starting a champion whose definition was not linked threw out of the monster-count
        clamp and left the event half-armed: active, with nothing running. The empty list
        is answered before the index is clamped, as upstream does (CCChampion.cpp:640).

FIX - A red candle counted itself towards the level threshold, so the level, the wave
        change and @Level all fired one candle early. The threshold is measured against
        the candles already standing (:441).

FIX - New candles could still be created after the boss was out, rebuilding progress
        markers the reference closes off (:346/:443). Re-linking a candle read back from
        a save is a separate path and still works at the final level.

FIX - Champion candles were placed at the altar's own Z instead of four above it, and
        never ran their own ITEMDEF @Create - upstream lifts each candle and calls
        GenerateScript on it (:409/:411/:529/:532).

FIX - A successful boss spawn skipped the counter update every other spawn gets, so
        SPAWNSCUR and KILLSNEXTWHITE stayed one short for good once the boss appeared
        (:337).

FIX - Deleting a champion altar left its candles standing in the world, ownerless and
        immovable: only the STOP path cleaned up. Upstream clears them from the
        component's destructor, so it happens however the altar dies (:92).

FIX - A live LEVELMAX or SPAWNSMAX change was kept only in memory and the definition's
        values were read back over it on the next load, and the state snapshot was taken
        only when a candle or a kill happened to trigger one. Both fields are persisted
        and every live write saves on the spot (r_Write, :794).

[2026-09-06] - Map facets, board bodies and the weather a sector actually has (11C-12A)
----------------------------------------

FIX - A board message loaded from a classic save showed its author but no text: the
        save's BODY lines land in the shared page storage a book uses, while the board
        read only its own BODY_n tags. Upstream keeps a board message and a book in ONE
        page list (CItemMessage.cpp:53) and the full-message packet reads it with
        GetPageText (send.cpp:2222). A message posted in game now writes that same
        storage, so it reads back through the script BODY surface too; boards written by
        an earlier save are still read.

FIX - The map window went out as the old 0x90 to every client, so a modern one was never
        told which world the map depicts and two maps of the same coordinates on
        different facets were indistinguishable on the wire. A client at 7.0.13.0 or an
        enhanced one gets 0xF5, which carries the facet (send.cpp:5358); older clients
        keep 0x90.

FIX - A map's own OVERRIDE.MAPWIDTH / OVERRIDE.MAPHEIGHT were never read, so every map
        was drawn at the 200x200 default. The modern packet applies them, with height
        before width as upstream sends it (:5363); the old packet reads no override, and
        that difference is kept.

FIX - The map gump was queued at a lower priority than the pin packets that follow it,
        so it drained AFTER its own pins and the client was told to plot on a window it
        did not have yet. Same class of reorder as the vendor price list; the map now
        shares a queue with its pins.

FIX - A book page declaring more lines than the server stores left the extra lines in
        the buffer, and the next page header was read out of them: a 65-line page turned
        two bytes into "page 30720" and silently dropped the real second page of the same
        request. Every declared line is consumed now, and a page declaring more than
        upstream tolerates ends the request (receive.cpp:1032).

FIX - A read-only book was opened announcing itself as writable, because the flag came
        from the item type rather than the book. The client offered editing and the
        server then refused every page, so the writer's text vanished on reopen.

FIX - The DRY and RAIN sector commands wrote 0 and 1, but WEATHER_TYPE is 255 dry, 0
        rain, 1 storm, 2 snow (sphereproto.h:514) - the engine's own enum already used
        those codes. A script comparing WEATHER numerically read dry as rain and rain as
        storm. Saved sectors keep their meaning: the new numbers are written under a new
        key and the old key is still read on its old scale.

FIX - Setting a sector's WEATHER or SEASON changed a field nothing published, so a local
        snowfall or season set by staff or script never reached the players standing in
        it. Both now reach every character in the sector and fire @EnvironChange, as
        upstream does (CSector.cpp:879/904).

FIX - A sector light set by hand was stored but excluded from the light calculation, so
        it read back correctly as a property while everyone in the sector still saw the
        time of day - and the next refresh walked it away. A pinned light is marked and
        wins over the clock, the moons and the dungeon table (:684/818), until it is
        cleared; sphere.ini AllowLightOverride turns the whole mechanism off.

FIX - RAINCHANCE and COLDCHANCE were accepted and stored and then never read: weather
        generation used one flat 5-in-1000 roll and a season table, so a region prepared
        as rainless still rained and a cold coast never saw snow out of season. The
        sector's own climate decides, as GetWeatherCalc does (:857).

FIX - Weather was keyed by a region's visible NAME, so two Britains on two facets shared
        one sky and renaming a region orphaned its weather. It is keyed by the region's
        own identity now, and the broadcast picks its listeners the same way.

[2026-09-06] - Books, maps and bulletin boards: the window, and who may edit it (11A-11B)
----------------------------------------

FIX - The book window packet was built as a variable-length packet with a length
        header and length-prefixed strings, which is the shape of the newer 0xD4
        header. Opcode 0x93 is a fixed 99 bytes with 60- and 30-byte string fields
        (send.cpp:2871), and that is what the client reads for it, so every field
        after the uid was landing at the wrong offset. The incoming 0x93 handler was
        already reading the fixed layout, so the two disagreed with each other.

FIX - A writable book was opened offering 16 pages while the page handler accepted
        writes up to 64, so text written past page 16 was in the book but outside
        the range the window ever asked for. Upstream offers a writable book
        MAX_BOOK_PAGES and a finished one exactly the pages it holds (:2919), and
        refuses a write past that ceiling (receive.cpp:1039). A script-declared
        BOOK_PAGES still stands in for a system book's resource page count.

FIX - Book page edits ran on any item at any distance: a window left open after the
        book was carried away still wrote it, and the same call wrote page tags onto
        whatever else the serial named. Reading a page needs to be able to see the
        book (receive.cpp:1002) and writing one needs a real book that is writable
        (:1017); retitling needs to reach it (CClientEvent.cpp:165).

FIX - A book's title and its name drifted apart: the title change wrote only a tag,
        so the book window and the item name showed different text and NAME readers
        saw the old one. Upstream a book's TITLE *is* its name (CItemMessage.cpp:73).
        The tag is still written and still wins on read, so a legacy book keeps its
        title.

FIX - Pages added through BODY were stored one below the first page anything reads,
        so a book built from BODY lines was missing its first page everywhere it was
        shown. BODY is zero-based upstream and the client's page N is page N-1 of the
        list (:53, send.cpp:1709). A book already carrying an orphaned page is lifted
        into place the first time it is read or written.

FIX - Map pin edits ran on any item at any distance, the same way. A pin edit needs
        a real map within reach (receive.cpp:867).

FIX - Pins glued in place through MOREZ could be added, moved and cleared by any
        player. Upstream says so and then refuses everyone but staff (:875).

FIX - Opening a map told the client that plotting was off but left the server's own
        flag on, so the first toggle afterwards answered "off" again and a map that
        had been edited before needed two clicks to start editing. addMapMode sets
        both from the same value (CClientMsg.cpp:2542).

FIX - Bulletin board reads and deletes ran with no visibility check at all, so a
        window left open after walking away still read messages and deleted the
        player's own. Every sub-command is gated on seeing the board
        (receive.cpp:1193).

FIX - A newly posted notice was not fixed in place, so every rule that asks the
        object itself was told it could be carried away. Upstream sets ATTR_MOVE_NEVER
        on it as it is created (:1252).

FIX - A classic save's pins were dropped on load: it writes one bare PIN= line per
        pin (CItemMap.cpp:100) and only the indexed PIN.n form was understood, so
        every map out of an old save came back with an empty pin list.

[2026-09-06] - Ships: registering one, owning one, and what a turn may refuse (10C-10D)
----------------------------------------

FIX - A ship out of a classic Source-X save was never registered with the engine. Its
        owner is kept in the plain OWNER field (CItemMulti.cpp:2572, CItemShip.cpp:118),
        and only the native SHIP.OWNER tag was read - so the hull loaded as a bare item
        with no region, no helm and invisible to every ship lookup.

FIX - Writing OWNER on a ship that is already running only set the tag, so the transfer
        looked successful while the old owner kept the authority to redeed it. The
        reference routes the write through SetOwner (CItemMulti.cpp:3068).

FIX - A deleted plank kept its place in the ship's plank list, so scripts read a count
        that included it and PLANK.0 came back empty while a good second plank sat
        behind it. Source-X rebuilds the list from the components that are actually
        there before answering either question (CItemShip.cpp:285).

FIX - @Ship_Move fired once per tile with no direction at all, so a three-tile order
        looked like three separate moves. The reference reports one event per completed
        move command with the direction in ARGN1 (CCMultiMovable.cpp:863) - and none at
        all for an order an obstruction cut short, which returns one branch earlier.

FIX - A hull sitting at a negative Z was announced to the client with its Z clamped to
        zero, so it disagreed with the very objects it was carrying, which were sent
        unclamped. The reference sends the signed value (send.cpp:5414).

FIX - MULTIDEF REGIONFLAGS never reached the region a placed multi realizes: a hull or
        a house declared Safe or NoBuild in its definition lost it the moment it was
        placed, and a ship lost it again on every step and every turn. The reference
        starts the region's flags from the definition's own (CItemMulti.cpp:232).

FIX - A turn was refused by obstacles inside the water the ship was already floating
        on, so a static laid under a moored hull could lock a turn that needs no new
        ground. Source-X skips every point already inside the ship's region and checks
        only what the turn newly claims (:545).

FIX - An item on a NEIGHBOURING tile refused a turn the hull geometrically fits: the
        placement check searched a one-tile radius and never checked the coordinate
        back, though the character loop beside it already did. The reference asks about
        the one cell (CanMoveTo, :481).

FIX - SHIPGATE demanded sailable water at the destination. Upstream resolves the point,
        turns it into a delta and hands it to MoveDelta with no such check (:1041); the
        coordinate validity check and the region veto still apply. Ordinary sailing is
        unaffected and still needs water.

FIX - SHIPFACE rounded a diagonal to a neighbouring heading instead of refusing it, so
        a script asking to face SE silently swung the ship east. A hull has four sides
        and the reference accepts only those (:510). Eight-direction movement is
        untouched.

[2026-09-06] - Ships: what the deck carries, and what a plank and a pilot are (10A-10B)
----------------------------------------

FIX - A ship carried away fixed world decor. Source-X excludes ATTR_STATIC items from
        what a multi takes with it (ListObjs, CCMultiMovable.cpp:200) - and only that
        flag: a MOVE_NEVER crate is still carried, so the two are not interchangeable.

FIX - Who counts as aboard was measured from the hull's anchor with a fixed window, so
        anything in the footprint but UNDER the deck was dragged along, and a taller
        ship would have left its own passengers behind. The reference measures from the
        deck plane - shipZ + max(3, height) - and takes what sits from two below it to a
        player's height above (:142).

FIX - On a hull whose definition is not centred on its anchor, passengers and cargo out
        on the far deck fell outside the search and were left behind by a move or a
        turn. The radius reaches the farthest edge from the anchor, not half the width
        (GetDistanceMax, CItemBase.cpp:1958).

FIX - @Ship_Turn reached only the hull, and with no directions at all. Source-X runs it
        for every item it moved - hull, components and loose deck cargo - with the new
        facing in ARGN1 and the previous one in ARGN2 (:628).

FIX - A magic ship could be flown into the space the world reserves at its ceiling and
        floor. The reference refuses the move before anything is moved (MoveDelta,
        :284).

FIX - Double-clicking an open plank decided what to do by comparing coordinates, so a
        passenger trying to shut the hatch was walked onto it instead - and anyone
        standing on a LOCKED plank could close it without the key. The reference asks
        whether the user is inside the ship's region and keeps the key check
        (IT_SHIP_PLANK, CCharUse.cpp:1810).

FIX - Stepping aboard from the dock left a hidden player hidden. The reference boards
        through a teleport, which reveals (:1827 -> CCharSpell.cpp:232 ->
        CCharAct.cpp:3491).

FIX - Turning a ship stamped the new heading's CLOSED graphic over an open plank, and
        the graphic it remembered to return to was still the old heading's - so closing
        it afterwards showed a piece of the wrong side. Source-X re-picks the new side's
        open art for a plank that is standing open (Face, CCMultiMovable.cpp:614).

FIX - Taking the wheel was only recorded in the ship's own field, so anything reading
        the pilot off the character saw nobody there. Source-X equips the pilot with a
        ship-pilot item linked back to the ship, and removes it when they stand down
        (SetPilot, :212).

FIX - Turning a ship back into its deed ran no @Redeed at all: only the house path was
        wired. The reference fires it on the OLD multi with the new deed as its object
        argument and the deed's graphic in ARGN1 (CItemMulti.cpp:1225). As upstream, it
        is a notification and does not veto the redeed.

[2026-09-06] - Party and guild state that has to add up (09C-09D)
----------------------------------------

FIX - @PartyDisband was fired after the party had already been taken apart, on every
        former member, with nothing left to describe and no way to refuse. Source-X asks
        it at the top of Disband, on the member left holding the party, and RETURN 1
        stops that stage (CParty.cpp:375).

FIX - A disconnect changed the party in memory and told nobody: the members still
        connected went on showing a member - or a leader - who had gone. Source-X takes
        the ordinary removal path when a character disconnects (SetDisconnected,
        CChar.cpp:528 -> :296), which sends the new member list and clears the map pins.

FIX - The script party-add commands did not check who they were adding. The ordinary
        add put someone into a second party without leaving the first, and the forced
        one accepted a uid belonging to nobody, which then took up a place. Both resolve
        the uid to a real character upstream, and the ordinary add refuses someone who
        already belongs to a party (CParty.cpp:767/:443).

FIX - Changing the party leader left the member list alone, so PARTY.MASTER and
        PARTY.MEMBER.0 named two different people. Source-X moves the new leader to the
        front of the list (SetMaster, CParty.cpp:62), which is what indexed access and
        the member packets read.

FIX - A guild's MEMBER.COUNT.n counted every role AT OR ABOVE the one asked for, so a
        candidate tally included members and masters and every script count came out
        inflated. The reference compares the role exactly (CItemStone.cpp:461).

FIX - A guild member leaving promoted whoever came next in the list instead of holding
        an election, so the leadership could pass to someone the remaining votes did not
        support. Source-X elects on every membership removal (CStoneMember.cpp:400 ->
        ElectMaster, CItemStone.cpp:1135).

FIX - A guild whose last full member left kept its wars, on both sides. The reference
        forces peace on a stone with no members (CItemStone.cpp:1226 -> WeDeclarePeace,
        :1494).

FIX - Deleting a guild stone left the guild running: membership questions still
        answered, wars still standing. The records live and die with the stone upstream
        (CItemStone.cpp:30).

FIX - A disbanded guild came back at the next world load. Disbanding removed the
        manager's record but left the stone's GUILD.NAME / GUILD.MEMBERS tags, which
        rebuilt it - master, membership and all.

FIX - A member title containing a backslash was mangled on load: ':' and ',' were
        escaped but the escape character itself was not, so "Ranger\camp" came back as
        "Ranger:amp". The encoding is lossless now, and an older save's unescaped
        backslash is kept rather than eaten.

FIX - A guild's abbreviation and charter were silently trimmed to four and two hundred
        characters WHEN LOADED, so text that had been accepted and stored in full shrank
        every time the world was loaded. What the write side accepts, the read side now
        keeps.

[2026-09-06] - Party invitations and membership (09B)
----------------------------------------

FIX - An accept or decline settled whichever invitation had arrived LAST, not the one
        the client named. Two invitations in flight meant a late answer to the first
        joined the second inviter's party, and a decline went to the wrong person. The
        protocol carries the inviter's uid and Source-X settles that invitation
        (receive.cpp:2708 -> AcceptEvent, CParty.cpp:443); an answer to an invitation
        that is no longer pending now settles nothing and consumes nothing.

FIX - Nothing re-checked, at the moment of acceptance, that the inviter could still see
        the player accepting - two hundred tiles apart still joined. The reference asks
        CanSee there (:457).

FIX - PARTY_AUTODECLINEINVITE did nothing: neither invitation path read it, so a player
        who had turned invitations off still received them. Source-X leaves before the
        invitation is sent (CClientTarg.cpp:2455).

FIX - @PartyAdd never ran. Source-X fires it on the character joining, before any
        membership changes, and RETURN 1 stops the join (CParty.cpp:481) - the
        conditions between invitation and acceptance are re-judged there.

FIX - Removing a member ignored @PartyRemove's answer and never ran @PartyLeave at all,
        so a script could not keep anyone in the party. Both stages refuse the removal
        upstream (RemoveMember, CParty.cpp:315).

FIX - A leader leaving through the standard client command handed the leadership on
        instead of disbanding. RemoveMember's fDisband defaults to true and the packet
        handler does not override it (receive.cpp:2673; CParty.h:92), so the party ends
        with its disband notifications; passing the leadership on is a separate,
        explicitly requested move.

[2026-09-06] - Chat channels: switching, passwords, voice and the list (09A)
----------------------------------------

FIX - A refused channel switch cost the speaker the channel they were in. The old
        channel was left before the new one was checked, so a wrong password left them
        in no channel at all - and a one-member channel was destroyed on the way out,
        password and moderators with it. Source-X checks the destination first
        (JoinChannel, CChat.cpp:70).

FIX - The standard join command could not carry a password. It is
        <c>"Name" password</c> - the name inside the quotes, the password after them
        (CChat.cpp:169) - while creating a channel uses <c>Name{password}</c>. Both
        commands went through the create-shaped parser, which threw the tail away, so
        the right password never reached the engine.

FIX - Creating and joining were the same operation. Joining a channel that no longer
        exists quietly recreated it, and creating one that someone else already owned
        walked into their conversation instead of reporting the name as taken. Source-X
        keeps CreateChannel and JoinChannel apart (:12/:70).

FIX - Turning a channel's default voice off silenced everyone already talking in it.
        The channel default decides what a member STARTS with; whether they may speak is
        their own record (HasVoice, CChatChannel.cpp:272). Someone arriving after the
        change still starts silent, and an individual mute still silences.

FIX - A channel founder's moderation could not be revoked: it was read off the owner
        field, so the call succeeded and changed nothing. Source-X seats the creator in
        the moderator list like anyone else, and RevokeModerator removes them (:517).

FIX - Channel-list changes were never announced outside the channel itself: a new
        channel did not appear in anyone else's list, and a rename left everyone else
        looking at the old name. Source-X announces a create globally (CChat.cpp:48) and
        a rename as a global remove-and-add (CChatChannel.cpp:138).

[2026-09-06] - Eating, practising, traps and shrines (08D)
----------------------------------------

FIX - A full player still lost the food. The meal engine already answered that nothing
        was eaten; the double-click ignored it and took the unit anyway, deleting the
        last one outright. Source-X leaves Use_EatQty before it consumes anything when
        there is no room left (CCharUse.cpp:889).

FIX - Grain and grass were treated as inexhaustible fixtures: the last unit was never
        consumed, and a locked-down one still fed whoever clicked it. They are food, and
        go down the same Use_Eat path as anything else (:1844) - it is the move rule
        that protects a placed trough, not an exemption from being eaten.

FIX - A new game board opened empty. Source-X sets the pieces out first (Game_Create,
        CItemContainer.cpp:1123): 32 for chess, 24 for checkers, 30 for backgammon, at
        the container coordinates the client draws them on, and nothing for any other
        game type. A board with a game already on it is left alone.

FIX - A training dummy started no training at all: the double-click played a sound and
        handed the dummy to the generic skill pipeline, and it chose the skill from
        whatever filled the two-handed layer rather than from the weapon. Source-X wants
        it on the ground within a tile, refuses a mounted or ranged-armed trainee and
        anyone past the practice cap, then swings - the dummy spins for three seconds and
        the weapon's own skill is credited (Use_Train_Dummy, CCharUse.cpp:337).

FIX - A sprung trap wrote its damage straight into the victim's hit points, so @GetHit
        could neither refuse it nor change it. The reference springs a trap through
        OnTakeDamage with blunt/general damage (:1753), which is what carries the hook
        and the resistances.

FIX - A shrine resurrection skipped @SpellEffect. Source-X resurrects THROUGH the
        Resurrection spell with the shrine as its source (CClientUse.cpp:327), so a
        script watching spell effects can refuse it before anything happens.

[2026-09-06] - Practising, drinking and linking (08C)
----------------------------------------

FIX - A pickpocket dip could be STOLEN by practising on it. The double-click was routed
        into the ordinary Stealing skill, which on success carried the dip itself into
        the thief's pack - a fixed one included. Source-X treats it as a training aid:
        on the ground within a tile, not while mounted, not past the practice cap, rolled
        against and left exactly where it stands (Use_Train_PickPocketDip,
        CCharUse.cpp:397).

FIX - An archery butte never gave its ammunition back. Standing right against it hands
        over what is stuck in it before anything else happens (Use_Train_ArcheryButte,
        CCharUse.cpp:453) - the kind in MORE1, the count in MORE2, both cleared
        afterwards. SphereNet went straight to the skill, so arrows a script or a legacy
        save had left in a butte could not be recovered.

FIX - A torn web left a "spider silk" behind that the reference does not create: its
        IT_WEB damage branch simply deletes the web (CItem.cpp:5886), and the graphic
        SphereNet used for the silk (0x0DF8) is wool.

FIX - Drinking ran no drink hook at all - it fired @Eat instead. Source-X has its own
        @Drink, carrying the effect delay in ARGN1, the amount to consume in ARGN2 and
        the empty bottle in LOCAL.BottleId, and reads them back, with RETURN 1 stopping
        the drink (Use_Drink, CCharUse.cpp:1003).

FIX - Alcohol did not make anyone drunk: the drink fed the drinker and said "hic". The
        reference puts a Liquor effect on the drinker, of a strength rolled per drink
        (:1031).

FIX - A communication crystal was linked through the generic target cursor, so nothing
        re-checked the crystal itself: one that changed hands or was destroyed while the
        cursor was open was still linked by whoever opened it, and @TargOn_Item never
        ran. It now goes through the item-bound cursor the reference's own use targeting
        implies (CClientTarg.cpp:1683).

[2026-09-06] - Blades, cannons, wheels and bedrolls (08B)
----------------------------------------

FIX - A fish only had to be within REACH to be cut up, and the steaks were made in the
        cutter's own pack. Source-X gates a consuming use behind CanUse(target, MOVE) -
        reach plus the move rules and the take-crime check (CCharStatus.cpp:1736) - and
        converts the catch where it lies (CClientTarg.cpp:1919). A fixed decorative
        fish no longer vanishes, and another player's catch stays theirs.

FIX - Loading a cannon checked neither end: a muzzle out of reach took a charge, and
        the charge could be a fixed one or another player's. Use_Cannon_Feed checks
        both (CCharUse.cpp:301).

FIX - A shorn sheep never grew its fleece back. Source-X hangs a timed wool item on the
        sheep and turns the shorn body back into a sheep when it expires
        (CClientTarg.cpp:1862; OnTickEquip, CCharAct.cpp:4067) - half an hour by
        default. Shearing one that is still growing now says so instead of doing
        nothing.

FIX - A fruit or raw reagent could not be cut open for its seed at all: the blade had
        no such branch, so the fruit-to-seed-to-plant chain began nowhere. The reference
        swaps in the DEFAULTSEED graphic, retypes it and renames it in place (:1939).

FIX - A spinning wheel could be used again the instant it finished. Source-X parks it
        in a two-second animation state that is no longer a spinning wheel while it
        lasts (SetAnim, CClientTarg.cpp:2029 -> CItem.cpp:4128), so a second batch has
        to wait.

FIX - A bedroll never opened or closed; it only said it had been laid out. The
        reference rolls it out on the ground - keeping the direction a directional roll
        carries - and rolls it back up when used again, and tells the player to put a
        rolled one down first (Use_BedRoll, CCharUse.cpp:1534).

FIX - Internal equipment layers are kept off the wire, as the reference keeps them
        (uofiles_enums.h:589): a memory, a spell effect or a regrowing fleece has no
        paperdoll slot to be sent to.

[2026-09-06] - Smelting ore and mending gear (08A)
----------------------------------------

FIX - Every ore smelted into iron. The ingot an ore yields is named by the ore
        definition's TDATA1 (Skill_Mining_Smelt, CCharSkill.cpp:1150), which was never
        read - only a local SMELT_TO tag was, and everything else became iron carrying
        the ore's hue. The explicit tag still wins; the definition is now the fallback
        ahead of plain iron.

FIX - A failed smelt burned the whole pile. The reference loses rand(amount/2)+1 of
        it (:1247), so ten ore cost at most five, not all ten.

FIX - @Smelt was reduced to a veto. Source-X hands it the smelter's Mining skill in
        ARGN1, the number of resource kinds in ARGN2, a skip-the-minimum-skill flag in
        ARGN3 and the produce in LOCAL.resource.0.ID / .amount, then reads all of it
        back (:1138/:1196). SphereNet passed the ore COUNT as ARGN1 and threw the rest
        away, so a script could refuse a smelt but never steer it.

FIX - @Create ran on the wrong item. The new ingots were merged into whatever pile was
        already in the pack and the trigger fired on the MERGED result, so a creation
        script that recoloured four new ingots recoloured the ten that were already
        there with them. Source-X builds the ingot and bounces it afterwards
        (:1260/:1284).

FIX - A repair needed no anvil and no materials: a smith mended anything anywhere for
        free. Source-X looks for an anvil within two tiles and spends half the damage
        percentage of what the piece is made of, tested before the work and spent after
        it (Use_Repair, CCharUse.cpp:781/794/854).

FIX - A repair skipped Arms Lore entirely. The reference identifies the piece with it
        first, and failing that ends the repair before the craft skill is rolled at all
        (:764); the gain is applied afterwards, negative on that failure.

[2026-09-06] - Farming, the hive and the water's edge (07Z)
----------------------------------------

FIX - A seed went into any ground at all: bare rock, a floor, even a tile a tree was
        already standing on, and a second crop could be stacked onto the first.
        Source-X asks for soil before it spends anything, refuses ground a tree or
        foliage holds, and REPLACES an existing crop rather than piling another on it
        (Use_Seed, CCharUse.cpp:1467). Staff still plant anywhere, as they do there.

FIX - A crop could be reaped in mid-growth. Only the presence of a fruit definition
        was checked, so a plant whose TDATA2 and TDATA3 are both set gave its produce
        and reset as though it had been harvested. Source-X refuses a stage that still
        has somewhere to grow, and an unripe plant is neither harvested nor reset
        (Plant_Use, CItemPlant.cpp:52).

FIX - Reaping ran no script stage at all. Source-X runs @ResourceTest first, carrying
        the growth stage, the fruit and the per-instance fruit override, and reads all
        three back - so a script can ripen or re-aim a harvest - then @ResourceGather
        with the amount and the produce itself, where RETURN 1 destroys the produce and
        leaves the plant standing (:38/:73).

FIX - Everything a plant yielded was turned into food, so cotton picked from a field
        could be eaten. The produce keeps the type its own definition gives it
        (CreateScript, :65); this applies to the growth timer's own drop as well.

FIX - A beehive was an endless supply: a flat 60% honey roll with no stock and no
        timer. Source-X keeps the stock in MORE1 and spends it - an empty hive gives
        nothing, a stocked one rolls honey, beeswax or a sting, and only a product
        costs a unit - then rests 15 minutes and refills to five on its own tick
        (CCharUse.cpp:1692; CItem.cpp:6380).

FIX - A pitcher could only be filled from wet land: the target the player actually
        clicked was thrown away, so a water trough or basin standing on dry ground
        filled nothing. Source-X resolves the target itself through CanTouchStatic
        (CClientTarg.cpp:2340). The same resolution now serves the bloody-bandage wash.

[2026-09-06] - Using things: unequip macro, scissors, bandages, house keys and the dye vat (07Y)
----------------------------------------

FIX - The unequip macro took gear off without running @Unequip, so a script's
        worn-item cleanup never fired for that path. Source-X raises it from
        OnRemoveObj, which every unequip passes through (CCharAct.cpp:398). NOTE: the
        reference cannot refuse there, but SphereNet's pickup path has always treated
        RETURN 1 as a refusal - the macro now honours it too, so the two paths keep
        one contract and the macro is not a way around it.

FIX - Scissors only rewrote the target's type field: cloth stayed cloth-shaped with a
        bolt's type on it, and no bandage was ever produced. Source-X cuts - it creates
        the output item, carries the hue and the count across and deletes the input
        (CClientTarg.cpp:2110). Cloth and clothing now become bandages (clothing by its
        weight, as the reference measures it) and hides become leather, honouring a
        hide definition's own TDATA1 leather.

FIX - Bloody bandages are washed in water, not cut with scissors. Source-X handles
        them under IT_BANDAGE_BLOOD (CClientTarg.cpp:2244) and its scissors branch does
        not mention them; the message SphereNet reported success with,
        itemuse_bandage_clean, is the reference's "clean these in water" REFUSAL.
        Double-clicking a bloody bandage now asks for a target and cleans the whole
        pile on water; scissors leave it alone.

FIX - A house key opened nothing. The game's own key carries the house multi's lock
        code, which every door of the house shares through its link, but the active key
        path demanded that the key name the door itself. Source-X matches LOCK CODES
        (Use_Key -> IsKeyLockFit, CItem.cpp:4278) - the rule the pack search already
        used. Both now share one resolution, and a key cut for somewhere else still
        opens nothing.

FIX - A dye vat handed out a private DYE_HUE tag rather than the colour it is wearing,
        so a vat whose hue came from a script or a save applied nothing at all, and a
        stale tag applied the wrong colour. The vat's own hue is the source, as it is
        in the reference (CClientTarg.cpp:2331); an old vat with no hue of its own
        still reads its tag once.

FIX - Anything within reach could be dyed: undyeable gold, an object lying on the
        ground, another player's goods. Source-X requires the actor to own the
        top-level object and the item to be clothing or CAN_I_DYE, with a staff
        exception (CClientTarg.cpp:2302/2325).

FIX - @Dye could veto a dye but never change its colour. Source-X SetHue seeds ARGN1
        with the colour and ARGN2 with the sound and takes both back when the script
        falls through (CObjBase.cpp:324); the arguments were thrown away here. The
        dye sound the reference plays is emitted with them.

[2026-09-06] - Equipping: the other hand, the end of the drag, and a refused item (07V-07X)
----------------------------------------

FIX - A shield was taken off whenever the other hand equipped a sword. Riding the
        two-handed layer was treated as being a two-handed weapon, and a one-handed
        equip bounces a two-hander out of the way. Source-X asks whether the item is a
        WEAPON before pairing the hands (CanEquipLayer, CCharStatus.cpp:410), which a
        shield is not - so sword plus shield stands. The layer still counts for a real
        weapon whose definition is missing, which is what that check was for.

FIX - A successful equip never ended the drag it completed. The DRAGGING tag and the
        lift origin were left standing, so the NEXT pickup treated the worn item as
        still on the cursor and restored it out of its equipment layer - back into the
        pack, or onto the ground tile it had been lifted from. Picking the same item up
        again was refused for the same reason. Source-X ends the drag as soon as the
        equip request is validated, whatever the outcome (PacketItemEquipReq,
        receive.cpp:542).

FIX - A refused equip left the item nowhere: parented to the character, on no layer,
        in no container and with no drag to recover it from. Too weak, or an @EquipTest
        veto, both produced it. Source-X hands a refused item to Event_Item_Drop_Fail,
        which puts it back in the container slot, on the character or on the ground it
        came from (CClientEvent.cpp:248); an item lifted without a drag - the
        EquipLastWeapon macro - goes to the pack, as ItemBounce does.

FIX - A refused equip could still disarm the wearer. The double-click and macro path
        emptied the target layer into the pack before the equip was allowed to run, so
        an item that was then refused left the wearer with nothing in that hand.
        CanEquipLayer settles the strength requirement before it touches any layer
        conflict (CCharStatus.cpp:333). The occupant is now moved only once the equip
        is going ahead - and for the packet path too, which had left it to the
        low-level placement.

[2026-09-06] - Pets: what they wear, and what they put down (07R-07U)
----------------------------------------

FIX - A pet could hold a sword and a two-handed bow at the same time. The spoken
        equip command only checked the item's own layer, while Source-X pairs the two
        hands inside CanEquipLayer (CCharStatus.cpp:410): a weapon taking HAND2
        conflicts with whatever HAND1 holds, and a HAND1 equip conflicts with a weapon
        on HAND2 - never with a shield, so sword plus shield stays a legal pair. The
        scan now leaves an occupied hand alone rather than stripping it: above
        CanEquipLayer, ItemEquipWeapon looks for a weapon at all only while neither
        hand holds one (CCharUse.cpp:2051), so the reference never displaces gear the
        owner did not ask to have taken off.

FIX - The spoken equip fired neither @EquipTest nor @Equip. Source-X runs both from
        ItemEquip for an NPC exactly as for a player (CCharAct.cpp:3308/3348): a
        RETURN 1 on @EquipTest refuses the item and leaves it in the pack, and @Equip
        runs once it is actually worn. An item the callback deletes or moves out of the
        pack is dropped rather than equipped, as the reference re-checks at :3331.

FIX - A stack was worn whole. Five of an item went onto the equipment layer as one
        five-piece stack; Source-X splits a single piece off first
        (UnStackSplit(1), CCharAct.cpp:3337 -> CItem.cpp:1251). The worn piece keeps
        the original identity and the remainder stays in the pack as a full clone, so
        tags, durability, hue and attributes survive the split.

FIX - "Drop all" did nothing that plain "drop" did not: it repeated the same pack
        loop and never touched the equipment, so an owner could not get a weapon off a
        pet - and an empty pack ended the command before the equipment was looked at.
        Source-X PC_DROP_ALL is DropAll (CCharNPCPet.cpp:255 -> CCharAct.cpp:564),
        which dumps the pack and THEN moves the worn equipment into it
        (UnEquipAllItems, :592). The order is kept: stripping into the pack first would
        put the worn gear on the ground with everything else. The pack, the mount, hair
        and beard stay where they are, as does everything above the visible layers
        (memories, spell effects, the bank and vendor containers).

FIX - A conjured creature now drops nothing at all on "drop all" (CCharAct.cpp:567);
        its gear leaves with it. Plain "drop" is unaffected, as in the reference.

FIX - Both pet drop verbs threw the whole pack on the ground. Source-X hands
        ContentsDump an ATTR_OWNED filter and adds ATTR_NEWBIE / ATTR_MOVE_NEVER /
        ATTR_CURSED2 / ATTR_BLESSED2 to it (CContainer.cpp:502), so a hireling's own
        stock and an owner's blessed goods stay in the pack.

FIX - A pet GO order that could not reach its last tile relied on a random side step
        failing before it ended; the test that covered it was correspondingly
        intermittent. The behaviour is the reference's own roll (CCharNPCAct.cpp:497);
        the regression now pens the creature in so the outcome is decided rather than
        drawn.

[2026-09-06] - Pets: decks over water, unseen owners, and orders that keep their word (07L-07Q)
----------------------------------------

FIX - A creature that cannot swim could not walk across a dry deck laid over water.
        The swim requirement was read from the raw land tile underneath rather than
        from the surface actually stood on, so a jetty, bridge or ship deck demanded
        swimming. Source-X resolves the surface first and only then asks for the
        ability that surface needs - swimming for water, walking for a platform
        (CCharStatus.cpp:1812/1858). Open water still needs swimming, and the
        pathfinder answers the same way, since a search that disagrees with the step
        plans routes the step will refuse.

FIX - A pet tracked an owner who had just hidden, across the map, to wherever they
        went. Source-X takes the follow point only from a target it can SEE
        (NP_Act_Follow, CCharNPCAct.cpp:1386; CanSee, CCharStatus.cpp:1189). A
        concealed target now leaves the last place it was seen standing, which is what
        the follower walks to, and following resumes when it reappears.

FIX - The @NPCActFollow contract was collapsed into a bool. Source-X gives it three
        outcomes - give up, handled, carry on - and arguments the script may rewrite:
        ARGN1 flee, ARGN2 the distance to keep, ARGN3 move away, with the target as the
        object argument (CCharNPCAct.cpp:1357). The adapter passed none of them and
        threw the answer away, so a script's distance never reached the engine and
        "handled" was indistinguishable from "carry on". ARGN2 is seeded with
        SphereNet's own following distance rather than the reference's 1, so a pack
        that scripts nothing keeps the behaviour it has.

        A script's RETURN 0 still cannot be told from falling off the end, because the
        interpreter maps zero onto Default for every trigger; only a native handler can
        answer "handled" today. Recorded rather than papered over.

FIX - Completing a GO order threw ArgumentException. PetAIMode is byte-backed and
        Enum.IsDefined THROWS when handed a boxed Int32 for such an enum instead of
        answering false - so every GO issued by the real command, which does record the
        previous mode, blew up on arrival with the destination tag already deleted,
        leaving the pet stuck in Come.

FIX - A GO order counted as complete one tile short, so an order to an adjacent tile
        never moved the pet at all and a distant one stopped beside the spot. Source-X
        keeps stepping while a direction remains (NPC_WalkToPoint, CCharNPCAct.cpp:437).
        An unreachable last tile - someone standing on it, or walled off - ends the
        order rather than being retried forever.

FIX - An attack order destroyed the mode it had just saved to fall back on. It wrote
        PREV_PET_MODE and then superseded the previous order, which clears that very
        tag, so a pet told to kill returned to Follow and trailed its master instead of
        going back to Guard or Stay. The clear now happens first, and the fallback is
        written after it.

FIX - A pet's `equip` command skipped the strength requirement: a ten-strength pet
        wore a weapon needing eighty. Source-X scores a candidate through CanEquipLayer
        with fTest, which turns the requirement on for an NPC too (CCharNPCStatus.cpp:688
        -> CCharStatus.cpp:297). Character.Equip is the low-level placement and enforces
        nothing, so the command now asks CanEquip first; a refused item stays in the
        pack and the scan moves on to the next one.

[2026-09-06] - NPC movement: what is really in the way, and leave to move (07H-07K)
----------------------------------------

FIX - A character standing on the storey above blocked the ground beneath them. The
        collision test compared X and Y only, in both the direct step and the A* search,
        so anyone on an upper floor closed the tile under their feet. Source-X skips a
        character more than five Z from the step's destination (ShoveCharAtPosition,
        CCharAct.cpp:4622); the same reach now applies on both paths.

FIX - A wall or door on another storey did the same. Blocking was decided from the
        item's TYPE alone - Wall, Door or DoorLocked - with no regard for where the item
        sat or how tall it was, so it closed the whole vertical column. Source-X hands
        the item's Z and height to CheckTile_Item, which tells a floor below from a
        ceiling overhead (CServerMap.cpp:178). An item now blocks only where its own
        extent reaches the creature standing there.

FIX - The direct step never ran the real walk geometry. Because it answered from item
        TYPE, an impassable object that happened not to be a Wall or Door was walked
        straight into, and a ceiling too low to fit under was walked beneath; the height
        it moved to was whatever a separate resolver guessed, and a failure to find a
        surface was silently answered with the NPC's current Z. The step now goes
        through the shared walk check - the same one a player's step uses, which knows
        tile heights, ceilings, climb limits, the diagonal double-edge rule and the
        surface to land on - and takes its landing height from it. Source-X routes the
        real NPC step through CanMoveWalkTo -> CheckValidMove for the same reason
        (NPC_WalkToPoint, CCharNPCAct.cpp:493). A world with no map data has no geometry
        to consult, so the older tile checks still stand in there.

FIX - A frozen, stoned or exhausted NPC still walked. Source-X asks CanMove at the real
        step (CCharAct.cpp:4716/4571): a GM is exempt, freeze and stone refuse it
        (OnFreezeCheck, :4525), and a living creature out of stamina refuses it too -
        the reference reads the CURRENT stamina pool there, not the base dexterity. The
        test sits where the step is applied rather than where the decision was made, so
        a freeze landing between the two now stops the walk. NpcAI.OnTickAction's own
        comment claimed "living, non-frozen NPC" while nothing checked it; combat had
        the test and movement did not.

        The stamina half applies only where a stamina pool exists. A creature with no
        MaxStam has no stamina model at all - the Dex setter raises the ceiling but
        never fills the pool - and reading its empty pool as exhaustion would have left
        such creatures standing still forever.

[2026-09-06] - NPC movement: the corner, the stale route and the landing height (07E-07G)
----------------------------------------

FIX - An NPC walked diagonally through the corner where two walls meet. Source-X tests
        both of the orthogonal tiles beside a diagonal, from the tile being left, and
        refuses the step if either is blocked (CheckValidMove, CCharStatus.cpp:1988).
        SphereNet checked only the destination, so a creature or pet took the shortcut
        between two obstacles it could not step on directly. The test is on terrain and
        structures alone: the reference weighs blocking mobiles against the DESTINATION
        only, so someone standing beside the NPC does not wall off the diagonal.

        The reference deliberately skips this while pathfinding (fPathFinding), which
        is why the A* search was never the place for it - the direct step never runs
        A* at all.

FIX - An NPC that changed position while a route was cached snapped back onto the old
        one, moving six tiles in a single step with nothing walked in between. Source-X
        throws a stored route away when its next point is no longer one tile off
        (NPC_WalkToPoint, CCharNPCAct.cpp:463); the cached step is now refused and the
        route recomputed unless it really is the next tile, on the same map. The
        general MoveCharacter API is untouched - it is what teleports and every other
        placement use.

FIX - A routed step was applied at the search's approximate height. Pathfinder uses
        MapData.GetEffectiveZ to keep the search cheap, and says so; it sees neither
        multis nor dynamics, so an NPC forced to route around an obstacle on a ship
        deck or a raised floor was moved onto the terrain underneath. The landing
        surface is now resolved at the step, as the reference does (CheckValidMove,
        CCharStatus.cpp:1972), and a step with no surface under it is refused so the
        route is recomputed rather than committed at a guessed height. The direct step
        already resolved its surface and is unchanged.

[2026-09-06] - Doors: vertical gates, custom art, containers and levers (07A-07D)
----------------------------------------

FIX - A portcullis changed its picture instead of moving. Source-X gives the vertical
        gate its own routine, which moves it between the two heights in MORE1 and
        MORE2 and never touches the art (Use_Portculis, CItem.cpp:4583). SphereNet sent
        it down the hinged-door path, which swapped the graphic by an offset of two and
        left the gate at its old Z, so the designed heights never took effect and the
        classic door's 20-second timer came along with them. A gate whose two heights
        are the same now does nothing, and PORTCULISSOUND is honoured.

FIX - A locked portcullis opened for anyone who double-clicked it. Source-X refuses it
        to all but a GM unless the use arrived through a LINK (CCharUse.cpp:1771), and
        only then falls through to the gate move.

FIX - DOOROPENID was not supported at all, so a door scripted with a custom open
        graphic and its own offset behaved like an ordinary one. Source-X resolves the
        alternate graphic first and shifts by MOREP instead of the hinge table, storing
        the graphic it replaced so the next use swaps straight back (Use_DoorNew,
        CItem.cpp:4633). A door with no alternate still falls through to the classic
        table. A custom door's state is its own flag rather than its graphic, because
        the alternate art is not part of the classic closed/open pairing and can land
        on another set's closed slot.

FIX - A door inside a container was still treated as a door in the world. Its slot
        coordinates were read as map coordinates, the hinge shift was added to them,
        and the door was dropped out of the pack onto the map - and the auto-close
        timer reached the same code, so a door picked up while open left the backpack
        on its own. Source-X refuses a door that is not top-level before touching
        anything (:4637, :4695), and both the use and the timer paths now do.

FIX - A lever flipped its own graphic and nothing else. Source-X follows the item's
        LINK after the use itself: Use_Item walks m_uidLink for up to 64 hops, stopping
        on a missing target or a return to the item it started from, and signals each
        one (CCharUse.cpp:1962). The chain applies the LINKED use rather than a fresh
        double-click - a link only ever opens a door, so pulling the lever twice does
        not close it, and the link carries the authority that works a locked gate. The
        chain is followed from a switch; wiring it to every item use is left alone.

[2026-09-06] - Food ceilings from the definition, and a pet that changes hands (06N-06O)
----------------------------------------

FIX - A definition that declares MAXFOOD=0 got the classic ceiling of 60 instead of
        none. Source-X falls back to the definition's food maximum whenever the
        instance one is below 1 (Stat_GetMax, CCharStat.cpp:276), and a maximum of zero
        means the creature cannot be fed at all - Use_Eat refuses it outright
        (CCharUse.cpp:934). A creature written to eat nothing therefore had a full
        appetite and accepted meals. An explicitly written MAXFOOD is now honoured,
        zero included, while a definition that says nothing about food keeps the
        classic ceiling; an instance MAXFOOD still wins, and only a positive one, since
        the reference reads a zero there as "unset".

        Recorded divergence: the reference also falls back to the maximum DERIVED from
        FOODTYPE. The live pack writes bare FOODTYPE lists (FOODTYPE=t_food,t_drink,
        ...), which derive 1, so honouring that half would collapse every human's food
        pool to a single point and take the player hunger model with it. Only an
        explicit MAXFOOD is resolved from the definition.

FIX - A creature whose definition raises MAXFOOD above 60 spawned at 60. The spawner
        wrote the starting food before the ceiling, and the Food setter clamps to
        whatever MaxFood is at the time of the write. Resolving the ceiling from the
        definition removes the hazard from that path, and the spawner's order was
        corrected as well, which is what still protects a creature whose ceiling comes
        only from an instance tag.

FIX - A transferred pet kept the previous owner's friends and its old bond. Ownership
        moved to the new owner, but the FRIEND_ links stayed, so the old owner's
        friends went on commanding a pet that was no longer theirs, and the new owner
        inherited a bond they never earned. Source-X routes a transfer through
        NPC_PetSetOwner, which returns untouched when the owner is unchanged and
        otherwise calls NPC_PetClearOwners first (CCharNPCPet.cpp:600) - clearing
        MEMORY_IPET | MEMORY_FRIEND and the bond (:558). The same now happens on a
        genuine change of owner only: re-assigning the SAME owner, which stable
        retrieval, figurine restore and dismount all do, changes nothing, and a
        transfer refused by the follower cap leaves the pet exactly as it was.

[2026-09-06] - Poisoned meals, and what a bandage checks before it raises the dead (06I-06M)
----------------------------------------

FIX - Poisoned food poisoned nobody. The Poisoning skill writes POISON_SKILL onto a
        meal (Skill_Poisoning, CCharSkill.cpp:2197) and nothing ever read it back, so a
        successfully poisoned ration was eaten - by a player or by a pet - with no
        effect at all. Source-X applies it inside Use_EatQty, ahead of the meal itself
        (CCharUse.cpp:905), so it now sits ahead of @Eat too: a script that vetoes the
        gains does not undo the poison, and a full pet that never takes a bite is never
        poisoned either. The stored value is the poisoner's SKILL, not a poison level;
        it goes through the same OSI banding the poisoned-weapon path already uses, so
        a blade and a meal cannot disagree about what a given skill is worth.

FIX - A dead pet could not be revived unless it happened to die in war mode. Source-X
        asks for STATF_INSUBSTANTIAL, which death sets only on a PLAYER who was not
        already in war mode - "manifest war mode for ghosts" (CCharAct.cpp:4468) - and
        never on an NPC. SphereNet demanded war mode of every dead target, and nothing
        in a pet's life makes it die in war mode, so a bonded pet that fell out of
        combat was unrevivable. The requirement now applies to players only, which is
        where SphereNet's war flag carries the manifest meaning.

FIX - Corpse-targeted healing never checked the GHOST. It verified that the HEALER
        could reach and see the corpse - which is a different condition - so a healer
        standing over a corpse could raise its owner from anywhere on the map, or from
        another one. Source-X requires the ghost to be within two tiles of its own
        corpse and to have line of sight to it (CItemCorpse.cpp:48, :55).

FIX - A corpse whose owner could not be resolved - already resurrected, or deleted
        with its NPC - fell through to the "no target given, treat myself" default, so
        aiming a bandage at a stale corpse quietly bandaged the healer and spent the
        bandage doing it. Source-X stops with HEALING_BEYOND (CCharSkill.cpp:2769) and
        refuses an owner that is no longer dead (CItemCorpse.cpp:37). A genuine
        self-heal with no target given still works.

FIX - The corpse's region had no say. Source-X refuses a corpse resurrection in a
        region carrying ANTIMAGIC_ALL, RECALL_IN or TELEPORT (CItemCorpse.cpp:73), and
        does so before the bandage is spent. SphereNet only ever consulted NoMagic, at
        the very end, inside Character.Resurrect - by which point the bandage was gone
        and the skill had already reported success. All three flags are now checked up
        front, with the bandage intact.

        These four are one missing precheck: Source-X gathers every corpse-side
        condition into CItemCorpse::IsCorpseResurrectable and runs it once, ahead of
        the cost (Skill_Healing, CCharSkill.cpp:2796). SphereNet carried only the
        container half of it, so the rest is now in one place for the same reason.

[2026-09-06] - Pets: one relationship, one bond, one food pool, one eating path (06E-06H)
----------------------------------------

FIX - A creature already carrying a rider, sitting in a stable or shrunk into a
        figurine accepted a second mount. Source-X builds the mount item with
        Make_Figurine, which refuses a disconnected creature or a player outright
        (CCharAct.cpp:3619), and Horse_Mount fails with it (:3989). SphereNet checked
        only that the RIDER was free, so a second player could mount a horse already
        carrying one, and a double-click carrying the old uid of a stabled or shrunk
        pet started a mount while its stable entry or figurine still held the same
        creature. The engine now refuses it, and the double-click visibility gate no
        longer lets a stale click reach a parked creature at all.

FIX - A released or deserted pet stayed BONDED. Source-X drops the bond in the same
        breath as the owner and friend memory - "pets without owner cannot be bonded"
        (NPC_PetClearOwners, CCharNPCPet.cpp:559). Keeping it meant the pet's later
        death took the bonded branch, so an ownerless body stayed in the world tables
        instead of being cleaned up like any other NPC. An OWNED bonded pet still
        leaves its body behind, which is what bonding is for.

FIX - Feeding a pet destroyed the whole stack. Source-X sizes the bite against the
        free space: it refuses outright when the creature is full (Use_EatQty,
        CCharUse.cpp:891), takes only as many units as the space calls for (:894), and
        returns the rest to the player (NPC_OnItemGive, CCharNPCAct.cpp:2119).
        SphereNet added ten per unit and removed the item whatever its size, so a
        hundred rations dropped on a full pet simply vanished. The pet now eats what it
        has room for and the remainder goes back to the owner's pack.

FIX - FOOD and the pet's hunger were two independent pools. Feeding moved one while
        the FOOD script property read the other, so a script could set FOOD=60 and
        watch the pet desert at the next loyalty tick anyway. Source-X has a single
        STAT_FOOD that eating raises, the hunger tick lowers and FOOD reads
        (CChar.cpp:3180). They are now one value - which also means a pet's hunger
        finally survives a save, since only the player-side field was ever persisted.
        The ceiling comes from the creature's own MAXFOOD rather than a constant.

FIX - Feeding a pet fired no @Eat at all, so nothing a shard scripted around feeding
        ever ran. Player eating did fire it, but passed ARGN1=5 - the reference's ARGN1
        is a STAT LIMIT that starts at zero - prepared none of the LOCAL.Hits / Mana /
        Stam / Food values the event carries, and then applied a flat five regardless,
        so a script that wrote those values changed nothing. Both now go through one
        path with the reference's arguments and read-back (EatAnim, CCharAct.cpp:3436).

        RETURN 1 skips the gains but no longer saves the food: EatAnim returns early
        and its caller consumes the meal either way (CCharUse.cpp:913). Two existing
        tests encoded the old ARGN1=5 and the uneaten-on-veto behaviour and were
        corrected to the reference contract.

        Deliberate divergence, recorded: EatAnim adds the current stat to each local
        and hands the sum to UpdateStatVal, which adds it to the current value again
        (CCharAct.cpp:757) - doubling what is already there before clamping. The plain
        gain is applied instead rather than doubling a shard's food and healing on
        every meal.

[2026-09-06] - Pets: command authority, stale orders and stored-pet identity (06A-06D)
----------------------------------------

FIX - Being a pet's FRIEND granted full owner authority. Source-X opens exactly
        PC_FOLLOW, PC_STAY and PC_STOP to friends and sends every other verb to the
        arm that requires NPC_IsOwnedBy (CCharNPCPet.cpp:129-152), so a friend could
        make the pet drop its cargo, or transfer the pet to themselves. Authority is
        now decided per verb, at the speech command AND again when a target cursor is
        clicked, since ownership can be revoked while the cursor is up. COME and
        FOLLOW ME are owner-only: PC_COME and PC_FOLLOW_ME are separate commands in
        the reference (:38, :43) and are not in its friend set.

FIX - A new pet order did not cancel the last one. GO stores its destination in a tag
        the pet AI consults before anything else, so a COME after a GO changed the
        mode and then walked the pet on to the old spot regardless - the recall
        looked simply ignored. Source-X starts a fresh NPC action per command
        (NPCACT_FOLLOW_TARG at CCharNPCPet.cpp:183, NPCACT_GOTO at :504), so a pending
        GO cannot outlive the order that replaced it. Every order now drops it. GO
        itself still keeps the mode captured by the first GO, so the pet resumes what
        it was doing before the detour rather than resuming the detour.

FIX - The stable service was keyed on the owner's serial alone, and it outlives the
        character holding that serial. Once a character was deleted and its number
        handed to a new player, that player was shown - and could claim - the previous
        owner's stabled pets. A cache entry now answers only for the character it was
        built for; for anyone else the list is rebuilt from that character's own tags,
        which for a new character is empty.

FIX - A pet stabled mid-fight came back out swinging. Parking cleared its fight
        target but not the order behind it, so the AI put the fight straight back on
        retrieval with no new command given. Parking now drops the transient order -
        attack, follow, guard and go targets and the pet's mode - leaving the pet idle
        as the reference does when a creature comes back out (Skill_Start(SKILL_NONE),
        CCharUse.cpp:1205). Bonding, script state and the follower-slot override are
        the pet's own and are untouched.

FIX - A stored pet reference that recorded a UUID fell back to the serial when that
        UUID no longer resolved. Serials are reassigned, so the fallback answered with
        whatever inherited the number. For a mount that was severe: the repair path
        hid that creature, moved it onto the rider and re-owned it, and dismounting
        handed it back - for a player, a real character flagged Ridden, teleported and
        stamped with pet ownership. A recorded UUID is now authoritative and the
        serial answers only a link that never carried one; a resolved object that is
        deleted, or is a player, is refused outright. The same rule covers stable
        entries and figurines, which shared the helper.

FIX - Deleting the creature someone was riding left the rider's half of the link
        intact, to be re-resolved later against a reassigned serial. Source-X breaks
        it in DeleteCleanup (CChar.cpp:395); SphereNet now records the rider on the
        mount so the same cleanup is possible, and clears the rider's tags, mount item
        and OnHorse flag when the mount is deleted.

FIX - Destroying a figurine orphaned the pet shrunk inside it. The creature stayed in
        the world tables and in every save - parked, flagged Ridden, skipped by the AI
        and unreachable by any normal route - along with its pack and cargo. Source-X
        ends that creature's lifecycle with the figurine (CItem::DeleteCleanup for
        IT_FIGURINE, CItem.cpp:209) and so does this, on the central delete path, so
        the container and owner cases are covered too. A successful restore clears the
        link first, so consuming the figurine cannot kill the pet that just came out
        of it, and only a pet still parked in that figurine is ever taken.

[2026-09-06] - Resources: REGEN is tenths on a curve, and a node lives one window (05C)
----------------------------------------

CHANGED - REGEN is read as TENTHS of a second, not whole seconds. The reference says
        so in its own loader ("Tenths of second once found how long to regen this
        type", CRegionResourceDef.cpp:73) and builds the node's timeout with
        GetRandom() * MSECS_PER_TENTH (CWorldMap.cpp:148). Every vein therefore used
        to last TEN TIMES as long as its script asked for. The reference pack's own
        REGEN=60*60*10 now means an hour, as the expression reads, instead of ten.
        Shards that tuned REGEN against the old reading will see nodes recycle ten
        times sooner and should divide those values by ten to keep the same economy.

FIX - REGEN is a value curve, like AMOUNT and REAPAMOUNT. A comma-separated setting
        parsed as one expression, collapsed to zero, and fell through to an invented
        ten-hour default, so the curve form silently did nothing. Each point is parsed
        on its own - the points may be expressions, which is why the plain integer
        curve parser could not be reused - and a sample now spans the whole curve.
        The ten-hour default is gone: an unset REGEN samples zero, which is what the
        reference does with an empty curve.

CHANGED - A resource node lives out the single window set when it was found, and is
        handed back exactly as it stands until then. Source-X returns the resource
        bit it finds without touching it (CWorldMap.cpp:71), calls MoveToDecay once at
        creation (:148), decrements the amount on use without re-arming the timer
        (CCharSkill.cpp:1046), and treats a node at zero as spent (:1456). SphereNet
        topped the pool up by elapsed time and pushed the deadline a full regen period
        out on every touch, so a player working one vein could keep it alive and
        drawing indefinitely. Both are removed, along with the RES_MAX / RES_LAST
        bookkeeping they needed. A spent node now yields nothing until its window
        closes, after which the next search rolls a fresh node with a fresh pool.

        This undoes the partial-node regen added on 2026-06-30, which was landed as a
        Source-X parity claim ("gradual vein regrow") that the reference does not
        support. It was not a recorded deliberate deviation, so parity wins.

[2026-09-06] - Gathering: the ResourceGather contract and the ore's own Create (05B)
----------------------------------------

FIX - @ResourceGather read ARGN1 as the reaped ITEM ID and ARGN2 as the amount.
        Source-X passes Init(wAmount, 0, 0, pResBit) and seeds LOCAL.ResourceID with
        the reap item (CCharSkill.cpp:1029): ARGN1 is the AMOUNT, the object argument
        is the resource marker, and the item id travels in the local, read back after
        the trigger (:1044). A script halving a yield with ARGN1=2 therefore produced
        four copies of item id 2 instead of two ore. The arguments now match the
        reference, and the marker is passed as the object argument.

FIX - ARGN1=0 was discarded as "unset" and the full reap handed over anyway. The
        reference consumes ARGN1 from the pool and yields no item when that comes back
        zero or less, so a script can now decline the reap through the amount as well
        as through RETURN 1. A negative amount yields nothing for the same reason, and
        an amount larger than the pool is clamped to what the node actually holds -
        which the trigger already sees, because the clamp happens before it runs.

FIX - The reaped item never ran its own ITEMDEF @Create. Source-X builds it with
        CItem::CreateScript (CCharSkill.cpp:1050), which runs GenerateScript and with
        it the Create block (CItem.cpp:404/415), setting the amount only afterwards -
        so an ore whose definition scripts its hue, tags or type came out of the ground
        bare. It now goes through the same scripted-creation hook loot, spawns and
        crafting already use; the trigger is instance-guarded, so the delivery path
        cannot run it twice, and a Create body that destroys the item no longer
        reports a successful gather.

[2026-09-06] - Crafting: recipe identity and the tool that wears (05A)
----------------------------------------

FIX - Two ITEMDEFs sharing a display graphic were the same recipe. The registry was
        keyed by the art id, so the second definition silently replaced the first: it
        disappeared from its own skill's craft list, and its defname built the other
        item. Source-X carries the ITEMDEF resource id through Skill_MakeItem and
        looks the definition up with it directly (CCharSkill.cpp:870/679) - the
        identity is never reduced to the graphic. Recipes are now keyed by that
        resource id, and MAKEITEM passes it instead of collapsing to the display id.

FIX - A lookup that only has a display graphic no longer gets a silent guess. It is
        still answered while exactly one recipe wears that art, which covers numeric
        ITEMDEFs and every ordinary pack; when several share it, there is no answer to
        give and none is invented.

FIX - The tool that PERMITTED a craft and the tool that WORE from it could be
        different items. The permission check refused to descend into a container it
        may not search while the wear lookup descended anyway, so a spare locked away
        in the pack took the damage owed by the tool in the crafter's hand - and a
        locked-away tool could be worn down without ever being usable. Source-X
        ContentFind skips an unsearchable container (CContainer.cpp:236). Both halves
        now run one search, so they cannot drift apart again. This is the same defect
        already fixed for ammunition and resource gathering; the tool lookup was the
        one walk left behind.

[2026-09-06] - Followers: a removed creature frees its slot at once (04D)
----------------------------------------

FIX - A dispelled or expired summon went on filling a follower slot for as long as
        the owner's cached count survived. CurFollower is recomputed by scanning the
        world and the answer is held for CurFollowerCacheMs; removing a creature does
        not rewrite its ownership fields, so nothing marked that cache dirty, and the
        owner's very next summon was refused for a creature no longer in the world.
        Source-X hands the slots back instead of recomputing them - its NPC teardown
        calls NPC_PetClearOwners (CChar.cpp:364), which subtracts the creature's cost
        from the owner (CCharNPCPet.cpp:597). The cache is now dropped in
        Character.Delete, which every removal path shares, rather than in the dispel
        branch alone. Only the cache is dropped: the ownership fields are left standing
        so a delete trigger can still read who owned the creature, and the next recount
        skips it on IsDeleted alone.

[2026-09-06] - Magic: the follower cost of a summon, and the life it keeps (04C)
----------------------------------------

FIX - A summon picked from the menu was weighed against the follower cap BEFORE its
        own creature definition was applied. The placeholder counted as the default
        single slot, the pick was applied afterwards, and the cap was never re-checked
        - so a five-slot creature walked through a one-slot allowance and left the
        owner permanently over their limit. Source-X builds the summon from the chosen
        id first (CreateBasic(m_atMagery.m_uiSummonID), CCharSpell.cpp:2640) and only
        then measures it with GetFollowerSlots() (:2662). The pick is now applied
        before the cap is weighed, and a refusal says so.

FIX - A refused summon was charged for in full. The creature was made in the effect
        stage, long after the mana, reagents and scroll had been taken, so a cast that
        could not legally summon anything still paid the success price. Source-X calls
        Spell_Summon_Try at CCharSpell.cpp:3002 and reaches the consumption only at
        :3010 - and deletes the summon again if that consumption cannot be met
        (:3012). The summon is now built ahead of every cost, and a cost that cannot
        be met takes it back down. The failure is priced as an abort, not refunded.

FIX - A summon's expiry went into the save as an absolute TickCount64. That is UPTIME,
        not wall time: restored on a machine that had since rebooted, the summon waited
        out a threshold that could be days away. Source-X stores a timer as the
        REMAINING milliseconds (CObjBase.cpp:2081) and re-arms it against the load time
        (:2037). The save now carries SUMMON_EXPIRE_REMAINING and the loader rebuilds
        the deadline on the running clock, the same way the POISON record already
        survives a restart. A save written before this change carries only the old
        absolute tick; its deadline is rebuilt from the summon's own duration, which is
        generous by up to one duration but bounded - a summon that outlives the server
        is not.

[2026-09-06] - Magic: what a spell field may reach, and how many may land (04B)
----------------------------------------

FIX - Stacked spell fields multiplied the cost of a single step. Walking onto a tile
        ran every field on it, so three Fire Fields cost three times the damage,
        while STANDING on the same three cost one - the two paths disagreed. Source-X
        caps a location check at ONE spell effect (CCharAct.cpp:4996) and documents
        the cap as the guard against exactly this, and against a Paralyze+Fire stack
        re-freezing the victim at every damage tick with no way out. Both paths now
        share the cap.

FIX - The cap follows the RESULT, not the attempt. Source-X assigns fSpellHit from
        the return of OnSpellEffect (:5008), which is false when the effect was
        refused, so an inert field never swallows the one behind it. The field touch
        hook answered a single "handled" boolean that could not express this, and now
        reports whether an effect actually landed (FieldTouchResult).

FIX - A field on the floor below reached the storey above. Neither the walking nor
        the standing path compared Z at all, so an item sharing the X/Y touched a
        character 50 Z above it. Source-X skips an item unless its height overlaps -
        zdiff = itemZ - charZ, the item's height counted as at least 3, rejected when
        zdiff > height or zdiff < -3 (CCharAct.cpp:4934). The test runs BEFORE @STEP,
        so a skipped item fires no trigger either; traps, moongates and webs on the
        tile are filtered by the same rule.

FIX - A Poison Field poisoned an invulnerable character, and a Paralyze Field froze
        one. The tick damage was already zeroed for them, but the poison itself was
        accepted - flag, timer, source UID and client status notification - and the
        freeze was applied outright. Source-X turns an invulnerable target away at the
        top of the harmful-spell branch (CCharSpell.cpp:3762), before any effect, and
        both field defs carry SPELLFLAG_HARM. Fire already made this check; poison and
        paralyze now go through the same gate. The existing tick-side protection for
        poison that was already running is unchanged.

[2026-09-06] - Magic: cast source re-resolution and target validation (04A)
----------------------------------------

FIX - The cast SOURCE was decided from the presence of a tag rather than from the
        item itself. Source-X re-resolves it at completion (m_Act_Prv_UID.ObjFind(),
        CCharSpell.cpp:2882) and hands that same resolved pointer to Spell_CanCast
        (:3010), which refuses a null source (:2330) and one whose top-level owner is
        not the caster (:2422). A scroll destroyed during the cast time therefore
        still bought the half-mana, no-reagent discount, and a scroll handed to
        another player was consumed out of the new owner's pack. The source is now
        re-resolved and must still be on the caster. There is no GM bypass, because
        the reference makes this test before its PRIV_GM shortcut (:2429).

FIX - A wand merely held decided the price. The wand test read the currently equipped
        one/two-handed item, so a player could equip a wand during the cast time and
        finish an ordinary spell at wand rates - no mana, difficulty 10, no reagents.
        A player's source is now only what they actually activated. NPCs never record
        a source (their AI hands them the wand), so the wielded-wand reading is kept
        for them alone and their pricing is unchanged.

FIX - The fizzle, insufficient-mana and insufficient-reagent paths cleared the cast
        state but not the source tags, and ClearCastState does not touch them. WAND_UID
        and SCROLL_UID were left on the caster, so the NEXT cast inherited the discount
        and consumed that scroll. All three paths now drop the tags.

FIX - An invalid target was rejected only after the costs had been taken. Source-X
        opens Spell_CastDone with Spell_TargCheck (CCharSpell.cpp:2878) and reaches
        the reagent/mana/charge consumption 130 lines later (:3010), while SphereNet
        re-resolved the target afterwards, emitted a message, and still reported
        success to @SpellSuccess. A target that died (:2740), changed map, walked out
        of range or vanished (:2728, with the TARG_XYZ exception) is now caught first,
        and the line-of-sight refusal takes the same route. This is not a refund: the
        failure is priced as an abort, which CCharSkill.cpp:3000 turns into
        SKTRIG_ABORT and Spell_CastFail prices through MANALOSSABORT / REAGENTLOSSABORT
        (CCharSpell.cpp:3316).

[2026-09-06] - Combat: swing-speed stat per era and the archery settle time (03C)
----------------------------------------

FIX - The swing-speed formula read the base DEX field in every era. Source-X picks
        the stat per era (CResourceCalc.cpp): era 0 takes Stat_GetAdjusted(STAT_DEX),
        the effective stat with equipment and buffs (:61, :69), while eras 1-4 take
        Stat_GetVal(STAT_DEX), which is the CURRENT STAMINA POOL (:90, :101, :116,
        :127 - CChar.cpp:4271 writes the STAM save field from it). So stamina loss
        never slowed an attack in the eras that price it that way, and a DEX bonus
        from equipment never sped one up in era 0. The formulas and their historical
        integer rounding are untouched.

FIX - The Dex setter raises MaxStam but never the current pool, so a character built
        from Dex alone had zero stamina - which under the fix above would swing at
        the formula's slowest rate. Three creation paths did not seed it: NPCs rolled
        from a chardef, newly created player characters, and stress-test bots.

FIX - An archer who walked DURING the windup was not made to serve the post-move
        settle time; it was checked only where a swing starts, and a pending shot
        never re-enters that path. Source-X applies it inside the hit phase
        (CCharFight.cpp:1854) and returns WAR_SWING_EQUIPPING, which spends the swing
        and restarts the recoil. Ranged only: SphereNet's melee movement delay has no
        Source-X counterpart and is deliberately not repeated at hit time.

FIX - Completes the previous commit's ammunition fix. Its comment said Source-X takes
        no ammo on a spent swing, but the code only excluded throwing weapons, so
        bows and crossbows still lost an arrow when the target stepped out of reach.
        The ammo block sits below both the range and the movement returns
        (CCharFight.cpp:1862 vs :1896 and :1857), so no ranged weapon pays for a
        swing that was never loosed.

[2026-09-06] - Combat: pending-swing state and unspent-swing ammo (03B)
----------------------------------------

FIX - Freeze and sleep were checked only where a swing STARTS, and a pending hit
        never re-enters that path, so a blow wound up before a paralyse still landed
        after one. Source-X re-runs Fight_CanHit at the top of the hit phase
        (CCharFight.cpp:1813) and refuses to proceed unless it answers READY. The
        disposition matters as much as the check: Fight_CanHit returns
        WAR_SWING_SWINGING for a frozen or sleeping attacker and for a sleeping
        target (:1696-1699), which HOLDS the swing rather than spending it, so these
        are deferrals and not drops. Dead / stone / invulnerable / insubstantial stay
        INVALID and still drop the swing. COMBAT_PARALYZE_CANSWING still excuses
        freeze, and still does not excuse sleep.

FIX - A sleeping target could be attacked from every entry point, because the
        invalid-participant test never looked at Sleeping. Both start paths (player
        and NPC) now refuse it, matching the pending-hit rule.

FIX - A swing spent because the target stepped out of reach consumed ammunition. In
        Source-X the ammo block sits below the hit-phase gate (CCharFight.cpp:1814)
        and the range re-check returns WAR_SWING_EQUIPPING (:1896), so an unspent
        swing never reaches it - an arrow is taken only on a real miss roll (:2023),
        and only for a player. Throwing weapons are worse than the general case: they
        have no pack ammo at all, yet their fallback type still matched any bolt
        stack, so a thrown spear ate crossbow bolts. The miss branch no longer looks
        for ammo for a throwing weapon. NPC behaviour was already correct and is
        unchanged.

FIX - A three-line comment in CombatHelper.FindAmmoInContainerCore was duplicated by
        the previous commit; deduplicated.

[2026-09-06] - Combat: reflected damage, on-hit ordering and searchable containers (03A)
----------------------------------------

FIX - Reflected damage went straight to attacker.Hits, so a character that cannot be
        harmed still lost hit points to its own victim. Source-X sends reflection
        back through the ordinary entry - OnTakeDamage(dam, src, DAMAGE_FIXED |
        DAMAGE_REACTIVE), CCharFight.cpp:1021 - whose first act is to bounce anything
        without DAMAGE_GOD off an invulnerable target (:642). The comment claiming
        fixed damage cannot recurse misread the reference: recursion is stopped by
        DAMAGE_REACTIVE (:1015), so writing HP directly bought nothing and skipped
        the guard. All three reflect branches (Blood Oath, Reactive Armor,
        REFLECTPHYSICALDAM) now share one immunity-respecting entry.

FIX - The same gate was missing on two more paths: the Reactive Armor reflection in
        SpellEngine, and poison ticks, which applied damage with no immunity check at
        all. A poisoned invulnerable character now takes no tick damage, while the
        poison still expires on schedule.

FIX - On-hit properties (leeches, hit-area splashes, on-hit spell procs) ran BEFORE
        the weapon's own damage. Source-X Fight_Hit applies the damage first
        (CCharFight.cpp:2259) and runs the procs after (:2270). The visible cost was
        not the HP a proc observed: when a proc killed the target, the strike's own
        damage never landed and was never recorded, so the kill, the murder count and
        the loot rights went to the proc instead of to the blow that was swung. The
        proc call stays outside the immunity block, because Source-X gates procs on
        `iDmg > 0` and discards OnTakeDamage's return.

FIX - The ranged-ammo search recursed into every container, so arrows locked in a
        chest inside the pack were still fired. Source-X gates the descent on
        IsSearchable (CContainer::ContentFind, CContainer.cpp:236). The same defect
        was in eight more container walks in CraftingEngine, which could consume
        materials out of a locked chest; all nine now use the searchable contract.
        Gold counting is deliberately left alone: Source-X excludes only
        IT_CONTAINER_LOCKED for gold (CContainer.cpp:443), not the full searchable
        set.

[2026-09-06] - Secure trade: window ownership, accept contract and recovery (02)
----------------------------------------

FIX - Both secure-trade windows were loose world items with no owner and no
        position. Source-X Cmd_SecureTrade equips each one on its character as
        IT_EQ_TRADE_WINDOW at LAYER_SPECIAL (CClientUse.cpp:1414/1420), and almost
        everything below follows from having that owner. ItemType.EqTradeWindow and
        Layer.Special already existed and were simply unused here.

FIX - A player could not take back what they had just offered. The container-reach
        gate added with the inventory work found neither an owner nor an opened
        container record for a trade window, so the only way out of a mistaken offer
        was to cancel the whole trade. Owning the window fixes it, and the partner's
        window stays correctly out of reach.

FIX - The accept packet's own flag was ignored and a stored bool flipped instead, so
        a repeated accept silently un-accepted and an explicit "I do not accept"
        completed the trade. Source-X Trade_Status assigns the value and clears BOTH
        sides when it is false (CItemContainer.cpp:144). The sender must also name
        their OWN container (receive.cpp:1132) - without that check, reading the flag
        from the packet would have let a client set the partner's acceptance.

FIX - Changing an offer did not invalidate an acceptance already given. The reset
        lived in two client handlers, so a script move or an engine deletion could
        swap the goods under an agreed trade. It is now a container invariant fired
        from add and remove alike, as Source-X does from ContentAdd
        (CItemContainer.cpp:557) and OnRemoveObj (:798).

FIX - Dropping an item on a player who refuses trades, is dead, is distant, or is
        already trading left the item parented to the character with no drag and no
        container - unreachable through any inventory view - while the client was
        told the drop succeeded. InitiateTrade now reports failure and the item
        bounces to its lift origin (Source-X Event_Item_Drop_Fail,
        CClientEvent.cpp:325).

FIX - A trade could be opened with a player who has no active client, leaving the
        offer in a session nobody could answer. Source-X refuses an offline partner
        outright (CClientUse.cpp:1338).

FIX - A save taken mid-trade came back with the windows but no session, and because
        they had no parent the loader placed them on the map at tile (0,0) as
        ordinary openable containers holding real player property. Trade windows are
        now owned, and RecoverInterruptedTrades empties each one back to its owner on
        load and removes it - the same end Source-X reaches through CItem::IsWeird
        (CItem.cpp:1005).

FIX - A RETURN 1 from @TradeAccepted cancelled the whole trade: goods returned,
        @TradeClose fired on both sides, session and windows destroyed. Source-X
        simply returns (CItemContainer.cpp:189), vetoing that exchange and leaving
        the window open with both offers intact so the script's author can adjust and
        accept again. Both check marks are deliberately left set, as in the reference.

[2026-09-06] - Inventory transfer rules: cursor, container targets and reach (01B)
----------------------------------------

FIX - A second pickup overwrote the DRAGGING tag and the lift origin without
        resolving the first one, leaving the previous item parented to the character
        with no layer and no drag: present in the world but absent from the pack, the
        equipment and the cursor alike. Source-X bounces the occupant of
        LAYER_DRAGGING before the new item takes it (CCharStatus.cpp:459). The held
        item now returns to where it was lifted from, and re-picking up the SAME item
        keeps the original drag instead of restarting it.

FIX - A drop aimed at a plain item passed it to TryAddItem, which never asked whether
        the target was a container - so a sword became one, and the gem inside it
        could not be reached through any container view again. Source-X redirects the
        drop to wherever the target item lives (CClientEvent.cpp:489/504); stack
        merges and real container targets are unchanged.

FIX - An item could be lifted out of a container the client had never been shown,
        a locked chest included, because nothing in the pickup path looked at the
        container at all. Source-X consults m_openedContainers (CCharAct.cpp:2895).
        The client now records each container it is sent an open for, along with what
        that container hung off at the time, so one that has since moved, changed
        hands or been nested elsewhere no longer counts as open. The character's own
        pack and bank need no explicit open.

FIX - The drop-target ownership check only looked at a DIRECT Pack/BankBox layer, so
        a bag nested inside another player's backpack fell through to a distance
        branch that does not apply to a character-held container - an item could be
        pushed straight into someone else's inventory, bypassing the secure-trade
        flow Source-X routes it to (CClientEvent.cpp:338). The ROOT of the target
        chain now decides; your own pet's pack still accepts items.

FIX - The bank item cap counted only what was already in the bank, so a player could
        pack items into a bag and walk the bag past the limit. Source-X adds the
        incoming container's children to the bank's own count
        (CItemContainer.cpp:941). The check is bank-specific in the reference and
        stays that way: a normal container caps its own slots, not the depth of what
        goes into one.

FIX - Equipping a weapon over a CURSED one displaced the cursed item into the pack -
        the very move the cursed flag exists to refuse. Source-X CanEquipLayer only
        frees a busy layer when its occupant can actually be moved
        (CCharStatus.cpp:470); the equip is now refused before anything is mutated.
        Script and loader Equip calls keep their forced-transfer semantics.

[2026-09-06] - Vendor object transfer and death transfer order (Source-X 01A)
----------------------------------------

FIX - A player vendor sells REAL objects out of its own store; an NPC vendor sells
        from a virtual template list. Buying treated both the same and always
        cloned, so buying a full bag from a player vendor handed over a copy with a
        new uid and nothing inside while the original and its contents were
        destroyed. Source-X Event_VendorBuy splits on exactly this
        (CClientEvent.cpp:1352); a whole-item buy from a player vendor now moves the
        object itself.

FIX - A non-stackable multi-buy was delivered as ONE object carrying Amount=3.
        Source-X creates separate Amount=1 objects (CClientEvent.cpp:1328), which is
        what equipping, durability, resale and container capacity all count.

FIX - Selling to a player vendor destroyed the goods while paying for them, so an
        owned vendor's trade loop could not work. Source-X moves a whole sale into
        the vendor's extra container and puts a dupe of a partial sale there
        (CClientEvent.cpp:1521); an ownerless NPC vendor still destroys it.

FIX - The world save excluded LAYER 27 (VendorExtra) along with the virtual SELL
        stock at LAYER 26, so anything a player vendor bought would have vanished on
        the next restart. Only the regenerable SELL stock is excluded now.

FIX - Death transferred equipment before the pack, and put protected equipment back
        ON the character instead of into the pack. Source-X DropAll transfers the
        pack first and equipment second (CCharAct.cpp:564), and UnEquipAllItems packs
        a protected piece (:664) - the order is what lets it stay there. A ghost no
        longer walks away still wearing its blessed gear.

FIX - An item on the cursor was settled into the pack before the corpse was filled,
        which judged it by the pack's narrower protected set: a plain Blessed item
        being carried went to the corpse. Source-X resolves a dragged item inside
        UnEquipAllItems (CCharAct.cpp:636), so it is judged by the EQUIPMENT set. It
        now travels with equipment, and Character.OnDragCancel cancels the client
        cursor without moving the item.

FIX - @Buy/@Sell passed the UNIT price in ARGN2 where Source-X passes the line total
        (amount x price), and @Buy carried no LOCAL.TOTALCOST at all, so a script
        charging commission or refusing a line above a value read the wrong figure on
        every multi-buy. A vetoed line is subtracted from the running total, as in
        the reference. Deliberate deviation: the line total is built from the
        server-resolved price rather than the price in the client packet.

[2026-09-05] - Pet storage, death inventory, spell costs and vendor authorization
----------------------------------------

FIX - Stabling and shrinking deleted the pet and rebuilt it from a hand-listed
        snapshot, so anything the snapshot did not name was lost: every TAG (BONDED
        and the bonding timer among them), the follower-slot override, the live
        mana/stamina pools, and the pet's whole inventory - a loaded pack animal
        lost its cargo outright. Source-X CChar::Make_Figurine parks the creature
        instead (STATF_RIDDEN + disconnected) and links the figurine to its UID, so
        the same CChar comes back. Both paths now share that model through
        PetStorage, the way MountEngine already did. Stable entries and figurines
        written by an older build still restore through the legacy snapshot path.

FIX - The world loader placed every character into a sector, so a parked pet or a
        mount came back from a save visible, ticking and attackable. A Ridden
        creature is now hidden from its sector on load.

FIX - An item held on the cursor is parented to the character but is neither
        equipped nor in the pack, so the death loot walk never saw it: it stayed
        attached to its owner and the ghost could drop it back into its own pack.
        Source-X reaches it because a dragged item sits on LAYER_DRAGGING and
        UnEquipAllItems walks that layer. Death now settles the held item into the
        pack first, and the drop handler refuses a dead character like the pickup
        handler already did.

FIX - Death used the equipment protected set for pack contents too. Source-X
        CContainer::ContentsTransfer keeps only NEWBIE / MOVE_NEVER / CURSED2 /
        BLESSED2 there, while UnEquipAllItems additionally keeps BLESSED, INSURED,
        NODROP, NOTRADE and QUESTITEM. Plain blessed and no-trade now protect what
        is worn rather than what is loose in the pack. (Both engines transfer the
        pack one level deep, so a protected item inside a plain bag still travels
        with the bag - that part was already parity.)

FIX - A scroll cast was exempt from the reagent check at cast start but not from
        the consumption at completion, so carrying reagents made the same scroll
        cost more than casting it empty-handed. Source-X gates
        Calc_SpellReagentsConsume on the cast source being the caster, on the
        success, fizzle and abort paths alike.

FIX - Cast completion consumed reagents without re-checking them, so moving them
        out of the pack mid-cast produced the spell for free. Source-X re-runs
        Spell_CanCast with fTest=false at Spell_CastDone and fails the whole cast
        when it cannot pay. Consumption is now all-or-nothing and reports failure,
        which fizzles the cast instead of granting it.

FIX - The reagent search only looked at the top level of the backpack, so reagents
        tidied into a pouch read as missing while gold and crafting materials were
        already counted recursively. Resource searches now descend through
        sub-containers, skipping the bank box, a vendor box, an open trade window
        and locked containers (Source-X CItemContainer::IsSearchable).

FIX - Vendor buy and sell checked the vendor, the map and the distance but not
        whether the player was alive, so a queued confirmation from a window left
        open at death still moved gold and stock. Source-X CanTouch refuses a dead
        character every item that is not death-immune.

FIX - The load-test bot's vendor payment exemption keyed on the character name,
        which the player chooses: an ordinary character called "SphereBotanist"
        bought for free, with bot mode switched off. The exemption now requires the
        server to actually be driving that character as a bot.

FIX - Host shutdown measured silence from the child's last log line rather than
        from the shutdown request. A quiet shard logs nothing for minutes, so the
        first evaluation already exceeded the quiet limit and killed the server
        after 250ms - worse than the flat timeout it replaced.

FIX - With Md5Passwords=0 the password is stored verbatim, but verification still
        guessed the comparison from the shape of the stored string: a password that
        happened to be 32 hex characters, or to start with the versioned prefix, was
        compared as a digest and could never be entered again. The comparison now
        follows the setting, as Source-X CAccount::CheckPassword does.

FIX - The account manifest and the removal of the files it supersedes are two
        halves of one commit. When the manifest could not be written the save still
        reported success, leaving the extension probe free to pick the stale
        snapshot; a failed manifest now removes any manifest left naming the
        previous file, and the save reports failure if superseded files also survive.

[2026-09-05] - Account name validation, panel authorization, save and scheduler fixes
----------------------------------------

FIX - Account names were accepted without validation and then written as bare save
        sections, so a name the reader skips ("EOF", "WORLDx") created an account
        that vanished on the next start, and a name carrying a line break made
        every later account write throw - silently, since SaveAccountsToDisk logs
        and continues. The 0x80 login packet reads its 30-byte name field without
        filtering, so with AccApp auto-creation a crafted client could reach both.
        Source-X CAccount::NameStrip is now ported: printable-ASCII stripping with
        the ACCOUNT_NAME_VALID_CHAR set, the EOF/ACCOUNT reserved prefixes, the
        account verb keys and the [OBSCENE] list. Lookup stays lenient so names
        already in a legacy file keep working, and the writer skips a single
        unwritable name instead of aborting the whole file.

FIX - The admin panel decided whether a request was in scope for the bearer token
        with a case-sensitive path test, while endpoint routing matches paths
        case-insensitively. "/API/server/running" therefore reached the same route
        handler as "/api/server/running" with no token at all, including the
        mutating endpoints. Every path comparison in the middleware is now ordinal
        and case-insensitive.

FIX - The panel hub validated its token only during the WebSocket handshake, and
        logout removed the token without touching connections already open, so a
        connection established before logout kept executing admin commands and kept
        receiving broadcasts. Hub calls re-validate, and token revocation or expiry
        now closes the connections opened with that token.

FIX - /api/setup/apply accepted an empty admin password and reported success, which
        left the UI claiming setup was complete while /api/setup/needed still said
        otherwise. It now validates the server name, port range, panel port and
        tick mode as well.

FIX - /api/setup/config returns the admin password as a fixed mask so the real one
        never reaches the browser, but /api/setup/apply stored whatever it was
        given. Re-running the wizard without retyping the password therefore reset
        panel access to the mask string. The mask is now understood as "keep the
        current password".

FIX - The setup wizard offered its script-install step before the admin password
        existed, so the step had no token and returned 401. Scripts now come after
        Apply, which establishes the panel session first; only the setup endpoints
        the wizard needs before that point are reachable anonymously.

FIX - spheredata.scp was assembled by hand instead of through the shared save
        writer, so global variables, list elements and GM page reasons skipped the
        value encoding every other save file uses and were truncated at their first
        line break on reload. They now round-trip verbatim, and the file stays
        byte-order-mark free for classic readers.

FIX - Changing SaveFormat writes the account file in the new format and deletes the
        old ones, but a failed delete only logged a warning and the loader picked by
        a fixed extension priority - so a surviving older file outranked the current
        one, hiding new accounts and rolling back password and ban changes. The save
        now records the active snapshot in sphereaccu.manifest; a directory without
        one still loads by extension probe.

FIX - The Host gave the game server a flat eight seconds to shut down and then
        killed its process tree. A clean shutdown drains an in-flight background
        save, runs a final save and waits for that too, so a large world on slow
        storage was killed while working correctly. The wait now follows the child's
        log output as a progress signal: it gets as long as it needs while it is
        reporting progress, up to a ceiling. HostShutdownQuietMs (20s) and
        HostShutdownTimeoutMs (180s) configure it.

FIX - TimerWheel.Remove could only clear the scheduled set, leaving the cancelled
        entry in its slot; rescheduling the same NPC then had the new scheduling
        consumed by that leftover, firing at the old time and never at the new one.
        Program.Tick.WakeNpc does exactly Remove+Schedule. Slot entries now carry a
        scheduling generation, so a leftover is dropped.

FIX - A TimerWheel slot index aliases every 25.6 seconds, so anything scheduled
        beyond one revolution fired on the aliased slot instead. NpcAI parks idle
        NPCs 30-60 seconds out, which meant every idle NPC woke roughly 2.3 times
        more often than intended. Entries now carry their deadline and are re-parked
        for another revolution when they are not due; the slot for a deadline is
        also rounded up rather than down, so a deadline between slot boundaries no
        longer misses its slot by a full revolution.

FIX - The per-IP connect attempt history had no removal, expiry or capacity limit,
        and ConnectionRateLimiter.Cleanup had no production caller at all, so both
        collections grew one entry per distinct source address for the lifetime of
        the process. Both now expire idle entries on the Source-X NETTTL schedule
        and enforce a capacity ceiling, driven from the write path so expiry cannot
        be left unwired.

FIX - The panel sampled CPU and thread count from its own process. Under the Host
        that is the Host process, so the dashboard showed the Host's load beside the
        game server's object and player counts. The sampler now runs in the game
        process and travels with the stats snapshot. It also returned 0 whenever two
        callers arrived inside the same half-second, so the HTTP endpoint and the
        SignalR push disagreed about the same moment; both now read one snapshot.

FIX - Md5Passwords was read from sphere.ini, copied onto every account and never
        consulted, because SetPassword hashed unconditionally. It now behaves as
        Source-X MD5PASSWORDS does: 1 stores an MD5 digest, 0 stores the password
        verbatim, and the login-time upgrade of a legacy plaintext password only
        runs when hashing is on. The default is 1, which is what SphereNet has
        always actually done (Source-X defaults to 0).

[2026-07-31] - View delta: an object could stay invisible until a resync
----------------------------------------

FIX - The view delta keeps two halves per client: KnownItems, what the client has
        been told about, and LastKnownItemState, what it was last told. A uid in
        the first with no entry in the second was treated as already up to date:
        the updated-items pass recorded the current state and sent nothing. From
        then on every tick compared equal, so the object was never drawn - it only
        reappeared once a resync cleared KnownItems, which is why walking to
        another sector and back brought it back. The pass now re-sends when it has
        no recorded state instead of assuming the client is in sync.

FIX - The death path put a corpse into KnownItems without recording its state,
        which is what produced that half-state. Both halves now move together, so
        a later change to the corpse (carving, hue, decay art) still reaches the
        killer's client.

FIX - A refresh request could be dropped. The tick cleared ViewNeedsRefresh for
        every client it looked at, including those whose BuildViewDelta returned
        null because the character was mid-handoff (not attached yet, not playing,
        spectating a replay). The flag is now consumed only when a delta was
        actually built and applied.

FIX - MoveDoorLeaf wrote the door's new position directly when the world refused
        the placement (an out-of-bounds hinge tile), leaving the leaf registered in
        its old sector while reporting new coordinates. Since the view scan walks
        sectors, the door was then found or missed depending on which sectors the
        viewer covered. The move is now skipped when the world refuses it.

[2026-07-31] - DEADCANNOTSEELIVING implemented
----------------------------------------

FIX - DEADCANNOTSEELIVING was parsed and never used, so a ghost saw every living
        NPC around it. The Source-X CanSeeAsDead rule now applies: a ghost keeps
        seeing other ghosts, living players, its own pets and healers, but loses
        sight of ordinary living NPCs. GMs and AllShow are exempt.

        The setting is a mode, not a flag - upstream reads 0/1/2 where 2 also
        stops an NPC from seeing living players that are not its owner. It was
        typed as a bool here, which could never express mode 2; it is an int now
        and an ini still written as true/yes keeps meaning mode 1.

CHANGE - Upstream applies the rule once, as a screen wipe at the moment of death,
        so the hidden NPCs drift back into view as the ghost walks. Here it is a
        standing rule in the per-observer view delta, so they stay hidden while
        the ghost moves and reappear the instant it is resurrected. Dying and
        resurrecting now request a rebuild of the character's own view through a
        new Character.OnOwnViewRefreshNeeded hook, since neither is a movement
        and the view would otherwise not refresh until the next step.

[2026-07-31] - NPCDISTANCEHEAR and MAPVIEWSIZEMAX were parsed but never used
----------------------------------------

FIX - NPCDISTANCEHEAR did nothing: NPCs were scanned on the speaker's talk-mode
        radius, so a yell reached every NPC within 48 tiles no matter what the
        setting said. Source-X keeps a separate radius for the NPC pass
        (CClientEvent.cpp:1892) and gates only on it. NPCs now use their own
        range with the reference semantics - 0 is the 18-tile default sight, a
        positive value is that many tiles, and a negative value is that many
        tiles with the line-of-sight check skipped.

FIX - MAPVIEWSIZEMAX did nothing: the upper bound on a client's 0xBF.0x1C view
        size request was hardcoded to 24, which happens to be the default, so
        lowering the key had no effect. It is now the configured value.

[2026-07-31] - House storage budget: vendor limit dropped, no bonus storage, unreadable access list
----------------------------------------

FIX - BASEVENDORS was read off the [MULTIDEF] into the definition and then never
        reached the placed house, so every house had a vendor budget of zero and
        MAXVENDORS could not be read at all. PlaceHouse now applies it, and the
        value persists with the house.

FIX - INCREASEDSTORAGE did not exist, so a house could not carry the percentage
        storage bonus Source-X applies. Every cap now derives from it the way the
        reference does: MaxStorage is the base budget plus that percentage,
        MaxLockdowns is MaxStorage * LOCKDOWNSPERCENT / 100 (it used the base
        budget before, ignoring the bonus), MaxSecure is the remainder, and
        MaxVendors scales the vendor budget by the same percentage. The bonus
        defaults to 0, so an existing house keeps its current caps.

FIX - The house access list could be written but never read: ADDACCESS/DELACCESS
        maintained it while the property surface had no entry for it, unlike every
        other house list. HOUSE.ACCESSES (count) and HOUSE.ACCESS.n now read it.

FIX - LOCKDOWNSPERCENT, BASESTORAGE, BASEVENDORS and INCREASEDSTORAGE written on a
        multi that is already a registered house only ever set a tag, leaving the
        running house on its defaults. They now reach the house as well as the tag,
        so a script can retune a placed house's budget.

CHANGE - Added the missing storage readouts: HOUSE.MAXSTORAGE, HOUSE.CURRENTSTORAGE
        (lockdowns plus secured containers, the shared pool Source-X counts),
        HOUSE.INCREASEDSTORAGE, HOUSE.LOCKDOWNSPERCENT, HOUSE.BASEVENDORS,
        HOUSE.MAXVENDORS and the HOUSE.ISOWNER.<uid> predicate.

[2026-07-31] - Ship steering: wheel spam stalled the ship, scripted sailing ran half speed
----------------------------------------

FIX - Every wheel order re-armed the move timer, so a pilot clicking or holding the
        wheel pushed the next step a full period further out each time and the ship
        stalled instead of sailing. Source-X drops a wheel order that arrives while
        the timer is still running (CCMultiMovable.cpp:78, "otherwise for each click
        with mouse it will do 1 move"). SetMoveDir now knows whether the order came
        from the 0xBF.0x33 wheel packet or from a script/tillerman command and
        applies that guard to the wheel path only.

FIX - Ships driven by a tillerman or script command sailed at half the reference
        speed. The move delay only stretches to the full period in the SLOW speed
        mode, and only a wheel order re-selects the mode (Source-X guards that
        assignment with fWheelMove), so the starting mode decides how a scripted
        ship sails. Upstream starts at SMS_NORMAL, the value its own comment labels
        "one tile"; SphereNet started at SLOW instead, which is the one value that
        doubles the interval. The default is now the reference one, so a scripted
        SHIPFORE advances on the halved interval like Source-X. Ships steered from
        the wheel are unaffected.

FIX - Repeating a one-tile order in the direction the ship already faces did not
        promote it to continuous sailing (Source-X CCMultiMovable.cpp:81), so a
        second click kept stepping one tile at a time instead of getting under way.

CHANGE - Added the rigging sound Source-X plays on roughly one move order in ten
        (0x12 or 0x13, skipped on an ATTR_MAGIC vessel), behind a new OnShipSound
        hook the server broadcasts to nearby clients.

[2026-07-31] - The three permanently skipped tests now run
----------------------------------------

FIX - The three StairThrowDiagnosticTests were permanently skipped and could never
        run: they carried a hard Skip attribute AND pointed at C:\mortechUO\mul,
        which no longer exists. They now probe a candidate list (the live server
        pack's own mul directory first) and are gated only on the data being
        present, matching how the other real-data tests stay CI-safe. Two of them
        run against the real client data on this box; the third still no-ops
        because the map0x UOP variant it compares is absent. They also wrote their
        dumps into wiki/ inside the repo, dirtying the working tree on every run;
        the output now goes to the temp directory. The suite reports 0 skipped.

CHANGE - Added a regression test for the worn-container delete path: removing a
        backpack that holds a nested bag must take the whole subtree with it and
        leave no auditor anomaly behind.

[2026-07-31] - Two blind spots closed in the world invariant auditor
----------------------------------------

FIX - The auditor had two blind spots, both on the "item vanished" side:

        The index-vs-Contents comparison bailed out early on a container whose
        server-side Contents were empty, so index entries under an empty bag - the
        ghost items a client draws and nobody can pick up - were never reported.
        Both directions are now checked regardless of content count.

        Only items were walked, never characters, so an item whose ContainedIn
        names a CHARACTER was never validated at all. Worn gear, memory items and
        the item being dragged legitimately live there; anything else is in limbo
        - invisible, unreachable, still saved and still counted. That is now
        reported as CharacterLimboItem.

[2026-07-31] - Four client packets Source-X answers were silently dropped
----------------------------------------

FIX - Four client packets that Source-X answers had no handler and were dropped:

        0xEC equip-item macro (PacketEquipItemMacro) - the client's "equip these"
        macro did nothing. Items are lifted and worn through the existing pickup
        and equip path, so reach, ownership and the @PickUp_*/@EquipTest/@Equip
        triggers all still run. Only items the character already carries qualify
        (upstream checks GetTopLevelObj) and the batch is capped at three, the
        same exploit guard upstream documents.

        0xED unequip-item macro (PacketUnEquipItemMacro) - strips the named
        layers back into the pack, with the same cap. Pack, hair and mount layers
        are never touched.

        0xC2 unicode prompt response (PacketPromptResponseUnicode) - the
        counterpart of the 0x9A ASCII reply, feeding the same handler.

        0xFB show-public-house-content toggle (PacketPublicHouseContent) - stored
        per connection like the reference _fShowPublicHouseContent.

        Still unhandled and routed to the unknown-packet path: 0xA7 (tip request,
        needs a tips/notice system that does not exist yet) and 0x3F (UltimaLive
        static update, an opt-in map protocol).

CHANGE - PROTOCOL_MATRIX.md documents the new opcodes, and the fixed-length
        unsupported-opcode framing test no longer uses 0xFB as its example since
        that opcode is now handled.

[2026-07-31] - Client version now survives the login-to-game-server handoff
----------------------------------------

FIX - The game connection decided every version-dependent packet format with no
        client version at all. The classic client sends its version only in the
        0xEF seed on the LOGIN connection; the game connection gets a bare 4-byte
        seed, so unless encryption keys happened to identify the client, the server
        was still at version 0 when it answered 0x91 - the 0xBD request it sends
        there cannot have been answered yet. With ClientEra left at Sphere56x that
        meant 0xB9 went out with a 2-byte flag field while any 6.0.14.2+ client's
        packet table expects 4, so the client read two bytes of the following packet
        as part of it. The same unknown version silently disabled the buff bar and
        AOS tooltips for the whole login sequence.

        Source-X solves this by stashing the detected version on the account
        (CClientLog.cpp:916 - "pass detected client version to the game server") and
        reading it back from the account rather than the live socket
        (send.cpp:3866). SphereNet now does the same: the login connection writes
        the CLIENTVERSION account tag, 0xBD writes REPORTEDCLIVER, and the game
        connection adopts the higher of the two before any capability decision.
        Both are plain account tags, so no account format change.

[2026-07-31] - Buff bar follow-up: magnitude persistence, Blood Oath, web stuck icon
----------------------------------------

FIX - The buff tooltip magnitude was session state, so a restart left stat-buff
        tooltips showing a value derived from the stat delta instead of the number
        the cast recorded. Source-X keeps it in the spell memory's
        m_itSpell.m_spelllevel, which is MOREY and therefore written to the world
        save with MOREP. SphereNet now mirrors it the same way: AttachSpellMemory
        fills the memory's MOREY (it was hardcoded to 0), and the effect record
        carries the value as a trailing field. Older records without the field
        keep the stat-delta fallback, so existing saves load unchanged.

FIX - Blood Oath raised the wrong icon on the wrong character. Its effect lives on
        the CASTER, so the generic per-effect notify gave the caster the victim's
        curse icon and the victim nothing at all. Source-X CCharSpell.cpp:1312
        raises two icons: BloodOathCurse on the victim carrying the caster's name
        twice, and BloodOathCaster on the caster carrying the victim's name. Both
        are now raised explicitly and both drop when the bond breaks; the spell was
        removed from the one-icon-per-spell map that cannot express this.

FIX - Being stuck in a spider web showed no icon. Source-X LAYER_FLAG_Stuck raises
        the paralyze icon while a character is held (CCharAct.cpp:358) and drops it
        when the layer goes (:466). The web step now raises it and destroying the
        web clears it. No countdown is sent - the web is escaped by struggling, not
        by a timer.

[2026-07-30] - Buff/debuff bar: version-gate race, missing icons, empty tooltips
----------------------------------------

FIX - Buff icons could be suppressed for an entire session. Packet 0xDF is gated on
        SupportsBuffIcon (Source-X PacketBuff::CanSendTo / MINCLIVER_BUFFS), and the
        game socket has no version of its own: the classic client sends the bare
        4-byte seed there, not the 0xEF seed that carries a version, so the server
        only learns it from the 0xBD reply it requests after 0x91. With ClientEra set
        to anything but Modern, every buff raised before that reply landed was dropped
        and never re-sent - including the whole login resend. HandleClientVersion now
        rebuilds the buff bar (SpellEngine.ResendBuffs) when a newly parsed version
        flips SupportsBuffIcon from false to true.

FIX - Buff tooltips rendered their cliloc placeholders empty. Source-X fills a NumBuff
        array with the effect magnitude and passes it to addBuff; SphereNet always took
        the no-argument branch of the packet. Character.OnClientBuffChanged now carries
        the formatter arguments, ScheduleEffectExpiry records the effect magnitude, and
        the argument counts follow the reference exactly: one per stat for
        Clumsy/Feeblemind/Weaken/Strength/Agility/Cunning, three for Bless and Mass
        Curse, and three plus four fixed 10s for Curse. After a world reload the resend
        derives the magnitude from the persisted stat delta.

FIX - The gargoyle fly toggle raised BuffIcon.GargoyleFly, but the icon had no catalog
        entry, so the lookup failed and no packet was ever sent. Added with the
        Source-X clilocs (receive.cpp:3366).

FIX - The criminal-status icon was never raised. Source-X toggles it with
        STATF_CRIMINAL (CCharAct.cpp:352 / :460); SphereNet now does the same from
        SetStatFlag/ClearStatFlag and includes it in the login resend.

CHANGE - BuffIcon completed to the full Source-X BUFF_ICONS table (was 51 of ~190
        entries, stopping at GargoyleFly). ADDBUFF/REMOVEBUFF now range-check the icon
        argument against BI_START..BI_QTY like Source-X CClient.cpp instead of emitting
        a packet the client discards.

CHANGE - ClientBuffCatalog extended from 21 to 36 icons: the polymorph-layer forms
        (Horrific Beast, Lich Form, Vampiric Embrace, Wraith Form, Reaper Form, Stone
        Form, Beast/Monster Form), Strangle, Corpse Skin, Blood Oath (both sides), Gift
        of Renewal, Attune Weapon, Thunderstorm, Essence of Wind, Ethereal Voyage, Gift
        of Life, Arcane Empowerment, Mortal Strike, Criminal Status and Gargoyle Fly.
        Cliloc pairs are copied verbatim from the Source-X call sites, including the two
        places upstream reuses another spell's clilocs.

[2026-07-30] - Panel dependency audit is clean again (CI unblocked)
----------------------------------------

FIX - The CI "Audit panel dependencies" step (npm audit --audit-level=high) failed on
        a high-severity postcss advisory (GHSA-6g55-p6wh-862q, arbitrary file read via
        attacker-controlled sourceMappingURL), pulled in transitively through vite.
        The panel lockfile was refreshed (postcss 8.5.10 -> 8.5.25, nanoid 3.3.11 ->
        3.3.16, brace-expansion 2.1.2 -> 2.1.4 / 5.0.7 -> 5.0.9). A second high
        advisory (GHSA-mh99-v99m-4gvg, brace-expansion DoS) reached the tree through
        vue-tsc 2.x -> @vue/language-core -> minimatch 9, which cannot resolve a patched
        brace-expansion; vue-tsc was upgraded to ^3.3.8 (the only available fix).
        npm audit now reports 0 vulnerabilities.

CHANGE - vue-tsc 3 reports template-only refs as unused, so two dead template refs
        (GumpDesignerPage stageEl, ServerPage cmdInput) were removed along with their
        ref= attributes. Neither was read from script. Panel typecheck, lint, tests
        and build all pass.

CHANGE - The CI audit step was split. "Audit panel runtime dependencies" runs
        npm audit --audit-level=high --omit=dev and still blocks the build - it covers
        everything that ships in the panel bundle. Build-time tooling (vite, vue-tsc,
        eslint, vitest) is audited in a separate report-only step with
        continue-on-error, so a new advisory in a dev dependency no longer blocks
        merges for code that never reaches users.

[2026-07-24] - SECTORSLEEP config is now applied instead of a fixed 10-minute delay
----------------------------------------

FIX - The SECTORSLEEP setting (minutes a clientless sector waits before sleeping)
        was read from sphere.ini but never applied — sectors always used the
        hardcoded 10-minute default. It is now wired to Sector.SleepDelayMs via a new
        SphereConfig.SectorSleepMs (minutes * 60000; 0 disables sleeping, negatives
        clamp to 0), matching Source-X _iSectorSleepDelay. The config default was
        aligned to Source-X (10 minutes) so unconfigured servers are unchanged.

[2026-07-24] - Character creation honors race (elf/gargoyle body) and shirt/pants colors
----------------------------------------

FIX - Character creation decoded the gender/race byte with the pre-7.0 encoding for
        every client, so on a modern (Stygian Abyss) client "human" (2/3) was read as
        elf and "elf" (4/5) as gargoyle. The race is now decoded per client era
        (7.0.0.0+: 2/3 human, 4/5 elf, 6/7 gargoyle; older: 0/1 human, 2/3 elf),
        matching Source-X PacketCreate.
FIX - The chosen race was never applied to the body — every new character got a
        human body. Elves (0x025D/0x025E) and gargoyles (0x029A/0x029B) now get their
        correct body.
FIX - The shirt and pants tints the client sends at the tail of the create packet
        were not read; they are now parsed and applied to the starter shirt/pants.

[2026-07-24] - Host locates the server exe by the current TFM, not a stale net9.0 path
----------------------------------------

FIX - The Host's development fallback that finds SphereNet.Server.exe hardcoded a
        "net9.0" output folder, so after the .NET 10 retarget it could no longer
        locate the server build and Host mode failed to start. It now derives the
        target-framework folder from the Host's own output directory (e.g.
        "net10.0"), so the path tracks future .NET version bumps automatically. The
        startup banner's ".NET 9 Port" label is corrected to ".NET 10".

[2026-07-23] - Ship speed now comes from the script SHIPSPEED, not a fixed value
----------------------------------------

CHANGE - The ship engine ignored the [MULTIDEF] SHIPSPEED=period,tiles directive and
        ran every ship at a fixed 500 ms period. It now parses SHIPSPEED (period in
        tenths of a second, Source-X MSECS_PER_TENTH; tiles per step) into the multi
        def and applies it when a ship is placed — matching Source-X and letting each
        ship's speed be tuned from the script (raise the period to slow a ship). The
        script speed is authoritative on load too, so existing ships pick up their
        def's SHIPSPEED. Ships whose def has no SHIPSPEED keep the 500 ms default.

[2026-07-23] - Freshly placed ships start unanchored; new default config values
----------------------------------------

CHANGE - A newly placed ship no longer starts anchored, so movement commands
        ("Unfurl sail"/"Forward"/etc.) work immediately without a "raise anchor"
        first. "Drop anchor" still parks it and "raise anchor" releases it.
CHANGE - Default config values updated (used when the sphere.ini key is absent):
        ClientEra=Modern, ToolTipMode=0 (tooltips off), AdminPassword=1234.
        StateRecordingEnabled=0, ClientMax=256 and ScpFiles=scripts/ were already
        the defaults.

[2026-07-23] - World save carries a cross-file generation stamp so a torn commit is detected
----------------------------------------

FIX - The three world-save files are committed with three back-to-back renames;
        a crash in that (now sub-millisecond) window could still leave a new
        sphereworld beside an old spherechars — a mixed generation each file
        validates individually, so the loader could not tell it apart from a good
        one and loaded it, silently dropping cross-file references. Every item/char
        shard now opens with a [SAVEID] record carrying the save index (spheredata
        already records it as SAVECOUNT), and the loader verifies all three agree.
        A generation whose stamps disagree is treated like a corrupt one and the
        loader falls back to the last internally-consistent generation. Legacy saves
        with no stamp are accepted unchanged (the check is skipped when any file
        lacks a stamp).

[2026-07-23] - Central oversize guard now catches raw over-length buffers (replay bypass)
----------------------------------------

FIX - The send-path oversize guard keyed only on PacketBuffer.IsOversize, a flag
        set while a packet is built (WriteLengthAt). A raw PacketBuffer(byte[]) —
        such as a recorded broadcast replayed verbatim by the diagnostics tooling —
        never runs that path, so an over-wire-length buffer carried the flag unset
        and slipped past the guard, desyncing the client stream. The guard now also
        drops any buffer whose length exceeds the 65535 wire ceiling regardless of
        the flag, closing the replay bypass and any future raw-buffer send path.

[2026-07-23] - OPL tooltip gains a total-size budget so an over-budget packet is refused early
----------------------------------------

FIX - The 0xD6 tooltip builder capped the property count (512) and each argument's
        length (8192 chars) but not the total, so 512 near-max arguments (~8 MB)
        passed both per-item checks and built a huge buffer that WriteLengthAt then
        dropped at send — protecting the client but leaving the server to allocate
        and format megabytes per hostile/buggy tooltip. A running total-bytes check
        (PacketBudget.MaxOplTotalBytes = the 65535 wire ceiling) now rejects such a
        packet before allocation, short-circuiting as soon as the total is exceeded.

[2026-07-23] - Loader rejects an incomplete sharded generation instead of loading it partially
----------------------------------------

FIX - When a shard manifest referenced a file that was not on disk, the loader
        logged a warning and loaded the surviving shards anyway — silently dropping
        every object in the missing shard. A generation with a missing manifest
        shard is now flagged incomplete and treated like a corrupt one: the loader
        falls back to the previous whole generation, or raises the clear error when
        no intact backup exists, rather than booting a world with holes.

[2026-07-22] - Boot recovers from a backup when the current save is missing, not just corrupt
----------------------------------------

FIX - The world loader fell back to a .bakN backup when the current save was
        corrupt, but returned a blank world the moment the current generation was
        empty — so if the current save files were deleted or lost while good
        backups sat on disk, the server silently booted an empty world. Load now
        keeps probing backup generations past a missing current save and starts
        fresh only when NO generation exists at any level; when generations exist
        but every one fails validation it still raises the clear error rather than
        blanking the world.

[2026-07-22] - Custom-house design load is bounded to the editor's tile cap
----------------------------------------

FIX - HouseDesign.LoadFromTags read every DESIGN_n tag with no total-count limit,
        while the interactive editor caps a design at 10,000 tiles per placement. A
        corrupt or hostile save/script with more DESIGN_n tags than that could
        balloon memory and, when the house was streamed to a client, overflow the
        0xD8 design packet's ushort tile-count and byte plane-count fields
        (desyncing the stream). The load now enforces the same cap
        (HouseDesign.MaxTiles), keeping the first N tiles by index and dropping the
        rest; CustomHousingEngine's placement cap now references the same constant.

[2026-07-22] - World save commits all three files transactionally (no mixed generations)
----------------------------------------

FIX - The world save writes three logical files (sphereworld, spherechars,
        spheredata) and used to commit each to its live name before the next was
        even written. A crash between them could publish a new-generation
        sphereworld beside an old-generation spherechars — a mixed, internally
        inconsistent world that the loader silently degrades into dangling
        CONT/EQUIP references (data loss, not a crash). WritePrepared now stages
        every file to its .tmp sibling first and only then commits all three
        back-to-back, so a write failure leaves the previous generation completely
        intact and the window in which the on-disk set is inconsistent shrinks from
        the whole multi-second write of the later files to a few adjacent renames.

[2026-07-22] - Version-correct broadcast framing for corpse contents (0x25) and damage (0x0B)
----------------------------------------

FIX - The corpse-contents broadcast sent one shared 0x25 add-to-container packet
        (21 bytes, with the 6.0.1.7+ grid-index byte) to every nearby observer. A
        pre-6.0.1.7 client in range expects 20 bytes, so it read the grid byte as
        the first byte of the container serial and desynced its whole packet
        stream. The corpse broadcast now dispatches a per-recipient variant sized
        to each observer's version.
FIX - The combat damage popup broadcast a single 7-byte 0x0B packet to everyone.
        Per Source-X CClient::addShowDamage, only clients >=4.0.7a understand 0x0B;
        4.0.0-4.0.6 need the 11-byte 0xBF.0x22 form and older clients get no packet
        at all. Damage broadcasts now bucket recipients by version and send each
        its own format. Recipients of the same variant still share one built buffer,
        so the common all-modern case keeps the broadcast fast path.

[2026-07-22] - Tick-yield idle-CPU measurement and opt-in adaptive yield mode
----------------------------------------

ADD  - [tick_stats] now reports loops (main-loop iterations this window) and lpt
        (loops-per-tick). This is the idle-CPU / hot-spin gauge: the default hybrid
        yield never actually sleeps, so an idle server churns thousands of loops per
        100ms tick (high lpt); an adaptive/sleeping yield reports ~1. Take this
        measurement before and after changing TickSleepMode on a small host.
ADD  - TickSleepMode=3 (adaptive): sleeps out the slack until the next tick is due,
        capped at a few ms, so idle CPU drops toward zero without moving the tick
        cadence and with input latency bounded by the cap. Opt-in; the default
        (2 = hybrid) is unchanged so existing latency behaviour is preserved until
        an operator measures and switches deliberately.

[2026-07-22] - Main-loop shutdown flag made volatile so cross-thread requests are seen
----------------------------------------

FIX - The main game loop's `_running` flag was a plain bool written from several
        threads (Console.CancelKeyPress signal handler, telnet accept thread,
        WinForms UI thread, IPC/panel callback) and read every iteration of the
        tight `while (_running)` loop. Without a memory barrier the JIT may cache
        the read, so a shutdown request from another thread could go unobserved for
        an unbounded time. The flag is now volatile, so every write is published and
        each loop iteration re-reads it — CancelKeyPress, telnet, panel and delayed
        SERV.SHUTDOWN all take effect on the loop's next pass.

[2026-07-22] - IPC server accepts host reconnects without a game-server restart
----------------------------------------

FIX - The named-pipe IPC server waited for a single host connection, served it,
        and returned when the host disconnected — so the panel / console bridge
        could never reconnect without restarting the whole game server. RunAsync
        now runs a cancellation-bound accept loop over one reused pipe instance,
        resetting it with Disconnect() between hosts (the canonical reconnect
        pattern; dispose-and-recreate-per-session races the OS release of the pipe
        name and wedges the loop with "all pipe instances are busy"). Each session
        gets a fresh reader/writer under a session-scoped token so the stats loop
        dies with its connection and can never write to a later session.

[2026-07-22] - Vendor list name-length overflow and 0xA7 tip-request framing
----------------------------------------

FIX - The 0x74 vendor buy list wrote each row's name length as a single byte from
        name.Length + 1. A name of 255+ characters overflowed the byte (255 -> 0)
        while the full string was still written, so the client mis-read the length
        and every following row of the buy list desynced. The length now always
        equals the bytes written; over-long names are truncated deterministically
        (254 name bytes + NUL). The 0x9E sell list gained the same clamp against its
        word-sized prefix.
FIX - Packet 0xA7 (Tip/Notice Request, client->server) was defined as variable
        length, so the framer read the request's tip-index word as a length field —
        dropping the connection (index 0 -> length 0) or stalling the stream.
        Source-X frames it as a fixed 4-byte packet; the definition now matches, so
        it is consumed cleanly (no handler needed) and any following packet frames
        correctly.

[2026-07-22] - Text save line policy: lossless multi-line values, bounded line reader
----------------------------------------

FIX - The text (.scp) save writer silently cut a property value at its first line
        break, dropping everything after it with no diagnostic (and a value crafted
        with an embedded CRLF could smuggle a second KEY=VALUE line into the record).
        A value containing a line break is now escape-encoded behind a leading
        control-byte sentinel so it round-trips losslessly; single-line values — the
        overwhelming majority — are still written byte-identically, and the reader
        returns any value lacking the sentinel verbatim, so legacy saves are
        unaffected.
FIX - TextSaveReader used an unbounded ReadLine, so a corrupt or malicious save
        with no line terminator could buffer the entire (multi-gigabyte) file into
        a single string and exhaust memory. A bounded line reader now caps one
        physical line and raises InvalidDataException on overflow, letting the boot
        generation-fallback recover instead of the loader OOM-ing.

[2026-07-22] - Vendor-stock filter now skips items nested at any depth, not just direct children
----------------------------------------

FIX - The save skipped only items whose DIRECT container was a vendor-stock
        container. An item nested in a sub-bag inside vendor stock was still
        persisted, with a CONT pointing at a parent that was never saved — so it
        dropped to the ground at a stale position on the next load (world clutter).
        The filter now walks the full ancestor chain (depth-capped) and skips an
        item if any ancestor is vendor stock; normal player-container nesting is
        unaffected.

[2026-07-22] - Text save reader no longer drops a section when a record is read partially
----------------------------------------

FIX - TextSaveReader.NextRecord consumed its stashed next-header BEFORE draining
        the previous record. A caller that stopped mid-record (or read no
        properties) made the drain stash the next header, but that stash was never
        re-checked in the same call — so NextRecord skipped a whole section and the
        stale header came back out of order later. NextRecord now drains the
        current record first, then consumes the single stashed header, then reads
        forward. Fully-drained records (the common load path) are unaffected.

[2026-07-22] - Duplicate SERIAL/UUID in a save no longer corrupts the UUID index
----------------------------------------

FIX - When a save record's SERIAL or UUID duplicated an already-loaded object,
        the loader deleted the just-parsed object but kept processing its
        properties. The UUID branch then re-indexed the deleted object, so
        FindByUuid returned a dead object. The loader now drains the rest of a
        retired record with no side effects (the skip check moved to the top of
        the property loop).

FIX - Deleting an object whose UUID had been set to a value another live object
        owns (a duplicate-UUID record) evicted that other object's index entry.
        DeleteObject now removes a UUID entry only when it actually points at the
        object being deleted, mirroring the serial-registration guard.

[2026-07-22] - Object/world exports are atomic; a failed export can't corrupt the rollback snapshot
----------------------------------------

FIX - ExportObject/ExportObjects/ExportWorld/ExportStatics wrote straight to the
        final path (FileMode.Create), so a crash mid-write left a truncated file.
        Because destructive world-ops use ExportObjects as their rollback
        snapshot, a truncated snapshot could overwrite a good one and leave the
        deleted objects unrecoverable. Exports now write a sibling .tmp, verify it
        re-reads with the expected record count, then atomically move it over the
        final. A crash leaves the previous final intact (only a stray .tmp), and a
        write that doesn't validate throws without replacing the final.

FIX - Because a failed export now throws, destructive RestoreFile aborts before
        its delete phase when the rollback snapshot can't be written — it no
        longer deletes live objects against a snapshot that isn't there.

[2026-07-22] - StateRecorder no longer shares one SQLite connection across threads
----------------------------------------

FIX - StateRecorder used a single SqliteConnection from the flush thread, the
        ThreadPool cleanup, and caller-thread pin/share/query calls.
        Microsoft.Data.Sqlite connections are not thread-safe, so concurrent use
        produced sporadic native "misuse"/transaction errors. The flush thread now
        owns its connection exclusively; cleanup, pins, shares and all queries open
        their own short-lived connection. WAL (persisted in the DB file) lets them
        run concurrently with the flush writer, and busy_timeout waits out a
        transient write lock instead of throwing SQLITE_BUSY.

FIX - A failed flush re-queues the drained move/snapshot records (bounded, so a
        persistent failure can't grow the queues without limit) instead of
        dropping them, so a transient error no longer loses recorded data.

[2026-07-22] - Version-correct framing for the tracking arrow (0xBA) and multi target (0x99)
----------------------------------------

FIX - The 0xBA quest/tracking arrow was always sent as the 10-byte 7.0.9+ form.
        A pre-7.0.9 client expects 6 bytes and reads the trailing 4 as the next
        opcode, desyncing its stream. PacketArrowQuest now emits 6 or 10 bytes,
        and the tracking-skill sender (a unicast to one player) picks the form
        from that client's version.

FIX - The 0x99 multi-placement target added its trailing hue dword only at
        7.0.13; the correct boundary is 7.0.9 (Source-X send.cpp), so 7.0.9-7.0.12
        clients were sent 26 bytes where they expect 30. The gate is now 7.0.9.

NOTE - The remaining T3 items are the mixed-version BROADCAST packets — 0x0B
        combat damage and the 0x25 corpse-contents grid byte — which are built
        once and shared across nearby clients of differing versions. Fixing those
        needs per-recipient (version-bucketed) broadcast and is a separate change;
        if old-client support is not a goal, raising the configured minimum client
        version is the alternative.

[2026-07-22] - A failed multicore tick no longer replays and double-applies itself
----------------------------------------

FIX - When a multicore tick threw or timed out after partially running (some
        sectors ticked, spell expirations applied, NPCs consumed from the timer
        wheel), the fallback re-ran the SAME tick single-threaded, double-applying
        those side effects. It now abandons the partial tick instead: it logs,
        drops to single-thread mode, and lets the next scheduled tick run cleanly,
        so no side effect is applied twice and the world tick count advances once.

FIX - The NPCs a multicore tick consumed from the timer wheel are tracked, so an
        abandoned tick reschedules them instead of dropping them out of the wheel
        (which would freeze those NPCs). The reschedule is idempotent — the wheel
        dedups on NPC id — so recovering a batch the apply phase already
        rescheduled never double-fires it.

[2026-07-22] - Containment-cycle guard: a corrupt CONT chain can't hang the main thread
----------------------------------------

FIX - The parent-chain walk in SendItemVisualUpdate had no depth cap or cycle
        guard. A corrupt or hand-edited save can store a containment cycle
        (A cont B, B cont A, or an item contained in itself) straight into
        ContainedIn, bypassing the runtime ContainsInSubtree check — the walk
        then looped forever on the main thread and hung the shard. It is now
        bounded to 16 levels (like every other parent-chain walk) and logs and
        skips the item if the chain is a cycle or pathologically deep.

FIX - On load, an item whose CONT would form a containment cycle is quarantined
        to the ground (its ContainedIn cleared) instead of being left in the
        cycle, so the corrupt relationship never reaches the runtime. Item.AddItem
        now returns whether the add succeeded so the loader can detect the
        rejected (cyclic) case.

[2026-07-22] - Sector tick no longer crashes when a callback removes several objects
----------------------------------------

FIX - Sector.OnTick and TickItems walked the live character/item list by index
        while firing each object's OnTick. A callback that removed more than one
        object from the same sector — a death cascade, or an item @Timer script
        that deletes several items — shrank the list under the loop index, so the
        next access threw ArgumentOutOfRangeException on the world tick. Both
        loops now tick a snapshot (reusing a per-thread buffer so the hot path
        stays allocation-free): a callback-removed object is skipped, an object
        that retires is removed only if still present, and a newly-added object
        waits for the next tick.

[2026-07-22] - Gump / tooltip size budgets: reject compression bombs and wrapping fields
----------------------------------------

FIX - Script gumps and object-property tooltips (OPL) now enforce size budgets
        the raw 65535 wire guard cannot: a compressed 0xDD gump is tiny on the
        wire but announces a decompressed size the client must allocate, and the
        inner ushort length fields (per-text, text count, OPL args) wrap silently
        below a 65535 total. The 0xDD/0xB0 gump builders cap layout bytes, text
        count, per-text length and total decompressed size; the 0xD6 OPL builder
        caps property count and per-property args length. Over-budget content is
        rejected up front and dropped by the send path (with a logged reason)
        instead of emitting a frame that would wrap a field or force an unbounded
        client allocation.

[2026-07-22] - A corrupt current save no longer aborts boot — fall back to a good backup
----------------------------------------

FIX - The loader previously read the current save's files directly, so a
        corrupt or truncated gzip/binary file threw out of Load and the server
        refused to boot even with intact .bakN backups sitting next to it. Load
        now tries the current generation, and if any of its files fail an
        integrity read it falls back to the newest intact backup generation
        (.bak1, then .bak2, ...). Every logical file's .bakN rotates together on
        each save, so a whole generation is loaded or skipped as a unit — a
        fallback never mixes new and old files. If every generation is
        unreadable, boot stops with a clear, actionable error instead of silently
        starting a blank world.

FIX - Generation files are validated (read end-to-end) before any object is
        materialized, so a corrupt generation is rejected without partially
        loading it. SaveIO.FormatFromPath ignores a trailing .bakN/.tmp suffix so
        a binary/gzip backup is opened with its real format instead of being
        misread as text.

NOTE - This uses the existing flat save layout and its aligned .bakN backups as
        generations; the classic on-disk file names are unchanged. Write-side
        cross-file commit ordering (a torn write of individually-valid files) is
        a separate follow-up.

[2026-07-22] - Binary save records are now atomic; a failing record aborts the save
----------------------------------------

FIX - BinarySaveWriter.EndRecord wrote the section header and property count
        before validating each property's length, so an oversized field threw
        mid-record and left a truncated record whose promised count no longer
        matched the following bytes — every later record was then misaligned and
        the whole shard unreadable (and unbootable on next start). EndRecord now
        encodes and length-checks the entire record up front and writes it
        all-or-nothing, so a bad record fails without corrupting the stream. Field
        limits are measured in UTF-8 bytes, not characters.

FIX - WorldSaver no longer swallows per-record write errors and commits the shard
        anyway (which silently dropped the entity). A record that fails to encode
        or a stream/IO error now fails the whole save: the exception propagates,
        the .tmp files are deleted, the previous committed save and its backups
        are left untouched, and Save returns false.

FIX - The synchronous save path now honours that return value — it reports a save
        failure instead of broadcasting "save complete" when the world write did
        not commit. The background save path already gated its success broadcast.

[2026-07-22] - Multicore tick no longer runs world-mutating sector updates in parallel
----------------------------------------

FIX - GameWorld.OnTickParallel ran Sector.OnTick across sectors with
        Parallel.ForEach once 50+ sectors were active. Sector.OnTick is not a
        pure per-sector computation: spawn components create/place/delete world
        objects, poison and standing-field deaths route into the death engine
        (corpse creation, equipment moves, broadcasts), and item @Timer /
        corpse-decay fire script triggers. Those mutate shared, non-thread-safe
        state — the sector character/item lists and the world Dictionary/HashSet
        indexes — so concurrent sectors raced them and could wedge a worker in a
        corrupted-bucket probe, hanging the main loop with no watchdog. The sector
        phase now runs sequentially. The safe parallelism (the NPC read-only
        pathfinding prestage, BuildDecision) is unchanged, so multicore mode still
        parallelizes the heavy A* work.

FIX - As a consequence, no script trigger or expression evaluation runs on a
        worker thread anymore, so the shared expression evaluator / trigger runner
        is single-use again (closes the parallel-reuse hazard noted with the
        expression hardening work).

[2026-07-21] - Outbound packet-length overflow + poisoned-book client freeze fixed
----------------------------------------

FIX - PacketBuffer.WriteLengthAt no longer casts an over-65535 body to a ushort
        (which wrapped 65536 to 0). It flags the buffer oversize instead, and the
        send path (EnqueueAt / EnqueueShared) drops such a packet with a log and
        returns it to the pool rather than emitting a corrupt frame that desynced
        or froze the client. This guards every variable-length packet (books,
        gumps, tooltips, ...), not just books.

FIX - Incoming 0x66 book pages cap each line's stored length (256 bytes) via a
        bounded ASCII read that still consumes the whole field. A crafted line
        with no NUL can no longer be stored verbatim and echoed in an oversized
        0x66.

FIX - OpenBook clamps BOOK_PAGES to at most 256 and iterates with an int. A
        BOOK_PAGES tag of 65535 previously drove a "for (ushort i = 1; i <= n)"
        past 65535, wrapped i to 0, and looped forever on the main thread.

FIX - Book page content is split across as many 0x66 packets as needed so each
        stays under the wire ceiling; a large (multi-page) book is delivered in
        full via several in-spec packets instead of one overflowing one.

[2026-07-21] - Expression engine hardening: pathological input can no longer crash the tick
----------------------------------------

FIX - The arithmetic evaluator's recursion (right-fold operators, parentheses,
        and unary chains, integer and float) had no depth cap, so a crafted
        expression — thousands of nested parens, a long "1+1+..." chain, or
        "----1" — caused a StackOverflowException, which is uncatchable and
        terminates the process. Recursion is now bounded (MaxEvalDepth) and an
        over-deep expression fails controlled (returns 0 with a diagnostic). A
        very long single expression is also refused up front.

FIX - The {lo hi} brace-random range computed hi+1, which overflowed and threw
        ArgumentOutOfRangeException when hi was long.MaxValue (e.g.
        {1 0x7fffffffffffffff}). It now uses an inclusive-range helper that
        never overflows; {max max} collapses to the value.

FIX - ASCPAD clamps its pad count (max 4096, negatives to 0); an
        attacker-influenced count such as int.MaxValue previously built a
        multi-GB string and ran the server out of memory.

FIX - CHR rejects UTF-16 surrogate code points (0xD800..0xDFFF) via
        Rune.IsValid instead of passing them to char.ConvertFromUtf32, which
        threw. Valid BMP and supplementary code points are unchanged.

[2026-07-21] - Spell-effect removal no longer crashes the tick on reentrancy
----------------------------------------

FIX - ProcessExpirations, ClearAllEffectsOnDeath, BreakInvisibility,
        BreakParalyze, StripCurseEffects and StripDispellableEffects walked the
        live active-effect list by index while firing removal callbacks. A
        callback that removed further effects on the same target (typically by
        killing it, which clears every remaining effect) shrank the list under
        the index, so the next access threw ArgumentOutOfRangeException on the
        world tick and terminated the server. They now snapshot the matching
        effects first and remove each via reference (Remove), matching the
        already-safe ProcessDotTicks pattern — each effect is reverted and its
        removal observed at most once, with no live-list index access after a
        callback runs.

[2026-07-21] - Inbound exception firewall: one bad packet can no longer crash the server
----------------------------------------

FIX - A throw from a packet handler (game logic or a synchronous .scp trigger)
        is now caught per-connection: the offending connection is logged with
        its remote endpoint and closed, and the input phase keeps running.
        Previously any unhandled exception in handler dispatch propagated out of
        the main loop and terminated the whole process — killing every player
        and skipping the shutdown save (all state since the last periodic save
        was lost).

FIX - The same containment now guards OnConnectionAccepted (a bad accept drops
        only that connection), per-connection output flush (a non-socket throw
        in outbound compression/encryption closes one connection instead of the
        loop), and each main-loop iteration (a fault in any phase is logged with
        the phase name and the loop continues, log throttled to avoid spam).

FIX - Shutdown and the on-exit world save moved into a try/finally around the
        main loop, so cleanup and the final save still run if the loop exits
        abnormally. The exit hook dispatch is guarded so it cannot mask that.

[2026-07-21] - Custom-house door fixtures: no more double render/collision after commit
----------------------------------------

FIX - Committing a custom house design no longer keeps door/container tiles
        as a second, virtual copy. Source-X semantics: fixture tiles the
        commit materializes as real items are marked invisible in the
        committed design, so the 0xD8 stream, walk geometry and LOS use only
        the real item. Previously the design copy rendered a second door and
        kept blocking movement/sight even with the real door open.

FIX - The committed 0xD8 design stream (post-commit send and 0xBF 0x1E
        query answers) excludes fixture tiles; the working stream sent to
        the designer still includes them, matching Source-X.

NOTE - Legacy designs committed before fixture materialization existed (no
        COMMIT_FIXTURES tag, no real door items) keep their virtual doors
        visible until the next commit, so no house loses its door on load.

[2026-07-21] - dialogs/ pack audit: property/verb gaps behind the inspector, party, stylist, moongate and where dialogs
----------------------------------------

FIX - CANSEELOS / CANSEE accept a whitespace-separated uid argument
        (CANSEELOS <uid>) in addition to the dot form (Source-X OC_CANSEELOS
        skips separators before the uid). The space form is what <SRC.CANSEELOS
        <UID>> produces, and it guards the stylist's apply handlers.

FIX - ISNEARTYPE also scans nearby dynamic items by IT_TYPE (e.g. T_MOONGATE),
        not only terrain/static TileFlags (Source-X ISNEARTYPE). The item search
        no longer requires loaded terrain data.

NEW - FINDLAYER.<layer> resolves a layer DEFNAME (layer_hair, layer_beard, ...)
        as well as a number, and the dot form chains to the found item's
        properties/verbs (FINDLAYER.<layer>.COLOR / .REMOVE), matching the paren
        form (Source-X LayerFind with an expression-evaluated arg).

NEW - PARTY.MASTER.<sub> and PARTY.MEMBER.<n>.<sub> forward to the master /
        n-th member as a reference (Source-X CParty r_GetRef), so member-list
        rows can read <...NAME>, <...REGION.NAME>, etc. Added the PARTY.CREATE
        verb (ensures a party exists with this char as master and adds any
        argument that resolves to a real char).

NEW - Item RESCOUNT (bare) = contained-item count (Source-X CContainer);
        INSTANCES = count of world items sharing the display id; RESOURCES and
        SKILLMAKE expose the craft lists (bare = list, .COUNT, and .<n>[.KEY|.VAL]
        indexed 1-based) from the itemdef (Source-X CBaseBaseDef/CItemBase).

NEW - MAXFOOD is readable: derived on the chardef from the FOODTYPE resource
        quantities (Source-X CCharBase::SetFoodType, largest quantity) and read
        back on the char; VIRTUALGOLD is readable on the char (both were
        write-only before).

NEW - SEXTANTP returns the sextant coordinate string for a point (Source-X
        Calc_MaptoSextant; zero point 1323,1624, real-world width 5120 on maps
        0/1).

FIX - The dialog TOOLTIP control forwards its argument string
        (TOOLTIP <cliloc> @<args>), so parameterized clilocs render with their
        argument (Source-X GUMPCTL_TOOLTIP emits "tooltip <cliloc> <args>").

FIX - The SERV.AREA accessor accepts the paren form AREA(<defname>).<prop> in
        addition to the dot form (Source-X CServerConfig r_GetRef), and a dialog
        layout's first-line "x,y" position is expression-evaluated so a DEF-based
        start position resolves instead of anchoring at 0,0.

[2026-07-21] - Dialog FUNCTION calls in layouts; comma-only ARGV parsing
----------------------------------------

FIX - A DIALOG layout line whose command is not a built-in gump keyword is now
        dispatched to the matching script [FUNCTION] and the gump controls it
        emits (RESIZEPIC, DHTMLGUMP, BUTTON, ...) are injected into the dialog,
        matching Source-X (CDialogDef::r_Verb runs the layout as a script with
        the dialog as target). A helper such as a bordered-panel-with-title
        function called from a dialog body now renders. RESIZE had been
        hardcoded as a fake built-in keyword that shadowed the pack function and
        misparsed its arguments, so the function never ran.

FIX - ARGV/ARGC now split the argument string on COMMAS only (Source-X
        CScriptTriggerArgs), instead of on spaces and commas. Spaces inside a
        field are preserved, so a multi-word argument (e.g. a dialog panel
        title passed as the last field) survives as one ARGV element instead of
        being truncated at the first space. Empty comma fields are kept so the
        remaining indices stay aligned (e.g. ",,230,90,Title" keeps width/height
        /title on ARGV[2..4]).

[2026-07-21] - systems/ pack audit: per-char SPEECH list, stone keywords, point terrain type
----------------------------------------

NEW - Per-char dynamic SPEECH list (Source-X CChar m_Speech / DSPEECH). DSPEECH
        +<resource> / -<resource> attach or detach a SPEECH fragment on an
        individual char at runtime; a bare single value appends (so saved lines
        accumulate) and a multi-value assignment replaces the list. When the char
        HEARS speech the list runs first, before the CharDef's static SPEECH
        blocks (Source-X CChar::OnTriggerSpeech order). ISDSPEECH.<resource> reads
        membership. The list is persisted with the char. This lets the join/resign
        flows wire town/guild speech reactions onto members individually; before,
        DSPEECH was an opaque tag, ISDSPEECH was unimplemented, and per-char
        speech blocks never fired.

NEW - Char GUILD / TOWN keyword resolves the affiliation's stone: bare returns
        the stone uid, and a sub-key (GUILD.<key> / TOWN.<key>) forwards to the
        stone item (Source-X CChar GUILD/TOWN). An unaffiliated char reads empty.

NEW - SERV.GUILDS counts all guild + town stones in the world, and
        SERV.GUILDSTONES / SERV.TOWNSTONES iterate them (.COUNT and
        .<index>[.UID]), matching the Source-X server stone enumeration. GUILDS
        was a hardcoded 0.

NEW - <point>.TYPE reads a terrain type for a map point (Source-X CPointBase
        TYPE), derived from the land tiledata (water/dirt/sand/snow/grass/rock
        ...), enough for the terrain whitelist checks the pack runs before
        placing objects.

NEW - GUILDTITLE reads/writes the guild title alongside GUILD.TITLE, and TARGPRV
        round-trips (Source-X CChar keywords). Joining a town/guild now records a
        MEMORY_TOWN / MEMORY_GUILD link to the stone and resigning removes it, so
        membership queries stay consistent.

[2026-07-20] - events/ pack audit: script EQUIP, @Unequip on engine paths, SKILL FAIL, CRAFTEDBY
----------------------------------------

FIX - The char EQUIP verb now honors its UID argument (Source-X CHV_EQUIP):
        SRC.EQUIP <uid> equips that item and fires @Equip. It used to drop
        the arg into an unused flag, so script-created worn gear was never
        equipped and its @Equip effect never ran — the weapon-special combat
        bonuses that hang their whole effect on it were dead.

FIX - @Unequip fires on the script/engine unequip paths: the item UNEQUIP
        verb and a worn item's REMOVE (Source-X ItemBounce / RemoveSelf run
        the unequip trigger). Only the client pickup path fired it before,
        so a worn item's cleanup block (e.g. clearing a combat-bonus freeze)
        never ran and the effect got stuck.

FIX - SKILL FAIL cancels the active skill (Source-X Skill_Start(SKILL_NONE));
        it was dropped because FAIL is neither a skill name nor an id.

NEW - CRAFTEDBY item property reads/writes the crafter uid alongside the
        existing CRAFTER (Source-X CItem CRAFTEDBY).

NEW - STATTOTAL character property = STR+DEX+INT (Source-X Stat_GetSum), and
        the ISVALIDE spelling resolves like ISVALID (a widely-used pack
        variant of the genuine ISVALID keyword) so ref-validity checks stop
        silently reading empty.

Pack-custom tokens with no Source-X counterpart were skipped.

[2026-07-20] - functions/ pack audit: strftime, CommandTrigger, CHR, CONTP
----------------------------------------

FIX - RTIME.FORMAT / RTICKS.FORMAT translate C strftime tokens instead of
        feeding them raw to DateTime.ToString. The scripts pass POSIX tokens
        (%Y %m %d %H %M %w %A %B ...); .NET read %m as MINUTES, %H:%M as
        hour:MONTH, and threw on %Y/%w (falling back to the default date),
        so every pack date/time function (SERV.ZAMAN/SAAT/AYNAME/DAYNAME/
        DAYNUM) produced garbage. The dispatch now passes the original-case
        property so case-sensitive tokens survive.

NEW - CommandTrigger (sphere.ini CommandTrigger=f_oncommand): a global
        function fired before every command with ARGS = the command line;
        RETURN 1 consumes it. Lets a pack add or override commands - the
        pack's f_oncommand staff tools (.REMOVE/.FLIP/.MTELE) were dead
        because nothing invoked the hook.

NEW - CHR intrinsic (inverse of ASC): <CHR n> is the character for byte
        value n (Source-X CScriptObj_functions.tbl CHR).

NEW - CONTP item verb: CONTP x,y repositions a contained item inside its
        container (Source-X CItem IC_CONTP); a ground/equipped item is a
        no-op like the reference.

Note: pack-custom features with no Source-X counterpart were intentionally
skipped (FLAGSIL, UOSOFT_CLIENT_LOGOUT, ISFORBIDSTRING/ISSTRING). The
packet-scripting framework (F_PACKET_RECEIVE/SEND hooks) is unimplemented
and left for a dedicated pass.

[2026-07-20] - Scripted item timers + chained-object verbs (flash robe, staff mount)
----------------------------------------

FIX - The TIMER verb arms the SCRIPT timer, not only decay. Sphere has one
        item timer that drives both @Timer and decay; a scripted `TIMER n`
        line used to set only DecayTime, so the flash robe's color loop,
        trap re-arms and door autoclose written as a property never fired.
        Negative TIMER disables it (the scripts' off switch).

FIX - Worn / contained items with a timer are pumped by the world tick.
        Sector ticks only reach GROUND items, so a WORN item's @Timer never
        ran — the flash robe did nothing while equipped. A registry
        (SetTimeout tracks off-ground timed items) fires them in the serial
        tick phase.

NEW - The classic short random <Ra,b> / <Rn> intrinsic. The live packs use
        it everywhere (the flash robe DORAND color table, array picks); only
        the long RAND(...) form existed, so every <R...> resolved to 0.

NEW - Chained object verbs/reads: TOPOBJ.<x>, CONT.<x>, LINK.<x> on items
        and FINDLAYER(n).<prop> route the remainder of the line to the
        resolved object as a command OR a property set. Item scripts drive
        their wearer/container this way (the robe's
        TOPOBJ.FINDLAYER(25).COLOR gear-and-mount recolor).

FIX - MOUNT as a VERB on an NPC seats its owner (Source-X Horse_Mount), and
        MAKEMYPET <char> makes the NPC that character's pet. Both were
        missing — MOUNT existed only as a property read — so the staff .bineq
        script (NEW.MAKEMYPET SRC then REF1.MOUNT) did nothing.

[2026-07-20] - Apply-phase spike fix: multi index + NPC scan cadence
----------------------------------------

PERF - GameWorld keeps a ground-multi index (houses, ships, addons, custom
        foundations; maintained on place/remove/retype - the ItemType
        setter notifies on late retyping and the placement check resolves
        def-typed multis from legacy saves). Walk geometry
        (AddVirtualMultiComponents) iterates this short list with bounds
        checks instead of a 32-tile spatial range query per step - the
        per-NPC-step cost that dominated the live apply phase (50-180 ms
        slow ticks) after NPC steps moved onto the standing resolver. Open
        wilderness now costs nothing; player walk checks got cheaper too.

PERF - The NPC ground-item desire scan runs on a per-NPC 10-15 s cadence
        instead of a 1-in-8 dice roll per act - thousands of NPCs no
        longer issue sector item queries in the serial apply phase every
        few acts. Timer state is swept with the other per-NPC caches.

[2026-07-20] - Gem-spawned NPCs run their chardef loot scripts (drops fixed)
----------------------------------------

FIX - Spawn gems now run the chardef SCRIPT init for every NPC they create
        (@Create, @CreateLoot, then @NPCRestock - the Source-X
        NPC_LoadScript sequence), the same as the GM .add path. The live
        pack declares monster gear (ITEMNEWBIE) and backpack loot (ITEM=)
        inside ON=@NPCRestock bodies; the spawn path only applied the
        parsed K/V stats, so every gem-spawned monster had an empty pack
        and dropped nothing - only .add-created NPCs had loot. The death
        pipeline itself was correct (non-newbie pack contents already
        transfer to the corpse).

[2026-07-20] - Z-resolver tail: .FIX, .GO, drift diagnostic, end-to-end 0x20 test
----------------------------------------

FIX - .FIX re-seats a CHARACTER through the shared standing resolver - the
        terrain-only GetEffectiveZ dropped it under house floors, ship decks
        and dungeon statics. Item .FIX keeps the plain terrain drop.

FIX - .GO x y without a Z settles through the shared resolver (terrain
        seeds the reference): the destination's dungeon statics and multi
        floors are now seen instead of terrain-only Z.

FIX - The [z_drift] dclick diagnostic compares against the resolver instead
        of the multi-blind GetEffectiveZ - no more false alarms on ship
        decks and house floors. Still log-only per the audit do-not list.

NEW - End-to-end drift-chain test: a real MovementEngine.TryMove step on an
        upper story, then a double-click that turns the character - the
        self 0x20 the turn sends is captured off the packet queue and must
        carry the story's Z. Closes the audit's E2E gap.

[2026-07-20] - Standing-surface synthetic test matrix + flat multi-floor walk fix
----------------------------------------

NEW - The audit's synthetic scenario matrix runs mul-free on the in-memory
        map fixture: dynamic addon floors, multi house floors (two-story
        same-tile seating by reference Z), ship decks over water, low
        ceilings (settle refused, GM fallback keeps ground contact),
        impassable+surface furniture (never a seat), committed
        custom-house design floors, and walk/settle agreement per story.
        A synthetic multi layer (MapDataManager.SetSyntheticMulti) feeds
        house/ship definitions without multi.mul.

FIX - The matrix exposed a real walk-geometry hole: virtual multi/ship/
        custom-house components with flat floor tiles (Surface, height 0)
        were silently dropped by the loose-item height filter in
        ShouldTreatAsMovementGeometry - a house floor contributed NO walk
        geometry unless its tiledata carried Bridge or enough height.
        Virtual components (stamped MultiAddon) are structural by
        definition and now always count.

[2026-07-20] - Unified standing-surface resolver: the Z-drift root fix
----------------------------------------

FIX - One surface authority for character Z (the audit design). WalkCheck
        gained ResolveStandingSurface with two policies: IgnoreCollision
        (GM/AllMove/pass-walls steps - collision is bypassed, surface
        collection and Z selection are NOT) and Settle (login, mount,
        dismount, teleport-derive, stuck-menu, NPC step Z - nearest
        standable surface WITH headroom to a reference Z, so two-story
        houses seat the correct floor). It selects from the SAME geometry
        inventory the walk path uses: land average, statics with open-door
        handling, dynamic items, multi/ship components and custom-house
        tiles.

FIX - Every character seat path migrated off MapDataManager.GetEffectiveZ,
        which saw neither dynamics, multis, custom houses nor headroom, and
        whose closest-to-currentZ pick was SELF-REINFORCING for a drifted Z
        (the 5146,993 dungeon report: stored Z=10 held over a floor at 1,
        exposed as a snap by the dclick-facing self 0x20). Migrated: the
        GM/AllMove movement bypass, login seating, mount and dismount (both
        handlers), the world-features dismount, auto-derived teleport
        destinations, the stuck-menu teleport, and all NPC step-Z sites.
        GetEffectiveZ remains for non-character queries (spawn gems, ground
        items, previews) and - deliberately, for cost - the A* route
        heuristic, whose executed steps re-resolve through WalkCheck anyway.
        The dclick 0x20 stays; FaceUsePoint stays untouched (per the audit
        do-nots).

NEW - Z-drift regression tests run against the REAL mul data (path
        auto-detected, clean skip when absent): at the drift tile a normal
        player step, the GM bypass from both the floor and the drifted
        reference, and Settle from both references all agree on one floor;
        sloped-land Settle reproduces every walk step Z; the Britain bridge
        seats on the deck. All three passed against live map data.

[2026-07-20] - NPC AI wave 2: food counters, FOODTYPE diet, gold refusal, scan guards
----------------------------------------

FIX - NPC hunger uses one truth: a fresh spawn seeds the AI hunger counter
        (NpcFood) fed - it used to start at zero while MAXFOOD only filled
        the separate Food stat, so every newborn NPC read as starving
        (want 100 on all edibles, constant ground-food hunts).

NEW - FOODTYPE is a real diet (Source-X Food_CanEat): pack feeding, ground
        foraging, the hunger want score and pet feeding all check the
        chardef FOODTYPE list - a carnivore no longer eats fruit. Undeclared
        diets keep the generic edible classes.

FIX - Gold handed to any NON-banker NPC is refused outright (Source-X
        NPC_OnItemGive) - a vendor wants coins on the ground but never
        pockets a handed pile. An OWNED vendor pet now routes goods into
        its vendor-stock box (LAYER_VENDOR_STOCK equivalent).

FIX - Item/corpse scan guards: the desire scan now needs line of sight and
        CAN_C_USEHANDS on EVERY brain (the humanoid exemption is gone), uses
        a sight-driven look range, and corpse pickup/looting is blocked in
        both guarded AND safe regions (TryLoot got the same guards).

FIX - DESIRES keeps the raw want qty (DESIRES=999 t_corpse stays 999 for
        @NPCLookAtItem ARGN2 - the 0-100 clamp is gone) and r_* region
        aliases are typed as region preferences instead of being hashed as
        fake itemdefs (~167 Scripts-X lines affected).

FIX - Healer alignment is three-way: good healers (positive karma) serve
        only the innocent, evil healers only criminals/murderers, and a
        NEUTRAL healer serves everyone - zero-karma healers no longer
        behave as good ones.

FIX - The self-defense tests now assert a RESOLVED swing via the OnNpcAttack
        hook instead of an adjacent-start distance check that passed even
        when the NPC did nothing (audit round 8 test critique).

[2026-07-20] - NPC AI parity wave: brain alias, give-item semantics, DESIRES, self-defense
----------------------------------------

FIX - brain_animal_trainer resolves to the Stable brain: the Scripts-X
        stablemaster alias (defs.scp value 7) failed the enum-name parse and
        left stablemasters on the pre-spawn Monster fallback brain.

FIX - The 0x77 mobile-move packet and single worn-item updates route through
        the viewer adjustment (Source-X PacketCharacterMove calls
        GetAdjustedCharID per client): a tripping player no longer sees
        mobiles snap back to their real body/hue the moment they move, and a
        fresh equip recolors too.

FIX - Giving an item to an NPC follows Source-X NPC_OnItemGive: @ReceiveItem
        fires FIRST (before train/hire gold pay); an owned pet eats offered
        food and refuses what it cannot carry; a banker deposits gold into
        the giver's bank box; dropping goods on a vendor is a quick-sell
        offer; everyone else runs the NPC_WantThisItem gate - an unwanted
        gift is REFUSED by default, @NPCRefuseItem RETURN 1 overrides the
        refusal, @NPCAcceptItem RETURN 1 cancels the accept. The previous
        contract (default-accept, inverted trigger meanings) is gone and the
        tests now lock the reference semantics.

FIX - DESIRES entries keep their want quantity (Source-X NPC_WantThisItem
        returns the entry's GetResQty, bare defname = 1) instead of a flat
        100 - desiring NPCs are no longer maximally greedy; a hungry NPC
        wants edibles at 100-foodPercent and vendor brains want gold. The
        native item scan runs without the script-trigger gate and now also
        covers idle monsters, animals and vendor brains; pickup requires
        usable hands and corpse looting never happens in guarded territory.

FIX - Non-monster brains defend themselves: Human, Vendor, Banker, Stable
        and Healer NPCs fight (or flee, on fear motivation) the attacker
        assigned as their FightTarget, and animals fight back instead of
        only shying away from war-mode characters.

FIX - Healer alignment: an evil healer (negative karma) serves criminals
        and murderers; a good healer keeps serving only the innocent.

[2026-07-20] - FINDID memory precedence, full hallucination trip visuals
----------------------------------------

FIX - FINDID ranks the hidden spell-memory layer BEFORE the backpack: a
        form memory shares its graphic with the rune scroll, so the old
        pack-first order let the Reaper/Stone toggle delete a spare scroll
        from the pack while the form stayed on.

NEW - Full hallucination visuals (Source-X GetAdjustedCharID /
        GetAdjustedItemID): a hallucinating player sees every OTHER mobile
        as a random classic creature, every mobile plus its worn equipment
        in random dye hues, and ground items recolored too (multis exempt).
        The view refreshes the moment the effect lands, on every 15-30 s
        trip tick, and when the effect ENDS - so the last random colors
        never linger on the client.

[2026-07-20] - Spell-memory reverse link, timerless forms, hallucination visuals
----------------------------------------

FIX - FINDID now searches the hidden spell-memory layer alongside equipment
        and the backpack, and supports chained verb dispatch
        (FINDID.<id>.REMOVE). Removing an IT_SPELL memory item reverts its
        active effect through the new SpellEngine.RemoveEffectByMemory
        reverse link (Source-X: deleting the memory runs
        Spell_Effect_Remove). The Scripts-X Reaper/Stone Form @Select
        toggles (FINDID.<RUNE_ITEM>.REMOVE) now genuinely end the form.

FIX - A zero-duration form (the pack ships Reaper Form with DURATION=0.0)
        now holds with NO expiry - until dispel, death or the script toggle
        - matching the reference's timerless poly memory; the previous
        4-hour approximation is gone.

NEW - Hallucination is visible: a hallucinating player sees every mobile in
        a random dye hue (re-rolled on each view refresh, Source-X
        CClientMsg addChar), the trip tick refreshes the nearby view and
        plays its sound ONLY for the affected client (no more broadcast to
        bystanders). A petrified (Stone) mobile now shows HUE_STONE 0x482
        to everyone.

FIX - The drunk hiccup is visible: an overhead *hic* emote nearby players
        can see, plus a random facing turn and the bow animation when not
        mounted (Source-X Spell_Equip_OnTick), replacing the private system
        message.

[2026-07-20] - Poly forms, corpse-spell reach checks, periodic drunk/hallucination ticks
----------------------------------------

FIX - Animate Dead and Bone Armor validate the targeted corpse like Source-X:
        it must be a TOP-LEVEL world item on the caster's map within casting
        range. A known UID could previously consume a corpse from inside a
        container or across the world, dumping its contents at the corpse.

NEW - Reaper Form and Stone Form apply their form on first cast: body swap
        (Reaper 0xE6 CREID_REAPER_FORM, Stone 0x2C1 CREID_STONE_FORM) on the
        polymorph layer with a timed revert. A pack duration of 0 (Reaper)
        holds the form near-permanently like the reference's timerless poly
        memory. Re-casting another poly-family spell swaps forms cleanly -
        the base body is captured only after the previous form is reverted.

NEW - Chameleon, Beast Form and Monster Form ride the shared polymorph route
        (Source-X LAYER_SPELL_Polymorph, CCharSpell.cpp:4087): menu picks via
        the POLY cast-menu flow now work for all poly-family spells; without
        a pick, Beast/Monster Form roll a classic animal/monster body and
        Chameleon keeps the body (the reference ships its skin-match visual
        commented out). Only Enchant and Forget remain gated as inert.

FIX - Shrink kills a conjured/summoned creature outright (Source-X
        NPC_Shrink zeroes its STR) instead of rejecting it alive.

FIX - Bone Armor pieces run their ITEMDEF @Create metadata
        (ItemDefHelper.ApplyInstanceMetadata) - type, armor, durability and
        events - instead of spawning as graphic-only shells.

NEW - Periodic effect ticks (Source-X Spell_Equip_OnTick): Hallucination
        plays trip sounds (0x243/0x244) every 15-30 s for its duration;
        Ale/Wine/Liquor drain one current stamina and mana point per 5 s
        drunk tick with random hiccups and a 10% early-sober chance,
        replacing the earlier one-shot stat penalty. A charge-driven effect
        that retires early now reverts its applied stat flag.

[2026-07-20] - HARM-flag crime/reflect, @Select stage, custom Sphere spells 1000+
----------------------------------------

FIX - Harmful-spell classification now follows SPELLFLAG_HARM (Source-X
        OnSpellEffect/CheckAntiMagic): pack spells that carry HARM without
        DAMAGE/CURSE - Poison, Paralyze, Mana Vampire - now flag the caster
        criminal on innocent players, are bounced by Magic Reflect, and are
        suppressed in REGION_ANTIMAGIC_DAMAGE regions. Previously only
        Damage/Curse-flagged spells counted as harmful.

FIX - The [SPELL n] ON=@Select stage (Source-X SPTRIG_SELECT) now fires from
        SpellEngine.CastStart, the choke point every cast entry funnels
        through (client, scroll, wand, NPC, console); RETURN 1 cancels the
        selection before any resource is spent. Pack form toggles that live
        entirely in ON=@Select (Reaper Form, Stone Form) now run instead of
        silently burning mana. The client-path double-fire was removed from
        the char-trigger fall-through.

FIX - Noble Sacrifice heals/cures the paladin's PARTY members and own pets
        only; any nearby player - including enemies - used to receive the
        area heal+cure.

FIX - Animated Weapon (Mysticism) summons its classic body 0x2B4
        (CREID_ANIMATED_WEAPON); the summon table returned body 0 and the
        creature was invisible.

NEW - Native behaviors for the Sphere custom spells (1000+), mirroring the
        Source-X SPELL_* cases: Light (timed personal light), Hallucination
        (STATF_HALLUCINATING), Stone / Particle Form (petrify for the
        duration), Shrink (NPC packed into a figurine), Refresh / Restore /
        Mana (stat refills), Sustenance (food refill), Gender Swap (body
        toggle for human/elf/gargoyle), Trance (timed Meditation bonus),
        Shield / Steelskin / Stoneskin (timed AR bonus), Regenerate
        (heal-over-time, one tick per 2 s), Ale / Wine / Liquor (timed
        DEX/INT drunkenness penalty), and Bone Armor (converts a skeleton
        corpse into up to five bone armor pieces at reference odds).

FIX - The inert-spell guard now covers the custom id space (1000+): customs
        without a native handler or scripted stage (Chameleon, Beast/Monster
        Form, Enchant, Forget - inert in the reference too) are refused up
        front instead of consuming mana/reagents as silent no-ops.

[2026-07-18] - Startup wiring fixes, plant growth, multi-server list, gump fallback
----------------------------------------

FIX - Noble Sacrifice reaches its native handler: the pack def carries
        spellflag_heal, and the generic heal branch swallowed the cast into
        a plain caster heal — allies were never healed/cured and the
        paladin never paid the sacrifice.

FIX - Field polish: segments are Move_Never and typed IT_FIRE/IT_SPELL
        (players could pick field pieces up); poison field passes the
        caster as the poison SOURCE (kill/crime credit); harming an
        innocent player with any field marks the caster criminal, matching
        the direct harmful-spell rule.

FIX - Dual guild+town membership survives a restart: the reload used ONE
        claim set across every stone, so a character in both pools kept
        only whichever stone loaded first. Claims are now per pool, and
        the general FindGuildFor() excludes town records — guild chat,
        corpse-loot relations and notoriety never see a citizenship as a
        guild membership (FindTownFor added for town surfaces).

FIX - Remaining classic summons mapped: Rising Colossus (0x33D) and Sphere
        custom Summon Undead (random zombie/skeleton/lich, Source-X odds);
        custom Animate Dead (1001) rides the existing corpse-raise flow.
        Animated Weapon stays scripted — the reference has no native
        mapping either.

FIX - The inert-school gate no longer counts SCRIPTED (without ON= stages),
        POLY or TICK as actionable — those have no generic handler, so such
        defs burned mana and no-opped.

NEW - Spell id surface completed against the reference: Bard Masteries
        (701-706), Skill Masteries (707-745) and the full custom Sphere
        tail (1006-1026: Stone, Shrink, Refresh, Restore, Mana, Sustenance,
        Chameleon, Beast/Monster Form, Gender Swap, Trance, Particle Form,
        Shield, Steel/Stoneskin, Regenerate, Enchant, Forget, Ale/Wine/
        Liquor) now exist in the enum.

FIX - Field spells behave like fields (Source-X Spell_Field): segments use
        the CLASSIC field art per spell with E/W-N/S orientation (stone wall
        0x0080, fire 0x398C/0x3996, poison, paralyze, energy) instead of the
        pack's EFFECT_ID — Wall of Stone/Fire/Energy fields were INVISIBLE
        (EFFECT_ID=0). Typed step/stand behavior: fire deals resisted fire
        damage, poison field POISONS (level from the caster's skill at cast),
        paralyze field freezes through the timed Paralyze effect, stone/
        energy walls are pure barriers (blocking via their tiledata) and
        never materialise on top of a character. Duration follows the
        spell's DURATION curve (2-4 min) instead of a fixed 30 s.

FIX - Fixed summon spells carry their classic creatures (Source-X
        m_uiSummonID defaults): Blade Spirit, Energy Vortex, Air/Earth/
        Fire/Water Elemental and Summon Daemon now summon the right body —
        previously everything but the familiar spawned a bodyless creature.

FIX - Town and guild memberships are independent pools (Source-X
        MEMORY_GUILD vs MEMORY_TOWN): a guild member can become a town
        citizen and vice versa; the overhead abbreviation is a guild-only
        surface (town tags never leak onto names); the record type persists
        via the stone (GUILD.ISTOWN tag + item-type fallback).

FIX - The inert-school-spell gate now also refuses school defs whose flags
        are only markers (good/harm/fx/targ with no damage/heal/bless/curse/
        field/summon/area/tick and no scripted stages) — they cast, consumed
        mana/reagents and silently no-opped; Source-X damages only on
        SPELLFLAG_DAMAGE, so such defs are inert there too.

FIX - Custom Sphere spell ids realigned with the reference: 1001 Animate
        Dead, 1002 Bone Armor, 1003 Light, 1004 Fire Bolt, 1005
        Hallucination — Hallucination previously sat at 1001 (Animate
        Dead's slot), so pack spells 1001-1004 resolved to the wrong enum.

NEW - Chivalry works natively: Cleanse by Fire cures, Close Wounds heals
        (generic heal flag), Dispel Evil strips/banishes conjured creatures
        around the paladin (shared with Dispel's DISPELKILLSUMMONS
        pipeline), Divine Fury refills stamina at a timed -20 defense cost,
        Holy Light bursts energy damage at creatures fighting the caster,
        Noble Sacrifice heals+cures nearby allies at the paladin's expense,
        Remove Curse strips ONLY curse-type effects (buffs survive), and
        Sacred Journey travels by rune like Recall. Spellweaving Gift of
        Renewal is a real heal-over-time (the DOT engine now supports heal
        ticks). Consecrate Weapon / Enemy of One and the Bushido/Ninjitsu
        combat uniques stay script territory — the Source-X reference
        itself implements none of them natively (its native surface is
        buff-icon bookkeeping); the pack's script defs run through the
        generic flag engine as before.

NEW - Classic tailoring material flows work: wool/cotton targeted at a
        spinning wheel spins into 3 balls of yarn / 6 spools of thread
        (Source-X IT_WOOL/IT_COTTON), and thread/yarn targeted at a loom
        accumulates on the loom (MORE1 type / MORE2 units, mixed material
        ejects the partial weave) until a bolt of cloth is finished —
        both previously printed only a hint message.

FIX - Craft strokes honour the Source-X @SkillStroke contract: the trigger
        fires per stroke with LOCAL.Strokes seeded and WRITABLE, so a
        script can lengthen or shorten a craft per recipe (the default
        stays the reference's fixed two strokes), and RETURN 1 aborts the
        craft mid-work. (Imbuing stays deferred: the Source-X reference
        itself has no native property/intensity/unravel model — it is
        script territory there too, tracked in PARITY.md.)

NEW - Town stones work: a town stone runs the same stone engine as guild
        stones (Source-X IT_STONE_TOWN == CItemStone). Staff (Counsel+)
        establish the town at a GM-named stone and become its Mayor;
        players request citizenship through the candidate flow, the mayor
        accepts/dismisses, citizens renounce, and town-town / town-guild
        wars and alliances use the shared relation system. Membership now
        stamps the MEMORY_TOWN / MEMORY_GUILD link on the character
        (readable by scripts) and clears it on leave/dismiss/disband —
        the gump paths previously never wrote the memory.

FIX - Throwing weapons are now truly ranged: they use the archery range /
        LOS / movement rules (itemdef RANGE honored), fly a projectile —
        TDATA4/AMMOANIM art, else the weapon's own graphic, with the
        Source-X returning-weapon animation back to the thrower — and
        consume NO pack ammo (the old resolver demanded crossbow bolts for
        any non-bow ranged weapon, so throwing fell back to melee range).
        NPCs with throwing weapons follow the same rules.

NEW - Cannons work: the muzzle runs the Source-X load/fire state machine —
        target sulfurous ash to load powder, a cannon ball to load shot
        (one unit consumed per load, state in MORE1), then target to FIRE:
        boom + muzzle smoke, cannonball bolt within sight range, 80-230
        blunt/fire damage to a character (items take hitpoint damage).
        Double-clicking a cannon ball targets the muzzle to feed it.
        Previously both types only printed a message.

FIX - The Polymorph and Summon Creature selection menus now run the REAL
        cast: the pick is stashed on the caster and the completed cast
        conveys it (Source-X stores the selection then Spell_CastStart), so
        mana, reagents, cast time, fizzle, interruption and the TIMED
        polymorph revert all apply — the menus previously swapped the body /
        spawned the creature instantly and permanently. The direct GM/script
        POLY and SUMMON verbs are unchanged outside a cast menu.

FIX - Mana requirement and consumption now share one discounted cost at
        both cast start and completion (wand = free, scroll = half, curse
        modifiers via the effective cost). Previously the start demanded the
        FULL effective cost even for a wand, while the completion checked
        only the base cost — wands needed mana they never consume, scrolls
        over-required at start, and a cast could complete on less mana than
        it consumed.

FIX - "Use item, pick target" flows now pin the used item (Source-X
        OnTarg_Use_Item): the response re-validates that the source item
        still exists in the same parent it had when the cursor opened —
        a key/tool moved or handed to another player mid-target refuses the
        use — and the item's @TargOn_Char/@TargOn_Item/@TargOn_Ground
        triggers fire before the native behavior (RETURN 1 cancels).
        Applies to bandage/lockpick/scissors/key/dye/smelt/pick/fish pole/
        seed/pitcher and the weapon prompts.

NEW - Classic blade targets are reachable again: a sharp weapon targeted at
        a corpse carves it (DeathEngine.CarveCorpse finally has a player
        input path), at a sheep shears wool, at a dead fish fillets raw
        fish steaks, at a crop/foliage harvests it, and an axe at the
        ground starts Lumberjacking at the spot — matching the Source-X
        sharp-weapon target block beyond the old poisoning/repair pair.

FIX - Potions are now spell-driven (Source-X Use_Drink): a potion conveys
        the spell in MORE1 at strength MORE2 through the spell engine, so
        strength/agility potions apply TIMED effects — the old code did a
        permanent Str/Dex += 10 (a stat exploit), defaulted every tagless
        liquid to a heal potion (a full water pitcher healed 10 hits), and
        deleted the container. Fixed-point script decimals (MORE2=50.0 ->
        500) now parse, and drinking returns the empty container from the
        itemdef TDATA1 (empty bottle / empty pitcher).

FIX - BOT/STRESS/RESPAWN/RESTOCK typed over the telnet admin console now
        work. The telnet console has its own command processor and those
        harness/maintenance events were only wired on the local console
        processor, so the documented telnet commands were silently dropped.

DOC - README performance section remeasured on the current build (2026-07-20,
        5-vCPU VM): fresh scenario table with verdicts, login/boot/save
        results, corrected tick-rate wording (100 ms / 10 Hz, Source-X
        MSECS_PER_TICK) and GC mode; roadmap pruned of shipped items
        (plant growth, light burnout); both README.md and README-TR.md.

NEW - Ships can be dry-docked in game: the OWNER (or staff) double-clicks
        the tillerman while standing OFF the ship and the ship converts back
        to a deed in the backpack (hold cargo moves to a crate). Aboard or as
        a non-owner the tillerman just talks — and his lines now come from
        the tillerman item instead of appearing above the player (Source-X
        pItem->Speak). Source-X leaves dry-dock to shard scripts; this pack
        scripts none, so the engine provides the classic OSI flow.

FIX - Ship speech commands ("forward", "back", ...) now move the ship. Sphere
        speech sections stack alias patterns over one shared body (on=forward
        / on=foreward / on=unfurl sail -> SHIPFORE); matching any alias but
        the LAST collected an empty body, so most command spellings silently
        did nothing. Any alias in a group now executes the body that follows
        the last consecutive ON= line. This also fixes every aliased NPC
        speech table in the pack, not just ships.

FIX - The ship boarding plank now visually opens. The open-plank art comes
        from the side itemdef's TDATA1 (Source-X model: 03eb1 -> 03ed5); the
        open code only looked at a MORE1 staging value that freshly-placed
        ships never carry, so the plank changed type but kept the closed
        graphic. TDATA1 is now the fallback; closing restores the side art.

FIX - Double-clicking the house sign from INSIDE the house no longer makes
        the sign vanish (and demolish/redeed is reachable again). The item
        double-click gate ran a line-of-sight raycast; with house walls now
        being real LOS occluders, the sign on the wall failed the ray from
        indoors and the server "corrected" the client by deleting the item
        from view — so the gump never opened and the house could not be
        redeeded from inside. Source-X Event_DoubleClick checks
        containment/distance only (no raycast), same as our mobile dclick
        path; the item path now matches. Combat/NPC/spell LOS is unchanged.

FIX - .dupe now makes FULL clones and honours its copy count. A duped item
        previously kept only graphic/hue/amount/name — a duped house or ship
        deed lost its More1/multi-reference tags and came out blank, and any
        scripted item lost its type, link, tags and events. Duplication now
        uses the full-clone path (Source-X CreateDupeItem). ".dupe 10" also
        copies 10 times (Source-X CIV_DUPE argument; top-level runs clamped
        to 25 like the reference complexity cap) instead of always once.

FIX - Ships and houses now render completely. Two halves of one root cause:
        placed multis (ships, classic houses) were sent to the client as plain
        items, and a plain item with a multi graphic renders as nothing — the
        client only draws a structure when the world packet carries the multi
        data type (0xF3 type 2 / 0x1A graphic|0x4000), which previously only
        custom-house foundations got. Now every placed structure is sent as a
        multi, so the client draws the full hull/walls itself from multi.mul.
        Accordingly the server materialises ONLY the invisible placeholder
        components (tiller, planks, hatch, doors, house sign) as working
        items, exactly like Source-X — visible statics are never created as
        server items (no double-draw). Ships placed under the previous build
        should be redeeded and placed again.

FIX - Target cursor no longer "auto-fires": a spell cast (or any target
        command) could resolve instantly against yourself with no cursor
        shown, and only the next attempt displayed one. Root cause: a target
        response left over from a cancelled cursor session (the client zeroes
        its cursor id after a server-side cancel) was accepted as a live pick
        outside a narrow 2s window. Responses must now echo the armed cursor
        id exactly — anything else is dropped. Additionally, per Source-X
        Spell_TargCheck, a ground pick for an object-target spell (e.g. Night
        Sight) now aborts with "target must be an object" instead of silently
        casting on yourself.

FIX - Deleting a ship or house multi with .nuke/.remove/script REMOVE no longer
        leaves a ghost structure in the registry. The ownership counters kept
        counting the deleted ship ("you cannot have more ships"), and its
        region and component items leaked. Deletion now tears the structure
        down like Source-X (~CItemMulti): keys removed, component items
        deleted, region freed, registry entry erased. No deed is refunded on
        destroy — use dry-dock/redeed to get the deed back.

PERF - World-save snapshot capture now runs in parallel. Building the save
        records (property formatting per object) dominated the save stall
        (~0.45s of a 0.75s save on a 32K-object world); the capture is a pure
        read of the frozen world, so it now fans out across cores. This shrinks
        the freeze in both sync (SAVEBACKGROUND=0) and background save modes.

FIX - Placed ships and houses now materialise their full structure. The multi
        component visibility flag was read inverted, so a placed ship consisted
        of only the five invisible placeholder pieces (mast anchor, tiller,
        hatch, planks) with no hull, and houses would have shown no walls. The
        flag now matches the client's own rule (ClassicUO: flags != 0 = drawn
        static part; flags == 0 = placeholder replaced by working server items
        like doors and the tillerman).

FIX - Items bought from NPC vendors now run their scripted @Create body. Vendor
        stock was built as bare items (graphic + name + price only), so a
        purchased ship or house deed carried no structure reference ("that deed
        is blank") and any vendor-sold item with @Create-driven properties lost
        them. Stock now materialises through the same path as loot/spawns/.add.

FIX - Deleting a player's backpack (GM .remove) no longer breaks item drops:
        the character kept a cached reference to the deleted pack, so the
        automatic pack recreation never fired and items dropped onto the player
        landed inside a deleted container and vanished. The cached reference is
        now invalidated on delete, a fresh pack is created on the next drop
        (reference-engine GetPackSafe behavior), and the wearer is redrawn so
        the client sees the new pack before its contents.

PERFORMANCE - NPC route-finding is budgeted per tick: at most four full A*
        searches run in one NPC decision phase; additional chasers wait ~150ms
        for their turn (they keep facing the target meanwhile). A chase burst
        used to stack several 500-node searches into a single phase, visible as
        a 130ms tick on small servers. The background world save is also
        gentler now: it writes on one below-normal-priority thread with
        sequential shard writes, so the game loop wins the CPU during the
        write window instead of competing with parallel compression tasks.

PERFORMANCE - Opening any gump, clicking any dialog button or requesting any menu
        no longer re-reads and re-parses the entire script pack from disk. The
        dialog/menu section lookups used to scan every .scp file per interaction
        (a ~400ms main-loop stall per click on a real pack — worst for ids that
        do not exist in the pack at all, like the native help page fallback,
        which paid the full scan on every Help click). The sections are now
        retained at script-load time and looked up in constant time; resync
        rebuilds the caches.

PERFORMANCE - World saves can now write in the background (sphere.ini
        SAVEBACKGROUND > 0): the main loop only captures an immutable snapshot of
        the world — the expensive shard/encode/write phase runs on a worker
        thread, so the multi-hundred-millisecond save stall shrinks to the
        capture time. Saves never overlap (a new cycle is skipped while one is
        writing), the shutdown save waits for any in-flight write, and the
        atomic temp-file commit is unchanged. SAVEBACKGROUND=0 keeps the old
        fully synchronous behavior.

PERFORMANCE - Network hardening: accepts are budgeted per pass (a reconnect flood
        no longer holds the main loop in the accept while-loop), the per-IP
        connection limit uses a maintained counter instead of scanning all 1100
        slots on every accept, and login connections now use the same
        non-blocking batched sends as game connections — a stalled client mid
        login can no longer block the flush pass. NetworkThreads/UseAsyncNetwork
        ini keys are documented as deliberate no-ops (main-loop network design).

FIX - The server now validates its engine wiring at startup and refuses to boot
        half-wired: eleven mandatory hooks (movement cast-interrupt, NPC night
        lights, speech routing, shutdown/broadcast commands, world resolvers and
        more) are checked after wiring completes, turning the silent
        "assigned before the engine existed" bug class into an immediate,
        named startup error.

FEATURE - House/ship deed placement now raises the multi-preview cursor (0x99):
        the client ghosts the full building footprint at the cursor instead of a
        plain crosshair, and the server applies the reference engine's anchor-Y
        correction so the house lands exactly where the preview showed it. The
        placement reply is also fully re-validated (deed still in the same pack,
        reachable, caster alive and unfrozen) so a deed traded or dropped while
        the cursor was up can no longer place.

FEATURE - Lit light sources now live a real lifecycle: one charge burns per
        minute (20 by default), the flame goes out by itself when the charges run
        dry, a burned-out source can never be relit, and static/decoration lights
        burn forever. The remaining burn time survives restarts.

FIX - Ship movement and placement now treat static water tiles (coast fills,
        harbour water laid as statics) as sailable, and blocking world items on
        the water line stop the hull the same way docks and rocks do. The smooth
        boat-move packet (0xF6) also carries the full deck-object list, so
        smooth-sailing clients move passengers and cargo with the hull instead
        of waiting for individual resends.

FIX - Characters younger than the MinCharDeleteTime day count (default 7) can no
        longer be deleted from the character-select screen; the client shows the
        proper "not old enough" reason and staff accounts bypass the gate. The
        creation timestamp now persists in saves using the reference engine's
        CREATE key (age-based), and pre-existing characters without a stamp stay
        deletable.

FIX - Casting a spell from the unimplemented spell schools (Bushido, Ninjitsu,
        Spellweaving, Mysticism and the rest of that id range) now clearly reports
        "not supported yet" up front instead of consuming mana and reagents and
        then silently doing nothing. Script packs that implement those spells
        themselves (flags, effect curves, or ON= trigger stages) are unaffected.

PERFORMANCE - The automatic worker-thread count now leaves one core to the main
        thread instead of saturating every core, which starved the serial tick
        phases on shared/overcommitted VMs. An explicit MulticoreWorkerCount
        still wins. Tick telemetry also gained honest sub-phases: the parallel
        world tick is measured separately from the pre-compute setup, and the
        previously unmeasured replay/recorder/macro/reschedule work between
        apply and flush now reports as its own post_apply phase.

FIX - Double-clicking an NPC while standing near a map static door no longer
        toggles that door and makes the NPC vanish. The static-door fallback ran
        before the character lookup and, without validating the clicked serial,
        toggled the nearest door (with its open/close sound) and rebroadcast the
        door art under the NPC's serial — the client then showed the mobile as a
        door graphic until the next view refresh. The fallback now runs only when
        the clicked serial is neither an item nor a character, and the door art
        broadcast only ever reuses the synthetic static-door serials. Also removed
        the LOS raycast from the character double-click gate (the reference checks
        distance and visibility only), which could likewise delete a drawn mobile
        from the client when clicked through a wall. Temporary [static_door] field
        diagnostics removed.

PERFORMANCE - NPC chase pathfinding is now bounded like the reference engine and
        runs off the main thread. Three changes: (1) route-finding only engages for
        targets 2-13 tiles away and searches inside the same 28x28 local box the
        reference uses, so a walled-off target exhausts a small area instead of the
        whole node budget; (2) a chase route no longer treats the tile the target
        itself stands on as a hard wall — that made every route toward a character
        "unreachable", burning the full search budget each time while the NPC fell
        back to blind side-steps; routes now actually find detours around obstacles;
        (3) the search itself is precomputed in the parallel NPC decision phase, so
        the single-threaded apply phase finds a ready-made route instead of spending
        up to ~17ms per NPC on it. Behavior stays serial-authoritative: the serial
        phase re-validates every precomputed step and recomputes if the world moved.

PERFORMANCE - Tooltip pushes during view updates are now version-only, like the
        reference engine's default tooltip mode: when objects enter view the server
        sends the tiny revision packet and the client requests the full property list
        only for tooltips missing from its own cache. Full data is still pushed
        immediately when the client asks, when the tooltip actually changed, or in
        the send-full mode. A login or teleport into a crowded area now costs one
        small packet per object instead of a full serialized tooltip each.

PERFORMANCE - The built AOS tooltip is now cached on the object itself (the reference
        engine's model): one build serves every observer, expiry is purely time-based
        (sphere.ini ToolTipCache seconds), and the entry is no longer thrown away when
        the object scrolls out of a client's view. Previously every client kept its
        own copy and dropped it on view-exit, so login/teleport bursts rebuilt every
        visible object's tooltip from scratch; now re-entry is served from the shared
        cache. Explicit invalidation still forces a rebuild and bumps the revision so
        clients re-request changed tooltips.

PERFORMANCE - Speech no longer sweeps every nearby ground item per spoken line. The
        reference engine keeps a per-sector count of items that can actually hear
        (comm crystals; here also multis/ships, which route speech the same way) and
        skips the item sweep entirely when that count is zero — the common case. The
        same counter model is now maintained on item place/remove/retype, so shards
        without a crystal or house in earshot pay nothing per utterance. Scripted
        item @Hear still forces the full sweep, since then any item may listen.

PERFORMANCE - Tooltips no longer run a line-of-sight raycast per object. The reference
        engine gates tooltips on distance and visibility only (the client draws objects
        through walls, so their tooltips must arrive too); the extra per-object raycast
        during view updates was both a behavior mismatch (objects visible on screen
        could silently lack a tooltip) and a measurable cost when many objects enter
        view at once. Now matches Source-X: distance + visibility, no raycast.

INTERNAL - Removed redundant unused code paths flagged by a code review: a second
        expansion feature-flag lookup table and two never-instantiated scripting shells
        (a global-variable holder and a conditional-evaluator wrapper) whose real
        behaviour already lives in the running engine, so wiring them would only risk a
        divergent second source of truth. Verified-intentional scaffolding that was
        wrongly flagged as dead (the bot-performance CI gate, the fast-walk protocol
        packets, and the speed-hack audit hook) was kept and documented instead. No
        behaviour change.

PERFORMANCE - Removed a redundant per-NPC network scan when speaking near a crowd.
        Each NPC that turned to face a speaker did its own immediate broadcast of the
        turn to nearby clients — a separate scan per NPC on every spoken line. The turn
        is already carried by the normal view update (the same movement packet, to the
        same clients), so the extra broadcast was dropped. NPCs still face whoever
        addresses them; the turn is just delivered on the next view refresh.

PERFORMANCE - Cheaper speech handling in NPC-dense areas. When a player speaks, every
        nearby NPC runs a hear handler; it fired the global f_onchar_speech hook and the
        first-contact greeting trigger for every NPC on every line, even when no script
        defined them. Both are now skipped when unused, so a spoken line near a large
        crowd of NPCs costs noticeably less. NPCs still turn to face the speaker and
        scripts that do define those hooks behave exactly as before.

PERFORMANCE - Smoother NPC wake-up after login near a crowd. Logging in lights up the
        sector window around the player and could wake ~140 NPCs into the exact same
        100ms timer slot, so their AI all ran in one tick and caused a brief stall. The
        wake is now spread across ~8 slots by a stable per-NPC offset, so the load is
        distributed over a short window instead of one spike. Single-target wakes
        (an NPC reacting to being attacked) are unaffected and still immediate.

PERFORMANCE - Faster view updates in crowded areas / on login. Every object entering a
        client's view builds a tooltip, and that path fired the @ClientTooltip script
        trigger for every object even when no script hooked it — re-parsing the object's
        script definition and allocating per object. It is now skipped when unused (the
        same IsTrigUsed gate the single-click path already uses), so entering a busy
        region or logging in near many objects costs far less. Scripts that do hook the
        tooltip trigger behave exactly as before.

FIXED - Customizable house foundations now render on modern (SA/High Seas) clients.
        The multi body was sent to the client as a plain item instead of a multi (the
        0xF3 packet's data-type byte was hard-coded to "item", and the 0x1A packet sent
        the raw multi index which is below the client's multi range), so the client
        never treated a foundation as a house and silently discarded the streamed
        custom-house design. Multi bodies are now flagged as multis on the wire
        (data-type 2 on 0xF3; the 0x4000 multi bit on 0x1A) while the stored id is
        unchanged. Fixed (non-custom) houses and ships are unaffected — they already
        render from their placed component tiles.

CHANGED - State recording is now OFF by default. This optional diagnostic/replay
        feature took a periodic full-world snapshot on every server whether or not
        anyone used it. It is now disabled unless you set StateRecordingEnabled=1 in
        sphere.ini, so servers no longer pay that cost by default. Existing setups
        that rely on recording must add that line to keep it on.

PERFORMANCE - Removed the periodic latency spike from sleeping-sector upkeep. Item
        timers, spawns and decay in sectors with no nearby player used to all run in a
        single tick every few minutes, causing a recurring stall. That upkeep is now
        spread across ticks with a small per-tick budget and a resume cursor, so it
        completes over a short window instead of one big burst. Nothing is skipped:
        every eligible sector is still serviced once per cycle.

PERFORMANCE - Two per-tick/periodic sweeps no longer scan the entire object list.
        The TIMERF timer sweep now walks only the handful of objects that actually
        hold a pending timer, and the item-decay catch-up now walks only loose
        ground items. Both cost now scales with the small working set rather than the
        total world size, so large worlds no longer pay a fixed per-tick tax for
        timers and decay. Behaviour is unchanged: the same timers fire and the same
        items decay, just found more cheaply.

IMPROVED - House/ship placement now explains why it failed. Instead of a single
        "Cannot place here", a rejected deed placement reports a specific reason
        (house/ship limit reached, structure not defined, off the map edge, location
        blocked, or a script veto). Classic small ships can be re-placed from a
        dry-dock deed again (a raw multi id of 0 was wrongly treated as "no id"), and
        the first custom-house foundation deed now opens as a customizable foundation
        even without a CUSTOMHOUSE tag — the type is read from its [MULTIDEF].

FIXED - Test/doc hygiene: three local-only MUL diagnostic tests reported as passed
        when their data was absent instead of skipping; they now report as Skipped.
        Trigger docs synced — NPCSeeWantItem is wired and fires (the outstanding
        not-fired char trigger is now UserVirtue, the virtue-gump path).

FIXED - Several sphere.ini settings were parsed but never applied. Now honored:
        GameMinuteLength (world clock; was a hardcoded 20s/game-minute), the speech
        hearing distances (DistanceTalk/Whisper/Yell), MaxFame/MaxKarma/MinKarma
        limits, UseHttp (the web status endpoint can now be turned off), MapReadId
        (which map*.mul group to load, distinct from the client-facing MapSendId), and
        MapViewSize (default network view range). TICKPERIOD is now a legacy alias for
        the canonical ServerTickMs, and the <TICKPERIOD> script value returns the real
        tick interval. Defaults were aligned to the previous behavior, so unset configs
        are unchanged; only explicit sphere.ini values now take effect.

FIXED - Houses and ships could be lost on restart. Their world item stores the type
        (Multi / MultiCustom / Ship) only at runtime, but the saver never wrote it, and
        the item's BaseId is a raw multi index with no ITEMDEF to re-derive it from — so
        after a reload the structure read as a plain item and the housing/ship engines
        never re-registered it. The saver now persists the structure TYPE and it is
        restored on load. Old saves without it keep the previous legacy behavior.

ADDED - Placed houses/ships now use their script [MULTIDEF] metadata. The multi
        registry held only the binary geometry from multi.mul; the script name, type,
        storage and vendor limits were parsed but never merged in, so houses got a
        blank name and a flat storage default. The metadata is now overlaid by the
        shared multi id, and a placed house takes its script BaseStorage (e.g. 489 for
        a small stone house) instead of the hardcoded default. (Dynamic components —
        doors, signs, tillermen — and script regions remain a separate step.)

FIXED - Scripted house/ship deeds could load as plain items. A deed written as
        "[ITEMDEF i_deed_house] ID=i_deed" with no TYPE of its own stayed
        ItemType.Normal, so double-clicking it never reached the deed handler and
        placement never started. ID=<defname> now inherits the referenced base's
        TYPE (matching Source-X, which dupes the referenced typed base), so such a
        deed becomes a real deed. Type only — a graphic-only ID= reference does not
        pull in unrelated layer/tiledata.

FIXED - House/ship placement failed ("Cannot place here") on modern multi.mul data.
        The multi reader assumed the original 12-byte component record, but High Seas+
        data uses a 16-byte record (a trailing ship-access dword). Read the wrong way,
        every component offset shifted and the footprint inflated to thousands of tiles,
        so the map-bounds check rejected every location. The reader now auto-detects the
        12- vs 16-byte layout (from the index block lengths, with an offset-plausibility
        tiebreak) and reads the ship-access field on High Seas records.

PERF  - The state recorder no longer copies the whole world object table on every
        server tick. It walks the character roster only when its move-scan (2s) or
        snapshot (15s) interval is actually due, and uses the character-only roster
        instead of the full ~52K-object table — eliminating a large per-tick
        allocation (~10x/sec) that added GC pressure.

FIXED - Movement-based spell interruption was dead at real startup. MovementEngine
        was given the SpellEngine reference before SpellEngine existed, binding
        null; the wiring now runs after SpellEngine is constructed.

FIXED - NPCs always assumed daylight. The NpcAI night-light hook was wired behind
        an "if WeatherEngine != null" guard that ran before WeatherEngine existed,
        so it was skipped entirely; it is now wired after WeatherEngine is built.

FIXED - .SHUTDOWN and .BROADCAST admin/GM commands were accepted but did nothing
        (no subscriber). .SHUTDOWN now ends the main loop cleanly; .BROADCAST sends
        the message to every in-world client.

ADDED - The world is now saved on a clean shutdown (SaveOnShutdown, default on;
        set 0 to disable), so changes since the last periodic save are not lost on
        a planned stop.



ADDED - Plant growth (Source-X CItemPlant crop regrow). Planted crops and foliage
        now advance through their growth chain on a timer: each tick a crop grows
        into its next stage (ITEMDEF TDATA2); a fully grown crop drops its fruit
        (TDATA3) and resets to the first stage (TDATA1), hidden as an invisible
        regrowing plot (staff still see it) until it grows back. A crop moved into
        a container dies. Planting a seed starts the growth chain, and reaping a
        mature crop resets and regrows it instead of a flat cooldown. (This is the
        Source-X crop model, not the OSI plant-bowl minigame, which Source-X has no
        implementation of.)

ADDED - Config-driven multi-server login list (0xA8). The server list now uses the
        configured ServName/ServIP instead of a hardcoded "SphereNet"/127.0.0.1
        entry, and can advertise additional shards via a new SERVERLIST ini key
        ("name,ip,port" entries separated by ';', capped at 32). Server selection
        relays to the chosen shard's ip/port. (KR/EC 0xFFFFFFFF-seed handshake is
        still deferred — it needs a live KR/EC client to verify the crypto.)

ADDED - Uncompressed 0xB0 gump fallback for very old (pre-3.0) clients. Modern and
        not-yet-detected clients keep the compressed 0xDD gump, so the established
        path is unchanged.

[2026-07-17] - Bandage macro, virtue transport, region jail, HCI/luck suit aggregation
----------------------------------------

FIXED - Hit Chance Increase (HCI) now aggregates the whole equipped suit. The
        AOS to-hit formula read HCI from the attacker's own tag plus the weapon/
        talisman only, so HCI on armor or jewelry was silently ignored. It now
        reads the full suit (all equipped layers), matching Source-X's equip-time
        accumulation. Defense Chance Increase (DCI) on the target is read the same
        way. Both keep the +45% cap.

ADDED - Luck now aggregates the equipped suit for the status display. Each item's
        LUCK adds to the number shown on the status bar (previously only the base
        character luck was shown). Matching Source-X, luck remains display-only and
        is not wired into loot or any other mechanic.

ADDED - Bandage macro (client bandage hotkey, 0xBF 0x2C). The client's
        bandage-target hotkey now applies a bandage to a pre-selected target with
        no target cursor. This subcommand was previously mislabeled as virtue
        invocation; it is the Source-X PacketBandageMacro carrying [bandage uid]
        [target uid]. The handler verifies the item is a bandage and routes it
        through the Healing skill against the given target.

FIXED - Virtue invocation moved to its correct transport. Virtue hotkeys
        (Honor/Sacrifice/Valor) are delivered by the real client on the 0x12
        text-command packet as ext-type 0xF4 (EXTCMD_INVOKE_VIRTUE), not on
        0xBF 0x2C. @UserVirtueInvoke now fires from that path with the virtue id
        in N1 and no longer collides with the internal skill-lock funnel.

ADDED - Region-driven jail cells. The JAIL staff command and the login
        re-confine now locate the cell from an AREADEF region named "jail" (or
        "jailN" for a numbered cell) instead of hardcoded coordinates, matching
        Source-X GetRegionPoint. Syntax: JAIL <serial> [minutes] [cell]. Shards
        with no jail region keep the previous Britain coordinates. New FORGIVE /
        PARDON staff commands release an inmate (alias of UNJAIL), and the
        .forgive character verb now actually frees a jailed character.

FIXED - RESPHYSICALMAX was never written to the world save. The other four
        per-element resist caps (fire/cold/poison/energy) were persisted but the
        physical cap was dropped on every save. These caps remain display/storage
        only, matching Source-X, which does not clamp damage to them.

[2026-07-16] - NPC keyword word-matching, GM yell broadcast fix, vendor leash
----------------------------------------

FIXED - Panel Scripts page listed nothing when the Host process served the
        panel. SphereNet.Host read the script directory with the key name
        "ScpFilesDir" (the C# property name) instead of the actual sphere.ini
        key SCPFILES, so the lookup always missed and fell through to a
        "scripts" folder next to sphere.ini. With SCPFILES pointing anywhere
        else, PanelContext.ScriptsPath stayed null and GET /api/scripts
        returned an empty list. The in-process SphereNet.Server host was
        unaffected — it resolves the path through SphereConfig.

FIXED - sphere.ini LOG= was parsed into nothing. SphereConfig declared LogDir
        but LoadFromIni never read the key, and no code consumed the property;
        "logs" was hardcoded at the Serilog file sink and in the Host crash
        handler instead. LOG= now binds to SphereConfig.LogDir and both call
        sites honour it. Relative values follow the same CWD-first rule as the
        other path settings.

ADDED - sphere.ini now ships WORLDSAVE= and ACCTFILES= entries. Both keys were
        already read by SphereConfig, but were absent from the shipped file, so
        their directories could only be changed by knowing the undocumented key
        names. Values match the previous defaults (save/, accounts/) — no
        behavior change for existing installs.

ADDED - Panel self-update (Updates page, new sidebar entry). A new workflow
        (.github/workflows/release.yml) builds a win-x64 package on every main
        commit and refreshes the assets of a rolling "nightly" prerelease; the
        panel checks it, downloads it, and applies it in place. New endpoints:
        GET /api/update/status, POST /api/update/check, POST /api/update/apply.
        New sphere.ini keys: APPUPDATEREPO (empty = feature off and the page is
        hidden), APPUPDATECHANNEL, APPUPDATERUNTIME, APPUPDATECHECKMINUTES
        (0 = no background check), APPUPDATETOKEN. Distinct from the existing
        APPUPDATEREPODIR / update.cmd flow, which builds from source and needs
        git + SDK + Node on the box; this one only needs network.
        Release assets, not Actions artifacts: artifact downloads require an
        actions:read token even on a public repo and expire after 90 days,
        whereas releases/download/<tag>/<name> is anonymous, unauthenticated
        and not API-rate-limited. The artifact is still uploaded for CI use.
        The workflow uploads version.json LAST, after the zip is fully up, so
        the "latest" pointer can never reference a half-uploaded package.
        Version identity is version.json next to the exe; ordering uses
        github.run_number since SHAs cannot be compared. No version.json means
        a source build, which reports IsDevBuild and refuses to apply so a
        developer's own binaries are never silently overwritten — build.ps1's
        existing keep-list already strips version.json from local Release
        output, so this is automatic.
        Applying is always an explicit operator action; the background check
        only lights the sidebar badge. The flow saves the world first and
        aborts if the save is rejected, verifies the package SHA256 against the
        published checksum, rejects zip entries that escape the staging dir,
        and validates that the extracted tree actually holds Host, Server, the
        panel assets and a matching version.json before touching anything.
        Because the running Host holds a lock on SphereNet.Host.exe, the swap
        is done by a short-lived external PowerShell process (kept as a string
        in UpdaterScript.cs so it cannot be replaced by the very update it is
        applying): it waits for the Host to exit, backs up, swaps, and
        relaunches — restoring the backup and still relaunching on failure.
        config/, save/ and scripts/ are absent from the package and never
        touched; panel/ is replaced wholesale so stale hashed bundles cannot
        linger. Details land in logs/update.log.

ADDED - AdminPanelAutoFill (sphere.ini, default 0): pre-fills the admin panel
        login field with ADMINPASSWORD so a local operator does not retype it.
        Handing the panel's only secret to the browser needs all three gates:
        the flag on, the request from loopback, and ADMINPASSWORD still stored
        as plaintext — the setup wizard writes an MD5 hash, which has no
        plaintext to recover, so autofill stops after setup runs. New endpoint
        GET /api/auth/local-hint returns {password: null} when any gate fails.
        Leave off for anything reachable through a reverse proxy: the proxy
        also connects from loopback, so remote users would receive the password.

FIXED - Monster corpses were empty (Balron field report): ITEM=loot_x in
        @NPCRestock bodies resolved the defname, saw RES_TEMPLATE instead
        of RES_ITEMDEF and silently spawned nothing. Source-X
        CItem::CreateHeader accepts a template in defname position and
        ReadTemplate pours the whole loot list into the pack
        (CItem.cpp:461-539/586). The ITEM verb now expands sequential
        templates: per-row R# 1-in-N chance and {lo hi} dice amounts
        (CreateHeader parity), inline weighted pools in defname position
        ({ a w b w }, "0" = empty slot), nested templates, CONTAINER=
        sub-bags, and numeric itemdef rows (ITEM=0EED). The template
        loader keeps raw row args and CONTAINER rows; vendor stock
        enumeration skips container rows. NOTE: the live pack shipped
        with 33 ON=@NPCRestock and 423 ITEM= loot lines commented out in
        the monster chardefs (likely because the loot rows did nothing) —
        those are re-enabled pack-side.

FIXED - A dismounted mount ignored its owner's fights: dismount put the
        pet in Follow mode, which only self-defends. Source-X leaves the
        dismounted pet idle (CCharUse.cpp:1205); an idle owned pet
        defaults to guarding its owner (NPC_LookAtChar,
        CCharNPCAct.cpp:1036) and NPC_Act_Guard attacks whatever the
        owner fights (CCharNPCAct.cpp:1300-1303). Dismount now sets
        Guard mode on the rider, so the mount joins the owner's attack.
        MountEngine.Dismount also clears STATF_RIDDEN itself (parity
        with Use_Figurine) instead of relying on each caller — a path
        that forgot left the mount skipped by its own AI tick.

FIXED - Fast consecutive chat messages silently vanished: an invented
        speech rate limit (10 messages per 5s sliding window, commands
        consuming the same quota) dropped everything past the cap.
        Source-X Event_Talk has no such limit — the throttle is removed;
        extreme packet floods remain handled at the network layer
        (MaxPacketsPerTick / FloodDetectionCount).

FIXED - Service NPCs answered unrelated player-to-player chatter: the
        built-in keyword dispatch used substring Contains(), so any word
        embedding a keyword triggered a response — Turkish "buyur"/
        "buyuk"/"buyu" contain "buy" (vendors opened the buy gump and
        barked on nearly every sentence), "banka" contains "bank". All
        service keywords (buy/sell/purchase/train/teach/heal/resurrect/
        cure/stable/claim/balance/help/guards) now match whole words;
        "bank" matches as a word prefix so "banka"/"bankaya" keep
        working. The withdraw/check amount parser is word-bounded too —
        "check" inside another word can no longer trigger a withdrawal.

FIXED - A staff yell disconnected the client: GM yell ran the NPC-hear
        loop over every character in the world AND swept every world item
        for @Hear (9999-tile scans), stalling the main loop until the
        client timed out. Source-X promotes a Counsel+ yell to
        TALKMODE_BROADCAST (CCharAct.cpp:3575) — a CLIENT-only broadcast.
        Now the speech packet goes to every playing client via a direct
        client-list path (BroadcastNearby global branch; the sector walk
        is never entered for huge ranges), while NPC/item hearing stays
        at normal yell earshot (48 tiles). Ghost yell per-recipient
        garbling is also clamped to earshot.

CHANGED - Service NPCs (vendor/banker/stable brains) keep to their post:
        they wandered up to ~3 tiles plus drift and strolled out of their
        buildings. They now pull back beyond 1 tile from home and only
        wander while standing on it (max ~2 tiles drift, ignores MOREZ
        by design — same shard rule as spawn-on-gem).

[2026-07-16] - Vendor detection unified across dclick, speech, and trade paths
----------------------------------------

CHANGED - NPC spawners now place the child ON the worldgem bit (deliberate
        deviation from Source-X, shard owner request). Source-X GenerateChar
        scatters the child MoveNear(pt, rand(MOREZ)+1) up to MOREZ tiles
        from the gem (CCSpawn.cpp:433); SphereNet mirrored that. MOREZ
        (third MOREP field) keeps its wander-leash role (HOMEDIST,
        CCSpawn.cpp:640). The random scatter survives only as a fallback
        when the gem's own tile is impassable (gem buried in a wall).
        @Spawn scripts that set an explicit position still win. Item
        spawners are unchanged (still scatter within MAXDIST).

FIXED - Double-clicking some vendors opened the paperdoll while "buy"
        speech listed their goods: the speech path widened BRAIN_HUMAN
        NPCs into vendors by role name (legacy packs ship e.g. c_h_vendor
        with brain_vendor commented out and NPC=brain_human active), but
        the double-click, context-menu, and 0x3B/0x9F trade-validation
        gates checked NpcBrain == Vendor strictly. All paths now share one
        VendorEngine.IsVendorLike predicate (brain Vendor, or Human/None
        brain with a vendor/shopkeep/merchant name keyword), so any NPC
        that answers "buy" also opens the buy/sell dialog on double-click
        and can actually complete purchases and sales. Restock, the script
        BUY verb vendor resolution, and the owned-vendor CASH command use
        the same predicate.

FIXED - Context-menu "Sell" on a vendor only printed a sysmessage stub;
        it now opens the sell list like the speech path.

[2026-07-16] - Field batch 2: message formatting, speech color, dismount follow
----------------------------------------

FIXED - "You pull out a %s!" printed literally: nine call sites passed
        %s-bearing messages through Get() with no arguments (fishing success,
        detect-hidden, begging, resurrect, pet guard, party decline/leave).
        All now format with the proper name per the Source-X SysMessagef
        call. The hiding-fail path printed DEFMSG_HIDING_STUMBLE (a
        walk-into-hidden-char message) with a raw %s — Source-X Skill_Hiding
        FAIL sends no text at all (CCharSkill.cpp:2481), so it was removed.

FIXED - Skill-gain text removed: Source-X Skill_SetBase sends no "skill
        increased" message — only the 0x3A skill-window refresh
        (CCharSkill.cpp:222). The invented blue line is gone; shards can add
        one via @SkillChange.

FIXED - Dismounting left the mount masterless: it fell into the Animal brain
        and wandered off, and GM inspection showed no IPet memory. Source-X
        Use_Figurine re-owns the mount to the rider on every dismount
        (NPC_PetSetOwner → MEMORY_IPET + follow, CCharUse.cpp:1202-1206);
        MountEngine.Dismount now assigns ownership (which also mirrors the
        IPet memory item) and sets follow mode.

ADDED - NPC speech honors SPEECHCOLOR/SPEECHCOLOROVERRIDE (Source-X
        CChar.cpp:4147): default stays the reference light gray 0x03B2, but
        scripts can now color an NPC's overhead speech per character.

FIXED - First cast after a pause hit the PREVIOUS target with no cursor:
        when a new cursor replaces an open one, ClassicUO zeroes its stored
        cursor id after our cancel, so its queued last-target reply arrived
        as id-0 + old serial and slipped past the session-id guard. Any
        id-less response inside the 2s replacement window is now swallowed
        (cancel echo or stale pick alike); cursor teardown also retires the
        session id and echo window so nothing lingers across exchanges.

FIXED - Fixed-point spawner NPCs (MOREZ=0, e.g. the bank's vendor) strolled
        away from their spawn: HomeDist=0 was mapped to "unlimited wander"
        (the non-spawn default). Source-X CCSpawn writes the spawner's max
        dist verbatim as the wander leash (CCSpawn.cpp:640); spawn children
        now get a leash of at least 1 tile around the gem.

ADDED - [slow_gump] log: a 0xB1 gump response that holds the receive path
        >=100ms (field: 211ms) now logs its gumpId/button so the offending
        dialog script can be identified.

FIXED - Invisible-door self-heal: the client purges ground items on its own
        (distance culling / resync races) while the server's known-set still
        counts them as delivered — the player then collides with a door they
        cannot see (Britain bank report; the item was server-side, walking
        far and back redrew it). A rejected step now re-sends the dynamic
        items on the forward tile; redrawing an item the client already has
        is a no-op. The client's own 0x22 resync (Ctrl+R) also rebuilds the
        full view.

ADDED - End-to-end gather regression tests: fishing schedules through the
        real client path and delivers the fish at completion; mining's
        pending window must span strokes × DELAY (3.2-9.6s), never the old
        1.6s total. NPCRestock chardef-body loot verified through the full
        dispatcher chain.

[2026-07-16] - Gather-skill stroke timing, skill gain for GMs, spell-memory info, NPC loot
----------------------------------------

ADDED - slow_tick reports now break npc_apply into commit/purge/dirty
        sub-phases with decision and dirty-object counts, so the field
        46-157ms npc_apply spikes can be attributed (decision commit vs
        path purge vs dirty-view marking) instead of guessed at.

FIXED - Spawner NPCs came up with empty backpacks and dropped no loot: the
        pack declares monster gear and backpack loot in ON=@NPCRestock
        blocks (~192 chardefs), but the trigger only fired for vendor-brain
        NPCs. Source-X NPC_LoadScript fires @NPCRestock for EVERY freshly
        created NPC (CCharNPC.cpp:289-290); both the spawner hook and the
        .add pipeline now do the same. The ITEM/ITEMNEWBIE trigger verbs
        (and the top-level chardef key path) now also stamp ATTR_NEWBIE on
        ITEMNEWBIE items so worn gear stays with the owner on death while
        plain ITEM loot drops to the corpse.

FIXED - Fishing/mining/lumberjacking finished 2-6x too fast with a burst of
        cosmetic animations: the skill DELAY curve was treated as the TOTAL
        duration and strokes were animated every DELAY/5 (clamped 0.5-2s).
        Source-X runs strokeCount x DELAY — the stroke count is rolled at
        skill start (fishing 1-2, mining/lumberjacking 2-6) and each stroke
        is one animation+sound a full DELAY apart (Skill_Stroke re-arms with
        SetTimeout(delay)). Non-gather delayed skills no longer repeat
        strokes at all (Source-X routes SKTRIG_STROKE only for CRAFT/GATHER
        skills). Each fishing stroke now also drops the 1s water-splash FX
        item at the cast point, and completion lands together with the final
        stroke so the end of the attempt is unambiguous.

FIXED - Skill gain never happened for GM-level characters (reported as
        "anatomy won't gain"): GainExperience had an invented PrivLevel>=GM
        early-out. Source-X Skill_Experience has no priv gate — PRIV_GM only
        forces the success roll in Skill_CheckSuccess. Also matched the
        failed-use credit: a fail arrives as negative difficulty and clamps
        to the minimum 1 (we used to Abs() it to full credit, wrongly
        passing the GAINRADIUS gate on failures).

FIXED - Double-clicking a spell-effect memory (e.g. the Night Sight rune
        shown by .edit) answered "Object not found": the mirror item lives
        only in ch.Memories with no world UID, and the edit menu routed it
        into a FindObject(0) lookup. The pick now lands in the memory
        readout branch and prints the spell id, strength and caster.

[2026-07-16] - GM pass-through movement follows the ground
----------------------------------------

FIXED - GM/AllMove (and pass-walls) movement stepped on pure delta KEEPING Z
        frozen: a GM entering a dungeon at Z=10 crossed the whole floor at 10
        while the client rendered its own terrain-following Z — invisible
        drift until any self-redraw (dclick facing) snapped the character up.
        Confirmed by the [z_drift] report at 5146,993 (stored 10, floor 1) and
        a real-mul probe (WalkCheck lands at 1 from every neighbor). The
        bypass keeps its cannot-be-blocked property but now seats Z on the
        nearest surface each step.

[2026-07-15] - RESPAWN FULL: incremental, orphan sweep; dungeon Z-drift diagnostic
----------------------------------------

FIXED - RESPAWN FULL froze the main loop for ~47s (4445 spawners reset
        synchronously; the freeze also invited double-issuing the command).
        The reset now runs incrementally (~25ms budget per tick) with an
        in-progress guard and progress/summary logs.

FIXED - RESPAWN FULL left the old broken 1-hp NPCs alive while spawning fresh
        ones: their spawner link (ADDOBJ) was broken, so KillAll never saw
        them. The reset now finishes with an orphan sweep deleting non-owned
        NPCs that carry the Spawned flag/SPAWNITEM tag but belong to no
        spawner's child list (tamed ex-spawns are never swept).

ADDED - [z_drift] diagnostic: the dclick-facing redraw re-imposes the stored
        server Z; when that differs from the map-derived standing Z (the
        "dungeon chest dclick bumps me up" report), the pair is logged to
        locate the drift source.

[2026-07-15] - All target-cursor paths record their session id
----------------------------------------

FIXED - The cursor session guard only knew the ids sent by SetPendingTarget;
        every other cursor path (.info/.kill/teleport/script TARGET — 19 call
        sites) sent an unrecorded random id, so the guard dropped the player's
        legitimate clicks ("target broken", ".kill shows no cursor"). All
        sends now route through a shared SendTargetRequest that records the id.

[2026-07-15] - Cursor echo window, sqlite DQS compatibility, log noise
----------------------------------------

FIXED - First-cast "Targeting cancelled": the replaced cursor's cancel-echo can
        arrive with cursorID 0 (ClassicUO zeroes its stored id after a server
        cancel), slipping past the session-id match and cancelling the fresh
        spell target. One id-less cancel inside a 2s replacement window is now
        swallowed; a real ESC always echoes the matching id and still works.

FIXED - Script DB: classic packs write SQL string literals in double quotes;
        Microsoft.Data.Sqlite's bundled sqlite disables that legacy quirk
        (DQS), so F_RESTYPES_SETUP's INSERTs failed with "no such column:
        C_TROLL". DQS is now enabled on every script DB connection (Source-X's
        sqlite behavior).

CHANGED - [NAME_CHANGE] dropped from Warning to Debug: script @Create renames
        are routine pack behavior and flooded the log during RESPAWN FULL.

[2026-07-15] - Target cursor session guard (first cast mis-target fix)
----------------------------------------

FIXED - Casting while another target cursor was still open fired instantly at
        self/the previous target instead of showing the spell cursor: replacing
        an open cursor sends the client a cancel, and the client's cancel-echo
        for the OLD cursor raced the newly armed spell target, consuming or
        cancelling it. Target responses now echo-match the 0x6C request id —
        a response carrying a replaced cursor's id is ignored. The second cast
        always worked because no stale cursor existed by then.

[2026-07-15] - Script engine: assignments inside IF/DORAND/DOSWITCH were dropped
----------------------------------------

FIXED - LOCAL.x= / ARGN / ARGS / REFn= / REFn.prop= / FLOAT.x= assignment lines
        were only handled by the top-level interpreter loop; the IF, DORAND and
        DOSWITCH block executors dispatched their body lines straight to the
        generic verb path, so every such assignment INSIDE an IF block silently
        vanished. Field symptom: F_RESTYPES_SETUP set LOCAL.TABLENAME inside
        IF (<LDB.CONNECTED>) — the CREATE TABLE targeted an empty name, every
        later restypes query failed, and "<EVAL <SERV.RTICKS>-<LOCAL.HOUR>>"
        printed the whole epoch. The handlers are now shared with all block
        executors (regression probe: ScriptLocalScopeProbe).

[2026-07-15] - Character roster snapshot for follower scans
----------------------------------------

CHANGED - GameWorld keeps a char-only mirror of the object table; the
        follower-count recompute now scans ~9K characters instead of copying
        and walking the 100K+ full object array. The [slow_packet] 0x34 lines
        (20-27ms per status request on cache expiry) drop to sub-millisecond.

[2026-07-15] - Static-door auto-close: open-leaf art fix + field diagnostics
----------------------------------------

FIXED - A map-static door whose .mul static is baked as the OPEN leaf
        auto-closed by re-broadcasting its own (open) art — visually never
        closing. The sweep now derives the closed art through the same
        GetDoorDir slot math the manual toggle uses (open leaf → tile-1).
        Sweep logic extracted to StaticDoorSweeper (unit-tested end to end).

ADDED - [static_door] log lines on open/manual close and on every auto-close
        sweep pass (with a dropped-no-static counter): an "opened" with no
        auto-close within ~21s pinpoints a dead sweep; an auto-close logged
        while the door still looks open pinpoints a stale client ghost.

[2026-07-15] - Invented-values audit, wave 5: craft quality, Blood Oath, throwing
----------------------------------------

FIXED - Crafted item quality ported to the Source-X Skill_MakeItem band table
        (CCharSkill.cpp:724-794): skill picks a band (shoddy..superior), a
        logarithmic ±0-2 band variance shifts it, the value rolls inside the
        band. The invented linear 100+excess/10 curve, the "exceptional"
        rename at 150+ and its +20% durability boost are gone; the reference
        grandmaster maker's-mark (skill > 99.9 and quality > 175) remains.

FIXED - Blood Oath reflect level now the exact Source-X formula on the layer
        owner (caster): MagicResistance*10/20 + 10, unclamped
        (CCharSpell.cpp:1313) — was an invented /20+10 clamped 10-90 computed
        from the enemy's skill.

FIXED - NPC rock-throwing stamina cost 4+rand(6) (CCharSkill.cpp:3372); the
        Dex/4 damage roll was verified as reference (CCharSkill.cpp:3447).

NOTES - Verified as genuine Source-X (citations added, no change): the entire
        NPC hostility coefficient set (51/100/-50/kin softenings/+50 grudge,
        NPC_GetHostilityLevelToward), SpiritSpeak 24s (4*60 tenths). Deferred:
        NPC brain-tick cadence (field-tuned; Source-X (150-DEX)/2-tenths model
        needs in-game validation), Discordance 20s (scripted skill in the
        reference — engine fallback kept).

[2026-07-15] - Invented-values audit, wave 4: repair, traps, stamina regen
----------------------------------------

FIXED - Tinker repair rebuilt to Source-X Use_Repair (CCharUse.cpp:792-852):
        difficulty = the item's SKILLMAKE craft-skill level scaled by damage
        percent (floor: level/4) rolled against THAT craft skill; success
        restores FULL durability; failure rolls 1/6 max-durability loss, else
        1/3 one-point chip. The old invented formulas (fixed 20-90 difficulty
        vs blanket Tinkering, skill/25 restore, 10% fail damage) are gone.

FIXED - RemoveTrap: difficulty is rand(95) (CCharSkill.cpp:2913) and a botched
        disarm deals the trap's OWN damage value, default 2 (CItem.cpp:5507) —
        not an invented 5-19 roll.

FIXED - Stamina regen is a flat max(1, REGENVAL) per tick like hits/mana
        (Source-X Stats_GetRegenVal) — the invented dex/30 (players) and
        maxStam/20 (NPCs) scaling made big creatures recover far too fast.

NOTES - Verified as genuine Source-X (citations added, no change): healing
        difficulties 85+25/50+50/0-80 (CCharSkill.cpp:2836), Focus regen
        bonuses /100 stam /200 mana (Skill_Focus), NPC book fallback = the
        classic 64-spell magery window still gated by the book's spell bits.

[2026-07-15] - Invented-values audit, wave 3: skill gain, unarmed damage, NPC skill, tools
----------------------------------------

FIXED - Skill gain followed an invented (1000-skill)/5 curve whenever a skill
        def lacked ADV_RATE. Source-X GetChancePercent: no curve = no gain.
        The substitute curve is removed; gain strictly follows ADV_RATE.

FIXED - Unarmed damage came from an invented Str/4 formula. Source-X
        Fight_CalcDamage uses the CHARDEF's DAM line (players: c_man DAM=1,4
        classic fists); a chardef with no DAM has attackBase 0. STR now only
        contributes the era damage-bonus percent.

FIXED - Skill-less NPCs no longer get an invented stat*10 combat-skill floor
        (min 25.0): Source-X fights at the raw chardef skill, so an
        under-scripted NPC hits at 0 skill instead of being silently buffed.

FIXED - Gathering tools without UsesRemaining broke at an invented 1-in-50
        per-use chance. Source-X damages the tool's HITPOINTS by 1
        (CCharSkill.cpp:2447) — a tool whose def has no hitpoints never wears.

NOTES - Verified as genuine Source-X values (citations added, no change):
        spider-web strength 60+rand(250) (CCharUse.cpp:638), NPC flee steps 20
        (CCharNPCAct.cpp:412), base wrestling swing speed 50 (CResourceCalc.cpp:49).

[2026-07-15] - Invented-values audit, wave 2: loot/summons/prices/hunger/spawn windows
----------------------------------------

FIXED - Synthetic NPC loot removed: every kill also minted stat-tier gold,
        reagents and gems ON TOP of the script-defined loot, inflating the
        economy. Source-X drops only what the chardef/@CreateLoot scripts
        define — that path (MaterializeDeathLoot) remains.

FIXED - Necromancy summons (Animate Dead, Vengeful Spirit) had flat invented
        stats (60/60/40/20, 80/80/60/40 + 60.0/80.0 skills). Summons now take
        the creature's own chardef — name, brain and the @Create stat block —
        like Source-X NPC_LoadScript, then spawn at full vitals.

FIXED - Vendor price fallback derived from the ART TILE ID (BaseId/10+5) when
        an item had no PRICE tag. Both the display and the server-side check
        now price from the itemdef VALUE (Source-X GetMakeValue).

FIXED - Item quality defaulted to 50; Source-X starts at 0 (only crafting sets
        it). Saver now persists only non-zero QUALITY.

FIXED - Invented "starvation bite" removed: a starved character lost stam/hp
        every hunger tick. Source-X starvation only halts regeneration.

FIXED - Spawner default respawn windows aligned to Source-X CCSpawn (random
        1..30 minutes when MOREP declares nothing): char spawners used a fixed
        15-30 min window, item spawners refilled in 1-5 minutes (up to 6x too
        fast for resource nodes).

[2026-07-14] - Invented-values audit, wave 1: combat/poison/decay/caps/training
----------------------------------------

FIXED - Weapon with no DAM in its def derived damage from the ART TILE ID
        (BaseId/10 — high-graphic items hit absurdly hard). Source-X
        Weapon_GetAttack: attackBase 0. Now 0-1.

FIXED - Two leftover synthetic-durability-50 paths: tinker REPAIR and the script
        DAMAGE verb both invented (and persisted!) a 50-point pool onto items
        whose def has no HITPOINTS — permanently converting unbreakable items
        into breakable ones. Both now follow Source-X: no hitpoints, no wear.

FIXED - Poison tables rebuilt to Source-X OSI values (CCharSpell.cpp:1815,
        CCharAct.cpp:4229): charges 3/3/6/6/8 (was invented 5..20), tick delay
        2/3/4/5/5s (was inverted 4s..1.5s), damage = % of max STR per level
        (4-7/5-10/7-15/15-30/16-33%, floors 2/4/6/8/10) instead of flat ranges.

FIXED - Ground item decay was hardcoded to 10 minutes; sphere.ini DecayTimer was
        read but never wired. Now wired (Source-X m_iDecay_Item default 30 min).

FIXED - Container caps aligned to Source-X: CONTAINERMAXITEMS default 255
        (MAX_ITEMS_CONT; was 125 config / 500 hard ceiling), BANKMAXITEMS 1000
        (was 125), BANKMAXWEIGHT 1000 stones (was 1600).

FIXED - NPC paid training used invented 100%/30.0-cap and disagreed with the
        "train" keyword path (30%/42.0). Single source now: Source-X defaults
        30% / 420, wired from new sphere.ini keys TrainSkillPercent /
        TrainSkillMax / TrainSkillCost.

[2026-07-14] - Field-report batch: spawn distance/LOS, boot stat repair, resource
               gem lifecycle, mounted skill animations, item durability
----------------------------------------

FIXED - Spawner children scattered up to 15 tiles from the gem:
  - Source-X loads MOREZ verbatim (0 = spawn adjacent, MoveNear dist 1); the
    component kept its 15-tile default whenever MOREZ was 0. Children now also
    require line of sight back to the spawn point (Source-X CCSpawn), so they
    cannot materialize behind walls.

FIXED - NPCs with STR=0 persisted by older builds:
  - Saves written before the OSTR fix baked STR=0 into native format, beyond
    the loader fix and RESPAWN FULL (non-spawner NPCs). Boot repair now restores
    base stats from the O-mirror or re-runs the chardef @Create, then tops off
    vitals ([boot_repair] log line).

FIXED - Resource-marker worldgems lived forever (TIMER=-1):
  - Fishing/mining/lumberjack markers only armed a decay once fully depleted and
    cleared it while regenerating — one immortal invisible item per worked tile.
    Now every touch re-arms decay one regen period out (Source-X MoveToDecay
    lifecycle: by expiry the vein is fully refilled, so deletion is lossless).
    Boot repair arms decay on markers already saved without one.

FIXED - Skill animations dismounted/remounted the rider visually:
  - Fishing/mining/lumberjack swings sent the on-foot animation for mounted
    characters. Skill animations now use the mounted variant (shared
    BodyAnimTranslator.ToMounted, same table the spell-cast path used).

FIXED - Weapons/armor without HITPOINTS broke and vanished:
  - The combat wear path synthesized a default durability for items whose def
    defines none — classic packs define HITPOINTS on almost nothing, so all
    gear slowly self-destructed. Source-X only wears items that actually have
    hitpoints; the synthetic assignment is removed.

ADDED - Item destruction emote:
  - When worn gear breaks, everyone nearby sees "*<item> is destroyed*" over
    the wearer (Source-X ITEM_DMG_DESTROYED emote) in addition to the owner's
    private message.

[2026-07-14] - RESPAWN FULL admin command
----------------------------------------

ADDED - RESPAWN FULL (console/telnet/.serv.respawn full): kills every spawner's
        children first, then refills fresh. Plain RESPAWN only tops missing
        children up, so NPCs materialized in a broken state by an older build
        stayed in the world; FULL replaces them all in one pass.

[2026-07-14] - Legacy stat load (OSTR->STR) and spawn vitals top-off
----------------------------------------

FIXED - Every NPC loaded from a classic Sphere save had base stats of 0:
  - Classic saves persist only OSTR/ODEX/OINT; Source-X r_LoadVal maps those to
    Stat_SetBase — the same slot as STR/DEX/INT. WorldLoader mirrored them into
    the O-fields only, so Str/Dex/Int stayed 0 after load. This zeroed combat
    math worldwide: the relative-strength morale term made stat-0 monsters
    afraid of everything (they ignored valid targets such as a dismounted
    mount and only fought via retaliation memories). OSTR/ODEX/OINT now seed
    the base stats exactly like STR/DEX/INT.

FIXED - Spawner-created NPCs materialized at 1 hp:
  - Classic packs assign STR/DEX/INT inside the chardef's @Create block, which
    raises the max vitals AFTER SpawnComponent seeded them from the stat-less
    section header — a fresh orc arrived at 1/102 hp. The spawn pipeline now
    tops off Hits/Stam/Mana after @Create, same as the .add pipeline.

ADDED - Regression tests: classic O-stat load, live-pack orc spawn (name from
        #NAMES_, @Create stat ranges, full vitals), dismounted mount remains a
        valid monster target.

[2026-07-14] - Script trigger leaked across sections (fishing pole dclick dead)
----------------------------------------

FIXED - A trigger fired by name could execute an ON= block belonging to a LATER,
        unrelated definition in the same .scp file:
  - TriggerRunner's on-demand file scan opened the resource at its stored section
    header but then searched EVERY section to end-of-file for the trigger. Any
    definition without its own handler silently inherited the next same-named
    ON= block below it. Live symptom: double-clicking i_fishing_pole did nothing —
    [ITEMDEF 0dbf] has no @DClick, so the scan drifted into the arrow-shaft def's
    "@DClick ... RETURN 1" further down sphere_item_profession.scp, blocking both
    the dclick-equip and the fishing target. Trigger search is now confined to
    the resource's own section (Source-X CResourceLock stops at the next
    [header]). Live-pack regression probes added (FishingPoleLivePackProbe).

[2026-07-14] - net_in stall attribution and follower-count scan cache
----------------------------------------

ADDED - [slow_packet] diagnostic: a single inbound packet handler running longer
        than SlowPacketWarnMs (new sphere.ini key, default 20, 0=off) logs the
        opcode, handler name and duration. [loop_stall] lines now also carry
        pkts= (packets handled that pass) and slowest_pkt=0x??@ms, so a stall in
        the net_in phase names its dominant packet instead of an opaque total.

CHANGED - CurFollower no longer walks the whole world on every read:
  - The follower count recomputed by scanning ALL world objects (100K+ on large
    saves, plus a full array copy per call), and the status packet (0x11, sent
    on every 0x34 status request) reads it. The scan result is now cached for
    2 seconds; pet ownership changes (SetOwnerControllerRaw) and mount/dismount
    (Ridden flag transitions) invalidate the owner's cache so follower-cap
    checks never act on a stale count. Source-X maintains a stored counter
    (CCharUse.cpp GetCurFollowers) and never scans, so caching is strictly
    closer to reference behavior.

[2026-07-14] - Map-edge spawn crash fix (multicore fallback trigger)
----------------------------------------

CHANGED - [slow_tick] / [loop_stall] diagnostic warnings are now configurable and
          less chatty:
  - The fixed 25ms / 50ms thresholds warned on routine GC and network-flush blips
    (a 30ms tick is under a third of the 100ms tick budget). New sphere.ini keys
    SlowTickWarnMs (default 50) and LoopStallWarnMs (default 100) set the
    thresholds; 0 disables that warning entirely.

FIXED - IndexOutOfRangeException in SpawnComponent.FindSpawnPosition crashed the
        multicore tick and forced a 30s single-thread fallback:
  - A spawner within spawn-range of the map edge can roll a candidate position
    with a negative X/Y. In MapReader/UopMapReader.GetCell, -3 / 8 truncates to
    block 0 (passing the block bounds check) while -3 % 8 = -3 indexed the cell
    array with a negative offset, throwing. Both readers now reject off-map
    coordinates and return a default cell, and FindSpawnPosition rejects off-map
    candidates up front (Source-X IsValidPoint gate) and retries instead.

[2026-07-14] - Standing-Z desync fix and map-static door auto-close
----------------------------------------

FIXED - Character elevation desynced from the client, causing a spurious fall sound
        and a Z snap on double-click:
  - The per-step walk path seats a character on the averaged land-tile center (the
    same value the client and Source-X derive), but the seat/correction path
    (login, dismount, mount, teleport) used a second algorithm that returned the raw
    NW-corner tile Z instead. On sloped land these disagree, so the stored Z drifted
    off the surface the client renders. The drift stayed hidden until a self-update
    (0x20) re-imposed it — which a double-click sends when the character turns to
    face the used object — snapping the character up (near building floors) or down
    (on open slopes). The same drift made the next walk step re-derive the true
    center, tripping the client's fall "Ouch" with no real height change. The seat
    path now derives land Z from the averaged tile center, matching the walk path.

ADDED - Map-static doors now auto-close after 20 seconds, matching item doors:
  - Doors baked into the map statics (as opposed to real item-doors) were tracked
    only as an open-overlay set and never received the item timer that swings a door
    shut, so they stayed open indefinitely. They now carry the same 20s auto-close
    deadline (Source-X Use_Door timer); a post-tick pass swings the expired ones
    shut and broadcasts the close to nearby clients.


[2026-07-13] - Consumable single-use, pile-merge cap, and scissors data-loss fixes
----------------------------------------

FIXED - Consuming a stacked item destroyed the whole pile and could delete a fixture:
  - Booze and potions deleted the entire stack on a single use, and none of the
    eat/drink handlers checked movability first, so one sip wiped a pile and
    double-clicking a placed keg/pitcher/food decoration (a Move_Never fixture)
    destroyed it. They now consume exactly one unit (Source-X Use_Eat/Use_Drink,
    wConsume=1) and refuse an item the user cannot move instead of destroying it.

FIXED - Merging one stack onto another mis-handled the size cap and remainder:
  - The merge grew a pile up to the raw 65535 amount ceiling instead of the target's
    effective stack limit (per-item MAXAMOUNT override or ITEMSMAXAMOUNT, Source-X
    GetMaxAmount), so a pile could exceed its configured cap. It now caps at the
    target's MaxAmount and the overflow remainder is preserved beside the pile.
  - Dropping a stack onto a matching pile is a merge, not an insert into a container,
    so it is no longer gated by the destination's container item-count / weight /
    nesting caps. Previously the container weight cap wrongly rejected large stacks
    (e.g. dropping 5000 gold onto a ground pile when the container weight limit is
    lower). A remainder whose target's parent lookup fails now bounces to the target's
    top-level tile instead of being duplicated.

FIXED - Scissors could delete a bloody-bandage stack and cut a fixture:
  - Cutting a stack of bloody bandages deleted it; it now cleans the stack back to
    plain bandages (type convert, whole stack kept). Scissors also refuse a
    Move_Never cloth fixture instead of type-converting it.


[2026-07-13] - World consistency auditor and boot-time canary
----------------------------------------

ADDED - Post-load world invariant auditor with a boot-time consistency canary:
  - A single read-only pass over the loaded world flags the class of "looks fine in
    the property editor but broken on the client" inconsistencies that only surface
    with the real pack and save: a container whose client-facing index disagrees
    with its authoritative contents (an empty-looking bag), an item whose raw type
    was never materialised from its definition, a contained item with no parent, and
    a char spawner tracking far more live children than its cap. The server now runs
    it right after world load and logs a summary (clean, or the anomaly counts and
    first offenders), so these surface in the log instead of only in-game. The same
    checks back a test that audits the live pack+save when present and skips in CI.
  - Diagnostics-only sweeps added alongside: a double-click pass over every item
    type (asserting nothing crashes or is wrongly destroyed) and a headless-bot
    check that flags an item vanishing from view while the bot stands still (the
    client-side symptom of an object being desynced out of view).


[2026-07-13] - Item @Create on materialization, spawner child re-link, invisible-item double-click
----------------------------------------

FIXED - Loot/spawn items never ran their itemdef @Create trigger, so magic items came out inert:
  - A magic weapon's magic (damage bonus, magic attribute, hit points, magic hue)
    lives entirely in its itemdef @Create block. Source-X fires @Create on every
    item it materialises from a def (loot, spawn, NEWITEM, vendor restock, carve,
    .add); SphereNet never did, so a rolled magic def produced a named-but-inert
    item. ApplyInstanceMetadata now fires the @Create trigger once per fresh
    instance through a wired hook, guarded so re-stamping metadata cannot compound
    the effect, and the world load never routes through it (reloaded items keep
    their persisted state and do not re-fire).

FIXED - Char spawners forgot their live children on reload and respawned their whole quota:
  - The re-link that restores a spawner's current spawned-children count from its
    saved ADDOBJ entries only ran for classic SPAWNID-tagged spawners. A native
    MORE1/AMOUNT spawner has no SPAWNID, so on reload its child count reset to zero,
    it saw current-count below its cap, and it respawned a fresh quota every restart
    — the population near one spawn point grew without bound. The re-link now runs
    inside spawn-component init for every char spawner (matching the reference
    rebuilding current-spawned purely from ADDOBJ). It never alters the cap, which
    stays the AMOUNT field; any excess carried from an over-accumulated save drains
    off by attrition.

FIXED - A GM double-clicking an invisible spawner made it vanish from view instead of triggering:
  - Invisible items (spawn worldgems, triggers) are sent to a client with AllShow
    OR GM privilege, but the double-click and skill-target can-see gates only
    honoured AllShow. A GM who legitimately saw an invisible spawner double-clicked
    it, the server judged it unseeable, and "corrected" the client by removing it
    from view — the item looked deleted (it was untouched server-side) and the
    spawner never fired. Both gates now use the same audience as the view send
    (AllShow OR GM), so the double-click reaches the spawner and produces a spawn.


[2026-07-13] - Movement pacing, container index on load, item type materialization, double-click gaps
----------------------------------------

FIXED - Saved container contents were invisible to the client after a restart:
  - The container reverse index (read by the client's open-container batch) is
    maintained by the Item.ContainedIn setter, which only updates the index once the
    world resolver is wired — but the initial save load ran BEFORE that wiring. Every
    contained item therefore landed in its parent's Contents (visible to the property
    editor) yet never entered the index, so bags rendered empty on the client while
    still holding their items server-side. The world resolver is now wired before the
    load, and WorldLoader rebuilds the index once from the authoritative ContainedIn
    at the end of the load.

FIXED - A legacy item's TYPE and TDATA were not applied to the instance:
  - A save stores neither TYPE nor TDATA — an item's type is defined by its ITEMDEF.
    The lazy type getter resolved it, but the raw type field stayed at its default, so
    the <TYPE> script read, the property editor, and every code path that reads the raw
    type directly (spellbook/book/map/ship/multi/container checks, static blocking,
    content-by-type search) saw t_normal. TYPE/TDATA are now materialized from the
    resolved definition after load, matching a script-created item, and the TYPE
    property resolves through the definition. The save format is unchanged.

CHANGED - Movement pacing aligned to Source-X (fixes an on-foot in-combat stutter):
  - Removed the 20% war-mode foot-movement penalty. Neither the 2D client nor Source-X
    applies a combat-stance walk penalty; pacing the server slower than the client
    sends saturated the client's unconfirmed-step budget and produced a repeating
    stutter while running on foot in war mode.
  - The next allowed walk is now paced 30ms early (Source-X Event_CheckWalkBuffer iDelay
    offset) so player ping/jitter does not false-throttle a legitimately timed step. The
    walk-token bucket still caps the sustained rate.

ADDED - Double-click behavior for grain, grass and water troughs:
  - t_grain and t_grass are eaten and t_water_wash (water troughs/basins) are drunk
    from, restoring hunger, matching Source-X Use_Eat/Use_Drink. Previously these fell
    through to the generic "can't think of a way to use that" default. A placed fixture
    is never consumed; only a movable stack loses a unit.


[2026-07-13] - House/ship region events on load; server tick aligned to Source-X
----------------------------------------

FIXED - House and ship region scripts (@Enter/@Step/...) were not applied after load:
  - A dynamically realized house or ship region carried its EVENTS only as a
    REGION.EVENTS tag on the multi item; the tag round-tripped through save but was
    never turned into region events, so the region's @Enter/@Step triggers never
    fired. House and ship region creation now parses REGION.EVENTS into the region's
    event list (comma-separated, leading '+' trimmed, de-duplicated) using the same
    key form TriggerDispatcher resolves, so the scripts fire on entry/step again.

CHANGED - World-loop tick interval now matches Source-X exactly:
  - The tick (ServerTickMs) default is 100ms (10 ticks/s), matching Source-X
    MSECS_PER_TICK (TICKS_PER_SEC=10) instead of the previous 20 ticks/s. Regen and
    timer cadence now advance on the reference unit. Network I/O and the movement
    queue still drain every main-loop iteration, so responsiveness is unchanged; only
    world-simulation timing is aligned. Configurable via the ServerTickMs ini key
    (clamped to [20,250]).


[2026-07-13] - Spell-effect memory items; synthetic-index resolution sweep
----------------------------------------

ADDED - Active spell effects are mirrored as worn IT_SPELL memory items:
  - When a durational effect is applied, an item carrying the spell (graphic from the
    spell's RUNE_ITEM, spell id in MOREX, strength in MOREY, caster in LINK) is equipped
    on the hidden memory layer, matching Source-X CChar::Spell_Effect_Create. It is
    visible to GM .edit and deleted on every effect removal; the active-effect list
    stays authoritative for stat deltas and expiry. Hidden from the client (buff icon is
    the visible feedback). Persistence is unchanged: the item is not double-saved, and it
    is recreated from the effect record on load; the save-time stat unwind no longer
    deletes it.

FIXED - Named definitions resolving to Type/graphic 0 (synthetic-index truncation):
  - A non-numeric definition header ([ITEMDEF i_xxx], [CHARDEF c_xxx]) is keyed by a
    synthetic 24-bit index above 0xFFFF. Several paths truncated it to 16 bits and then
    looked the definition up by the truncated value, missing it and reading Type/TData/
    MORE as 0. Fixed across:
    - Legacy item load: a [WORLDITEM i_xxx] with a non-numeric ITEMDEF header now pins
      the defname as an ITEMDEF tag, so the instance recovers its Type/TData from the
      real definition instead of the 16-bit graphic. MORE1 display uses the resolved
      type so a spawn item shows its defname.
    - Item spawners: ItemSpawnComponent kept the target item-def index in a ushort;
      widened to the full 32-bit index so a spawner targeting a named itemdef resolves
      the correct def and no longer rejects synthetic indices.
    - SERV.NEWITEM: now applies the full item-def metadata (graphic, type, tags) instead
      of a raw 16-bit cast.
    - Spell RUNE_ITEM/SCROLL_ITEM/EFFECT_ID: a defname now resolves through the item
      def's display graphic (DisplayIdRef chain) instead of the truncated resource
      index, fixing corrupt rune/scroll/effect graphics for the necromancy/AOS spells.
  - Audited region/area/room types, EVENTS/TEVENTS, dialogs, functions, typedefs, menus
    and speech: all resolve by full-width key and were already correct.

[2026-07-13] - Container display, worn-item load, spawn body, deed placement
----------------------------------------

FIXED - Contained items missing from the client container view (present in .edit):
  - Opening a container (SendOpenContainer) and refreshing the backpack now send the
    whole content list as a single 0x3C batch (Source-X CClient::addContainerContents
    / PacketItemContents), instead of one 0x25 per child. ClassicUO's 0x25 handler
    silently drops any item whose container entity it does not yet know; a per-item
    stream could lose children while they still existed server-side. The batch is
    processed atomically (client clears and rewrites the list), matching the reference.
    The server-side _contents list and the container reverse index were already fully
    consistent on load (verified against a 92k-item save); the defect was in the wire
    format, not the data.

FIXED - Worn clothing/weapons dropping under NPCs or leaving NPCs naked:
  - Classic Sphere saves store worn gear as CONT=<char> with no LAYER when the itemdef
    also lacks one (TYPE=t_clothing); the layer now derives from tiledata (the graphic
    Quality byte, matching Source-X CItemBase m_layer), then from the item type
    (hair/beard/container) as a final fallback.
  - EquipLoadedItem no longer evicts an already-worn item: a tiledata-derived layer
    that collides with an occupied slot sends the newcomer to the backpack instead of
    bouncing the incumbent to the ground (the "weapon in hand and a weapon dropped
    underneath" case).
  - ITEMDEF DUPEITEM now inherits Type/Layer/TDATA from the master itemdef, so items
    defined only as a dupe (e.g. a fishing pole variant) resolve their real type and
    equip layer instead of Type=0.

FIXED - Spawned NPCs named Spawn_FFFF with body 0xFFFF:
  - Non-numeric CHARDEF headers (e.g. [CHARDEF c_alchemist]) receive a synthetic 24-bit
    definition index above 0xFFFF. The spawn component clamped it to 0xFFFF, producing a
    nameless body-0xFFFF creature; it now carries the full index and resolves the body
    through CharDefHelper.ResolveBodyId (DispIndex alias chain).

FIXED - Character Z jumped on the first double-click without moving:
  - Login/entry Z snapping to the surface no longer applies a fixed tolerance cap; the
    server places the character on the effective surface Z on entry, matching the height
    the client would otherwise correct to (Source-X addPlayerView behavior).

FIXED - Ship deed placement rejected valid multis ("cannot place house here"):
  - Deed-to-multi resolution now accepts multi id 0 (a valid multi.mul index) and
    resolves the multi from the deed's MORE/MORE1 defname tag via the multi registry,
    with the ship class detected from the deed graphic.

[2026-07-13] - Legacy save load order and staff item visibility
----------------------------------------

FIXED - Legacy NPCs loaded invisible from classic Sphere saves:
  - Char/item/spell definitions are now built (DefinitionLoader.LoadAll) before the
    world save is loaded, instead of after. Legacy Sphere saves store an NPC's body
    only in the [WORLDCHAR <defname>] section header, resolved through the definition
    tables; loading definitions afterwards left every such NPC at body 0 (invisible,
    though its name still resolved from the save) and left spawn items without their
    ITEMDEF type inheritance. Item graphics survived only because item id resolution
    has a defname-index fallback that character body resolution lacks.

CHANGED - Staff visibility of invisible items:
  - GM and higher now see ATTR_INVIS items (spawn worldgems, triggers, and similar)
    rendered dimmed even with AllShow off, so staff can audit spawners on sight.
    Normal players remain gated by AllShow.

ADDED - CLAUDE.md contributor/agent guide at the repository root.

[2026-07-12] - Sub-90 parity backlog, thirty-sixth batch (suit max-pool aggregation)
----------------------------------------

ADDED - Effective max hit/mana/stamina from equipped items (Wave 264):
  - A character's effective maximum hit/mana/stamina pool is now the stored base
    pool plus the sum of the matching item bonus across every equipped item,
    using the Source-X property names BONUSHITSMAX/BONUSMANAMAX/BONUSSTAMMAX
    (BONUSHITS in Source-X is a separate regeneration field, not a max bonus).
  - The max-pool getters report the effective total, so the status/paperdoll and
    health bars, the ceilings that healing and regeneration fill toward, and the
    ratio checks all stay consistent. The current pool can be healed up into the
    suit bonus while the piece is worn.
  - Unequipping a max-pool piece clamps the current pool down to the reduced
    maximum, matching the reference stat-mod behavior.
  - The stored base pool is what the world save serializes; the suit contribution
    is derived on read and never written back as base, so it cannot compound
    across save cycles. Old saves with a zeroed max still trigger the login
    backfill unchanged.

[2026-07-12] - Sub-90 parity backlog, thirty-fifth batch (suit stat aggregation)
----------------------------------------

ADDED - Effective STR/DEX/INT from equipped items (Wave 263):
  - A character's effective strength/dexterity/intelligence is now the base stat
    plus the sum of the matching item bonus (BONUSSTR/BONUSDEX/BONUSINT) across
    every equipped item, derived on read (no equip-time mutation, floored at 0),
    the same live-scan approach as the resist slice.
  - The consumers that reflect the suit now read the effective stat: the
    status/paperdoll STR/DEX/INT, melee strength damage (unarmed base and the
    per-era strength damage bonus), carry weight (max weight now scales with the
    strength suit, with the movement overload check and the status max-weight
    unified onto a single source), the equip strength-requirement gate, and the
    stat contribution to adjusted skill.
  - The stored max hit/mana/stamina pools stay derived from the base stat, so a
    stat suit never inflates them; the base stat field and the STR/DEX/INT script
    property keep reporting the base. Max-pool item bonuses (BONUSHITS and the
    like) are intentionally left for a separate pass.

[2026-07-12] - Sub-90 parity backlog, thirty-fourth batch (suit resist aggregation)
----------------------------------------

ADDED - Effective elemental resist from equipped items (Wave 262):
  - A character's effective fire/cold/poison/energy/physical resist is now the
    base resist plus the sum of the same resist across every equipped item,
    derived on read (no equip-time mutation, so the base stays pristine and
    nothing is written to the save). Combat damage resolution, poison ticks and
    the status/paperdoll display all use the effective suit total; the stored
    base field and the RESxxx script property keep reporting the base.
  - Stat (STR/DEX/INT) and max-hits/mana/stam item bonuses are intentionally not
    aggregated yet (they feed derived values and need a separate pass).

[2026-07-12] - Sub-90 parity backlog, thirty-third batch (Necromancy school)
----------------------------------------

ADDED - Necromancy spell school (Waves 254-261):
  - The Necromancy school is now functionally complete (16 of 17 spells; only
    Exorcism is left, which the Source-X reference itself leaves unimplemented).
  - Debuffs: Corpse Skin shifts elemental resists; Mind Rot raises the victim's
    spell mana cost by 10%.
  - A generic periodic damage-over-time engine drives Pain Spike (fixed direct
    damage over ten ticks) and Strangle (poison that accelerates and scales with
    the victim's fatigue). In-flight ticks are not persisted across a restart.
  - Damage reactions on the victim side: Blood Oath reflects a bonded enemy's
    blow back at it (and hurts the victim slightly more); Evil Omen makes the
    next harmful hit land harder (+25% melee, or one poison level), then is spent.
  - Area damage: Poison Strike hits the primary in full and splashes half within
    two tiles; Wither deals cold damage to everything within four tiles of the
    caster.
  - Forms: Lich Form and Vampiric Embrace shift resists; Vampiric Embrace also
    leeches life on any hit. One polymorph-layer form is active at a time.
  - Summons: Vengeful Spirit raises a revenant that hunts the target; Animate
    Dead raises an undead from a targeted corpse (a humanoid corpse yields a
    zombie, a creature corpse its own kind); Summon Familiar summons a default
    familiar. Summons are caster-owned, conjured and time-limited.

[2026-07-12] - Sub-90 parity backlog, thirty-second batch (deferred groups)
----------------------------------------

ADDED - Owned-vendor cash dispense and restock faucet fix (Wave 251):
  - The "cash" pet command now empties an owned vendor's purse into the owner's
    pack (Source-X NPC_VendorGetChkVerb PC_CASH). To prevent a free-gold faucet,
    the periodic restock no longer tops up an owned vendor's purse - only plain
    shopkeepers keep the infinite buy fund; owned vendors accumulate real earnings
    only. The bought/samples/stock verbs no longer raise a bogus target cursor.

ADDED - Effective-skill layer (Wave 252):
  - GetAdjustedSkill now includes the per-character SkillMod<n> equipment bonus on
    top of the base skill and stat contribution (Source-X Skill_GetAdjusted). The
    skill window (0x3A) reports the adjusted value with the raw base as the second
    field. A SkillMod<n> script property reads/writes the bonus (bare key or TAG.
    prefix), persisted as a tag. The raw base used by combat, crafting and skill
    gain is unchanged, so existing content sees no behavior change until the bonus
    is set.

ADDED - Necromancy foundation: Curse Weapon and Wraith Form (Wave 253):
  - On a damaging melee hit, a Curse Weapon state adds its level to the weapon's
    life-leech percent (weapon-gated), and a Wraith Form state drains the target's
    mana to the attacker scaled by Spirit Speak (Source-X CCharFight on-hit hooks).
  - The Curse Weapon and Wraith Form spells set these timed states with the normal
    effect expiry, persistence and revert; the Curse Weapon level survives a
    save/load round-trip.

[2026-07-12] - Sub-90 parity backlog, thirty-first batch
----------------------------------------

ADDED - Account aging admin command (Wave 250):
  - ACCOUNT UNUSED <days> [DELETE] lists (or, with DELETE, removes) accounts idle
    for at least N days, based on last login or creation date (Source-X
    Cmd_ListUnused). Staff accounts are never aged out, and an online account has
    a recent last-login so it never matches. Command-driven, not automatic.

ADDED - Per-item and global stack-size cap (Wave 250):
  - MAXAMOUNT is now readable/settable on items and enforced when merging piles:
    stackable items cap at the global ItemsMaxAmount (default 60000, Source-X
    m_iItemsMaxAmount) or a per-item override; non-stackable items report 0. The
    override is saved. Previously stacks were only bounded by 65535.

ADDED - Communication crystal tooltips (Wave 250):
  - A comm crystal now shows its state on the AOS tooltip: activated (linked to
    another crystal) or deactivated, plus the broadcast line.

[2026-07-12] - Sub-90 parity backlog, thirtieth batch
----------------------------------------

ADDED - Character and item script properties (Wave 249):
  - STAMINA (alias of STAM), STATPERCENT.<stat> (a pool as a percentage of its
    adjusted max; STR maps to hits, DEX to stamina, INT to mana), BLOODCOLOR
    (blood-splatter hue), and FOLLOWERSLOTS (how many follower slots a creature
    occupies, with a script override) are now resolvable on characters.
  - BASEWEIGHT exposes and overrides an item's per-unit weight; setting it drives
    the item's total weight and carry checks. BLOODCOLOR, FOLLOWERSLOTS and
    BASEWEIGHT overrides are saved.

[2026-07-12] - Sub-90 parity backlog, twenty-ninth batch
----------------------------------------

ADDED - EquipLastWeapon client macro (Wave 248):
  - The 0xD7 0x1E "re-wield last weapon" macro is now handled. The character
    tracks the last weapon it equipped (Source-X m_uidWeaponLast); the macro
    pulls that weapon out of the player's own pack and equips it, reusing the
    normal equip path (a dropped or traded weapon can't be summoned back).

ADDED - Targeted-skill fast path (Wave 248):
  - The 0xBF 0x2E packet uses a skill against an already-picked target without a
    target-cursor round-trip. It runs the same busy/can-use checks and
    PreStart/Start trigger chain as a normal skill use, then resolves directly
    against the supplied target.

ADDED - Per-character regen amount overrides (Wave 248):
  - Characters now honour per-char REGENVALHITS/REGENVALMANA/REGENVALSTAM/
    REGENVALFOOD (Source-X Stats_GetRegenVal): the number of points recovered
    per regeneration event, effective value max(1, stored). The human +2 hit
    racial still applies on top. Saved and restored across a restart. This
    completes the per-char regen surface begun in the previous batch.

FIXED - Modern-client spell casting from the 0xBF 0x1C packet (Wave 248):
  - The client's spell-select packet (0xBF 0x1C, used by clients 6.0.1.42+) was
    mis-wired to a viewport-size handler, so spells cast from the modern client
    UI were silently dropped. Screen size already travels on 0xBF 0x05 / 0xC8, so
    the duplicate handler was dead; 0x1C now casts the selected spell.

[2026-07-12] - Sub-90 parity backlog, twenty-eighth batch
----------------------------------------

ADDED - Per-character stat regeneration rate overrides (Wave 247):
  - Characters now honour per-char REGENHITS/REGENMANA/REGENSTAM/REGENFOOD (and
    the tenths-of-a-second REGEN*D variants) rate properties, resolved the way
    Source-X CChar::Stats_GetRegenRate does: 0 falls back to the global rate, a
    positive value is the per-char delay in seconds, and a negative value disables
    regeneration of that stat. The override is saved and restored across a restart.
  - Global food decay is now driven by a configurable rate (REGEN3) instead of a
    hardcoded 60 minutes.

FIXED - Old-style menu (0x7C) TESTIF entry gating (Wave 247):
  - A skill-menu entry's TESTIF= expression was never evaluated, so entries that
    should have been hidden were always shown. The expression is now evaluated
    against the character and a false result hides the entry.

FIXED - Old-style menu could desync the client on oversized lists (Wave 247):
  - The item-count byte was clamped to 255 but the full item list was still
    written, corrupting the client's read cursor when a menu exceeded 255 entries.
    Menus are now truncated to the Source-X MAX_MENU_ITEMS limit and never write
    more items than the count byte advertises.

[2026-07-12] - Sub-90 parity backlog, twenty-seventh batch
----------------------------------------

CHANGED - Stat regeneration now matches Source-X rates (Wave 246):
  - The REGEN0/1/2 rates are now read as SECONDS to recover one point (Source-X
    m_iRegenRate * MSECS_PER_SEC), not tenths of a second, so regeneration is
    roughly 5-10x slower than before (default hit regen 40s, mana 20s, stam 10s).
  - Fixed the stat order: REGEN1 is INT/mana and REGEN2 is DEX/stam (they were
    swapped). A Source-X / mortechUO sphere.ini setting Regen1/Regen2 is now
    applied to the correct pool.
  - Hunger decay slowed from 10 minutes to 60 minutes per point (Source-X food
    rate), and the human race now regenerates +2 extra hit points per event.
  - OPERATOR NOTE: this is a deliberate, player-visible balance change toward
    Source-X parity. A shard that relied on the old fast regeneration should set
    Regen0/1/2 explicitly (in seconds) to tune it.

FIXED - GM page queue survives a restart (Wave 245):
  - Pending GM/help pages were held only in memory and were lost on every
    restart, and GMPAGE sections in an imported classic save were silently
    dropped. The queue now lives on the world (like Source-X g_World.m_GMPages)
    and is written to and read from the server-data save.

FIXED - Guild member titles containing a comma no longer drop members (Wave 245):
  - Guild members are packed into one comma-separated tag with colon-separated
    fields. Colons in a title were escaped but commas were not, so a player-set
    title containing a comma split the record on load and dropped the trailing
    members. Commas in titles are now escaped as well.

ADDED - Communication crystal player linking (Wave 245):
  - Double-clicking a communication crystal now opens a target cursor; targeting
    another communication crystal links them so speech near one is relayed to the
    other (previously only a GM script verb could link them). The relay also now
    verifies the linked partner is itself a crystal.

[2026-07-12] - Sub-90 parity backlog, twenty-fifth batch
----------------------------------------

FIXED - Guild and quest buttons use the correct 0xD7 transport (Wave 244):
  - The guild and quest button presses are sent by the client on the 0xD7
    (EXTAOS) subcommand space, not 0xBF (Source-X PacketGuildButton /
    PacketQuestButton). They were handled on 0xBF, so a real client never fired
    the @UserGuildButton / @UserQuestButton triggers. They are now handled on
    0xD7 0x28 / 0x32, and the stale 0xBF 0x28 mapping is removed.

ADDED - Gargoyle flight toggle (Wave 244):
  - 0xBF 0x32 (previously mis-mapped to the quest button) now toggles gargoyle
    flight for a living gargoyle carrying the flight racial: it flips the
    hovering state and the gargoyle-flight buff, and the flight state now rides
    the mobile-flags 0x04 bit to the player and every nearby observer (the
    hovering bit was previously never sent).

[2026-07-12] - Sub-90 parity backlog, twenty-fourth batch
----------------------------------------

FIXED - Ship fast-mode speed scaling (Wave 243):
  - Continuous ("normal") sailing now advances twice as fast as one-tile
    steering, matching Source-X CCMultiMovable::SetNextMove which halves the
    tick interval in the fast speed mode. The ship speed mode was previously
    dead state (never set from steering input, never read in the timing path),
    so normal sailing ran at the slow rate. Tiles-per-tick was already correct.

FIXED - Pet with a loaded pack can no longer be stabled (Wave 243):
  - A pet carrying items in its backpack is refused at the stablemaster
    (matching Source-X), preventing the carried items from being lost with the
    stored snapshot. The owner must empty the pet's pack first.

ADDED - Pre-AOS wrestling special moves (Wave 243):
  - 0xBF 0x09 (disarm) and 0x0A (paralyzing blow) now fire the @UserSpecialMove
    trigger with the ability id, the same sink as the AOS special-move packet
    (0xD7 0x19).

[2026-07-12] - Sub-90 parity backlog, twenty-third batch
----------------------------------------

FIXED - Invulnerable characters no longer take damage (Wave 242):
  - STATF_INVUL was checked at death but not at damage application, so an
    invulnerable character still lost hit points from every source. Source-X
    CChar::OnTakeDamage bounces the blow (deals zero) while invulnerable, unless
    the strike is flagged DAMAGE_GOD. A shared CombatEngine.IsDamageImmune guard
    now protects every damage site: script DAMAGE, melee (including reflected
    damage), spells, container and step traps, and field damage.

FIXED - Corpse resurrection requires a top-level corpse (Wave 242):
  - The Healing/Veterinary corpse-target resurrection now refuses a corpse that
    sits inside a container, matching Source-X CItemCorpse::IsCorpseResurrectable
    (a corpse must be a ground object). The self-resurrection path already
    enforced this.

FIXED - Region vendor restock interval unit (Wave 242):
  - The region RestockVendors tag was parsed as minutes; Source-X
    NPC_Vendor_Restock measures it in tenths of a second for legacy script
    compatibility. A legacy RestockVendors=6000 now correctly means 600 seconds
    (10 minutes) instead of 6000 minutes.

[2026-07-12] - Sub-90 parity backlog, twenty-second batch
----------------------------------------

FIXED - Line-of-sight multi and custom-house wall occlusion (Wave 241):
  - Placed house and ship walls no longer let sight (and ranged attacks) pass
    through them. The static LOS path reads MUL map statics, not the virtual
    walls of runtime-placed multis, so those were invisible to the ray. LOS now
    synthesizes multi geometry the same way the walk check does (Source-X
    LOS_NB_MULTI): both multi.mul components and committed custom-house design
    tiles occlude the ray.

ADDED - Remaining line-of-sight passes (Wave 241):
  - BLOCKLOS_HEIGHT: items whose definition carries CAN_I_BLOCKLOS_HEIGHT block
    sight even without a wall flag, on the static, dynamic and multi paths
    (shared TerrainEngine.GraphicBlocksLos helper).
  - LOS_FISHING: CanSeeLOS now takes a LosFlags argument; with LosFlags.Fishing
    the ray must stay over water once it is two or more tiles from the caster.
    Engine-level for now (the fishing skill wiring is a separate opt-in).

NOTES:
  - The deep region-crossing flags (LOS_NO_OTHER_REGION, LOS_NC_MULTI, the
    region-local LOS_NB_LOCAL_* forms) remain deferred; they need a per-cell
    region model and have minimal gameplay impact.


[2026-07-12] - Sub-90 parity backlog, twenty-first batch
----------------------------------------

FIXED - Line-of-sight dynamic item occlusion (Wave 240):
  - The LOS ray only checked terrain and MUL statics, so an item placed in the
    world at runtime never blocked sight — a wall or crate dropped between two
    characters could be seen and shot straight through. The ray now consults
    live items along its path (Source-X CanSeeLOS_New LOS_NB_DYNAMIC): a
    wall/impassable/no-shoot item whose Z span covers the ray blocks it, exactly
    like a static.
  - Windows no longer block line of sight (Source-X LOS_NB_WINDOWS default for
    archery/magery), on both the static and dynamic paths.

NOTES:
  - Partial LOS parity: the niche flag variants (LOS_FISHING, multi-region
    occlusion / LOS_NC_MULTI, LOS_NO_OTHER_REGION, BLOCKLOS_HEIGHT, the
    region-local LOS_NB_LOCAL_* forms) remain deferred.


[2026-07-12] - Sub-90 parity backlog, twentieth batch
----------------------------------------

ADDED - Sector sleep model (Wave 239):
  - Sectors now decide whether they may sleep with the full Source-X
    CSector::_CanSleep model instead of a bare client-count check: sleeping is
    vetoed by SECF_NoSleep or a zero sleep-delay, blocked while a client is
    inside, granted immediately under SECF_InstaSleep, and otherwise granted
    once the sector has been clientless past the sleep-delay (default 10
    minutes). An 8-neighbour adjacency sweep keeps the ring around an active
    sector awake so a player never walks into a cold sector.
  - New SectorFlag enum (SECF_NoSleep / SECF_InstaSleep) with FLAGS / NOSLEEP /
    INSTASLEEP script access; <CANSLEEP> now reports the full model.
  - A SECF_NoSleep sector stays in the world tick set regardless of player
    proximity, and the player's sector stamps its last-client time each tick.

NOTES:
  - The engine's actual awake/asleep decision remains the spatial active-set
    (a window of sectors around each online player) rather than the temporal
    timeout; the CanSleep predicate and SECF_NoSleep behaviour are faithful,
    while InstaSleep is modelled at the predicate/script level.


[2026-07-12] - Sub-90 parity backlog, nineteenth batch
----------------------------------------

FIXED - Custom-house design valid-item enforcement (Wave 238):
  - The design editor accepted any non-zero tile graphic, so a crafted 0xD7
    stream could place arbitrary graphics (multi bodies, blocking statics,
    unrenderable ids) into a house. Build/Stairs/Roof now gate placement on
    Source-X CItemMultiCustom::IsValidItem: the graphic must be a real static
    item (0 < id < 0x4000), a check applied to everyone including GMs; GMs then
    bypass the piece whitelist while ordinary designers must match it.
  - HouseDesignValidItems holds an optional whitelist (Source-X
    ValidItemsContainer). Empty means range-only enforcement so custom housing
    works without the client's house-design data files; once pieces are
    registered, non-GM placement is restricted to them.

NOTES - Verified non-gaps (no change needed):
  - Design packet streaming already exists: PacketHouseDesignDetailed (0xD8
    multi-plane stream, ClassicUO-parse verified) is SendStructureTo and
    PacketHouseDesignVersion (0xBF 0x1D) is SendVersionTo. The stream splits
    planes by tile count in mode 0 rather than by Z, which the client parses
    correctly.


[2026-07-12] - Sub-90 parity backlog, eighteenth batch
----------------------------------------

ADDED - Guild-owned houses and ships (Wave 237):
  - A guild stone now tracks its owned house and ship multis (Source-X
    CItemStone multi storage). ADDHOUSE/ADDSHIP register a multi by uid,
    DELHOUSE/DELSHIP unregister one regardless of kind, and HOUSES/SHIPS
    report the counts. A uid is only ever a house or a ship, never both.
  - MAXHOUSES/MAXSHIPS expose the per-guild caps to scripts (advisory, as in
    Source-X where the limit is enforced at placement).
  - The lists persist through GUILD.HOUSES/SHIPS/MAXHOUSES/MAXSHIPS tags; a
    multi that no longer exists is dropped from the guild on reload.

NOTES - Verified non-gaps (no change needed):
  - The guild r_Verb subset, the guild-stone gump buttons (accept/title/war/
    peace/charter/dismiss/ally/rename/abbreviation/fealty) and the STC_*
    property surface (ABBREV/ALIGN/MASTER*/CHARTER/WEBPAGE/MEMBER.COUNT) were
    already implemented; only guild-owned structures were missing.


[2026-07-12] - Sub-90 parity backlog, seventeenth batch
----------------------------------------

COMPLETED - @EnvironChange environment snapshot (Wave 236):
  - Character now tracks perceived light, weather and season together; login
    establishes the first baseline without firing the trigger.
  - Sector/region light changes, weather callbacks and season broadcasts fire
    @EnvironChange once when any tracked field changes.
  - Temporary LightFlash packets do not mutate environment state or fire it.

TESTS:
  - Added 5 Wave 236 regression tests. Full SphereNet.Tests run:
    1580 passed, 0 failed, 0 skipped.

[2026-07-12] - Sub-90 parity backlog, sixteenth batch
----------------------------------------

ADDED - Sector LightFlash flow (Wave 235):
  - LightFlash sends full brightness followed by calculated sector light to
    living, online players in the sector who do not have Night Sight.
  - Lightning and Chain Lightning effects now flash the target sector like
    Source-X; IsMoonVisible was completed with Wave 234's visibility table.

TESTS:
  - Added 5 Wave 235 regression tests. Full SphereNet.Tests run:
    1575 passed, 0 failed, 0 skipped.

[2026-07-12] - Sub-90 parity backlog, fifteenth batch
----------------------------------------

ADDED - Per-sector moon-phase light model (Wave 234):
  - Added sector-column local time, the 105/840-minute Trammel/Felucca phases,
    and Source-X's moonrise/moonset visibility table.
  - Night light applies both moon brightness tables and cloud cover; dungeons
    use the configured fixed light level.
  - Login, resync, resurrection, season, region and tick light packets now send
    the character's sector light instead of the flat global ramp.

TESTS:
  - Added 18 Wave 234 regression tests. Full SphereNet.Tests run:
    1570 passed, 0 failed, 0 skipped.

[2026-07-12] - Sub-90 parity backlog, fourteenth batch
----------------------------------------

VERIFIED - Cartography craft linkage (Wave 233):
  - The stale backlog entry was a false alarm: cartography tools open the
    Cartography recipe gump, and SKILLMAKE recipes enter the normal craft
    stroke/consume/create flow with t_cartography and blank-map requirements.
  - Cartography craft strokes now use Source-X's 0x249 drawing sound from
    Skill_Cartography/Skill_GetSound.

TESTS:
  - Added 4 Wave 233 regression tests. Full SphereNet.Tests run:
    1552 passed, 0 failed, 0 skipped.

[2026-07-12] - Sub-90 parity backlog, thirteenth batch
----------------------------------------

CHANGED - Layered gathering resource yield (Wave 232):
  - REGIONRESOURCE AMOUNT and REAPAMOUNT now preserve every CValueCurveDef
    point; new node pools use AMOUNT.GetRandom().
  - Gather yield averages the skill-linear and random REAPAMOUNT values; zero
    falls back to AMOUNT random-linear / 2 with a minimum yield of one.
  - Human Workhorse adds one ore to Felucca mining nodes and two logs to
    Trammel tree nodes. Source-X has no literal REGIONRESOURCE BONUS key.

TESTS:
  - Added 12 Wave 232 regression tests. Full SphereNet.Tests run:
    1548 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, twelfth batch
----------------------------------------

CHANGED - Resource-specific gathering difficulty curves (Wave 231):
  - REGIONRESOURCE SKILL now preserves every CValueCurveDef point in script
    order instead of collapsing the definition to two endpoints.
  - Every gather attempt computes difficulty from a 0..999 curve sample like
    Source-X m_vcSkill.GetRandom()/10; the fixed (min+max)/2 was removed.
  - Removed the non-upstream hard SkillMin rejection; low-skill attempts now
    pass through the normal skill S-curve with the resource difficulty.

TESTS:
  - Added 10 Wave 231 regression tests. Full SphereNet.Tests run:
    1536 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, eleventh batch
----------------------------------------

ADDED - OSI craft material/color selection flow (Wave 230):
  - The crafting engine exposes primary-resource variants with hue, available
    amount, name and display id; insufficient stacks are excluded.
  - Multiple eligible materials open a paged selection gump with tile previews
    and hues; a single eligible material is locked directly.
  - The chosen hue is revalidated at completion, exclusively consumed and
    inherited by the result; failure loss no longer consumes other colors.

TESTS:
  - Added 5 Wave 230 regression tests. Full SphereNet.Tests run:
    1526 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, tenth batch
----------------------------------------

ADDED - Horrific Beast and Gargoyle Berserk damage parity (Wave 229):
  - With AOS update-B enabled, the timed Horrific Beast polymorph memory sets
    unarmed base damage to 5-15 and adds 25 percent Damage Increase.
  - Added the Source-X RACIALFLAGS mask to config and Character runtime.
  - Gargoyle Berserk adds 15 percent Damage Increase per 20 lost hit points,
    capped at 60 percent and applied after the base INCREASEDAM cap.

TESTS:
  - Added 10 Wave 229 regression tests. Full SphereNet.Tests run:
    1521 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, ninth batch
----------------------------------------

ADDED - Shield AR scaling and Protection AR parity (Wave 228):
  - With PARRYERA_ARSCALING enabled, shield AR scales with base Parrying, is
    capped at half the shield's base AR and applies at 100 percent coverage.
  - With scaling disabled, shield AR retains Source-X's legacy 7 percent hands
    coverage; non-shield two-handed items are excluded.
  - Protection and Arch Protection effect strength now applies as temporary AR;
    they refresh their shared spell layer and revert on expiry/save-reapply.

TESTS:
  - Added 8 Wave 228 regression tests. Full SphereNet.Tests run:
    1511 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, eighth batch
----------------------------------------

ADDED - Samurai Empire/Bushido parry parity (Wave 227):
  - Added the COMBATPARRYINGERA mask to config and Character runtime: legacy/SE
    formulas plus shield, one-hand, two-hand and AR-scaling capability bits.
  - The SE formula applies Parry-Bushido for shields, layer-specific Bushido
    multipliers for weapons, the legacy chance maximum, GM bonus and low-DEX erosion.
  - Successful SE weapon parries attempt Bushido gain; Parrying attempts now use
    the calculated parry chance as their difficulty.

TESTS:
  - Added 5 Wave 227 regression tests. Full SphereNet.Tests run:
    1503 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, seventh batch
----------------------------------------

ADDED - SE/ML combat speed parity (Wave 226):
  - Ported COMBATSPEEDERA 3 Samurai Empire using SPEEDSCALEFACTOR, DEX,
    effective weapon speed and the 0.25-second OSI tick conversion.
  - Ported COMBATSPEEDERA 4 Mondain's Legacy with its own weapon-speed format
    and Source-X integer SSI evaluation order; both eras retain the 5-tick floor.
  - INCREASESWINGSPEED aggregates the character and every equipped item/ITEMDEF
    and is recognized by Character/Item/CHARDEF/ITEMDEF script surfaces.
  - Item.Speed prefers instance or ITEMDEF OVERRIDE.SPEED over regular SPEED.

TESTS:
  - Added 4 Wave 226 regression tests. Full SphereNet.Tests run:
    1498 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, sixth batch
----------------------------------------

ADDED - FC/FCR cast timing parity (Wave 224):
  - FASTERCASTING now aggregates the character and every equipped item/ITEMDEF,
    subtracting two tenths from CAST_TIME per point like Source-X with a
    one-tenth floor. @SpellCast ARGN3 is seeded from the same adjusted time.
  - Removed the skill-based 400-1500 ms post-cast cooldown absent in Source-X.
  - Added FASTERCASTRECOVERY to Character/Item/CHARDEF/ITEMDEF script surfaces;
    no cooldown behavior was added because it is unimplemented upstream.

FIXED - MAGICFLAGS and spell reflection (Wave 225):
  - Aligned MAGICFLAGS bits 0x1-0x8000 with Source-X and moved SphereNet-only
    flags into a non-conflicting high range.
  - MAGICF_IGNOREAR bypasses the elemental resistance/AR stage for magic damage;
    the Magic Resistance skill remains a separate calculation.
  - Magic Reflect now handles single reflection, double-reflect bounce-back and
    NOREFLECTOWN / DELREFLECTOWN consume/absorb chains with correct SRC attribution.

TESTS:
  - Added 8 Wave 224-225 regression tests. Full SphereNet.Tests run:
    1494 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, fifth batch
----------------------------------------

ADDED - Remaining Source-X verb parity (Waves 219-220):
  - CClient ADD/ADDCHAR/ADDITEM, CLOSEPAPERDOLL, CTAGLIST, GMPAGE, INFORMATION,
    RESEND, SAVE, SELF, SKILLSELECT and VERSION now reuse the existing target,
    command, skill and network paths. ADD stack amounts survive target response.
  - ObjBase DAMAGE shares the combat pipeline for target @GetHit rewrite/veto,
    elemental split/resist, attacker/action interruption, packets/death, and
    item @Damage/durability handling.
  - The verified verb-route debt that started at 26 in Wave 217 is now zero.

ADDED - @UserMailBag fire site (Wave 221):
  - Added the legacy 0xBB mail packet through PacketHandler -> NetState ->
    GameClient. The trigger fires on the recipient with the sender as SRC;
    RETURN 1 suppresses the notification. Protocol and trigger docs were synced.

FIXED - Script/save write-side parity (Wave 222):
  - TextSaveWriter now emits one file-level [EOF] like Source-X, making
    multi-section exports compatible with the classic Sphere save format.
  - Values are truncated at CR/LF like WriteKeyStr; section and property keys
    are validated as single-line tokens.

ADDED - Typed, sorted VarMap (Wave 223):
  - CVarDefMap parity now keeps native string/integer types in a
    case-insensitive sorted store. TAGAT/save enumeration is deterministic and
    CopyFrom preserves numeric types.

TESTS:
  - Added 24 Wave 219-223 regression tests. Full SphereNet.Tests run:
    1486 passed, 0 failed, 0 skipped.


[2026-07-12] - Sub-90 parity backlog, fourth batch
----------------------------------------

ADDED - Source-X verb implementation guardrail:
  - The pinned CObjBase/CChar/CItem/CClient/CServer inventories are now checked
    against SphereNet's real dispatch sources, including inheritance, client
    bridges, interpreter meta-verbs and the SERV resolver.
  - Verified gaps live in an explicit debt list; a newly missing route or an
    implemented verb left stale in that list now fails the test.

ADDED - Object script verb parity (Wave 218):
  - ObjBase gains CLICK, MESSAGEUA, MOVENEAR, MOVETO, MSG and USEITEM. Movement
    uses world/sector placement; click and use replay the normal client paths.
  - Character gains DUPE, SKILL and SYSMESSAGELOCEX, including core state/skill
    cloning, the live SkillHandlers bridge and the 0xCC localized-affix packet.
  - Item gains DROP, UNEQUIP and USEDOOR, including container/equipment detach,
    bounce to the invoking character's pack and privileged locked-door toggling.

TESTS:
  - Added 10 Wave 218 behavior tests and strengthened the three inventory
    guardrails. Full SphereNet.Tests run: 1462 passed, 0 failed.

NOTES:
  - The verified verb-route debt fell from 26 to 13. DAMAGE remains deferred
    until it can share the combat/death/resist and item-durability pipeline;
    twelve client-only management/target verbs remain queued.


[2026-07-12] - Sub-90 parity backlog, third batch
----------------------------------------

ADDED - Account IP-block scheduling:
  - Blocked IPs may now carry an expiry: BLOCKIP <ip>,<seconds> lifts itself
    once the clock passes the requested window; a bare BLOCKIP stays permanent
    (Source-X BLOCKIP decay). Expiry is enforced lazily on lookup.
  - IPBlockList tracks absolute expiry per entry and exposes an injectable
    clock so the decay is deterministically testable.

ADDED - Party member map waypoints:
  - Live party stats now also refresh each member's radar waypoint pin every
    tick on waypoint-capable clients (Source-X CPartyDef::UpdateWaypointAll),
    so members track each other on the world map out of visual range.
  - Leaving, being kicked, or disbanding removes the departed member's pin
    from the remaining members' maps and clears the party pins from the
    departed member's own map.

FIXED - Champion candle requirement per level:
  - The red-candle-per-level vector was built by appending, reversing the tail
    relative to Source-X (which inserts each value at the front). For a
    non-default LEVELMAX this mis-assigned the candle requirement per level
    (e.g. LEVELMAX 7 gave [3,2,2,3,3,3] instead of [3,3,3,3,2,2]).

ADDED - Champion script surface:
  - ADDREDCANDLE / ADDWHITECANDLE re-link (or create) a candle by uid;
    NPCGROUP<n>[.<i>] reads a group member's chardef and NPCGROUP<n>=list
    overrides a level's spawn group; MULTICREATE is accepted as a no-op
    (Source-X stub) — the remaining ICHMPL/ICHMPV accessors.

NOTES - Verified non-gaps (no change needed):
  - Party SendAddList/SendRemoveList/AddStatsUpdate already exist server-side;
    only the waypoint refresh was missing.
  - Champion InitializeLists is computed in Source-X too (no MONSTERS/CANDLES
    list is read from the def) — only the candle build order was wrong.


[2026-07-12] - Sub-90 parity backlog, second batch
----------------------------------------

ADDED - DB script object:
  - DB.AQUERY / DB.AEXECUTE run a query or non-query without blocking: the
    statement is queued onto the connection's worker thread when one is
    running, or executed inline otherwise (Source-X DBO AQUERY/AEXECUTE).
  - DB.NUMCOLS and db.row.numcols expose the last result set's column count.

NOTES - Verified non-gaps (no change needed):
  - Cartography and Camping skill handlers already exist and are functional.
  - [SPHERECRYPT]/[KRDIALOGLIST] sections are already mapped and intentionally
    skipped; client crypto keys are computed (CalcCrypt), not table-loaded.
  - Item BONUSSKILL/BONUSCRAFTING keys need an effective-skill layer over the
    base-skill hot path, not just a key add — deferred as a separate change.


[2026-07-12] - Sub-90 parity backlog, first batch
----------------------------------------

ADDED - Global list runtime mutation:
  - LIST.<name>[.op]=value now mutates global lists at runtime (Source-X
    CListDefMap::r_LoadVal): clear/add/set/append/sort(asc|i|desc|idesc),
    index set, .N.remove and .N.insert. Read side gains .findelement.
  - GameWorld.MutateGlobalList / ResolveGlobalListRead own the grammar;
    the interpreter routes LIST.* writes over the _SET_LIST. bridge.

ADDED - Character script surface:
  - CChar verbs UNEQUIP (bounce an item to the pack), WHERE (report location),
    SUMMONTO (teleport to a uid or the caller) and CONTROL (caller takes
    ownership) — Source-X CChar_functions entries that were missing.
  - CChar properties CANMOVE <dir> (destination-tile passability) and
    NOTOGETFLAG <uid> (full notoriety flag as seen by a viewer).

CHANGED - Combat AOS on-hit:
  - REFLECTPHYSICALDAM now bounces a percentage of melee damage back at the
    attacker (capped 250%), separate from the Reactive Armor spell.
  - AOS hit-chance (era 2) applies the Hit/Defense Chance Increase multipliers
    (100 + min(HCI|DCI, 45)); the defender term was previously dropped.

CHANGED - Poison level derivation:
  - Weapon-envenom and creature-bite poison use the Source-X SetPoison skill
    bands (650/850/1000) with distance falloff (-dist/2 at range >= 4) and an
    Evil-Omen hook, instead of a flat skill/200 curve. The Poison spell gains
    the same distance falloff. Tick counts are unchanged.

ADDED - Health-bar colour packets:
  - PacketHealthBarStatus (0x17) and PacketHealthBarStatusNew (0x16) tint a
    character's health bar green while poisoned and yellow while frozen; sent
    to SA+/KR observers when poison state flips.

ADDED - NPC skill training for pay:
  - VendorTrainingEngine ports NPC_GetTrainMax / NPC_OnTrainCheck /
    NPC_TrainSkill: a teacher raises a student up to a fraction of its own
    skill, bounded by the absolute cap and the student's per-skill and total
    caps, draining DOWN-locked skills for headroom.
  - Saying "train <skill>" to a vendor quotes the price and records the offer;
    handing over gold trains the skill in proportion to the amount paid.


[2026-07-12] - Champion spawn engine (Source-X CCChampion port)
----------------------------------------

ADDED - Champion spawns:
  - [CHAMPION x] resource sections now load as retained defs (NAME, LEVELMAX,
    SPAWNSMAX, CHAMPIONID, NPCGROUP[n] per level) instead of being skipped.
  - t_spawn_champion altars attach a ChampionComponent that drives the full
    Source-X state machine on top of the existing spawn component: kills earn
    white candles, four whites convert to a red, reds advance the level, the
    boss (CHAMPIONID) comes out at LEVELMAX and its death fires @Complete.
  - Candle items (skull candle 0x1853, reds hued 33) are placed on the
    Source-X ring geometry around the altar and re-link on load.
  - The 10-minute decay tick removes a red candle and refunds kill progress;
    with no reds left the spawn stops (players lose the run).
  - Script surface: START/STOP/INIT/ADDSPAWN/DELREDCANDLE/DELWHITECANDLE/
    ADDOBJ/DELOBJ verbs plus the LEVEL/ACTIVE/REDCANDLES/KILLSNEXT*/
    CHAMPIONID/CHAMPIONSPAWN... read/write keys on the altar item.
  - Triggers: @Start/@Stop/@Level (ARGN1=level, ARGN2=monsters,
    ARGN3=candles), @Complete, @AddRed/WhiteCandle (RETURN 1 deletes the
    candle) and @DelRed/WhiteCandle (ARGN1=reason, RETURN 1 keeps it) — the
    item-trigger backlog in the coverage guardrail is now empty.
  - Kill credit flows through the SPAWNITEM back-link at death time; state
    persists via CHAMPION_* item tags so the save format is unchanged.

[2026-07-12] - Script FILE object parity with Source-X (CSFileObj)
----------------------------------------

CHANGED - The script FILE object now matches Source-X CSFileObj semantics:
  - Default MODE is append+read+write (was read-only), so a bare FILE.OPEN
    followed by FILE.WRITELINE appends instead of silently no-opping.
  - OPEN is refused while a file is already open; MODE changes are refused
    while open (both logged through the diagnostic sink); CREATE and APPEND
    clear each other like upstream.
  - READCHAR returns the numeric byte value; READBYTE <n> reads n bytes as
    text; both guard reads past EOF. READLINE restores the pre-read POSITION
    (non-destructive) and trims trailing non-graphic characters.
  - LENGTH returns -1 when closed; FILEPATH returns the full resolved path.
  - FILEEXIST/FILELINES/DELETEFILE resolve against the sandbox root instead
    of the open file's directory (previously failed with no file open);
    DELETEFILE refuses the currently-open file.

ADDED - Server-global FILE surface:
  - Scripts running WITHOUT a client console (server hooks, NPC triggers)
    now reach the full FILE surface through the shared server handle
    (Source-X g_Serv._hFile model) — previously only FILE.FILEEXIST worked
    there. The interpreter routes FILE.* verbs to the host as a no-console
    fallback; client consoles keep their existing path.
  - Sandbox unchanged: everything stays under <scpdir>/files with traversal
    and reparse-point rejection (deliberate hardening over Source-X).
  - CSFileObjContainer (multi-file pool) is intentionally not ported: it is
    not wired to the FILE. reference in Source-X either.

[2026-07-11] - Source-X verb long tail completed (48 deferred verbs, backlog now empty)
----------------------------------------

ADDED - Object verbs (CObjBase surface):
  - GOAWAKE/GOSLEEP flip the object's tick-sleep state; EFFECTLOCATION sends
    the location-targeted effect family (bolt -1 normalization included);
    SAYUA speaks Unicode with color/mode/font/lang from any object.
  - PROPLIST/BASEPROPLIST/BASETAGLIST diagnostic dumps with the "log"
    argument routing to the server log; per-type property surfaces on
    Item/Character; base-def dumps read the creating ITEMDEF/CHARDEF and its
    TAGs.
  - REMOVECLILOC/REPLACECLILOC mutate the custom tooltip line list and
    CLILOCLIST prints the object's tooltip lines (cached or live).

ADDED - Character verbs (CChar surface):
  - AFK toggle (arg forces on), HEAR (players get a private sysmessage, NPCs
    process the line as heard speech via a host hook), UNDERWEAR (0x8000 nude
    hue bit), NEWBIESKILL (runs a [NEWBIE name] section on the char),
    TARGETCLOSE (drops the client target cursor).
  - GOCHARID/GOTYPE world scans (body id / chardef, IT type or t_* typedef)
    and GOCLI/GOSOCK via host client-list resolvers.

ADDED - Client verbs (CClient surface, all 22 remaining):
  - UI packets: CHARLIST (char-select list resend), CLOSEPROFILE/CLOSESTATUS
    (0xBF sub 0x16), CODEXOFWISDOM (0xBF sub 0x17), SCROLL (0xA6 from a
    [SCROLL] section), SHOWSKILLS (full 0x3A), SKILLUPDATE (single-skill
    0x3A), MAPWAYPOINT (0xE5/0xE6), DYE (0x95 hue picker; the response path
    already existed).
  - GM tools: NUKE/NUKECHAR (optional verb line applied instead of deleting),
    NUDGE (dx dy dz args), TILE (two-corner flood with cycling item ids),
    EXTRACT/UNEXTRACT (multi text files, version 6 format, sandboxed under
    the script database root), LINK (two-click item cross-link with the key
    lock-uid special case), REPAIR, EVERBTARG (prompt then verb on the last
    target), GOTARG/LAST (last-picked-target memory), BADSPAWN (teleport to a
    spawner whose resource no longer resolves), EDIT (property editor).

ADDED - Server verbs:
  - SERV.BLOCKIP/UNBLOCKIP reachable from scripts behind a PLEVEL_Admin gate
    (srcUid protocol); SERV.CALCCRYPT ports the CCryptoKeyCalc login-key
    derivation and returns/prints the SphereCrypt.ini-style line.

CHANGED:
  - The Source-X verb inventory guardrail's deferred backlog is now EMPTY —
    every pinned CObjBase/CChar/CItem/CClient/CServer verb has an
    implementation site.

[2026-07-11] - Script reading parity, second audit pass (adversarial re-verification)
----------------------------------------

FIXED - Lexer:
  - Source-X Str_Parse rule restored: a WHITESPACE key/value separator consumes
    one following separator char, so "name = value" no longer leaks the '=' into
    the value. This recovered 81 numeric [DEFNAME] constants in the reference
    pack (brain_*, LOGL_*, RES_* tables) that silently parsed as text.
  - Source-X GetArgStr quote semantics ported (ScriptKey.StripQuotePair): the
    script runtime strips a surrounding quote pair from verb/property values
    (TAG.X="a b", SYSMESSAGE="msg"), and DEFNAME/GLOBALS/LIST loaders store
    de-quoted values — quoted numerics parse as numbers again.
  - Dialog gump position line ("5,25") and classic MENU titles read the raw
    line; the comma separator had collapsed them (dialogs anchored at 0,0).

FIXED - Expressions:
  - Leading-zero hex now sign-extends like Source-X GetSingle: <=8 significant
    nibbles reinterpret as int32 ("0ffffffff" = -1), 9-16 as int64, more
    overflows to -1; decimal overflow warns and yields -1.
  - QVAL: '?'/':' split skips <...> spans so nested <QVAL ...> works, and the
    numeric form accepts 3-4 args with 0 defaults (was requiring all 5).
  - STR*/MULDIV/BETWEEN/SETBIT-family intrinsics split their argument lists on
    the RAW text (bracket-aware) before resolution — a resolved value
    containing commas ("1,2" coordinates) no longer corrupts the split; also
    fixed STRCMPI's off-by-one prefix slice.
  - Conditional splitter: an unmatched '<'+letter (no-space less-than,
    "IF a<b || c") no longer swallows the rest of the condition.

FIXED - Resource loading:
  - Duplicate script files dedup by file TITLE (basename) like Source-X
    FindResourceFile; directory expansion sorts case-sensitively by filename
    like AddResourceDir.
  - A second [LIST name] block appends to the existing list (AddList reuse).
  - [TYPEDEFS] values are also exposed through the DEF text table so
    <DEF.t_xxx> and numeric resolvers see bulk-declared type names.
  - [NOTOTITLES] threshold lines accept '=' as a separator; the [GLOBALS]
    legacy "VAR." prefix strip is case-sensitive like Source-X.

Known deliberate deviations from Source-X (documented): 0x-prefix hex is
accepted (Source-X yields 0), RAND(hi,lo) normalizes the range, the <...>
separator suppression in key parsing is kept (Source-X computes but ignores
angle depth), and the anywhere-match "VAR." strstr bug is not replicated.

[2026-07-11] - Script reading parity with Source-X (sections, lexer, expressions)
----------------------------------------

ADDED - Section types that were previously ignored or unknown now load:
  - [GLOBALS]/[WORLDVARS] and [LIST name]/[WORLDLISTS] script sections seed the
    world's global VARs and LISTs at boot (world save still overrides).
  - [OBSCENE] word list loads and backs a real ISOBSCENE intrinsic
    (Source-X-style *word* wildcard matching); it previously always returned 0.
  - [FAME]/[KARMA] title lists and [NOTOTITLES] (karma/fame thresholds plus the
    (k+1)*(f+1) title matrix with male,female split) load with a
    GetNotoTitle(karma, fame, female) accessor.
  - [RUNES] letter table loads; spell RUNES letter abbreviations ("IPY") now
    decode into real power words through it instead of a hardcoded fallback.
  - [BOOK name page] sections are retained with per-page access; [MENU],
    [SCROLL] and [TIP] keys are retained as well.
  - [TYPEDEFS] bulk declarations register type-name defnames.
  - [RESOURCES] sections inside ANY script file queue extra files for loading
    (Source-X AddResourceFile order: appended after the manifest), with
    spheretables refusal, .scp extension append, root guard and dedup.
  - spheretables.scp: ALL [RESOURCES] sections are processed (not just the
    first), and unlisted top-level *.scp files load afterwards like Source-X
    AddResourceDir.
  - CHAMPION/SPHERECRYPT/KRDIALOGLIST/ACCOUNT/GMPAGE/SECTOR/WORLDCHAR/WORLDITEM/
    WC/WI/WORLDSCRIPT/WS/TIMERF/STAT are recognized (no more Unknown warnings).

CHANGED - Expression evaluation now matches Sphere semantics:
  - Expressions fold right-to-left with NO operator precedence (Source-X
    GetVal/GetValMath): "2*3+1" is 2*(3+1)=8. Old packs rely on this.
  - IF/ELIF/WHILE conditions split at top-level || and && with equal precedence
    and left-to-right short-circuiting (Source-X GetConditionalSubexpressions),
    including !(...) negation and parenthesized recursion.
  - Divide/modulo by zero keeps the left operand (was 0) and logs.
  - SIN/COS/TAN use radians with truncation (was degrees x1000);
    ARCSIN/ARCCOS/ARCTAN and STRASCII added; STRINDEXOF is case-sensitive;
    ID() masks to the low 20 index bits; RAND supports 64-bit ranges; RANDBELL
    is the deterministic Source-X log curve; resolver depth cap raised to 128.
  - Number literals follow Source-X dot rules: leading "0" not followed by '.'
    is hex; dots inside decimals are grouping separators ("100.000"=100000).
  - Weighted {v w ...} ranges reject odd token counts (error, 0) and log
    non-positive weights.
  - <...> references in a numeric context dispatch through the full variable
    resolver (previously <EVAL ...>/<TAG.X> evaluated to 0 there).

CHANGED - Script lexer parity (Source-X Str_Parse):
  - Key/value separator set is now "=, \t" (comma included) with double-quote
    awareness and {} [] () <> depth tracking.
  - ScriptKey.RawLine preserves the verbatim line; raw-line consumers (dialog
    TEXT, [TELEPORTERS], [STARTS], [MOONGATES], [NAMES], title lists) read it,
    fixing multi-word/comma entries that the Key/Arg split used to mangle.
  - FLOAT.-namespace compound assignment rewrites through <FLOATVAL ...>;
    the operator set now includes '!'; section headers accept '=' and ','
    separators ("[ITEMDEF=i_x]").

[2026-07-11] - Source-X script pack, load ordering and runtime compatibility transition
----------------------------------------

CHANGED - Source-X resource loading and text encoding:
  - SCPFILES now points directly to oldSphere/Scripts-X-main. The
    spheretables.scp RESOURCES order loads exactly 756 active files; commented
    and not_complete sources are no longer pulled in by a recursive scan.
  - The script reader supports AUTO, UTF8 and LEGACY modes. AUTO validates strict
    UTF-8 first and falls back to a configurable legacy code page only for an
    invalid UTF-8 stream. SCRIPTENCODING and SCRIPTLEGACYCODEPAGE are available
    in sphere.ini.
  - UTF-8 BOM, Windows-1252 and Turkish Windows-1254 paths are covered by
    regressions, and changing the encoding configuration clears stale file cache.

FIXED - Definitions, PLEVEL and Source-X hook lifecycle:
  - Static ITEMDEF/TEMPLATE loading stops at the first ON=@ trigger body, so
    runtime NAME, TYPE and DISPID statements cannot mutate the shared base def.
  - Every PLEVEL block for the same level is merged across files, preserving the
    complete Source-X command privilege table.
  - Server start/exit/save/save_ok/save_fail/save_finished, resync, TimerCall,
    IP block, connect-request and connection-acquired hooks are wired to their
    real lifecycle events. Save vetoes and non-boolean hook results are retained.
  - Character creation now follows f_onchar_create_init/f_onchar_create and
    passes client feature flags, profession and race through trigger arguments;
    cancellation leaves no partial character record.

ADDED - SERV and global script surface:
  - SERV.SPELL, extended ITEMDEF fields, combat/magic/option flags, SCPFILES,
    decay, housing, NPC training and guild/town stone lookups are available.
  - SERV.NEWITEM and SERV.NEWNPC work as client-independent global factories,
    retaining NEW references and core definition state.
  - DB, LDB and MDB commands/row lookups work from server hooks; MDB.IMPORTDB and
    FILE.FILEEXIST support the Source-X startup functions.
  - Unknown ITEMDEF fields remain diagnostic-visible while also being retained
    as extended definition properties for runtime and SERV queries.

FIXED - DIALOG, SPEECH and EVENTS script compatibility:
  - DIALOG layout lines run through the shared script interpreter, allowing gump
    commands produced by functions, CALL/RETURN and conditional control flow.
    BUTTONTILEART, XMFHTMLTOK, ITEMPROPERTY and PICINPIC are supported.
  - SPEECH matching supports embedded *, ?, character ranges and legacy <ANY>
    patterns. NPC, item and MULTIDEF TSPEECH/SPEECH chains retain the speaking
    client's script context and run from their actual event points.
  - HouseDesignBegin, AddMulti and NPCSeeWantItem fire from native gameplay
    flows; the legacy @ItemUnEquipTest spelling is compatible with @ItemUnequip.
  - Scripted ITEMDEF identity and metadata application is centralized across
    item creation paths. TEVENTS is distinct from dynamic EVENTS and legacy
    saves cannot execute the same handler twice.

FIXED - Deep parser, definition and script-verb compatibility:
  - NPC skills written as 15.0/100.0 are converted to the runtime tenths scale;
    the script parser supports *=, /= and %= assignments.
  - ITEMDEF CAN, CANUSE, LAYER and TDATA1-4 plus CHARDEF SOUND, DEFNAME2,
    FOODTYPE, COLOR, ERA and AOS resistance fields load symbolic DEFNAME values
    and forward references without losing their identity. Named ITEMDEF runtime
    metadata no longer collapses to the 16-bit client graphic id during lookups.
  - ADDCLILOC, ADDCONTEXTENTRY, PROMPTCONSOLE/U, SPELLEFFECT, NOTOUPDATE/CLEAR,
    CHANGEFACE, SERV.DELETEFILE and SERV.CONSOLE are wired to operational client
    and server paths. Enhanced Client face selection uses Source-X gump context
    0x2B0.
  - Unterminated SCP section headers emit file-and-line diagnostics and tolerate
    the missing closing bracket, preserving the affected resource body.

VERIFIED - The real Scripts-X-main manifest parses all 756 declared files and
  builds the definition registries. The complete solution, including Source-X
  smoke/regression coverage, passes 1355/1355 with zero build warnings/errors.

================================================================================

[2026-07-10] - General gameplay integrity: inventory, trade, social systems and world lifecycle
----------------------------------------

FIXED - Items, containers, weight and object lifecycle:
  - Container cycles, duplicate parent/equipment references, deep stack weight
    and full pack/container operations are safe. Bank, vendor and special service
    layers no longer count toward carried weight.
  - Deleted items and characters are removed from the object table, sectors,
    equipment and complete child-container trees together. Stale references can
    no longer delete recycled UIDs, and consumed objects leave no ghost records.
  - Death, corpses, resurrection, NPC looting, crafting, script DUPE/CONT/BOUNCE,
    stack splitting and drag/drop paths restore or ground items instead of losing
    them when a destination reaches capacity.

FIXED - Vendors, banking and secure trade:
  - Duplicate vendor rows cannot exceed stock; malformed BUY templates no longer
    accept everything; transient clones are removed; full-pack purchases safely
    fall back to the ground. Stock, payment and payout state stays world-consistent.
  - Bank withdrawals and check creation now guarantee delivery before deducting
    the balance, so capacity or carry failures cannot consume banked gold.
  - Secure trade enforces target eligibility, REFUSETRADES, TradeCreate/DropOn
    triggers, incoming weight and slot limits, returning every item safely after
    cancellation or failed completion.

FIXED - Party, guild, pet and mount behavior:
  - Party invitations enforce leader authority, a 120-second timeout, membership
    and capacity checks. Forged private party messages and destructive force-add
    attempts are rejected.
  - Guild candidate/member/master privileges are distinct. Master election,
    server-side gump authorization, fealty, guild/alliance chat, mirrored war and
    alliance relations and legacy save repair now follow Source-X behavior.
  - Wild and foreign mounts cannot be ridden, while Mount/Dismount triggers veto
    before mutation. Stable and figurine restore failures preserve both pets and
    items when follower limits or destinations are invalid.

FIXED - Magic, travel, scripts and client interactions:
  - Recall, teleport, summon, field and NEWDUPE operations validate map/sector
    bounds; failed placement leaves no orphan object or ownership state.
  - Loaded active spell effects validate duration and flags. Consumed scrolls,
    potions, bandages, bulletin posts and trade containers fully leave the world.
  - Script/GM item placement, legacy figurine ownership, container capacity and
    complete stack state/attribute cloning no longer lose or corrupt data.

VERIFIED - With 23 new general gameplay integrity regressions, the complete
  solution passes 1328/1328 in Debug and Release. The git diff check is clean.

================================================================================

[2026-07-10] - Deep house and ship code audit and Source-X parity
----------------------------------------

FIXED - House privileges, storage and lifecycle:
  - Owner/co-owner/friend/access/vendor/ban roles now follow mutually exclusive,
    centralized privilege rules. Public access, GM operations, immediate ban
    ejection and ownership/limit revalidation during transfer match Source-X.
  - Lockdown and secure operations validate the house footprint, item state/type,
    duplicates and privileges. Release, transfer, demolition and redeeding clear
    stale links, attributes, keys and guard-memory records.
  - Demolition/redeeding creates a real deed while preserving hue, magic, UUID
    and base multi data. Protected and loose player items move into capacity-safe
    moving crates with bank and ground fallbacks.

FIXED - House components and custom housing:
  - Multi components are deduplicated and regain Move_Never and house links after
    loading. Sign resolution, door opening, gump authorization and stale owner,
    guild, access, vendor and component-tag cleanup are complete.
  - Only the current owner or a GM can customize, with one active architect per
    house. Multi bounds, tile/coordinate ranges, maximum design size and fixture
    replacement are validated without losing contained items.

FIXED - Ship placement, movement and drydock:
  - Placement uses the correct facing variant and checks map bounds, water and
    statics, houses, characters, blocking items and other ship hulls. Directional
    components are immovable and linked; pack/bank keys and per-character/account
    MaxShipsPlayer and MaxShipsAccount limits are implemented.
  - Turning and movement safely handle old/new footprints, coordinate overflow,
    rider directions, carried objects, map-changing SHIPGATE travel and trigger-
    driven deletion. Anchor, stop, speed and direction inputs are validated.
  - Pilot/wheel control requires the assigned pilot to be aboard, unmounted,
    non-hovering, unbanned and on an unanchored ship. Plank boarding, pilot ban
    ejection and recursive ship-key lookup in nested containers are complete.
  - Drydock/redeeding clears owner keys and the pilot while retaining plan type,
    facing, hue, magic and UUID data. Hold and movable deck contents are preserved
    through moving crates with bank and ground fallbacks.

FIXED - Persistence and reload safety:
  - House/ship regions, owners, components, direction, storage and pilot data are
    bounded and relinked on load. Repeated deserialization into the same engine no
    longer leaves duplicate regions or stale tags.

VERIFIED - With 11 new house/ship integrity regressions, the complete solution
  passes 1305/1305 in Debug and Release. The git diff check is clean.

================================================================================

[2026-07-10] - Source-X gameplay parity: interaction, client capabilities, buffs and death
----------------------------------------

FIXED - Double-click, NPC loot and spawner behavior:
  - Double-click validates facet, visibility, LOS, range and facing before
    triggers. GMs can inspect non-human NPC packs, while regular players can
    open pack-horse and pack-llama inventories with their loot contents.
  - Character/item spawners immediately spawn when empty and clear children
    when populated. Using a spawned item detaches it without deletion, and both
    spawner types now run PreSpawn/Spawn/AddObj/DelObj lifecycle hooks.

FIXED - Client version, feature flags and tooltip protocol:
  - FEATURE* settings are no longer ORed as raw bits. ResDisp and client version
    now translate them into Source-X 0xB9/0xA9 flags. AutoResDisp, effective
    character-slot limits and letter-patch versions such as 5.0.2b are handled.
  - Tooltip modes 0/1/2, 0xDC revisions, 0xD6 full property lists, caching and
    invalidation are implemented. Nearby clients receive refreshed tooltips for
    changed objects.

FIXED - Buff icons and death/ghost lifecycle:
  - Source-X BUFF_ICONS and the 0xDF cliloc layout are implemented. Spell,
    poison, hidden and meditation icons are added and removed correctly across
    refresh, dispel/reveal, expiry, death and reconnect.
  - The invalid self 0xAF packet and persistent custom death particle are gone.
    PACKETDEATHANIMATION=0 now suppresses 0x2C, while ghost season/light and
    corpse waypoint add/remove behavior match Source-X.
  - The free-resurrection path through death-menu responses is closed; the
    response now resynchronizes ghost play instead of resurrecting the player.

VERIFIED - With Source-X gameplay regression coverage, the complete solution
  passes 1294/1294 in Debug and Release with zero warnings/errors. The git diff
  check is clean.

================================================================================

[2026-07-10] - Deep skill-system code audit: use flows, crafting and state management
----------------------------------------

FIXED - Skill core and client flows:
  - Skill IDs, locks, caps, gain and difficulty calculations are safe against
    overflow and invalid/reserved IDs. Disabled, scripted, passive, magic and
    combat skill distinctions are enforced by central use validation.
  - Targeted, informational and multi-stage skills share one lifecycle; stale
    target/menu state, duplicate triggers, SkillWait and cancellation leaks are
    removed. Scripted skills can safely override built-in behavior.

FIXED - Active skills and world interactions:
  - Map, LOS, range, ownership and tool validation is complete for Hiding/
    Stealth, Detect Hidden, Meditation, Spirit Speak, Tracking, Healing,
    Veterinary, Taming, bard skills, theft, snooping, lockpicking, poisoning,
    herding and gathering.
  - Focus passive regeneration, the Imbuing crafting route, Throwing combat route
    and Camping safe-logout flow are implemented. Damage, movement, death,
    deletion and disconnect consistently cancel pending actions.

FIXED - Crafting, resources and item safety:
  - Recipe trigger ordering is singular; resource hue selection, consumption,
    quality, crafter, durability and ITEMDEF property transfer are corrected.
    Concurrent crafting/gathering consumption is locked and malformed amounts
    are bounded.
  - Full-container, stack merging, ground fallback and rollback paths no longer
    lose data or items. Items with different quality or properties cannot be
    merged incorrectly.

CHANGED - Session and persistence behavior:
  - ClientLinger is configuration-driven; unsafe logout characters remain visible
    and attackable in the world for the configured period. Reconnection cancels
    lingering, while sector movement and expiry are broadcast correctly.
  - The legacy 120.0 skill storage ceiling is removed and cap/skill values are
    safe across the ushort storage range.

VERIFIED - With new skill regressions, the complete solution passes 1279/1279 in
  Debug and Release with zero warnings/errors. The git diff check is clean.

================================================================================

[2026-07-10] - Deep combat code audit: swings, ranged combat, damage and kill credit
----------------------------------------

FIXED - Swing state and combat safety:
  - Pending swings are cleared on target changes, war-mode exit, death, deletion
    and resurrection. Map, life state, immunity, region, LOS and range are
    revalidated at hit time; weapon/range/Recoil_NoRange are committed at windup.
  - NPC and player HitCheck paths now use the same ordering and callback data.
    CHARDEF innate range, ITEMDEF melee range, NoDirChange, Reveal and owner crime
    reporting now match Source-X behavior.

FIXED - Ranged weapons and ammunition economy:
  - Ammunition is found recursively in nested containers with depth/cycle guards;
    ammo lost during windup cannot produce a free hit. StayInRange misses execute
    HitMiss, projectile, Arrow and player-ammunition handling completely.
  - Full-container stuck-ammo merging/orphan cleanup and NPC HitMiss weapon/Arrow
    UID callback data are corrected. A full parry is no longer treated as a miss
    and cannot incorrectly trigger ammunition economy.

FIXED - Damage, armor, speed and durability:
  - Miss, parry and proc-resolved outcomes are distinct; trigger cancellation
    always stops damage regardless of callback return value. A proc kill prevents
    the original strike from being applied again.
  - Armor-layer coverage, resist/slayer/AOS bonuses, the elemental Source-X
    formula, weapon damage overrides and ITEMDEF SKILL support are corrected;
    overflow and double-roll durability errors are removed.
  - Source-X swing-speed eras 0-2 are implemented. Invented mounted/two-handed
    penalties are removed, SPEEDSCALEFACTOR is bound to config/script and SSI
    aggregation is overflow-safe.

FIXED - Crime, combat memory and death credit:
  - CombatStart runs before mutations with the target as SRC and repeated attacks
    are idempotent. Aggressor/HarmedBy directions, memory expiry/death cleanup and
    attacker damage overflow/negative-kill handling are corrected.
  - When no final killer is supplied, the strongest logged attacker is used. One
    attacker's cancelled Kill callback no longer blocks other credits and the
    attacker list is cleared after distribution.

VERIFIED - With 21 new combat regressions, the complete solution passes 1264/1264
  in Debug and Release with zero warnings/errors. The git diff check is clean.

================================================================================

[2026-07-10] - Deep NpcAI code audit: targeting, combat, movement and roles
----------------------------------------

FIXED - Perception and target selection:
  - VISUALRANGE is now the actual NPC sight radius. Target starvation caused
    by the first 40 sector entries and failure to consider a visible enemy
    when the strongest three candidates are behind walls are removed.
  - The Source-X ally-family table now covers modern human/elf/gargoyle and
    creature body families. NPCAI/OVERRIDE.NPCAI and target UID tags accept
    Sphere-style leading-zero hexadecimal values.

FIXED - Combat and spell state machines:
  - A pending swing owns the NPC action until its hit phase completes; movement,
    spells, breath and throws cannot run during windup. Non-instant guards and
    fleeing casters follow the same one-action rule.
  - Bows, crossbows and custom AMMOTYPE weapons consume the correct ammunition
    while preserving ArrowHandled. Reactive-armor kills credit the defender;
    reversed/malformed THROWDAM and THROWRANGE values cannot crash the tick.
  - A cast rejected by SpellEngine is no longer reported as started, so wand
    charges and combo state are preserved. Fallback spells use spell flags to
    choose ally/enemy/self targets and honor reflection.

FIXED - Movement, home state and NPC roles:
  - Path caches are tied to their destination and stale routes/backoff are
    invalidated when the target or facet changes. Cross-map fake movement/pet
    teleports, HOME map loss, obstacle sector-index corruption and blocked
    wander behavior are fixed. HOMEDIST now defaults to Source-X unlimited and
    persists consistently.
  - Pet attack/guard ownership and friend checks; guard/healer/animal LOS;
    vendor restock overflow; food-item unlinking; repeated crime reports; and
    humanoid two-handed weapon/light handling are corrected.
  - Runtime path/LOS/notify cache cleanup now runs in single-core ticks too.

VERIFIED - NpcAI tests pass 105/105; the complete solution passes 1243/1243 in
  Debug and Release with zero warnings/errors. New regressions cover target
  starvation, swing atomicity, ammunition, cast rejection, path destinations,
  cross-map behavior, ally groups and role flows.

================================================================================

[2026-07-10] - Infrastructure hardening: IPC, panel lifecycle, paths and CI
----------------------------------------

FIXED - Managed Host/Server IPC now reports truthful results:
  - Mutation failures and remote exceptions are no longer converted into
    success. Responses carry structured error codes and the panel maps
    unavailable, timeout and rejected operations to 503/504/502.
  - Requests are serialized and every live-world/account operation is
    marshalled onto the game main loop instead of entering state concurrently
    from pipe threads.
  - Disconnect, restart and shutdown now cancel and join their background
    work; stale connection retries cannot interfere with a newer server.

SECURITY - File access is contained under trusted roots:
  - Script, database, world-operation and panel paths share one relative-path
    resolver that rejects absolute paths, traversal, sibling-prefix escapes
    and reparse/symlink descendants.
  - Panel script writes are atomic, backups are collision-safe, uploads have
    size limits and ZIP extraction uses the same containment checks.

FIXED - Panel sessions and API behavior:
  - Logout sends the original bearer token before clearing local state, so the
    server can actually revoke it. Expired tokens are removed and purged.
  - Setup configuration is public only during first-run setup; account/debug/
    lifecycle endpoints preserve backend false results and return meaningful
    HTTP failures instead of silent success.

CHANGED - Reproducible quality gates:
  - Axios/Vite and transitive dependencies were updated; npm audit reports no
    known vulnerabilities. ESLint and Vitest were added for the Vue panel.
  - CI now runs npm ci, lint, panel tests, dependency audit, production build,
    Debug/Release .NET builds, all tests and coverage collection.
  - Empty duplicate test projects were removed. Infrastructure regressions
    cover path containment, IPC serialization/results, tokens and panel HTTP
    statuses. A flaky adjacent-tactician test now restores its own precondition.

VERIFIED - Debug and Release build with zero warnings/errors; 1217 .NET tests
  and 2 panel tests pass. Current coverage is 45.93% lines / 36.98% branches.

================================================================================

[2026-07-04] - NpcAI wave A3: strict Source-X parity for the design-decision items
----------------------------------------

CHANGED - Target motivation matches NPC_GetAttackMotivation exactly:
  - The ServUO-style wounded/caster/healer/retaliation bonuses are
    removed; motivation is hostility + same-target hysteresis - distance
    + morale (+ the attacker-list THREAT term).

CHANGED - Hireling economy is the Source-X prepaid model:
  - The day wage comes from the chardef (HIREDAYWAGE; a legacy HIRE_WAGE
    tag still overrides). Gold handed to the NPC hires it and funds ITS
    OWN wage balance (NPC_OnHirePay); upkeep drains a period-fraction of
    that balance and never touches the master's bank. On depletion the
    hireling speaks the wage-cost/time-up messages and deserts.

CHANGED - Town healers are resurrect-only (NPC_LookAtCharHealer):
  - The cure-poison and heal-wounded passes are removed; they topped up
    any non-criminal living creature in range, including grey aggressors
    mid-fight.

================================================================================
[2026-07-04] - NpcAI parity waves A1/A2/A4: caster fixes, movement defaults, P3 tail
----------------------------------------

FIXED - Caster NPCs (the two P1s from the partial audit):
  - No fallback when the chosen spell was uncastable: the magery tick now
    iterates candidates with the CANCAST affordability check (per-spell
    mana + skill requirement) like the Source-X NPC_FightCast loop.
  - Melee-range behavior was inverted: unconditional kiting replaced with
    the Source-X Tactics>20.0 stand-and-fight (~50%).

FIXED - Movement/behavior P2s:
  - HOMEDIST default was 10 (Source-X: unlimited) — homed spawns were
    confined to a tiny box; home leash measured Manhattan, now Chebyshev.
  - Blocked NPCs froze facing the wall: every mover now sidesteps
    (~70%, turn 1-4) like NPC_WalkToPoint, with a 0.5s blocked retry.
  - Wander is heading-biased (curving walk) instead of uniform jitter.
  - Creature innate RANGE from the chardef now feeds the fight range
    (Fight_CalcRange); morale applies signed (strong NPCs gain, not
    only lose); guard outside guarded land teleports to its post or
    despawns; NPCNoCastTill honored; SAFE regions block casting; the
    low-mana cast abort (not in Source-X) removed.

FIXED - P3 tail: magery gate order (wand after the chance/backoff
  gates, 3/4-sight cap, exact dice), breath dist>=1 + unclamped
  BREATH.DAM tag, wand HAND1-only, ally-heal only for allies fighting
  the same enemy, target stickiness +8, berserk distance preference,
  hidden-NPC ambush, mid-fight target rescan only under multi-attacker
  pressure, obstacle TopZ window, guard yells per new target, vendor
  restock honors RESTOCKVENDORS/NORESTOCK region tags, pet GO resumes
  the previous order, @NPCActFollow RETURN 1 stops following.

================================================================================
[2026-07-04] - 56T spawner import: every def-typed spawner came up dead
----------------------------------------

FIXED - Imported legacy spawners never spawned (56T field report):
  - Item defname resolution returned 0 for any itemdef without an ID
    override (DispIndex 0 short-circuited the fallback), so every
    i_worldgem_bit imported with BaseId 0.
  - InitializeSpawnComponent gated on the raw TYPE field; Sphere saves
    omit TYPE when it matches the base itemdef, so def-typed spawners
    silently no-opped. It now gates on the virtualized item type.
  - MORE1=c_xxx (a raw chardef defname in legacy saves) fell through the
    ItemDef-gated resolver; the parked MORE1_DEFNAME tag is now consumed
    for both NPC and item spawners.
  - The world bootstrap no longer clobbers an explicitly saved TYPE with
    the itemdef type, and it initializes item spawners (t_spawn_item)
    which were skipped entirely.
  - Real-data test: 2660 NPC + 53 item spawners from C:\56T\save now
    import alive (1 dead worldgem is the shard's own MORE1=00 quirk).

CHANGED - NpcAI.cs decomposed into concern-based partial classes
  (Core/Combat/Magic/Movement/Perception/TownRoles/Pet), no behavior
  change.

================================================================================
[2026-07-03] - Field-report wave 2: ridden-mount targeting, door auto-close
----------------------------------------

FIXED - Monsters could target a ridden mount:
  - IsAttackable never skipped STATF_RIDDEN, so a hostile locked onto
    the mount under a rider and started an invisible one-sided fight
    (the ridden NPC skips its AI tick, so it could never respond).
    Attacks now go to the RIDER.

ADDED - Door auto-close (Source-X _SetTimeoutS(20) parity):
  - An opened door swings shut on its own after 20 seconds: the art
    flips back, the hinge shift is undone, the close sound plays and
    the visual is pushed to observers. A manual close cancels the timer.

VERIFIED - The pathfinder shares MapDataManager.IsPassable, so the
  mountain-walk fix covers A* routing as well as direct steps.

================================================================================
[2026-07-03] - Field-report wave: door hinge shift, pet self-defense, mountain walk, 56T saves
----------------------------------------

FIXED - Doors appeared to open backwards:
  - Opening only flipped the art +1; the Source-X Use_Door HINGE SHIFT
    (per 0-15 door-set slot) was never applied, so the open art rendered
    anchored at the wrong tile. The reference door-base table is ported
    verbatim (declaration order, overlap quirks); player, NPC and
    map-static door paths all move the leaf now. The map-static path
    also restored the wrong closing art (tileId-1 below the closed art).

FIXED - A following/staying pet never defended itself:
  - Follow/Come/Stay never read FightTarget, so an attacked pet just
    stood there. Pets now fight back (Source-X Memory_FightStart);
    follow/come/stay/stop orders call the pet off.

FIXED - NPCs walked over mountains:
  - IsPassable only checked land for water; the land IMPASSABLE flag
    (mountain rock) was never consulted, and each slope step passes the
    +-12 z gate on its own. Impassable land now blocks unless a
    surface/bridge static covers the spot.

ADDED - Sphere 0.56T save compatibility:
  - A real-data test loads C:\56T\save (76k items, 4.2k chars). Item
    TIMER=seconds now restores the decay/spawn schedule, book AUTHOR
    maps to its tag, DISCORDANCE resolves to the Enticement skill and
    char TIMER (transient AI tick) is accepted. Unknown 56T-custom keys
    stay preserved as SAVE.* tags.

ADDED - In-game world-ops routing:
  - EXPORT/IMPORT/RESTORE/SAVESTATICS/LOAD (already implemented on the
    script surface) are now reachable from .serv.* in game.

================================================================================
[2026-07-03] - NEWNPC verb, Source-X ghost manifest model
----------------------------------------

ADDED - The NEWNPC script verb (Source-X SSV_NEWNPC):
  - Spawns a chardef NPC through the full GM-add pipeline (@Create, brain
    finalisation, @CreateLoot, appearance broadcast) at the invoker's
    feet, reachable as SERV.LASTNEWCHAR. RECALL was closed by research:
    it is not a char verb in Source-X either (only the spell).

CHANGED - Ghost manifest now keys off STATF_INSUBSTANTIAL (Source-X):
  - A fresh ghost is born insubstantial; the war toggle while dead flips
    the flag (war = manifest); resurrection clears it; the view filter
    reads the flag (war-mode fallback for pre-existing saves). Scripts
    now see and can set the same bit as the reference.

================================================================================
[2026-07-03] - Movement wave M2: synthetic map fixture, CI-stable z-rule tests
----------------------------------------

ADDED - MapDataManager synthetic map fixture:
  - AddSyntheticMap / AddSyntheticStatic / SetSyntheticItemTile build a
    tiny in-memory map (flat land, hand-placed statics, tiledata) that
    every terrain/static/tiledata query consults BEFORE the file readers.
    The real MUL/UOP path is untouched.

ADDED - CI-stable z-rule assertions (SyntheticMapZRuleTests):
  - Bridge half-height stair climbing step by step, tall-ledge rejection
    with in-limit descent allowed, and impassable-wall blocking — all run
    against the real WalkCheck logic without any MUL data. The real-MUL
    stair diagnostics previously self-skipped in CI, so no z rule was
    asserted anywhere.

================================================================================
[2026-07-03] - In-game SERV bridge, ship REDEED verb-gate fix, guard tests
----------------------------------------

ADDED - In-game .serv.* commands (Admin/Owner, plevel 6+):
  - The command handler routes SERV.-prefixed lines to the same processor
    the telnet console uses and echoes every output line to the invoker.
    Previously SERV.* only worked from telnet and scripts.

FIXED - The ship REDEED verb route was unreachable:
  - Ship multis carry ItemType.Ship but the verb gate only admitted
    Multi/MultiCustom; caught by the new verb-path integration test.

ADDED - Tests: ship REDEED through the item verb (deed delivery), and the
  lockdown/secure outside-house-region refusal.

================================================================================
[2026-07-03] - Housing/ship wave P3: ship map boundary, lockdown footprint, ship dry-dock
----------------------------------------

FIXED - A ship could sail off the map edge; the leading hull edge is now
  validated against the map bounds (negative coordinates included).

FIXED - Lockdown/secure accepted items anywhere in the world; the target
  must now lie inside the house region.

ADDED - The REDEED verb dry-docks ship multis through the ship engine:
  cargo moves to the crate, the hull tears down fully and the deed is
  delivered to the owner. Ships previously had no decommission path.

================================================================================
[2026-07-03] - Script-surface wave S1: unknown-key diagnostics, VISIBLE/SFX verbs
----------------------------------------

ADDED - Unknown def-key diagnostics:
  - ITEMDEF/CHARDEF lines with unrecognized keys were dropped silently;
    they are now counted per def-type+key and a top-10 summary is logged
    after every definition load, so a real pack reports what it loses.

ADDED - Legacy verbs:
  - VISIBLE (drops invisibility and hiding, the CHV_VISIBLE reference)
    and SFX (the legacy alias of SOUND). NEWNPC/RECALL stay deferred
    (spawn machinery / rune-travel flow are separate work).

================================================================================
[2026-07-03] - Housing wave H1b: house placement footprint validation
----------------------------------------

FIXED - House placement validation gaps (Source-X Multi_Create loop):
  - NOBUILDING is now checked across the footprint plus a +5-tile margin
    (previously only the single anchor point).
  - A docked ship hull under the footprint blocks placement (new ship
    engine hook); blocking ground items (ITEMDEF I_Block) reject cells.
  - The living-character check was silently a no-op (a range-0 scan
    returns nothing); it now works, with the GM pass-through the
    reference has.

================================================================================
[2026-07-03] - Housing/ship wave H1: redeed cargo crate, REDEED teardown, placement overlap
----------------------------------------

FIXED - Ship redeed deleted the hold cargo:
  - RemoveShip now moves component contents to a moving crate, delivered
    to the owner's bank box or dropped (with decay) at the ship's spot —
    the Source-X TransferAllItemsToMovingCrate behavior ships lacked.

FIXED - The REDEED verb leaked the house from the registry:
  - It called house.Redeed directly, leaving a stale registry entry (a
    burned house-count slot) and a ghost dynamic region. The verb now
    routes through the housing engine's full teardown.

FIXED - Ship placement did not check hull overlap:
  - CanPlaceShip only verified water; two ships (or a ship over a hull)
    could be spawned overlapping. The footprint now rejects any cell
    already covered by another ship.

================================================================================
[2026-07-03] - Movement wave M1: map edge-case crash fixes
----------------------------------------

FIXED - Negative coordinates slipped past the sector bounds guard:
  - Truncating integer division resolved X/Y in [-63,-1] to sector 0, so
    off-map placements/moves/teleports were accepted and the map readers
    then crashed on a negative cell index. GetSector now rejects the
    negative side explicitly.

FIXED - Pathfinder explored off-map neighbor tiles:
  - An NPC pathfinding from the x=0 / y=0 map edge passed -1 into the
    map readers (crash reachable by ordinary NPC AI); the far edge read
    garbage cells. Neighbors are now bounds-checked against the map size.

FIXED - The GM/AllMove/CanPassWalls walk bypass could step off the map;
  the bypass branch now validates the target sector too.

================================================================================
[2026-07-03] - Death/corpse wave D3: kill record, corpse hair, antimagic rez, summon burst
----------------------------------------

ADDED - Kill record (Source-X LOGM_KILLS + party echo):
  - A player death logs "'<victim>' was killed by 'A', 'B'." (or
    "accident.") and echoes the line to the victim's party members.

ADDED - Corpses render hair and beard:
  - Hair/beard are copied onto the corpse as marked render dupes
    (Source-X CreateDupeItem); the originals stay on the ghost and the
    dupes are discarded on corpse rejoin instead of restoring as loot.

ADDED - Antimagic resurrection gate:
  - A NoMagic region refuses the resurrection (Source-X
    REGION_ANTIMAGIC_ALL); a GM ghost bypasses.

ADDED - Corpseless summon death bursts the spell-fizzle effect (0x3735)
  instead of silently vanishing (Source-X MakeCorpse).

NOTE - Deferred with reasons (wiki/death-corpse.txt): the corpse
  max-weight anti-exploit (no container weight-capacity model exists to
  hook), blessed-items-to-pack (deliberate feel choice), the dead-season/
  waypoint client niceties, and the ghost manifest model (needs a
  visibility-model decision).

================================================================================

[2026-07-03] - Death/corpse waves D1-D2: death penalties, carve crime, death-screen config
----------------------------------------

ADDED - Player death penalties (Source-X CChar::Death):
  - A dying player loses a tenth of their experience (min 1) and a tenth
    of their fame (suppressed by DEATHFLAGS & DEATH_NOFAMECHANGE); the
    deaths counter increments. The fame loss runs through @FameChange.

FIXED - Death-time state cleanup:
  - Kill() now clears Hidden/Invisible/Freeze/Stone/Sleeping/Hovering
    (Source-X Reveal + StatFlag_Clear) — a character that died while
    hidden previously stayed an unseen ghost.
  - The victim's FIGHT/HARMEDBY memories are cleared at death.

ADDED - Corpse carving rules:
  - Carving an innocent player's corpse now flags the carver criminal,
    like looting it (Source-X CheckCorpseCrime with fLooting=false).
  - Corpses of bonded pets, summoned creatures and sleeping bodies are
    born uncarvable (Source-X m_carved = 1).

ADDED - sphere.ini HitpointPercentOnRez (Source-X default 10):
  - The resurrection HP percent is configurable; it was a hardcoded 50%.
    Shards that want the old behavior set HitpointPercentOnRez=50.

ADDED - sphere.ini PacketDeathAnimation (Source-X default 1):
  - When 0, the 0x2C death-screen packet is not sent, so ClassicUO never
    enters its 1.5-second death-screen render freeze; the ghost redraw
    sequence carries the full transition (the reference addPlayerUpdate
    fallback).

FIXED - Open secure trades survive a participant's death:
  - The dying side's trade is cancelled before the corpse forms: items
    return to the packs (and so reach the corpse loot), both windows
    close and @TradeClose fires. A clientless victim takes a packetless
    fallback path.

NOTE - @CreateLoot stays a spawn-time trigger in SphereNet (deliberate
  divergence): death-time ITEM= materialisation is covered by the
  existing deferred-loot drop, and firing the script again at death
  would double the loot.

================================================================================

[2026-07-03] - NPC AI wave N1: dormant NPC_AI flags, trigger contracts, CAN-flag hex fix
----------------------------------------

ADDED - Dormant NPC_AI_* flag behaviors:
  - NPC_AI_FOOD now routes through the feeding pass (previously only
    INTFOOD did); the basic/intelligent distinction is collapsed into the
    one implementation.
  - NPC_AI_COMBAT gates the ally heal/cure targeting: without it the
    Source-X friend list is self-only, so group support no longer runs
    unconditionally. Self-heal stays ungated.
  - NPC_AI_EXTRA (NPC_ExtraAI): humanoid-brain NPCs fire @NPCAction
    (RETURN 1 skips the pass), equip a weapon and shield from the pack in
    war mode, and carry a hand light by night / stow it by day. Lighting
    the torch (IT_LIGHT_OUT -> LIT) stays deferred.
  - NPC_AI_MOVEOBSTACLES: a hands-capable, smart-enough NPC shifts a
    movable blocking item onto its own tile before giving up the path.
  - NPC_AI_VEND_TIME stays deferred: it is define-only in Source-X too.

CHANGED - NPC trigger contracts:
  - @NPCActCast now carries ARGN2 = wand-use (the documented Source-X
    contract; upstream's own value is always 0 due to an early reset).
  - @NPCLookAtItem matches Source-X: ARGN1 = distance, ARGN2 = want-score
    (writable, read back), ARGO = the item; RETURN 1 takes the item over,
    RETURN 0 ignores it; move-never/locked/secure items never fire it.
  - NPC wands require ATTR_MAGIC (Source-X NPC_FightMagery); a wand with
    no CHARGES tag stays infinite for legacy-import compatibility.

FIXED - CHARDEF CAN flags parsed leading-zero values as decimal:
  - Sphere's numeric convention treats a leading zero as hex, so a legacy
    chardef's CAN=0300 (mt_equip|mt_usehands) resolved to decimal 300 and
    mis-flagged every imported chardef. The item-side parsers already
    followed the convention; the chardef CAN parser now does too.

================================================================================

[2026-07-03] - Combat parity wave C7: hit-path ammo takeover, slayer magic path
----------------------------------------

ADDED - @Hit LOCAL.Arrow (hit-path ammo takeover):
  - The @Hit chain now sees LOCAL.Arrow = the live pack ammo stack UID on
    a ranged hit (previously miss-only); LOCAL.ArrowHandled=1 hands the
    ammo to the script — the engine neither consumes the stack nor runs
    the 40% stick-in-body economy then, matching Source-X's nulled pAmmo.

ADDED - COMBAT_SLAYER on the magic path:
  - Spell damage now applies the slayer scaling with the equipped
    spellbook as the slayer source (wielded weapon fallback, talisman
    inside the shared scaler), mirroring the Source-X DAMAGE_MAGIC branch.

ADDED - Hit-area splash sparkle:
  - Each splashed victim shows the Source-X sparkle effect (0x3779) in
    addition to the per-element epicenter sound.

================================================================================

[2026-07-03] - Combat parity wave C6: CombatFlags guardrail and doc refresh
----------------------------------------

ADDED - CombatFlags contract guardrail (CombatFlagGuardrailTests):
  - The enum is pinned name+value against Source-X COMBATFLAGS_TYPE, and
    every member must register its wired behavior site — a new flag with
    no behavior ("enum exists, behavior missing", the leak the C-wave
    audit found four of) now fails the test.
  - The 14-name AOS on-hit surface is pinned, with the Source-X-deferred
    names (HitLowerAtk/Def, HitCurse, HitFatigue) locked out until a
    behavior lands.

CHANGED - Docs refreshed for the C1-C5 combat contracts:
  - docs/TRIGGERS.md combat rows now describe the real SRC/ARGO/ARGN and
    LOCAL contracts of @HitTry/@HitCheck/@Hit/@GetHit/@HitMiss/@HitParry,
    including the writable damage, the armor-damage locals and the
    RETURN 1 semantics.
  - docs/PARITY_MATRIX.md closes the "LOCAL.Anim/Arrow + elemental
    locals" open item and records the C1-C5 rows with what stays open
    (@Hit LOCAL.Arrow hit-path takeover, the slayer magic-damage path).

================================================================================

[2026-07-03] - Combat parity wave C5: AOS on-hit properties
----------------------------------------

ADDED - Life/mana/stam leech and mana drain (Source-X Fight_Hit formulas):
  - HITLEECHLIFE heals rand(0 .. dmg x prop x 30/10000), HITLEECHMANA the
    x40 window, HITLEECHSTAM restores the full damage prop% of the time,
    HITMANADRAIN transfers 20% of the damage from the victim's mana
    (clamped) prop% of the time. Any leech plays the 0x44D sound.

ADDED - On-hit spell procs (weapon required, like Source-X):
  - HITDISPEL/HITFIREBALL/HITHARM/HITLIGHTNING/HITMAGICARROW apply the
    spell's effect directly on the victim at the attacker's Magery — no
    cast time, mana or reagents (SpellEngine.ApplyOnHitSpell).

ADDED - Hit-area splashes:
  - HITAREAPHYSICAL always, the FIRE/COLD/POISON/ENERGY variants only
    under COMBAT_ELEMENTAL_ENGINE: half the damage splashes the chars
    within 5 tiles of the victim through their own resists, skipping the
    two principals, the attacker's pets and the dead, with the
    per-element impact sound at the epicenter.

ADDED - Property surface:
  - The 14 property names are tag-backed on chars and items and accepted
    as CHARDEF/ITEMDEF lines (def-tag flow). The effective value sums the
    attacker's own tag with the weapon's and equipped talisman's
    (instance tag first, ITEMDEF def-tag fallback).
  - HitLowerAtk/Def, HitCurse and HitFatigue stay deferred — they are
    unimplemented in Source-X as well.

================================================================================

[2026-07-03] - Combat parity waves C1-C4: @GetHit contract, trigger locals, flags, Slayer
----------------------------------------

CHANGED - @GetHit armor-damage contract (wave C1):
  - The on-hit trigger pipeline moved into TriggerDispatcher.RunHitDamageTriggers
    with a HitDamageContext. @GetHit now seeds LOCAL.ItemDamageLayer (random
    pick from the Source-X sm_ArmorDamageLayers set) and LOCAL.ItemDamageChance;
    the item @GetHit fires on the worn piece at the script-final layer instead
    of only a TwoHanded shield.
  - Armor durability wear rolls ItemDamageChance after the trigger chain in
    BOTH armor modes (elemental combat previously never wore armor); a
    RETURN 1 cancel skips the wear roll.

FIXED - Elemental damage split (wave C1):
  - Removed the forced 1-damage floor: a fully-resisted elemental hit now
    deals 0, like Source-X.
  - An unset physical percent defaults to the remainder the elemental
    percents leave of 100, applied consistently at spawn def-apply time.

ADDED - Trigger local contracts (wave C2):
  - @HitCheck fires before the swing range/LoS validation (Source-X order)
    and seeds LOCAL.Recoil_NoRange; the read-back drives the per-swing
    range-ignore decision and the windup window.
  - @HitMiss LOCAL.Arrow carries the live pack ammo stack UID; ammo
    consumption moved to outcome resolution so LOCAL.ArrowHandled or
    RETURN 1 hands the ammo's fate to the script.
  - @Hit seeds LOCAL.ItemDamageChance and ItemPoisonReductionChance/Amount
    (shared with the weapon item @Hit); the read-backs gate the weapon
    durability wear and the poison charge spend.

ADDED - Dormant combat flag behaviors (wave C3):
  - COMBAT_ALLOWHITFROMSHIP: combat across a ship region boundary is blocked
    unless the flag is set; same-ship combat stays allowed.
  - COMBAT_DCLICKSELF_UNMOUNTS: a mounted char in war mode with an active
    fight opens the paperdoll on self-dclick instead of dismounting, unless
    the flag opts into the unmount.
  - COMBAT_NOPETDESERT: attacking your own pet makes it desert (cancellable
    via @PetDesert) unless the flag allows friendly fire.
  - COMBAT_ATTACK_NOAGGREIVED: skips the criminal marking when starting a
    fight against an innocent, on the melee and harmful-spell paths.
  - Config load normalizes COMBAT_PREHIT|COMBAT_SWING_NORANGE by clearing
    SWING_NORANGE with a warning (Source-X RC_COMBATFLAGS).

ADDED - Slayer system (COMBAT_SLAYER, wave C4):
  - SlayerFaction is a verbatim CFactionDef port: faction groups, the
    opposite/super/lesser matching tables and the damage constants (lesser
    +200%, super +100%, opposite-group penalty -100%), quirks included so
    script data behaves identically to Source-X.
  - Script surface matches Source-X: FACTION_GROUP/FACTION_SPECIES on
    chardefs and chars, SLAYER_GROUP/SLAYER_SPECIES on itemdefs and items
    (def-tag flow to spawns, tag-backed persistence).
  - ResolveAttack scales the settled damage under COMBAT_SLAYER: weapon
    slayer first, the equipped talisman as fallback; NPC victims take the
    bonus, a player victim takes the Source-X opposite-group penalty from
    an NPC's super slayer. The magic-damage/spellbook path is deferred.

================================================================================

[2026-07-03] - Parity wave W-I1: Source-X inventory guardrail, HEARALL/DEFMSG/config
----------------------------------------

ADDED - Source-X verb/function inventory CI guardrail:
  - Added SourceXVerbInventoryGuardrailTests, which pins the Source-X
    CObjBase/CChar/CItem/CClient function tables and CServer::sm_szVerbKeys.
    New upstream surface or backlog drift now shows up in tests.

FIXED - SERV.HEARALL / SERV.CHATFLAGS / SERV.GENERICSOUNDS:
  - CHATFLAGS and GENERICSOUNDS are now loaded from sphere.ini into
    SphereConfig and exposed through SERV.CHATFLAGS / SERV.GENERICSOUNDS.
  - SERV.HEARALL reads/toggles the Source-X LOGM_PLAYER_SPEAK bit; the
    script command SERV.HEARALL [0|1] now routes through the server resolver.

FIXED - DEFMSG runtime override:
  - DEFMSG name=value now writes to the ServerMessages override layer.
  - DEFMSG.* lookup sees runtime overrides. Persistent save/load semantics
    remain a separate parity task.

================================================================================

[2026-07-02] - Smoke test fixes: web status deadlock, CWD path resolution, T2A map bound
----------------------------------------
A headless boot smoke test with the real mortechUO script pack (554 .scp)
and UOP maps: 32845 itemdefs / 822 chardefs / 168 spells / 1645 recipes /
1271 teleporters loaded; telnet console (status/save/garbage/account add),
an account save-restart-load round trip and the web status endpoint were
exercised. Three real bugs found and fixed:

FIXED - The web status endpoint never answered (requests timed out):
  - WebStatusServer.Tick started a NEW BeginGetContext every tick; the
    previous pending accepts were abandoned and an arriving request was
    consumed by an accept nobody reaped (plus a per-tick async-op pileup).
    A single pending accept now survives across ticks.

FIXED - Relative save/accounts paths always resolved to the exe directory:
  - ResolvePath now applies the same CWD-first rule as FindConfigFile/
    FindDir: a relative path existing in the working directory wins, else
    it falls back to the exe directory. A server run from a data directory
    now writes its saves there.

FIXED - MAP0/MAP1 default 6144 -> 7168 (config/sphere.ini):
  - Width 6144 left the T2A/Lost Lands area (x>6144) outside the world;
    mortechUO teleport defs use x 6992-7153 and 10 Lost Lands teleporters
    failed to place. The UOP map0 file carries 7168.

================================================================================

[2026-07-02] - Parity wave W-H11: SMELT/CARVECORPSE verbs, MDB.*
----------------------------------------

ADDED - Item verbs SMELT and CARVECORPSE (Source-X CIV_*):
  - SMELT smelts the ore on SRC's behalf: the arg is a forge uid, else the
    nearest forge in reach is used; it joins the existing targeted smelt
    flow (@Smelt trigger, coloured ore and TAG.SMELT_TO preserved).
  - CARVECORPSE carves the corpse as SRC (meat/hides/feathers, the
    CORPSE_CARVED mark) — wired to DeathEngine.CarveCorpse.

ADDED - MDB.* secondary MySQL reference object (Source-X MDB):
  - The DB.* verb surface on its own independent connection: MDB.CONNECT/
    CLOSE/QUERY/EXECUTE; property reads MDB.CONNECTED, MDB.ROW.*,
    MDB.ESCAPEDATA. No ini auto-connect — scripts connect it explicitly.

================================================================================

[2026-07-02] - Parity wave W-H10: bulletin boards (IT_BBOARD) end to end
----------------------------------------

ADDED - Bulletin boards (Source-X addBulletinBoard/CItemMessage):
  - Dclicking a board sends 0x71 sub 0 (board name) plus the message items
    as container contents; the client then requests each header itself.
  - Posting (sub 5) creates a real message item inside the board (Name =
    subject, LINK = author, AUTHOR/TIME/BODY_n tags) — persists with saves.
    Past 32 messages the oldest rolls off; a reach check applies.
  - A header request (sub 3) answers with the sub 1 summary, a full read
    (sub 4) with the sub 2 body (author/subject/date + lines).
  - Deletion (sub 6) is restricted to the author or a GM.

FIXED - The 0x71 sub 3 parse DROPPED the message uid:
  - Source-X BBOARDF_REQ_HEAD carries the message uid; the handler chain
    (NetState/NetworkManager/Program) was renamed to RequestHead(board,msg).

================================================================================

[2026-07-02] - Parity wave W-H9: map display (IT_MAP), pin protocol, ISMYPET
----------------------------------------

ADDED - IT_MAP display (Source-X CClient::addDrawMap):
  - Dclicking a map now sends the 0x90 map gump with its world-coordinate
    bounds (MORE1 = top/left, MORE2 = bottom/right words) and replays the
    stored pins one by one via 0x56 — treasure maps render.
  - A blank map (t_map_blank or an invalid rect) only reports blank.

FIXED - 0x56 pin-edit commands realigned to Source-X MAPCMD:
  - The old mapping was wrong (assumed 6=insert, 7=move). Correct: 1 add
    (append), 2 insert, 3 move, 4 delete (tail shifts), 5 clear all,
    6 toggle plot mode — the server replies mode 7 with the new state.
  - Pins live as 1-based PIN_n tags (same surface as the script PIN.n).

ADDED - <ISMYPET> (Source-X CHC_ISMYPET):
  - Resolves in the interpreter against SRC: 1 when the target char is
    SRC's pet. An OWNER/NPCMASTER property read (owner uid) was added too.

================================================================================

[2026-07-02] - Parity wave W-H8: champion spawner, NPC head-turn, CANCAST, rune coords
----------------------------------------

ADDED - IT_SPAWN_CHAMPION (Source-X CCSpawn champion special case):
  - Champion spawners ignore the amount cap and never pause their timer —
    the wave keeps coming. TYPE=t_spawn_champion is recognized at runtime too.

ADDED - NPCs turn to face whoever addresses them (Source-X NPC_OnHear):
  - An NPC not in combat rotates toward the speaker and the facing change is
    broadcast to nearby clients.

ADDED - CANCAST.<spell> char property (Source-X CHC_CANCAST):
  - By number or spell name; answers mana, primary-skill requirement and
    region anti-magic checks through the engine hook.

ADDED - Dclicking a marked recall rune reports its destination:
  - Sextant coordinates in the "rune name: 40° 12'N, 12° 5'E" form; a blank
    rune keeps the old name hint.

================================================================================

[2026-07-02] - Parity wave W-H7: ship wheel (pilot), OPENTRADEWINDOW
----------------------------------------

ADDED - Ship pilot/wheel support (Source-X SetPilot + PacketWheelBoatMove):
  - The ship SETPILOT verb assigns the helmsman (arg = uid) or releases the
    wheel with an empty arg.
  - The 0xBF 0x33 wheel-boat move subcommand is registered and handled: a
    char aboard (only the assigned pilot when one is set) steers with the
    mouse — speed 0 stops, 1 moves one tile, 2+ sails continuously.

ADDED - Client verb OPENTRADEWINDOW:
  - Opens a secure trade with the target char from script (arg = uid, or
    the current target object).

NOTE - Deliberately deferred: MDB.* (secondary MySQL reference object — low
  usage, mechanical spread over 8 files) and the item SMELT/CARVECORPSE
  verbs (need char context); both stay open on the target list.

================================================================================

[2026-07-02] - Parity wave W-H6: custom house fixtures, EQUIP* verbs, client CAST
----------------------------------------

ADDED - Custom-house commit materializes real fixtures (Source-X CommitChanges):
  - INTERACTIVE design tiles — doors and containers — become real items on
    commit (they open, close and hold contents); walls/floors stay virtual.
    Door fixtures link to the multi (the house key opens them), get
    Move_Never and register as house components. A recommit tears down the
    previous fixture set (COMMIT_FIXTURES tracking). Detection uses ITEMDEF
    TYPE first, then the tiledata Door/Container flags.

ADDED - Char verbs EQUIPHALO / EQUIPARMOR / EQUIPWEAPON:
  - EQUIPHALO equips a glowing light source (arg = lifetime seconds);
    EQUIPARMOR dresses pack armor into free layers; EQUIPWEAPON equips the
    highest-attack weapon when the hands are free.

ADDED - Client verbs OPENPAPERDOLL and CAST:
  - OPENPAPERDOLL opens the target's (or own) paperdoll; CAST starts a spell
    from script by number or defname (targeted spells open the cursor).

================================================================================

[2026-07-02] - Parity wave W-H5: NPC skill training and ground-food seeking
----------------------------------------

ADDED - NPC skill training (Source-X NPC_OnTrainHear/Pay):
  - "train" makes a townsfolk NPC list the skills it can teach (mastered,
    >= 10.0); "train <skill>" quotes a price (exact or prefix name match).
    A trainer teaches up to 30% of its own skill, hard-capped at 42.0
    (Source-X TRAINSKILLPERCENT/TRAINSKILLMAX defaults).
  - Gold handed to the trainer while the offer is open buys skill: 1 gp =
    0.1 skill; overpayment beyond the cap bounces back to the student and
    reaching the cap closes the offer.

ADDED - NPC ground-food seeking (Source-X NPC_Act_Food):
  - Hungry creatures no longer eat only from their packs: within a range
    that widens with hunger (sight * (100-food)%), they find edibles on the
    ground, walk toward them and eat when adjacent. Grazing is preserved.

================================================================================

[2026-07-02] - Parity wave W-H4: ISSTUCK/CANMOVE, GO variants, CONTCONSUME, WOP styling
----------------------------------------

ADDED - Char properties (Source-X CHC_*):
  - ISSTUCK: returns 1 for a frozen/petrified char or one with all four
    cardinal exits blocked. CANMOVE.<dir>: whether the char can take one
    step that way (NPC AI scripts probe walls with it).

ADDED - Char verb GO variants:
  - GOUID/GOITEMID <uid> teleports to the object's top-level position,
    GOCHAR <name> to the first character matching the name (previously
    GM-speech only; as object verbs they were silent no-ops).

ADDED - Item verb CONTCONSUME:
  - Consumes N of a given id from the container's contents, recursing into
    sub-containers (Source-X ContentConsume) — the material-sink verb of
    craft/quest scripts.

ADDED - @SpellCast LOCAL.WOPColor / LOCAL.WOPFont:
  - The mantra's hue and font can be overridden from script; SpellEngine
    speaks through the new styled OnSpellWordsEx hook (the old OnSpellWords
    stays backward compatible).

================================================================================

[2026-07-02] - Parity wave W-H3: spider webs, @EffectTick stage, SECURE/SHRINKMEM, verb aliases
----------------------------------------

ADDED - IT_WEB behavior (Source-X Use_Item_Web):
  - Stepping into a web sticks the char (giant spiders/ghosts/staff pass
    through); dclick struggles with STR — a torn web leaves spider silk and
    frees whoever was stuck. The frozen-char use gate got a web exception so
    a stuck char can actually struggle.

ADDED - [SPELL n] @EffectTick section stage:
  - The poison tick bridge now runs the [SPELL] section @EffectTick alongside
    the char-level @SpellEffectTick with the same LOCAL pool (EFFECT/DELAY/
    CHARGES); RETURN 1 ends the effect.

ADDED - SERV.SECURE and SERV.SHRINKMEM script bridge:
  - SERV.SECURE toggles secure mode; while on, script-initiated SHUTDOWN is
    refused. SERV.SHRINKMEM runs a compacting GC pass.

ADDED - Object verb aliases:
  - Char WAKE (the missing counterpart of SLEEP), object DESTROY (hard
    removal bypassing the @Destroy veto), SMSG/SMSGL/SMSGLEX/SMSGU
    sysmessage aliases.

================================================================================

[2026-07-02] - Parity wave W-H2: GARBAGE integrity sweep, house verbs, LOCAL leftovers
----------------------------------------

ADDED - GARBAGE is now the Source-X FixWeirdness world-integrity sweep:
  - GameWorld.GarbageCollection: graphicless items deleted (0x2103), items
    with a missing container recovered to the ground or deleted when
    unreachable (0x2205), phantom equips dropped into the pack (0x2202),
    items missing from their container's content list relinked (0x2106),
    decay-flagged items with no timer re-armed (0x2236). Memory/Special-layer
    items are left alone; every action is logged with its reason code.
  - Both SERV.GARBAGE (script) and the console GARBAGE run the sweep + GC.

ADDED - CItemMulti management verbs drive the LIVE House:
  - ADDCOOWNER/DELCOOWNER/ADDFRIEND/DELFRIEND/ADDBAN/DELBAN/ADDACCESS/
    DELACCESS/LOCKITEM/UNLOCKITEM/SECURE/RELEASE/REDEED on a multi item now
    mutate runtime House state (previously they only landed in tags until a
    world reload). REDEED delivers the deed to the owner's backpack.

ADDED - Trigger LOCAL leftovers:
  - @HitTry LOCAL.Anim / LOCAL.AnimDelay: scripts can override the player
    swing animation and its frame pacing.
  - @SpellCast LOCAL.WOP: power words can be rewritten from script, or
    cleared for a silent cast (precast included).

================================================================================

[2026-07-02] - Parity wave W-H1: ammo economy, [SPELL] stages, dispensers
----------------------------------------

ADDED - Source-X ranged ammo economy:
  - 40% of a player's missed arrows fall at the victim's feet (recoverable,
    decays); @HitMiss seeds LOCAL.Arrow and LOCAL.ArrowHandled=1 lets a
    script take over. 40% of arrows that strike an NPC stick in its pack and
    surface on the corpse loot. NPC archers consume ammo from their packs.

ADDED - [SPELL n] section lifecycle stages:
  - @Start (with @SpellCast), @Select and @TargetCancel run alongside the
    char triggers; @EffectAdd/@EffectRemove fire when a buff applies/expires
    (timeout, refresh, paralyze break, dispel and death cleanup included).

ADDED - Item-use parity slice:
  - IT_SEXTANT reports real degree/minute coordinates (centered on Lord
    British's throne).
  - IT_ITEM_STONE dispensers work: give the MORE1 item, burn MORE2 charges
    (0 = infinite, exhausted = "dead"), honour the MOREX regen window.
  - Comm crystal relay: speech near a crystal is re-spoken at its linked
    partner; @Hear RETURN 1 suppresses the relay.

================================================================================

[2026-07-02] - Parity waves W-A..W-G: Source-X behavior surface alignment
----------------------------------------

The first seven waves of the category-by-category Source-X audit
(wiki/hedef.txt) were applied in one pass. Test suite grew 1040 -> 1076
(all green, ParityWaveA-G guardrail suites). Highlights:

FIXED - Trigger contracts:
  - Trigger names aligned with the Source-X tables: @Jailed, @Ship_Move/
    _Stop/_Turn, @itemSPELL (scripts using the reference names never fired).
  - @Hit/@HitTry/@HitCheck/@HitMiss: SRC=victim, ARGO=weapon; damage-type
    ARGN2 on @Hit/@GetHit/@HitCheck; @HitCheck ARGN1=swing state.
  - @Death SRC=the dying char (killer on ARGO), @Kill ARGN1=attacker count,
    item @Step ARGN1=standing flag.
  - @SpellEffect fires on the AFFECTED char (SRC=caster, ARGN2=skill level,
    writable LOCAL.Effect/Resist/Duration, RETURN 1 cancels per target);
    @SpellCast seeds ARGN2=difficulty + writable ARGN3 cast time.
  - [SKILL n] section stages (ON=@Start/@Stroke/...) execute for the first time.

FIXED - Combat core:
  - Players now gain weapon skill + Tactics from combat (never gained before).
  - Era-0 hit roll is the two-stage model: random difficulty 0..iDiff checked
    against the weapon-skill bell curve; frozen targets are near-certain hits.
  - Coverage-weighted whole-body AR replaces the single-region reduction;
    legacy parry formula (shield/40, DEX<80 erosion, GM +5, trains Parrying).
  - IT_TRAP dclick SPRINGS the trap (full Use_Trap state machine); equipped
    cursed items refuse to move; paralyze breaks on damage; Dispel strips
    active buffs/curses.

FIXED - Notoriety follows the Source-X decision order:
  - Party/guild resolve BEFORE criminal/murderer (a party-member murderer
    renders green); TAG.OVERRIDE.NOTO honored; PLAYEREVIL/PLAYERNEUTRAL
    karma thresholds added (sphere.ini); murderers see themselves red; your
    own pet renders neutral (the Source-X default).

ADDED - NPC AI / economy / social:
  - NPC hostility: karma alignment + CREID ally-group families (orcs no
    longer attack orc mages) + fight-memory grudges.
  - The vendor purse is always tracked (restock funds it, partial sell fill
    with a shortfall bark); VENDORMARKUP drives the sell payout.
  - Guild stone gump: dismiss member, declare/break alliance, set name/
    abbreviation, fealty vote. Party member health bars update beyond
    visual range.
  - Region names are announced only in ANNOUNCE-flagged regions.

ADDED - Housing/ships/persistence:
  - House placement generates a pack key + bank spare; lockdown/secure stamp
    the item ATTR; redeed sweeps loose floor items into the moving crate.
  - Ship planks open/close (5s autoclose); ships collide with statics and
    other hulls.
  - Sector environment (light/season/weather), ALL memory types (with
    remaining timeouts) and the attacker log survive save-load.
  - SERV.B / SERV.GARBAGE / SERV.INFORMATION bridged to scripts; DISTANCE,
    HITPOINTS/USESCUR aliases, item CONSUME/BOUNCE/DECAY verbs.

================================================================================

[2026-07-01] - World-ops parity: RESTORE rollback hardening (Wave 217)
----------------------------------------

FIXED - SERV.RESTORE no longer silently loses the old object on load failure:
  - RESTORE takes a temporary rollback .scp snapshot through WorldSaver.ExportObjects
    before deleting colliding serials.
  - If loading the restore file throws after those objects were removed, possible
    partial new records from the restore file are cleaned up and the backup snapshot
    is loaded through the normal LoadFile path.
  - Rollback behavior is covered by a test that corrupts the restore file after the
    backup is taken.

================================================================================

[2026-07-01] - World-ops parity: scoped IMPORT (Wave 216)
----------------------------------------

ADDED - SERV.IMPORT now has the matching flag/distance filter:
  - SERV.IMPORT supports `IMPORT file, flags, distance`. The command target is
    used as the center; flags are interpreted as 1=items, 2=chars, 3=both.
  - Scoped import reduces the source file to a temporary filtered text .scp and
    then reuses the existing LoadFile path, preserving duplicate-serial,
    container/equipment relink and SPELLEFFECT restore behavior.
  - Items under selected containers/chars are imported with their parent chain.

================================================================================

[2026-07-01] - World-ops parity: scoped EXPORT / SAVESTATICS (Wave 215)
----------------------------------------

ADDED - Source-X-style distance/flag export slice:
  - SERV.EXPORT now supports `EXPORT file, flags, distance`. The script SRC
    object is used as the center; flags are interpreted as 1=items, 2=chars,
    3=both.
  - Scoped export preserves the normal world-save filters: static items stay out
    of dynamic export and vendor stock is still excluded. Items under selected
    containers/chars are written with the selected root so CONT links stay valid
    on import.
  - WorldSaver.ExportStatics can use the same scope filter; argument-less
    SERV.SAVESTATICS now writes WorldSaveDir/spherestatics.scp.

================================================================================

[2026-07-01] - World-ops parity: RESTORE replaces colliding serials (Wave 214)
----------------------------------------

FIXED - SERV.RESTORE is no longer a LOAD/IMPORT alias:
  - WorldLoader.RestoreFile pre-scans WORLDITEM/WORLDCHAR SERIAL records and
    removes currently registered world objects with those serials before loading
    the file back in.
  - SERV.LOAD / SERV.IMPORT remain non-destructive; duplicate serials keep the
    existing object and skip the imported record. The script bridge now routes
    RESTORE through a separate _RESTORE channel.
  - World-ops restore behavior is covered by a test that locks the duplicate
    LOAD versus destructive RESTORE distinction.

================================================================================

[2026-07-01] - World-ops parity: SERV.EXPORT / LOAD first slice (Wave 213)
----------------------------------------

ADDED - Safe first slice of the Source-X world-ops long tail:
  - SERV.EXPORT can now write one object, or WORLD/ALL for the dynamic world, as
    classic text .scp records under WorldSaveDir. Export intentionally uses
    script-compatible text even when normal saves use binary/gzip.
  - SERV.LOAD / SERV.IMPORT / SERV.RESTORE load one .scp/save-format file at
    runtime with two-pass item/char parsing; container/equipment links are resolved
    after load. RESTORE is a non-destructive import alias in this slice; destructive
    replace semantics stay deferred for a data-integrity-focused slice.
  - SERV.SAVESTATICS exports ATTR_STATIC items to a text .scp file.
  - Paths are jailed under WorldSaveDir; absolute paths, "..", and drive prefixes
    are rejected.

================================================================================

[2026-07-01] - Persistence parity: spawn/restock timer audit (Wave 212)
----------------------------------------

FIXED - Spawn and vendor restock timings could drift across restart:
  - IT_SPAWN_CHAR component initialization no longer overwrites a loaded TIMERMS
    remaining-time value; the saved spawn timer is rebound to the same target tick.
  - IT_SPAWN_ITEM now mirrors its next spawn time to the object Timeout, so WorldSaver
    can write it as TIMERMS and component init can preserve the same remaining-time
    pattern after load.
  - Vendor RESTOCK_TIME is now written as UTC Unix milliseconds instead of process-local
    TickCount64, matching TradeEngine and staying meaningful across save-load.

================================================================================

[2026-07-01] - Persistence parity: active spell effects survive save-load (Wave 211)
----------------------------------------

FIXED - An active buff/debuff could become a permanent stat exploit after restart:
  - Timed spell effects such as Bless/Curse/Strength/Weaken are now written to the
    world save as remaining-time SPELLEFFECT records. The save keeps base stats clean;
    after load, SpellEngine restores the effect, reapplies live stat/flag/visual state,
    and lets the effect expire normally.
  - Incognito/Polymorph/NightSight state reverted for save is now fully re-applied
    after save, and the reapply path runs from finally even when saving fails.

================================================================================

[2026-07-01] - Network: server-initiated RTT ping disabled by default (Wave 210)
----------------------------------------

FIXED - The client ping meter showed absurd values (hundreds of thousands of ms):
  - The server sent an unsolicited 0x73 ping to each client every 30s to measure its
    own RTT. The stock ClassicUO client feeds EVERY incoming 0x73 into its own ping
    average (Time.Ticks minus a baseline that only advances when the client itself sends
    a ping), so the server's pings made the client display its own uptime as "ping" —
    an average in the 500k ms range with spikes to millions, dropping to the real 8-12ms
    only in the instant after the client sent its own ping. The server-side RttMs value
    was never consumed anywhere. The feature now ships disabled (RttPingIntervalMs=0);
    the client's own ping still works (the server echoes client-initiated pings). Set
    RttPingIntervalMs to a positive value only with a client that ignores unsolicited
    pings. NOTE: if RttPingIntervalMs is set in your .ini, change it to 0 there too.

================================================================================

[2026-07-01] - Persistence parity: active poison survives save-load (Wave 209)
----------------------------------------

FIXED - An active poison was lost on save-load:
  - A poisoned character's level, remaining ticks, next-tick schedule and poisoner were
    never written to the world save, so the poison silently ended on restart (and a
    poison kill lost its attribution). The poison is now saved as remaining time + tick
    count (like item decay / TIMERF) and restored exactly on load via a new
    CharacterPoisonState.Restore, so it resumes rather than re-arming fresh.

KNOWN - Active spell buffs/debuffs still become permanent across a restart:
  - Stat effects (Bless/Curse/Strength/Weaken/...) modify the character stat directly
    and are runtime-only, so save-load keeps the buffed stat but drops the expiration —
    the buff never wears off after a restart. Fixing this needs a design decision
    (persist the effect list so it expires, or revert effects at save so base stats are
    stored); tracked in PARITY_MATRIX.md rather than rushed here.

================================================================================

[2026-07-01] - Persistence parity: TIMERF timers survive save-load (Wave 208)
----------------------------------------

FIXED - A pending TIMERF / TIMERFMS timer was lost on save-load:
  - Per-object delayed function/verb timers (AddTimerF) were never written to the world
    save, so a scheduled delayed action was dropped when the server restarted — unlike
    Source-X, which keeps object timers across a save. Each pending entry is now saved
    per object as remaining time (not the absolute tick, which resets on restart) and
    re-scheduled on load, mirroring how item decay/TIMERMS already persist. The saver
    emits one TIMERF property per entry (remainingMs|function|args); the loader routes
    it through TrySetProperty, which re-adds the timer with the remaining delay.

================================================================================

[2026-07-01] - Scripting parity: SERV.VARLIST / PRINTLISTS write to the caller (Wave 207)
----------------------------------------

CHANGED - SERV.VARLIST and SERV.PRINTLISTS now output to the invoking client:
  - As commands they carry the caller's UID through the script bridge (like
    SERV.ALLCLIENTS / SERV.GMPAGE), so the server dumps the global VAR / list contents
    to that GM's console instead of only the server log (the Wave-197 deferred half). A
    log fallback is kept when there is no online caller, and the <...> read form still
    returns the count. The VAR enumeration was factored into a shared sink so the log
    and console paths share one implementation.

================================================================================

[2026-07-01] - Speech parity: @Speech text rewrite wired end-to-end (Wave 206)
----------------------------------------

ADDED - A player's @Speech trigger can now rewrite the spoken text:
  - The ARGS-mutation mechanism (Wave 204) is now carried through the speech path. The
    speaker's @Speech self-trigger runs with the utterance as ARGS; if the script
    rewrites it (ARGS=<new text>), the rewritten words are read back and become what
    the NPC-hear routing, guild/alliance routing and the spatial broadcast all use —
    matching Source-X, where @Speech can reshape the line. RETURN 1 still cancels the
    whole utterance. FireSpeechSelfTrigger gained an overload that returns the rewritten
    text; OnPlayerSpeech now returns (Cancel, Text) and ProcessSpeech an out finalText;
    a ghost's garbled speech is left untouched by the rewrite.

================================================================================

[2026-07-01] - Scripting parity: RETURN preserves string values (Wave 205)
----------------------------------------

FIXED - RETURN coerced string values to a number:
  - Both RETURN handlers evaluated the argument as a long and stored that number, so a
    [FUNCTION] returning a name, defname or message (RETURN of a resolved string)
    collapsed to a digit instead of its text. The expression evaluator only returned a
    long with no way to signal "this was not numeric", so the two cases were
    indistinguishable.
  - Added ExpressionParser.TryEvaluate, which reports whether the whole expression was a
    genuine numeric value — it clears an internal flag when an atom resolves to a
    non-numeric string (an unresolved bareword, or a <...> that expanded to text) or
    when content is left unparsed. RETURN now keeps the string when the arg is not
    numeric, and still evaluates numbers, hex and expressions. Plain Evaluate is
    untouched, so no numeric path changes.

================================================================================

[2026-07-01] - Trigger parity: ARGS string mutation (Wave 204)
----------------------------------------

FIXED - Scripts could not rewrite a trigger's string argument (ARGS):
  - <ARGS> was seeded and readable, but a line like ARGS=<new text> fell through to a
    no-op (the interpreter had no ARGS assignment path) and RunWrapped never copied the
    string back, so trigger text was read-only. ARGS assignment now writes the trigger
    arg and the rewrite is copied back to the caller — the mechanism a script needs to
    reshape trigger text (Source-X @Speech-style rewrite). FireResourceTrigger and
    FireSpellTrigger, which duplicated the ARGN copy-back inline, now share RunWrapped
    so they pick up the ARGS write-back too.

================================================================================

[2026-07-01] - Trigger parity: ARGN write-back from every trigger source (Wave 203)
----------------------------------------

FIXED - ARGN mutations were dropped from most trigger sources:
  - Only object EVENTS lists and character def triggers ran through RunWrapped, the
    method that copies a script's ARGN1/2/3 changes back into the caller's args. Item
    ITEMDEF / TEVENTS / TYPEDEF triggers (where most item behaviour lives), region and
    room events, and all global (EVENTSPLAYER / EVENTSPET / EVENTSITEM / EVENTSREGION)
    events called the runner directly and discarded the mutations — so an item @DClick
    that set ARGN1, or a global event adjusting a trigger number, had no effect on the
    caller. Every trigger firing path now goes through RunWrapped, so ARGN write-back is
    uniform regardless of where the trigger is defined.

================================================================================

[2026-07-01] - Trigger parity: ARGN3 seeding and ARGN mutation (Wave 202)
----------------------------------------

FIXED - <ARGN3> read 0 inside every trigger:
  - TriggerDispatcher.WrapArgs built the script args with only ARGN1/ARGN2 (the
    constructor's two number slots) and never seeded Number3, so a trigger fired with
    N3 set — e.g. @DropOn_* passing the drop Z, @SkillUseQuick passing the result —
    saw <ARGN3> as 0. WrapArgs now seeds Number3 as well.

FIXED - Scripts could not modify ARGN1/ARGN2/ARGN3:
  - The interpreter had no assignment path for ARGN, so a line like ARGN3=99 fell
    through to a no-op property-set — trigger numbers were effectively read-only,
    defeating the RunWrapped copy-back for script-driven changes (Source-X @Hit damage,
    @DropOn drop-Z, @NPCActFight dist/motivation). ARGN1/ARGN2/ARGN3 (and bare ARGN =
    ARGN1) assignment now writes the trigger args, and the value is copied back to the
    caller after the block.

================================================================================

[2026-07-01] - Scripting parity: TRYSRV runs at server privilege (Wave 201)
----------------------------------------

FIXED - TRYSRV did not elevate to server privilege:
  - TRYSRV is meant to run a verb as the server (Source-X g_Serv, PLEVEL 7) so it
    bypasses the calling source's privilege restriction, but it passed the original
    source through — a low-plevel player, or a Guest-level null console — so a
    privileged verb under TRYSRV still failed the very check it exists to bypass. It
    now runs the verb under a server (Owner) source; the original source's script
    bridge stays as the fallback so client-scoped verbs still reach a client.

ADDED - TRY-family coverage: tests pin TRYSRV's server elevation and TRYP's
  privilege gate (verb blocked below the required PrivLevel, allowed at or above).

================================================================================

[2026-07-01] - Scripting parity: SERV.ACCOUNT.n indexed access (Wave 200)
----------------------------------------

FIXED - SERV.ACCOUNT.<n> (indexed account access) was stubbed to return "0":
  - Source-X admin dialogs iterate SERV.ACCOUNT.0 .. SERV.ACCOUNT.<count-1> to list
    accounts, but the index path always returned "0". It now resolves the n-th account
    via the new AccountManager.GetByIndex, which orders by name so the index is stable
    across reads (a raw dictionary enumeration is not). SERV.ACCOUNT.<key>.<prop> also
    works now for both names and indices (e.g. SERV.ACCOUNT.0.PLEVEL), reading the
    property off the resolved account; an out-of-range index still yields "0".

================================================================================

[2026-07-01] - Scripting parity: script <LINK> reads the object's own link (Wave 199)
----------------------------------------

FIXED - <LINK> in scripts was aliased to the ACT object instead of the object's link:
  - The interpreter resolved both <ACT> and <LINK> to the trigger's second object
    (Object2), so <LINK> never reflected an object's actual m_uidLink — e.g. a lever's
    linked door was unreachable from <LINK> / <LINK.NAME>. <LINK> (and <LINK.prop>) now
    delegate to the current object's LINK property, which already resolves the linked
    object and its sub-properties for both items and characters. <ACT> still resolves
    Object2, so the two references are now independent (VarObjs chain consistency).

================================================================================

[2026-06-30] - Scripting parity: TIMERFMS and delayed-verb timers (Wave 198)
----------------------------------------

ADDED - TIMERFMS object verb (Source-X millisecond timer):
  - TIMERFMS <ms>, <payload> schedules a delayed function/verb on an object in
    milliseconds, the counterpart of TIMERF's seconds (which was the only unit
    supported). Both now share one parser (ObjBase.ScheduleTimerF) that differs only
    in the delay multiplier.

FIXED - A delayed TIMERF payload that was not a [FUNCTION] silently did nothing:
  - The timer executor only tried the payload as a script function, so a bare verb
    such as TIMERF 5, SAY hi or TIMERF 10, REMOVE never ran. It now falls back to
    executing the payload as a command on the object when no matching function
    exists, matching Source-X TIMERF (delayed verb OR function).

================================================================================

[2026-06-30] - Parity backbone: SERV.* world-ops verbs and parity matrix (Wave 197)
----------------------------------------

ADDED - Parity matrix measurement backbone (roadmap Faz 0):
  - docs/PARITY_MATRIX.md itemises the Source-X SERV.* verb surface with a status
    (Implemented / Partial / Stub / Missing / NotApplicable) and code location for
    each, turning the subjective category scores into a concrete, regression-guarded
    checklist. Points at the existing trigger guardrail for the trigger surface.

ADDED - Script-reachable SERV.* world-ops verbs (Source-X parity):
  - SERV.RESPAWN and SERV.RESTOCK now run from scripts (they delegate to the same
    engine actions the admin console fires; previously console-only).
  - SERV.CLEARLISTS drops all global script lists (optional name prefix) and returns
    the count — the list counterpart of SERV.CLEARVARS, backed by the new
    GameWorld.ClearGlobalLists primitive (the var store had a clear, the list store
    did not). SERV.VARLIST / SERV.PRINTLISTS dump the global var/list contents to the
    server log (caller-console routing deferred).

================================================================================

[2026-06-30] - Death parity: gender-specific player death cry (Wave 196)
----------------------------------------

FIXED - Every player death played one fixed sound:
  - A dying player always emitted sound 0x01FE regardless of gender. ServUO/Source-X
    Mobile.GetDeathSound for a human body returns a random gender-specific death cry —
    female Random(0x314, 4) (0x314..0x317), male Random(0x423, 5) (0x423..0x427). The
    death path now picks the correct cry (gender captured before the ghost-body swap,
    since IsFemale reads the live body). DeathEngine.GetHumanDeathSound centralises it.

================================================================================

[2026-06-30] - Vendor parity: refuse to buy a non-empty container or immovable item (Wave 195)
----------------------------------------

FIXED - Selling a non-empty container to a vendor destroyed its contents:
  - VendorEngine.ProcessSell validated amount, gold and the optional buy-list filter
    but not the item itself, so a vendor with no buy list (buys anything) would accept
    a bag and Delete() it — silently eating every item inside for the bag's sell price
    (an item-loss exploit). The sell path now refuses an immovable item (Move_Never)
    and any container that still holds items, mirroring ServUO BaseVendor's sell guard
    (skip !Movable and a Container with Items.Count != 0). An empty container stays
    sellable.

================================================================================

[2026-06-30] - Notoriety parity: criminal flag on attacking an innocent is region-independent (Wave 194)
----------------------------------------

FIXED - Attacking an innocent only flagged the attacker criminal inside a guarded region:
  - A melee attack on an innocent (NOTO_GOOD from the attacker's view) set the criminal
    flag only when the attacker stood in a guarded region, so attacking a blue player in
    the open left the aggressor blue. ServUO Mobile.CriminalAction sets Criminal = true
    unconditionally — only the guard RESPONSE (Region.OnCriminalAction) is gated on a
    guarded region — and the harmful-spell path here already flagged everywhere. The
    melee path now matches: an innocent attack flags the attacker grey regardless of
    region, with guard summoning still limited to guarded zones elsewhere.

================================================================================

[2026-06-30] - Speech parity: per-recipient ghost speech garbling (Wave 193)
----------------------------------------

FIXED - A dead player's speech was garbled identically for every listener:
  - A ghost's words were replaced once with a fixed "OooOoo" and broadcast that way
    to everyone in range — including other ghosts, staff and the speaker. Source-X
    (ServUO Mobile.MutateSpeech / CheckHearsMutatedSpeech) garbles per listener: the
    speaker, other ghosts and staff read the clear words, while each living listener
    who cannot hear the dead sees an independently-scrambled o/O string (spaces kept,
    length preserved). Delivery now runs through ForEachClientInRange for a dead
    speaker, choosing clear vs garbled per recipient (GhostSpeech.HearsGhostClearly).
    The NPC/guild routing copy stays garbled, so NPCs still ignore a ghost's words.

================================================================================

[2026-06-30] - Macro/message parity: macro engine tests, name macro tags (Wave 192)
----------------------------------------

ADDED - <CNAME> and <NAME_TITLE> message macros (Source-X name variants):
  - DEFMSG / speech templates may now use <CNAME> (the speaker's name with a
    capitalised first letter) and <NAME_TITLE> (the name prefixed with its title,
    e.g. "Lord Yunus"; falls back to the bare name when no title is supplied)
    alongside the existing <NAME> and <SEX a/b>. MessageMacros.Context gained an
    optional Title. The bare <NAME> pattern still cannot collide with the variants.

TESTS - Macro record/playback engine coverage (was zero):
  - The macro engine's deterministic control flow is now under test: capture step
    cap, target self/object/location classification, record↔playback mutual
    exclusivity, empty-recording discard, stop-on-missing-client during Tick (driven
    by an injected nowMs and getClient), and disconnect cleanup. No behaviour change.

================================================================================

[2026-06-30] - Party parity: decline notification, corpse-loot flag fix (Wave 191)
----------------------------------------

FIXED - Party decline (0xBF 0x06 sub 0x09) sent a null packet to the inviter:
  - Declining an invitation invoked the inviter's send path with a null packet
    (null!) — a latent NullReferenceException in the send pipeline. The inviter now
    receives the proper notification ("<name>: Does not wish to join the party.",
    party_decline_1) as a system message.

FIXED - Corpse-loot crime checked the wrong party member's loot flag:
  - Looting a party member's corpse consulted the LOOTER's loot flag instead of the
    corpse OWNER's. The flag means "the party may loot ME", so it belongs to the
    owner: as written, a looter who enabled their own flag could freely loot every
    party member's corpse without criminal penalty. The check now reads the owner's
    flag (DeathEngine.IsLootingCriminal).

================================================================================

[2026-06-30] - Guild parity: alliance declaration verbs, overhead abbreviation (Wave 190)
----------------------------------------

ADDED - Alliance declaration verbs on guild/town stones (symmetric to the war set):
  - DECLAREALLY <stone> records our side of an alliance; INVITEALLY <stone>, <who>
    records the other side's accept (who = 1 we / 0 they), mirroring INVITEWAR; the
    alliance only goes active once both sides have declared (IsAlly). DECLAREUNALLY
    <stone> withdraws our declaration (mirror of DECLAREPEACE). Previously the
    underlying AddAlly/RemoveAlly were only reachable from save-load, so alliances
    could not be formed at runtime from a guild stone script.

ADDED - Guild abbreviation on the overhead name:
  - A guilded player's single-click overhead label now carries the guild abbreviation
    (e.g. "Lord Yunus [TO]") when the guild has one and the member keeps it visible
    (TOGGLEABBREVIATION). The suffix is computed on-demand per click (not in the delta-
    tracked name path) and is suppressed for unguilded characters, hidden members and
    enemy/ally relationship records. GuildManager.GetAbbrevSuffix centralises the rule.

================================================================================

[2026-06-30] - Chat parity: conference moderation, private message, ignore list (Wave 189)
----------------------------------------

ADDED - Conference channel moderation (Source-X chat owner/moderator model):
  - A channel now tracks an owner, moderators, voiced members, an optional password
    and a default-voice flag. The member who creates an ad-hoc channel becomes its
    owner (and an implicit moderator). Channel state lives on ChatChannel; routing
    and packets stay in GameClient.

ADDED - Password-protected channels:
  - A protected channel rejects a join with a wrong or missing password. The 0x62/0x63
    payload parser extracts a trailing "{password}" suffix from the channel name, and
    0x41 sets/clears the password (moderator only). The channel list (0xB2 CreateChannel)
    advertises the password flag so the client shows the lock marker.

ADDED - Moderator actions (0xB3 sub-commands):
  - 0x76 kick, 0x6C/0x6D add/remove moderator, 0x69/0x6A grant/revoke voice, 0x77/0x78
    default-voice toggle, 0x64 rename. Each action is gated on the actor being a
    moderator of the channel they are in; rename re-keys the channel and rejects static,
    empty or taken names. AddUser (0xB2 0x03EE) now carries a userType marker
    (1 = moderator, 2 = voiced) so clients render the role beside the name.

ADDED - Moderated talk gating and emote:
  - With default-voice off, only moderators and explicitly voiced members may speak
    (CanSpeak gate); promoting a member to moderator overrides a revoked voice. 0x7A
    routes an emote (0xB2 EmoteMessage) instead of a normal channel message. The voice,
    moderator and default-voice toggles (0x6B / 0x6E / 0x79) and the OSI leave alias
    (0x58) are wired alongside their explicit on/off counterparts.

ADDED - Private messages (0xB3 0x65):
  - A player may whisper another by chat handle ("<recipient> <text>"). The sender
    always sees their own copy; the recipient receives it unless they are ignoring the
    sender.

ADDED - Ignore list (0xB3 0x66 / 0x67 / 0x68):
  - Per-character, channel-independent ignore set (add / remove / toggle) resolved by
    chat handle. A character cannot ignore itself. Channel lines and private messages
    from an ignored sender are suppressed server-side for that recipient (matching the
    OSI conference behaviour) while everyone else still receives them.

================================================================================

[2026-06-30] - Gathering parity: weight-based carry gate, partial node regen (Wave 188)
----------------------------------------

ADDED - Weight-based CanCarry on gather/craft delivery (Source-X CanCarry):
  - A gathered or crafted item that would push the actor over its carry weight now
    bounces to the ground instead of going into the pack (previously only a full-by-
    ITEM-COUNT pack bounced; weight was ignored). New Character.MaxWeight / CanCarry
    helpers (Str*7/2 + 40 + mod). Staff bypass the gate.

ADDED - Partial resource-node regen (Source-X gradual vein regrow):
  - A resource vein now slowly refills over time — one unit per (Regen / max) seconds,
    capped at its original pool — instead of only resetting after it is fully depleted.
    A recovering node clears its decay timer so it is not deleted mid-regrow. The node
    stores its full size and last-change time (RES_MAX / RES_LAST tags) alongside the
    remaining pool.

================================================================================

[2026-06-30] - Pet parity: pet figurine shrink / restore (Wave 187)
----------------------------------------

ADDED - Pet figurine (Source-X shrink / restore), the last vendor/trade/bank/stable gap:
  - A controlled pet can now be packed into a figurine item carrying a FULL snapshot
    of its state (name, body, stats, brain, AI mode, charDef, skills up to 1200,
    friends, original UUID) and recreated from it later — mirroring the stable
    claim/restore, stored on the item instead of the owner's tag list (PetFigurine).
  - "<pet> shrink" / "all shrink" packs the pet into a figurine in the backpack (the
    pet leaves the world); double-clicking the figurine restores the pet beside the
    player and consumes it. Restore is follower-cap aware (a capped owner keeps the
    figurine) and shrinking rejects summoned/un-owned creatures, like stabling.
  - The previous figurine double-click (which only re-placed a still-living world char
    by a More1 serial) is kept as a fallback for figurines without a snapshot.

================================================================================

[2026-06-30] - Bank parity: self bank box gated by banker proximity (Wave 186)
----------------------------------------

FIXED - Remote bank-box access (Source-X bank-open proximity):
  - Items inside the player's own bank box could be picked up or dropped from any
    distance — the self bank box was treated as unconditionally reachable, so a client
    could keep manipulating it after walking away from the banker. Pickup and drop into
    the self bank box (directly or via a nested bag) now require a live banker NPC
    within 3 tiles, matching the proximity required to open it. GM access is unchanged,
    and other containers / the self pack are unaffected.

================================================================================

[2026-06-30] - Vendor/stable parity: opt-in vendor money pool, stable target cursor + validation (Wave 185)
----------------------------------------

ADDED - Opt-in vendor money pool (Source-X m_Check_Amount):
  - A vendor with a VENDOR_GOLD tag now tracks finite funds: a player's purchase
    credits the pool and a sale debits it. A sale the vendor cannot fully afford is
    rejected (items kept, pool untouched). A vendor WITHOUT the tag keeps unlimited
    funds, so existing vendors are unchanged — the pool is entirely opt-in.

ADDED - Stable target cursor + pet validation (Source-X stablemaster):
  - Saying "stable" now asks the player to TARGET the pet to stable instead of
    auto-picking the nearest one (a headless/scripted call still falls back to the
    nearest owned pet).
  - Stabling now rejects a summoned/temporary creature and a pet out of range of the
    owner (≤12 tiles, same map), not just non-owned/player targets.

================================================================================

[2026-06-30] - Vendor/stable parity: buy-list, item clone, bank check redeem, stable skill snapshot (Wave 184)
----------------------------------------

FIXED - Stable skill snapshot (data loss):
  - Stabling a pet clipped every skill to 1000 (100.0), silently dropping the value of
    GM+ pets on retrieval. The snapshot now keeps the full value (up to 1200), so a
    claimed pet returns with the skills it went in with.

FIXED - Bought item is now a full clone (Source-X CreateDupeItem):
  - Buying from a vendor copied only id/name/hue/amount, dropping the stock entry's
    tags, durability, price and more-fields. The purchase now full-clones the stock
    item's per-instance state.

ADDED - Vendor buy-list restriction (Source-X NPC_FindVendableItem):
  - A vendor with a BUY list now only buys items on that list — the sell gump hides
    other backpack items and the authoritative sell path rejects them (anti-exploit).
    A vendor with NO buy list still buys anything, so existing vendors are unchanged.

ADDED - Bank check redeem:
  - Double-clicking a bank check now converts it to gold in the container it sits in
    (bank box or backpack) and consumes the check (previously checks were one-way —
    created but never redeemable).

ADDED - Stable capacity from skills / MAXPLAYERPETS:
  - Stable slots are no longer a flat 5: an explicit MAXPLAYERPETS tag overrides,
    otherwise the base plus one slot per ~60.0 combined Taming + Animal Lore +
    Veterinary. Never below the base, so unskilled owners keep 5.

================================================================================

[2026-06-30] - Combat parity: COMBAT_ANIM_HIT_SMOOTH swing animation pacing (Wave 183)
----------------------------------------

ADDED - COMBAT_ANIM_HIT_SMOOTH (0x10000), the last deferred swing flag:
  - When enabled, the swing animation is paced to the swing time (a slow weapon shows
    a correspondingly slow swing) instead of the fixed default speed. The legacy 0x6E
    animation packet's per-frame delay byte is now set proportionally to the swing
    delay on both the player and NPC swing paths; with the flag off the delay stays 0
    (unchanged). The exact pacing is client-interpreted; the value is proportional and
    byte-clamped. Enhanced/KR clients (0xE2) have no per-frame field and are unaffected.

FIXED - Test stability:
  - The damage-interrupt skill test looped only 20 swings to land a hit through the
    non-seedable Random.Shared combat RNG, so it could intermittently miss all 20 and
    fail; the loop now runs enough swings that a damaging hit is effectively certain.

================================================================================

[2026-06-30] - Combat parity: two-phase swing state machine (PreHit / StayInRange / SwingNoRange) (Wave 182)
----------------------------------------

ADDED - Two-phase windup -> hit swing (Source-X WAR_SWING_TYPE), giving meaning to
        COMBATFLAGS that were defined but inert:
  - A swing now splits into a windup (animation + recoil + a pending hit) and a hit
    phase (ResolveAttack + feedback). The hit resolves when the windup elapses, and
    reach/LoS are re-checked at that moment. Both the player and NPC swing paths share
    the model.
  - COMBAT_STAYINRANGE (0x200): if the target leaves reach/LoS before the hit lands,
    the swing MISSES instead of connecting.
  - COMBAT_SWING_NORANGE (0x8000): the swing may START out of range / without LoS; the
    damage is held until reach + LoS are regained (or dropped if never regained).
  - COMBAT_PREHIT (0x4): forces the hit back to swing-start (atomic) and disables
    SWING_NORANGE, matching Source-X precedence.
  - IMPORTANT: the flagless default keeps a ZERO-length windup, so the hit resolves in
    the same tick the swing starts — byte-for-byte the previous atomic behaviour. No
    existing combat test changed; the windup only opens for the flags above.
  - Deferred: COMBAT_ANIM_HIT_SMOOTH (animation-pacing packet field) — cosmetic and
    needs client-version verification; tracked as a follow-up.

================================================================================

[2026-06-30] - Speech parity: SPEECH RETURN 0 continues, item @Hear coverage (Wave 181)
----------------------------------------

CHANGED - [SPEECH] trigger scan (Source-X OnHearTrigger RETURN semantics):
  - A matched [SPEECH] ON= block that does RETURN 1 still consumes the line and
    stops, but a block that RETURN 0s (or falls through without a RETURN) no longer
    ends the scan — it continues to the next matching ON= block in the SAME speech
    resource. Previously the first matching block always won regardless of its return
    value, so a "didn't handle it" block blocked later handlers.

ADDED - Item @Hear coverage (Source-X item / multi OnHear):
  - Spoken speech now reaches nearby ITEMS: ProcessSpeech fires the new @Hear item
    trigger on every item within hearing range (same range as characters), so
    scripted listening items (signs, comm-crystal-style relays, multi components) can
    react to speech. Gated — the per-utterance item scan runs only when some item def
    actually hooks @Hear, so shards that don't use it pay nothing. S1 = spoken text,
    N1 = talk mode.

================================================================================

[2026-06-30] - Skill parity: @SkillChange as a cancelable setter guard for runtime skill changes (Wave 180)
----------------------------------------

ADDED - @SkillChange on runtime skill value-sets (Source-X setter guard):
  - A GM .ADDSKILL command and a script property assignment (e.g. MAGERY=80) now fire
    a cancelable @SkillChange BEFORE the value is set, so a script can rewrite the new
    value (ARGN2) or RETURN 1 to veto the change. Previously only a skill GAIN raised
    @SkillChange; these runtime setters changed the value silently.
  - Implemented as Character.SetSkillRuntime (fires Character.OnSkillChange, then
    applies the possibly-adjusted value); the GM command and script setter route
    through it. World-load, spawn, decay and gain keep using the raw SetSkill and do
    NOT fire the guard, so save/restore stays side-effect-free. Installed only when
    @SkillChange is hooked.

================================================================================

[2026-06-30] - Skill parity: @SkillGain fires pre-roll with chance/cap tuning + cancel (Wave 179)
----------------------------------------

CHANGED - @SkillGain timing (Source-X Skill_Experience):
  - @SkillGain now fires BEFORE the gain roll instead of after the gain, so a script
    can raise/lower the per-mille gain chance (ARGN2) or the effective skill cap
    (ARGN3), or RETURN 1 to cancel the gain attempt entirely. Previously it fired
    post-gain with the new value and its return was ignored.
  - The new pre-roll hook (SkillEngine.OnSkillGainCheck) is installed only when a
    script actually hooks @SkillGain, so unscripted shards pay a single null check.
  - @SkillChange and the blue "your skill increased" message still fire post-gain on
    the actual value change — only @SkillGain moved.

================================================================================

[2026-06-30] - Spell parity: wand/scroll consume fix, engine cast triggers, [SPELL] ON= dispatch (Wave 178)
----------------------------------------

FIXED - Wand charge / scroll consumption (Source-X Spell_CastDone consume stage):
  - A wand charge is no longer deducted at double-click, before the cast resolves —
    so it is no longer lost when the cast fizzles, is interrupted, or the target is
    cancelled. The double-click now tags the source wand (WAND_UID); the charge is
    spent only when SpellEngine.CastDone commits to success. An interrupted/cancelled
    cast clears the tag without spending.
  - Spell-scroll consumption moved from the client success path into the engine
    (CastDone), so precast casts consume the scroll too (previously they leaked it),
    and the consume happens at one shared point for every completion path.

ADDED - Engine-level cast resolution triggers (Source-X parity, NPC/direct casts):
  - @SpellSuccess / @SpellEffect / @SpellFail now fire from one engine hook
    (SpellEngine.OnCastResolved) at cast completion for ANY caster — player, NPC or
    direct — instead of only the client path. The duplicate client fires were
    removed, so player casts still fire each exactly once. (Cast-start failures and
    the targeting cursor's @SpellSelect / @SpellCast / @SpellTargetCancel remain on
    the client UI flow; NPCs gate casts via @NPCActCast.)
  - [SPELL] ON=@Trigger blocks now dispatch: TriggerDispatcher.FireSpellTrigger runs
    a spell def's own ON=@Success / @Effect / @Fail body (the [SPELL] link already
    retained its trigger bodies), wired through the same OnCastResolved hook.

================================================================================

[2026-06-30] - Multi parity: ship ban / eject bound to the ship region (Wave 177)
----------------------------------------

ADDED - Ship ban / eject (Source-X multi ban list, enforced through the ship region):
  - A ship now carries a ban list. Boarding is gated through the ship's dynamic
    region: a movement onto any tile resolved (region-bound) to a ship the mover is
    banned from is refused, exactly like the house ban gate. The owner is never
    barred.
  - Banning a player who is currently on deck ejects them — they are put on the tile
    just beyond the hull's starboard edge (this move fires region enter/exit normally
    since they really are leaving the ship), and the boarding gate then keeps them
    off.
  - Ship lookup is region-bound: FindShipAt resolves the ship via the Ship-flag
    region under a tile, so deck membership matches the footprint the engine already
    maintains rather than a separate bounding-box test.
  - Ban lists persist across save / load (SHIP.BANS tag).

================================================================================

[2026-06-30] - Multi parity: dynamic ship region tracking the hull as it sails (Wave 176)
----------------------------------------

ADDED - Ship region model (Source-X CItemMulti::MultiRealizeRegion + CRegionWorld move):
  - A ship now realizes a dynamic world region matching its hull footprint, flagged
    Ship and inheriting the surrounding region's flags (a guarded harbour / no-pvp
    sea carries through, with Ship added). Created on placement and on load, torn
    down on dry-dock. Being the smallest-area region over the footprint, FindRegion
    resolves to it on deck, so Ship-flag and region-flag-dependent systems apply
    aboard.
  - Unlike a house, the ship MOVES, so the region is re-positioned to the new
    footprint after every move and turn, and its inherited flags are RECOMPUTED
    (reset to base, re-inherit from the region the hull now sits in) rather than
    accumulated — a ship sailing out of a guarded zone correctly loses Guarded.
  - The ship's @RegionLeave / @RegionEnter boundary check now resolves the
    BACKGROUND region the hull sits in (excluding the ship's own region, which would
    otherwise win FindRegion by smallest area), so crossing a sea / harbour boundary
    still fires.
  - Deck characters carried by a moving / turning ship no longer fire region
    enter/exit or flip CURRENT_REGION: the region travels with the hull, so they are
    not crossing a boundary (new MoveCharacter fireRegionEvents=false path).

================================================================================

[2026-06-30] - Multi parity: house footprint region, leading-edge ship collision, placement Z-flatness (Wave 175)
----------------------------------------

ADDED - Housing region model (Source-X CItemMulti::MultiRealizeRegion):
  - A placed house now realizes a dynamic world region matching its footprint,
    flagged House and inheriting the containing region's flags (a guarded / no-pvp /
    safe town carries through, with House added). Being the smallest-area region over
    the footprint, FindRegion resolves to it inside the house, so House-flag and
    region-flag-dependent systems now apply indoors. The region is created on
    placement and on load, and torn down on collapse / redeed. Houses are static so
    the region never needs to track movement. (Ship regions remain a separate phase.)

FIXED - Ship movement collision (Source-X CCMultiMovable::Move):
  - A moving ship now tests only the LEADING EDGE in the move direction (both
    perpendicular edges for a diagonal) for water, instead of re-scanning its whole
    footprint. The cells the ship already occupies are water by definition; testing
    them again was redundant. Behaviour is unchanged for solid water but matches the
    engine's edge-scan model and is the basis for future deck-object collection.

FIXED - House placement Z-flatness (Source-X CItemMulti::Multi_Create):
  - House placement now rejects a footprint cell whose surface Z differs from the
    placement Z by more than 4, so a house can no longer straddle a slope or cliff.
    Guarded by map data being loaded (no effect in headless / no-MUL runs).

================================================================================

[2026-06-30] - Ship/housing parity: turn preflight, collapse moving-crate, deed targeting, @HouseCheck (Wave 174)
----------------------------------------

FIXED - Ship:
  - Turning a ship now preflights the new facing's footprint and ABORTS before any
    mutation if it would not fit (water/passable), instead of swapping the BaseId
    and rotating all components / deck characters / items with no fit check and no
    rollback — which could leave a ship half-rotated onto land. Magic ships still
    turn anywhere.

FIXED - Housing:
  - A collapsing house no longer scatters its locked-down / secured items on the
    ground to decay (item loss). They are reparented into a MOVING CRATE — a
    secured container moves whole — and the crate is delivered to the owner's bank
    box (fallback: the house tile). Reparenting, not copying, so no dupe.

ADDED - Housing:
  - House deeds now ask for a TARGET tile (Source-X) and place the house there,
    instead of dropping it at the player's feet on double-click.
  - @HouseCheck script hook: a script may veto house placement (RETURN 1) after the
    engine's NoBuild / footprint / terrain checks pass; ARGN1/2/3 = x/y/z.

================================================================================

[2026-06-30] - Region parity: InheritParent flag/tag/event inheritance (Wave 173)
----------------------------------------

ADDED - Region nesting (Source-X REGION_FLAG_INHERIT_PARENT_*):
  - A region flagged InheritParentFlags / InheritParentTags / InheritParentEvents
    now pulls the corresponding state from its containing parent region at load.
    The "parent" is the smallest region with a larger area that contains the
    region's representative point (P=, or the centre of its first rect). Inheritance
    is additive (the region keeps its own flags/tags/events) and the inherit-control
    bits themselves are not propagated. Runs once after all AREADEFs load.

================================================================================

[2026-06-30] - Region/spawn parity: NoMining, anti-magic sub-flags, underground light, item-spawn timing (Wave 172)
----------------------------------------

ADDED - Region flag behaviours (Source-X REGION_*):
  - NoMining (REGION_FLAG_NOMINING): mining is refused in the region.
  - Anti-magic sub-flags now enforced: RecallOut blocks recalling out, the Recall
    flag blocks recalling/gating IN, the Gate flag blocks gate travel, NoTeleport
    blocks teleporting into the region, and NoMagicDamage fizzles harmful spells.
    The legacy numeric region properties (RECALLOUT/RECALLIN/GATEOUT/GATEIN = 0)
    keep working alongside the flags.

FIXED:
  - Underground light no longer clobbered: the periodic global-light broadcast now
    skips clients standing in an Underground (dungeon) region, so a player in a
    dungeon keeps the fixed dungeon light instead of suddenly seeing surface
    daylight on the next light tick. Surface light is restored on region exit.
  - Item spawners (IT_SPAWN_ITEM) now honour TIMELO/TIMEHI (respawn interval, in
    minutes) and MAXDIST (scatter range) like the char spawner — and fixed a
    latent bug where these properties were stored as tags but never applied to the
    spawn component (affecting char spawners too).
  - Sector.Number is computed from the map's true column count (width / 64)
    instead of a hardcoded 96, so it is correct on non-6144-wide maps.

================================================================================

[2026-06-30] - NPC AI parity: wand casting (Wave 171)
----------------------------------------

ADDED - NPC AI:
  - A casting NPC holding a charged wand (IT_WAND with a spell id in More1 and a
    CHARGES tag) now throws the wand's spell ~50% of the time (Source-X
    NPC_FightMagery wand source). The wand path is mana-free and bypasses the book
    mana gates, so it fires even when the NPC is low on mana — and a wand-only NPC
    with no spellbook can cast from it. Each wand cast consumes a charge; at zero
    the wand's spell clears (mirroring the player double-click wand path). A wand
    with no CHARGES tag is treated as infinite. Additive — only NPCs with an
    equipped wand are affected.

================================================================================

[2026-06-30] - NPC AI parity: config heal threshold, @NPCActFight skip-hardcoded, @NPCActCast target (Wave 170)
----------------------------------------

CHANGED - NPC AI:
  - A casting NPC's self-heal decision now uses the configured wound threshold
    (NPCHEALTHRESHOLD, default 30%) instead of a hardcoded 50% (Source-X
    NPC_FightCast iHealThreshold); below half the threshold it prefers GreaterHeal.

ADDED - NPC AI triggers:
  - @NPCActFight LOCAL.skiphardcoded = 1 makes the engine bypass its hardcoded
    breath / throw specials while keeping flee / magery / melee (Source-X
    fSkipHardcoded), distinct from RETURN 1 which suppresses the whole action.
  - @NPCActCast LOCAL.target = a uid redirects the cast onto a different target
    (Source-X REF1 target override), in addition to the existing ARGN1 spell
    override and RETURN 1 abort.

================================================================================

[2026-06-30] - Attack-as-crime tied to personal notoriety (self-defense) (Wave 169)
----------------------------------------

CHANGED - Attack / spell as crime (Source-X CCharFight NOTO_GOOD):
  - Attacking is a crime only when the target is NOTO_GOOD (innocent blue) FROM
    THE ATTACKER'S OWN VIEW. Because personal grey now folds in the attacker's
    SawCrime / HarmedBy memory, retaliating against an aggressor who struck first
    — or against someone whose crime the attacker witnessed — is self-defense and
    no longer flags the defender criminal. A globally criminal / murderer /
    guild / party target is likewise not NOTO_GOOD. The aggressor and victim still
    record their IAggressor / HarmedBy memory, and attacking a true innocent in a
    guarded region still flags the aggressor.
  - The same personal-view rule now gates the harmful-spell crime: a spell on a
    target the caster holds SawCrime / HarmedBy of is self-defense, not a crime.

================================================================================

[2026-06-30] - Crime/notoriety parity: CheckCrimeSeen witness pipeline, fame/karma split (Wave 168)
----------------------------------------

ADDED - Crime witness pipeline (Source-X CChar::CheckCrimeSeen):
  - When a crime is committed, every character in view range with line of sight —
    and, for a covert crime, the winner of the perception skill contest (the
    viewer's vs the criminal's skill) — notices it: the witness records a per-
    viewer MEMORY_SAWCRIME (so the criminal shows grey to THEM via personal grey),
    a guarded-region NPC witness calls the guards (global criminal flag), and
    @SeeCrime / @SeeSnoop fire so a script can react or escalate. A crime no one
    sees has no consequence.
  - Stealing and snooping now run this witness pipeline instead of the old blind
    50% / config-toggle MakeCriminal: an unseen theft is consequence-free, a
    witnessed one makes the thief grey to the witnesses (and globally criminal in
    a guarded area). Snooping respects the snoop-criminal chance.
  - Personal grey now also keys on MEMORY_SAWCRIME (in addition to the aggressor
    HarmedBy memory), matching Source-X Noto_CalcFlag's SAWCRIME | AGGREIVED.
  - New @SeeSnoop character trigger.

CHANGED - Death credit (Source-X Noto_Kill):
  - Fame, karma and experience on a kill are now divided across every damaging
    attacker (iTotalKillers), so a group splits the spoils instead of the final
    blow taking all of it. A solo kill is unchanged.
  - Killing a criminal / red now yields NO karma — the would-be loss is clamped
    to 0 and no positive karma is awarded (previously a criminal victim's negative
    karma granted the killer a small positive gain).

================================================================================

[2026-06-30] - Notoriety display parity: personal-grey, pet-inherits-owner, @Criminal duration (Wave 167)
----------------------------------------

ADDED - Notoriety display:
  - Personal grey (Source-X aggressor memory): a player who harmed the viewer —
    an active aggressor such as a wilderness attacker who is not globally criminal
    — now shows grey to THAT viewer while the fight memory lasts, so a victim can
    see who is attacking them. The innocent victim stays blue to the aggressor.
    Display-only; it does not change attack-crime legality.
  - A tamed pet / summon now inherits its owner's notoriety colour (Source-X
    Noto_GetFlag pet->owner): the viewer's own pet shows friendly green, another
    player's pet takes that owner's colour, and an ownerless creature falls back
    to its NPC notoriety.

ADDED - Triggers:
  - @Criminal can override the criminal-flag duration via ARGN1 (seconds);
    RETURN 1 still cancels the flag entirely. OnCriminalCheck now returns the
    duration (null = cancel, 0 = engine default).

================================================================================

[2026-06-30] - Notoriety/crime parity II: multi-attacker murder marking, decay persistence, trigger contract (Wave 166)
----------------------------------------

CHANGED - Murder / crime:
  - A murder is now marked against EVERY unprovoked attacker of an innocent
    player, not just the final-blow killer (Source-X Noto_Kill loops the damage
    list) — ganking an innocent flags the whole group. Each attacker is resolved
    pet->master, deduped, and gated through the same unprovoked-innocent-kill
    check (self-defence / criminal-or-red victims are exempt).
  - The murder-decay countdown is now persisted (MURDERDECAY): a murderer's kill
    count keeps ageing across save/load instead of restarting the full decay
    window on every reload.

ADDED - Triggers:
  - @MurderMark gains an ARGN2 make-criminal toggle: a script can record the
    murder count while suppressing the temporary criminal flag (RETURN 1 still
    blocks the mark + flag entirely).
  - @MurderDecay can read back the seconds until the NEXT decay (ARGN2), letting
    a script override the decay interval per character (0 = engine default).

================================================================================

[2026-06-30] - Item/inventory parity II: bounce-to-origin, bank deep cap, contained-item reach (Wave 165)
----------------------------------------

ADDED - Drop / container:
  - Drag bounce-to-origin (Source-X CClientEvent): a failed drop now returns the
    item to the exact container slot / ground tile it was lifted from, instead of
    dumping it into the backpack. The lift origin is captured at pickup as
    transient per-client state (not item tags, which would break stack-merge
    equality); equip-origin and a gone/full origin fall back to the pack.
  - The bank item cap now counts the WHOLE bank tree (nested bags included), so a
    player can't nest containers to bypass the limit (Source-X deep count). A
    drop anywhere in the bank tree is measured from the bank root.

CHANGED - Use:
  - Double-clicking an item INSIDE a container now verifies reach through the
    container's top parent (Source-X CClientUse), closing a gap where a crafted /
    stale DClick could use an item in a far or moved container. Loose ground
    items keep their existing tile-distance check; GM bypasses.

================================================================================

[2026-06-30] - Item/inventory rule parity: move gate, equip gate, container cap, ground-drop args, decay veto (Wave 164)
----------------------------------------

ADDED - Inventory rules:
  - Central ItemMoveRules.CanMove gate (Source-X CChar::CanMoveItem): a frozen or
    dead mover can't lift, and ATTR_MOVE_NEVER items (corpses, static furniture)
    never drag. Wired into the packet pickup path; GM bypasses.
  - Central Character.CanEquip gate (Source-X CChar::CanEquipLayer): validates the
    target layer is a real wearable slot and the wearer meets the item's REQSTR.
    Wired into the player equip path (GM actor bypasses); a per-instance
    OVERRIDE.REQSTR tag overrides the ITEMDEF requirement.
  - Per-container OVERRIDE.MAXITEMS: a container's item cap (e.g. a small pouch, a
    quest box) now overrides the global default when dropping into it.

CHANGED - Drop / decay:
  - @DropOn_Ground now passes the drop point as ARGN1/2/3 (x/y/z) and a DECAY
    local (seconds); a script can relocate the drop (re-validated against map
    bounds + reach) or set a custom rot timer. RETURN 1 still bounces the drop.
  - Item decay now consults @Timer before destruction (Source-X CItem: decay IS
    the item timer). RETURN 1 keeps the item and re-arms a fresh decay window so a
    script can defer/cancel the rot; items with no handler decay as before.

================================================================================

[2026-06-30] - Death/corpse parity III: death shroud, resurrection robe, bonded-pet ghost, death-broadcast centralization (Wave 163)
----------------------------------------

ADDED - Death / Resurrection:
  - Death shroud: a Move_Never, Newbie robe is equipped on the ghost's Robe layer
    at death (the real robe has already dropped to the corpse), so the body shows
    the shroud and the player keeps a covering through resurrection. It rides the
    existing ghost appearance broadcast — no separate packet. Gated by the static
    DeathEngine.EnableDeathShroud flag (Source-X CChar::Death).
  - Resurrection robe: when no body covering is restored on resurrection (no
    corpse rejoined, or the corpse held no robe), a plain robe is granted so the
    player is not resurrected naked (Source-X Spell_Resurrection). The shroud is
    stripped first so a robe restored from the corpse can reclaim the Robe layer.

CHANGED - Bonded pets:
  - A bonded pet kept alive server-side as a ghost on death no longer receives the
    0xAF death animation + explicit 0x1D delete broadcast. Those forced the mobile
    off ghost-capable observers (staff / dead / SpiritSpeak) only for the
    view-delta to re-add it the next tick — a flicker. The view-delta now owns the
    bonded pet's appearance: it hides the dead pet from plain players (clean
    vanish) and re-draws it on resurrection. Ordinary NPCs, which are deleted
    server-side, keep their delete broadcast unchanged.

CHANGED - Internal:
  - The corpse broadcast + victim-client ghost transition shared by the three
    server death entry points (spell-kill, lifecycle-kill, GM/AI kill) is folded
    into one BroadcastDeathEffects helper — the player-vs-NPC packet order and the
    bonded-pet gate now live in a single place.

================================================================================

[2026-06-30] - Death/corpse parity II: no-corpse summons, two-stage decay, corpse-rez, @Resurrect args (Wave 162)
----------------------------------------

ADDED - Death / Corpse:
  - Summoned creatures and DEATH_NOCORPSE (DEATHFLAGS bit 0x02) leave no corpse —
    they vanish (Source-X MakeCorpse). DEATH_NOLOOTDROP (0x04) makes a corpse but
    keeps the loot off it. Corpses are flagged Move_Never (a corpse is looted, not
    dragged).
  - Player corpses now decay in two stages (Source-X): the first decay turns the
    corpse into a bones pile its owner can no longer rejoin and which keeps its
    contents for a second window; only the second decay scatters the loot. NPC
    corpses are unchanged (single decay, contents deleted).

FIXED - Resurrection:
  - Resurrecting onto a corpse is gated like Source-X FindMyCorpse: the corpse must
    be top-level (not in a container), not flagged NOREJOIN (a decayed bones pile),
    in line of sight, and owned by the resurrecting character.
  - @Resurrect now passes the character's own corpse (ARGO) and the post-rez hit%
    (ARGN1, default 50); a script may override the hit% via ARGN1. RETURN 1 still
    blocks the resurrection.
  - Healing and Veterinary can now target a corpse to resurrect its dead owner
    (Source-X corpse-target Skill_Healing), not only a living character.

FIXED - Tests:
  - Tightened a fishing gathering-test assertion that over-matched an unrelated
    RNG message, removing an intermittent test failure.


----------------------------------------

ADDED - Gathering:
  - Mining requires a pickaxe, lumberjacking an axe, and fishing a fishing pole
    (in hand or pack); the tool wears out and eventually breaks (Source-X). You can
    no longer gather barehanded.
  - A resource node now keeps the resource it rolled on the first strike instead of
    re-rolling (iron→gold) on every swing, and its regen timer no longer starts at
    creation — a partly-mined vein is no longer reset early. The depletion pool is
    stored as a full int.

FIXED - Crafting:
  - Recipe resources can now match by item TYPE (Source-X RES_TYPEDEF — e.g. "any
    ingot"), not just a specific BaseId. A t_* resource used to resolve to a type
    id stored as a bogus BaseId, making the recipe uncraftable; type resources now
    count/consume any item of that type across sub-containers.
  - The crafting menu is paginated (Prev/Next), so recipes past the first screen are
    reachable — long recipe lists were silently cut off at y>390.

FIXED - Smelting / Delivery:
  - Custom ore can smelt to a non-standard ingot via TAG.SMELT_TO (on the ore item
    or its itemdef); standard ore still uses the iron-ingot graphic with the ore
    hue carried over.
  - A gathered/smelted item now drops to the ground when the backpack is full
    instead of being silently lost (the old path "added" it to a full 500-item pack
    — a no-op — then told the client it was there, a desync). Source-X ItemBounce.

[2026-06-30] - Speech/command security + parity: privilege gate, speech cancel, greeting memory (Wave 160)
----------------------------------------

SECURITY - Commands:
  - Setting a property via the command prefix now requires staff privilege
    (Counsel+). A plain player could previously escalate or cheat by typing
    ".GM=1", ".INVUL=1", ".MAGERY=1200", ".STR=5000", ".NAME=...", ".EVENTS=..."
    etc. — the "verb=value" and object-verb fallbacks called TrySetProperty before
    any privilege check (Source-X Event_Command → CanUsePrivVerb). PRIVLEVEL /
    PLEVEL / FLAGS remain unsettable at any level.

FIXED - Speech:
  - A speaker's @Speech self-trigger RETURN 1 now cancels the utterance (Source-X
    Event_Talk): no nearby broadcast and no NPC hear. ProcessSpeech's result was
    previously ignored, so the speech always went out.
  - Being hidden/invisible no longer stops a listener from hearing a whisper
    (Source-X CanHear has no such gate) — a hidden GM still hears.

FIXED - NPC hearing:
  - @NPCHearGreeting now fires only on FIRST contact with a speaker (Source-X
    NPC_OnHear records MEMORY_SPEAK), not on every line. It used to fire each
    utterance, so a RETURN-1 greeting could swallow all of a player's speech and
    the NPC re-greeted endlessly.



FIXED - Smelting:
  - Smelting now carries the ore's hue onto the ingot, so a coloured/special ore
    (valorite, verite, …) produces its matching coloured ingot instead of always
    becoming plain iron (coloured ingots share the iron ingot graphic and differ
    only by hue).

FIXED - Gathering / Triggers:
  - @ResourceTest RETURN 1 now blocks the gather, and @ResourceGather RETURN 1
    cancels the reap; @ResourceGather can also override the reaped item id (ARGN1)
    and amount (ARGN2). Resource triggers now copy the script's ARGN1/2/3 mutations
    back (same mechanism as the char-trigger path) — previously their return value
    and arg changes were ignored.

FIXED - Gathering / Terrain:
  - Fishing now requires a water tile and Lumberjacking a tree at the target
    (Source-X CWorldMap), so you can no longer fish on dry land or chop wood on
    open ground. Mining already validated its tile. The checks stay permissive when
    no map tiledata is loaded.



FIXED - Stable:
  - Claiming a stabled pet no longer loses it when the owner's follower cap is
    full. The pet is only removed from the stable AFTER ownership is successfully
    re-assigned; if the cap check fails the temp NPC is deleted and the pet stays
    stabled (Source-X CCharNPCAct_Vendor). Previously it was removed from the
    stable list first and then spawned ownerless into the world.

FIXED - Vendor:
  - The server buy price now comes from the selected stock entry's own PRICE tag
    instead of the first same-BaseId entry's price. Two stock entries sharing a
    BaseId at different prices could otherwise let the server charge a different
    price than the client's selected row.
  - The per-item @Buy / @Sell trigger now fires BEFORE the transfer and RETURN 1
    vetoes that line (Source-X). It used to fire after the trade had already
    completed, so its return value was ignored.

FIXED - Secure trade:
  - @TradeAccepted now fires BEFORE any item changes hands; RETURN 1 from either
    party cancels the whole trade and returns the items to their owners (Source-X
    CItemContainer). It used to fire after the swap, so the cancel was a no-op.



FIXED - Ships:
  - Ship components are now classified by their ITEMDEF TYPE (tiller / plank /
    side / hold) instead of always being ShipOther, so steering, boarding planks
    and the cargo hold work on a freshly-placed ship.
  - Diagonal ship commands (SHIPFORELEFT / FORERIGHT / BACKLEFT / BACKRIGHT) move
    the ship diagonally again — the movement direction is no longer collapsed to
    the 4 cardinal directions (only the facing is 4-way).
  - Deck-object selection now gates on Z as well as the XY footprint (Source-X
    CCMultiMovable): an object well above or below the deck (a swimmer under the
    hull, a bird overhead) is no longer dragged along with the ship.

FIXED - Housing:
  - Custom-foundation houses are restored on world load: DeserializeFromWorld now
    reads MultiCustom items, not only Multi. A customized house no longer falls out
    of the house registry after a restart (which left it to decay unmanaged).
  - Persisted lockdown/secure entries are loaded without re-running the live
    capacity checks, so a saved house that holds more than the current
    MaxLockdowns/MaxSecure (config or storage changed since the save) no longer
    silently loses the overflow entries.



FIXED - Regions / Flags:
  - RegionFlag numeric values now match Source-X CRegion.h (NoMagic=0x1, Gate=0x8,
    NoBuild=0x80, Announce=0x200, Underground=0x800, NoDecay=0x1000, Safe=0x2000,
    Guarded=0x4000, NoPvP=0x8000, Arena=0x10000, NoMining=0x20000, plus the
    anti-magic / ship / house / inherit bits). A legacy numeric FLAGS=04000 from a
    Source-X / mortechUO script is now read as Guarded instead of Safe. SphereNet-
    specific flags (Mark/SafeZone/NoPeraCrime/Jail/RedZone/GuardedOff) moved to high
    bits so they never collide with a real REGION_FLAG_* value. All code references
    the flags by name, so behaviour is unchanged for symbolic FLAGS lists.
  - Added the NODECAY region flag: a loose ground item in a NODECAY region no longer
    decays (Source-X REGION_FLAG_NODECAY); it decays normally once it leaves.

FIXED - Spawns:
  - @Spawn that places the NPC at an explicit position is honored — the engine no
    longer re-randomizes the location over a script-chosen spawn point (Source-X
    CCSpawn).
  - Spawned items/NPCs whose placement is refused (out of bounds) are now deleted
    instead of being tracked as orphans with no sector. PlaceItem/PlaceCharacter
    return a success bool for this.
  - A misconfigured char spawner (no chardef and no spawn group) now reschedules
    its timer instead of re-entering and bailing on every item tick of its sector
    (spawn-tick storm).

FIXED - World commands:
  - The RESPAWN and RESTOCK console/IPC/panel commands are now wired to real
    implementations: RESPAWN tops every char/item spawner up to its max
    (independent of sector sleep), RESTOCK fires @NPCRestock on every vendor. They
    previously printed "requested" and did nothing.



FIXED - Notoriety / Murder:
  - The murderer (red) threshold is now strictly greater-than MurderMinCount
    (Source-X Noto_IsMurderer: murders > count), so with the default 5 the red
    title appears on the 6th kill, not the 5th. It was flagging red one kill early.
  - A PvP kill now only counts as a murder when it is an UNPROVOKED kill of an
    innocent (Source-X Noto_Kill: NotoThem < NOTO_GUILD_SAME). Killing a criminal
    or murderer (grey/red), or someone who aggressed the killer first (the killer
    holds a HarmedBy memory of the victim → self-defence), no longer adds a murder
    count or criminal flag. Previously every player-vs-player kill was marked as
    murder + criminal.

FIXED - Notoriety / Fame:
  - The artificial 200-point per-kill fame cap is removed (Source-X Calc_FameKill
    has no per-kill cap, only the 0..10000 running-total clamp, which is still
    applied). Killing a high-fame player now grants the full fame/10.



FIXED - Death / Triggers:
  - @Death and @Kill RETURN 1 are now honored. The firing is centralized in
    DeathEngine.ProcessDeath (every death entry point — combat, spell, NPC, GM,
    reactive-armor — routes through it): @Death RETURN 1 cancels the death entirely
    (no corpse, the victim stays alive), @Kill RETURN 1 skips that killer's credit
    (notoriety/karma/fame/murder/experience) while the death still proceeds.
    Previously the triggers were fired-and-ignored at each call site. An
    invulnerable character also no longer dies (Source-X STATF_INVUL).

FIXED - Death / Forensics:
  - Corpses now stamp DEATH_TIME (and CORPSE_SLEEPING when applicable), and carving
    sets CORPSE_CARVED — the exact tags the Forensics skill reads. Carving used to
    write a "CARVED" tag that was never read back, so Forensics always reported a
    corpse as un-carved with an unknown time of death.

FIXED - Death / Loot retention:
  - Corpse loot transfer now also keeps move-never, no-trade and stays-with-owner
    (cursed2) items on the owner, in addition to blessed/newbie/no-drop. A
    move-never item could previously land in a lootable corpse where no player
    could pick it up.

FIXED - Death / Looting crime:
  - Looting another player's corpse is now wired into the item-pickup path and
    flags the looter criminal (Source-X CheckCorpseCrime). It is correctly gated:
    only a still-present, innocent player owner makes looting a crime — monster
    corpses (the NPC is deleted on death) and criminal/own/party/guild-shared
    corpses are free to loot. The IsLootingCriminal helper was computed but never
    applied at pickup before.



FIXED - Items / Equipment layers:
  - The Layer enum now matches UO LAYER_TYPE numbering: LAYER_FACE=15 and
    LAYER_BEARD(FacialHair)=16 were swapped, which put beards on the face layer
    on the wire and mis-mapped legacy LAYER_BEARD=16 imports onto the Face slot.
    Character-creation beards and legacy beard imports now land on the correct
    layer. LAYER_DRAGGING=31 is defined for numeric parity (dragging is still
    tracked via the DRAGGING tag); the equip-layer validation still rejects the
    internal layers (31+), so client-equippable layers are unchanged.

FIXED - Items / Stack split:
  - Splitting a stack now produces a full clone of the original for the
    left-behind remainder (Source-X CreateDupeItem): custom tags, attribute flags
    (stored as tags), durability/quality, price/link, MORE/MOREB/MOREP, TDATA
    overrides, decay time and the runtime EVENTS list all travel with the split.
    Previously only id/hue/amount/type/name/more1/more2/direction were copied, so
    that state was silently dropped from the remainder.
  - Two piles now only merge when their tag sets match (Source-X CItem::Stack
    compares m_TagDefs and the ATTR_* flags, which SphereNet stores as tags), so
    stacking no longer silently drops one stack's tags.



ADDED - NPC AI / Config:
  - NPCAI, NPCHealThreshold and NPCWanderLookAroundChance are now read from the
    .ini. The global NPC_AI_* flag set is seeded from NPCAI instead of being
    hardcoded; the default stays PATH|COMBAT|PERSISTENTPATH|THREAT (0x0C41) so
    existing worlds keep pathfinding/threat. Set NPCAI=0 for bare Source-X.
  - Per-character OVERRIDE.NPCAI is honored over the global NPCAI mask, so the
    looting/food/path/threat behaviour can be set on a single creature (Source-X
    CCharNPC::GetNpcAiFlags). Looting, IntFood, Path, PersistentPath and Threat
    checks now resolve the per-character flags.

FIXED - NPC AI / Pathfinding:
  - A* pathfinding is gated on intelligence: a creature with effective INT < 30
    no longer routes around obstacles (it faces the target and steps as the line
    opens). NPC_AI_ALWAYSINT bypasses the gate (treated as INT 300). Source-X
    NPC_Pathfinding parity.

FIXED - NPC AI / Special attacks:
  - Breath and rock-throw now require FULL stamina (Source-X
    Stat_GetVal(DEX) >= Stat_GetAdjusted(DEX)) instead of half, so they fire on
    the opening exchange / after a rest rather than every tick.
  - Ogre/ettin/cyclops bodies throw rocks by default when carrying a throwable
    rock (ItemType.ARock/Rock) in their pack; the default throw range is 2-9 (was
    2-8). THROWOBJ/THROWRANGE/THROWDAM tag throwers are unchanged.

FIXED - NPC AI / Spellbooks:
  - NPCs derive their spell list from any spellbook type (necro/paladin/bushido/
    ninjitsu/arcanist/mystic/mastery/extra), reading the book itemdef's spell
    window (TDATA3 offset, TDATA4 max spells) instead of assuming the classic
    64-bit magery book. The classic book remains the fallback when undefined.
    Source-X NPC_AddSpellsFromBook parity.

FIXED - NPC AI / Triggers:
  - @NPCActCast RETURN 1 now aborts the cast and reverts to melee instead of
    being consumed as a successful action (Source-X NPC_FightMagery). The trigger
    receives ARGN1=spell and LOCAL.HealThreshold (from NPCHealThreshold) and may
    override the cast spell via ARGN1.
  - @NPCActFight now receives ARGN1=distance and ARGN2=motivation with ARGO=target
    (was the target UID in ARGN1). A script may flip motivation (ARGN2 readback),
    force a cast via LOCAL.skill + LOCAL.spell, or fully handle the action with
    RETURN 1.
  - Trigger ARGN1/ARGN2/ARGN3 mutations made by a script are now copied back to
    the engine after each character trigger block (was lost before; LOCAL.* was
    already shared), so @NPCActFight/@NPCActCast/@HitTry readbacks work.

[2026-06-30] - Skill/spell parity: flags, caps, gain, Discordance, spells (Wave 151)
----------------------------------------

FIXED - Skills / Definitions:
  - SKILLDEF now parses RANGE (gather use-distance) and PROMPT_CLILOC (target
    cursor cliloc); both were silently dropped before.
  - SkillFlag bit values now match Source-X SKF_* (GATHER=0x400, DISABLED=0x800,
    plus IMMOBILE/SELECTABLE/NOMINDIST/NOANIM/NOSFX/RANGED). The previous values
    diverged from 0x10 up and invented NOFAIL/RANKEDTITLE, so a script FLAGS bit
    was misread. SKILLDEF FLAGS now also accepts symbolic, pipe-delimited values
    (e.g. "skf_gather|skf_ranged"), like SPELL/CHARDEF already did.
  - Startup and ReSync logs now report how many spells/itemdefs/chardefs and
    skilldefs were loaded.

FIXED - Skills / Caps and gain:
  - The skill success S-curve variance is now 100 (Source-X SKILL_VARIANCE),
    not 150.
  - Per-character OVERRIDE.SKILLCAP_<n> and OVERRIDE.SKILLSUM tags are honored
    over the skill-class and global caps.
  - Skill gain no longer advances inside Safe regions (Source-X parity).

FIXED - Magic / Skills:
  - Casting awards the caster's casting-skill a gain attempt on every resolved
    cast, success or fizzle (previously the casting skill never advanced).
  - @SkillUseQuick now fires AFTER the success roll with ARGN3 = result; a script
    can flip the result or cancel the use (RETURN 1). It used to fire before the
    roll and could only hard-cancel.

ADDED - Skills:
  - Discordance/Enticement is now a working skill: it requires a musical
    instrument and a Musicianship+Discordance check, then temporarily lowers the
    target's defenses.
  - Crafting tools take durability damage on each craft attempt.

ADDED / FIXED - Magic:
  - Magic Lock / Unlock now lock and unlock containers and doors; Magic Trap /
    Untrap set and clear the trapped state; Arch Protection and Mass Curse now
    apply their group buff/curse (they were inert).
  - Reactive Armor now reflects a quarter of spell damage back at the caster, as
    it already did for melee.
  - A harmful spell cast on an innocent player now flags the caster criminal,
    matching a melee attack.
  - Teleport now validates the destination (off-map / impassable tile) like
    Recall and Gate, so it can no longer strand the caster in a wall.

[2026-06-30] - Combat parity: flags, scriptable damage/parry, ammo and skill overrides (Wave 150)
----------------------------------------

ADDED - Combat / Flags:
  - Several COMBATFLAGS that were defined but inert now affect the swing path:
    NODIRCHANGE (the attacker no longer auto-rotates to face the target),
    ARCHERYCANMOVE (archers may fire without waiting out the post-move settle
    delay), PARALYZE_CANSWING (a paralyzed/frozen attacker can keep swinging;
    sleep still blocks), and STACKARMOR (a body region covered by several worn
    pieces sums all their AR instead of only the primary layer's).
  - NPC_BONUSDAMAGE: a Damage Increase % (INCREASEDAM tag, Source-X
    PROPCH_INCREASEDAM, capped at ±100%) applies to players always and to NPCs
    only when this flag is set.

ADDED - Combat / Scripting:
  - @Hit, @GetHit and the weapon/armor item hooks now fire after armor/parry but
    BEFORE damage is applied to HP, so a script can raise, lower or cancel the
    damage via ARGN1 (RETURN 1 cancels). These fire from one shared pipeline used
    by both the player and NPC swing paths; previously they fired after HP was
    already applied, where a script could observe but not modify the damage.
  - The NPC swing path now fires @HitTry (adjust swing delay / abort) and
    @HitCheck (force a miss) before resolving, matching the player path.
  - Parry is now scriptable: a successful parry fires @HitParry and may let part
    of the damage leak through (ARGN1), instead of always being a full block.
  - Weapon skill override: a weapon may declare a non-default combat skill via
    TAG.OVERRIDE_SKILL (e.g. wield a blade with Fencing).

ADDED - Combat / Ranged:
  - Ranged weapons honor their ITEMDEF AMMOTYPE (which exact ammo item is
    consumed) and AMMOANIM (the in-flight projectile graphic). When a weapon def
    specifies neither, the legacy arrow-for-bows / bolt-for-crossbows defaults
    still apply.

[2026-06-29] - World object unlink, region cache and ground stack merge fixes (Wave 149)
----------------------------------------

FIXED - World / Object lifecycle:
  - High-level delete paths (script REMOVE, equipped-layer FINDLAYER().REMOVE,
    container DELETE/EMPTY, and consumable item use such as food, deeds, drinks
    and seeds) called the low-level Item.Delete() flag-set, which only marked the
    item deleted and cleared its own contents. The item stayed registered in the
    world object table (its serial was never freed) and, for worn items, lingered
    in the wearer's equipment slot — dead references that inflated object counts
    and could surface as a deleted item still counting toward equipment/weight.
    These paths now route through the full world removal, which unlinks the item
    from the object table, parent container, equipment slot and sector. Object
    teardown also clears the wearer's layer when an equipped item is removed.

FIXED - World / Regions:
  - FindRegion cached lookups per 8x8 tile cell but also stored null (no-region)
    results. Region edges are not 8-aligned, so a single cell can straddle a
    boundary; a region-less query then poisoned the cell and a later in-region
    query for another tile in the same cell wrongly returned null. This could let
    region-gated behavior (guarded, no-PvP, no-magic, enter/leave, gathering,
    weather) lapse near boundaries. Only successful lookups are cached now;
    non-region tiles re-resolve, and cached hits stay self-validating.

FIXED - Items / Inventory:
  - Merging a dragged stack onto a target stack that sits on the ground sent a
    container-content packet addressed to the ground item itself, desyncing its
    amount label. The merged ground pile now broadcasts a world item update, and
    any overflow remainder drops to the ground beside it instead of being lost.

FIXED - Host / Stability:
  - The Host had no last-resort handler for unhandled exceptions on background
    threads. The child server's output is mirrored to the console on an
    AsyncStreamReader thread with no surrounding try/catch, so when the console
    handle became invalid mid-run (an interactive/RDP session disconnecting, or
    redirected stdout) the resulting IOException went unhandled and terminated
    the Host, taking the child game server down with it and leaving only a
    0xe0434352 in the event log. Console mirroring now swallows IOException and
    InvalidOperationException, and a global handler persists any unhandled
    exception to logs/host-crash.log.

FIXED - Memory:
  - Server and Host now build with Workstation GC instead of Server GC. On the
    low-core/low-RAM VDS deployment target Server GC keeps a per-CPU heap and
    holds committed segments, so RSS climbed steadily under a steady allocation
    rate and read as a leak even with a single player.
  - NetState releases its grown per-connection send and crypt scratch buffers
    back to their initial sizes when a connection is cleared, so a transient
    large broadcast no longer pins that memory for the life of the slot.

SECURITY:
  - Pinned the native SQLite binary (SQLitePCLRaw.lib.e_sqlite3 3.50.3) above
    the 2.1.11 pulled in transitively by Microsoft.Data.Sqlite, clearing
    advisory GHSA-2m69-gcr7-jv3q (NU1903). The package is native-only, so the
    managed SQLitePCLRaw core/provider ABI is unchanged.

[2026-06-15] - Stack splitting and item weight system fixes (Wave 148)
----------------------------------------

FIXED - Items / Inventory:
  - Partial stack pickup marked the newly-created serial as the dragged item,
    while the client continues dropping with the clicked source serial, matching
    Source-X / ServUO behavior. That mismatch made the split-off item disappear
    and produced 0x28/05 "item does not exist" responses. The clicked serial now
    remains held with the picked amount, and a new leftover stack is created at
    the old container/ground location.
  - Stack-on-stack merge no longer relies on setting an item amount to zero,
    which was clamped back to one and could leave a phantom item. Fully moved
    stacks are removed from the world; overflow remains as a positive amount.
  - Ground pile items such as arrows, gold and bandages no longer get FLIP/DIR
    visual changes on drop. They keep the base graphic, and SA clients receive
    the 0xF3 world item packet with correct direction and amount fields.

FIXED - Items / Weight:
  - ITEMDEF WEIGHT parsing now matches Source-X: integer values are whole stones
    converted to internal tenths (WEIGHT=2 => 20), and decimal values parse
    correctly (WEIGHT=0.5 => 5). Previously a 1-stone item could display/count
    as 0 stones, and decimal weights were ineffective.
  - Container, bank, trade, status/tooltip and stealing weight calculations now
    use consistent units. Capacity checks compare tenths to tenths, while
    player-facing weight values are reported in whole stones.
  - Items explicitly defined with WEIGHT=0 stay weightless, while undefined
    items use tiledata or a minimal fallback instead of becoming weightless.

[2026-06-14] - Vendor stock no longer doubles on restock (Wave 147)
----------------------------------------

FIXED - Vendor:
  - Repopulating a vendor's virtual stock (SELL= / PopulateVendorStock) appended
    to the existing pack without clearing it, so the periodic @NPCRestock (every
    10 minutes) and any re-run of SELL= over an already-stocked vendor stacked a
    second copy of every row — the same item showing up twice in the buy gump,
    compounding on each restock and inflating the in-memory item count. A restock
    now clears the existing virtual stock before refilling to the template
    levels, matching Source-X NPC_Vendor_Restock.

[2026-06-14] - Two-handed weapons equip on the TwoHanded layer (Wave 146)
----------------------------------------

FIXED - Combat / Equipment:
  - A two-handed weapon (TWOHANDS=Y: bows, crossbows, two-handed swords/axes/
    maces, halberds, …) was equipping on the OneHanded layer because UO tiledata
    marks some of them there. The client picks the attack animation from the
    equipped layer, so a bow on OneHanded swung like an empty-handed punch and a
    two-handed melee weapon used a one-handed swing. Character.Equip now promotes
    such weapons to the TwoHanded layer (Source-X LAYER_HAND2) and the worn-item
    packet reports the actual layer.
  - The swing animation's one/two-handed choice now keys on IsTwoHanded
    (itemdef TWOHANDS) rather than the layer alone, so a two-hander animates
    correctly even if a save predates the layer promotion. The unmatched weapon
    case now falls back to BASH, matching Source-X GenerateAnimate.

[2026-06-14] - Honor the picked starting city on character creation (Wave 145)
----------------------------------------

FIXED - Character creation:
  - The starting city the player picked was ignored — new characters always
    spawned at city index 0 (Yew), so choosing Britain still dropped you in Yew.
    The create-character packet (0x00/0xF8) now reads the city index, and the
    character spawns at the matching city from the 0xA9 list the client was shown.

[2026-06-14] - Pile item display: amount label + ore-only graphic ramp (Wave 144)
----------------------------------------

ADDED - Items / Display:
  - Stacked pile items now show their amount on the single-click label and the
    property tooltip ("1234 gold coins"), via Item.GetDisplayName, matching
    Source-X CItem::GetNameFull. Single items (and non-piles) show just the name.

FIXED - Items / Graphics:
  - Only ore now changes graphic with stack size (ITEMID_ORE_1..4 per Source-X
    CItem::SetAmount); arrows, bandages, ingots, gold and other piles keep their
    base graphic and let the client render the count. The previous DUPELIST-based
    amount ramp wrongly reskinned stacked arrows/bandages, whose DUPELIST is a
    flip/alias list, not a size ramp. Also fixes an ItemType<->DispIdFull
    recursion the ore check would otherwise trigger.

[2026-06-14] - Armed-creature double hit sound fix (Wave 143)
----------------------------------------

FIXED - Combat / Sound:
  - An armed creature played both its own CharDef attack sound and the weapon
    hit sound on a hit (doubling up). Per Source-X SoundChar(CRESND_HIT) an armed
    strike makes only the weapon sound, so the creature attack vocalization now
    falls back only when the attacker is unarmed; an armed creature plays just the
    weapon sound.

[2026-06-14] - Defender pain sound, absorbed-hit feedback, parry spark (Wave 142)
----------------------------------------

ADDED - Combat / Sound:
  - A struck target now plays its pain vocalization (Source-X CRESND_GETHIT):
    creatures use their CharDef SOUNDGETHIT, humans use the gendered "oomf" set
    (male 0x154-0x159, female 0x14B-0x14F). Emitted on a damaging hit from both
    the player and NPC paths; players previously had no get-hit sound at all.
    The NPC creature get-hit emission was unified into the hit feedback so it now
    covers creature and player targets alike without doubling.

FIXED - Combat / Hit resolution:
  - A connecting hit that armor fully absorbed (0 damage) was reported as a miss,
    so it played the miss whoosh with no get-hit animation. ResolveAttack now
    returns -1 for a true miss/parry and 0 for an absorbed hit, so the player and
    NPC paths play the weapon hit sound and get-hit animation for an absorbed hit
    (no pain sound, damage number or blood), matching Source-X Fight_Hit.

ADDED - Combat / Visuals:
  - A successful parry/block now shows a spark on the defender (ModernUO 0x37B9);
    previously a parry produced no client feedback at all.

FIXED - Combat / Sound:
  - The NPC weapon hit sound is now only played when the attacker is armed, so an
    unarmed creature no longer overlays a human fist sound on top of its own
    CharDef attack sound.

[2026-06-14] - Per-weapon hit/miss combat sounds (Wave 141)
----------------------------------------

CHANGED - Combat / Sound:
  - Melee and ranged swings now play a per-weapon hit or miss sound instead of a
    single generic thud/clang. Hit sounds follow Source-X SoundChar(CRESND_HIT)
    by weapon class (mace 0x233, war axe 0x232, two-handed sword/axe 0x236/0x237,
    one-handed sword/axe and fencing 0x23B/0x23C, bow/crossbow 0x234, throwing
    0x5D2, whip 0x67E, unarmed 0x135). Miss sounds draw at random from the
    Source-X ranged set {0x233,0x238} or melee set {0x238,0x239,0x23A}.
  - A swing now emits exactly one combat sound (hit OR miss), matching Source-X
    Fight_Hit. The previous unconditional swing whoosh that played on top of the
    hit/miss sound (doubling up) was removed from the player and NPC paths, and
    the forced-miss feedback no longer plays two stacked sounds.

[2026-06-14] - Moving-bolt effect direction (oneDirection) fix (Wave 140)
----------------------------------------

FIXED - Combat / Visuals:
  - The graphical-effect packet (0x70) always wrote the oneDirection byte as
    true, so moving EFFECT_BOLT projectiles (arrows, bolts, energy bolt, magic
    arrow, throwing) did not rotate to face their flight path and looked wrong.
    Source-X writeBasicEffect (and ModernUO MovingParticles) only set
    oneDirection for stationary effects; bolts use false. The direction byte is
    now derived from the effect type (false for type 0 / EFFECT_BOLT, true
    otherwise) across the player ranged shot, the NPC ranged shot, and the
    script/character EFFECT emitters.

FIXED - Combat / Blood:
  - The blood-splatter graphic set was missing 0x1645 (BLOOD_SPLAT); it is now
    included to match the Source-X / ModernUO blood pool.

[2026-06-14] - Combat blood splatter + NPC ranged projectile (Wave 139)
----------------------------------------

ADDED - Combat / Visuals:
  - Landing a hit now scatters 1-2 short-lived blood items around the struck
    target (graphics 0x122A-0x122F), hued by the target's CharDef BLOODCOLOR and
    decaying after 5 seconds. Emitted on both the player and NPC hit paths via a
    shared GameClient.EmitBloodSplat helper; placed as ground items so the
    view-delta renders them.
  - NPC archers now emit the moving arrow/bolt projectile when they fire a bow
    or crossbow (bow -> 0x0F3F arrow, crossbow -> 0x1BFB bolt), mirroring the
    player's ranged effect. Previously only players showed the flying ammo.

[2026-06-14] - Stack-merge gold loss, mining depletion, pile graphics, archery loop + swing anims (Wave 138)
----------------------------------------

FIXED - Inventory / Stacking:
  - Dropping one stack onto a matching stack treated the target stack as a
    container and nested the dragged item inside it (invisible), so stacking
    gold onto gold lost the dropped pile. It now merges the amounts; an overflow
    tops the target off and keeps the remainder beside it.

FIXED - Skills / Mining:
  - The resource-depletion marker tracked its remaining pool in Item.Amount,
    which floors at 1, so a node never reached 0 and yielded ore forever. The
    pool is now stored in a tag, so veins deplete and then regenerate on the
    regen timer.

FIXED - Items / Display:
  - Stackable "pile" items (ore, ingot, log, bandage, scroll, arrow, ...) show a
    larger graphic as the amount grows, driven by DUPELIST (Source-X parity).
    Gated on pile-ness so DUPELIST used for animation/variant frames on non-pile
    items is unaffected. Computed in DispIdFull, so every packet path and the
    view-delta refresh the visual when the amount changes.

FIXED - Combat:
  - Firing a ranged weapon with no ammo returned without advancing the swing
    timer, so the combat tick re-entered every server tick — spamming the
    no-ammo message and looping. It now paces by the swing timer like Source-X.
  - Swing animation by weapon type: axe and pickaxe SLASH (were on the
    pierce/bash paths) and throwing weapons use the 1H-slash gesture, matching
    Source-X CChar::GenerateAnimate.

CHANGED - Admin:
  - Serv.AllClients runs a function-form payload (e.g. f_Admin_GetPlayers) as a
    script function first, before trying it as a verb, with the issuer as SRC.
    Added an [allclients] diagnostic for the online-player tally.

[2026-06-14] - Console memory cap, vendor double-stock, item name fallback, spell power words (Wave 137)
----------------------------------------

FIXED - Console / Memory:
  - The GUI console's pending-line queue had no cap. With packet debug logging
    on, lines were enqueued faster than the UI thread could render them, so the
    backlog grew without bound and process RAM climbed into the gigabytes over
    hours. The queue is now capped; lines past the cap are dropped and the count
    is surfaced in the console.

FIXED - Vendors:
  - Opening a vendor whose stock was empty ran BOTH the persisted-tag rebuild
    and the @NPCRestock script trigger, spawning the SELL list twice — every buy
    row appeared doubled. The trigger now only fires when the rebuild produced
    nothing.

FIXED - Items:
  - Item.GetName falls back to the tiledata tile name (resolved and pluralized)
    when neither a per-instance name nor an itemdef NAME= is set. An item such
    as i_bottle_empty (no NAME=) had been reaching the client with an empty
    name, so the client rendered its own raw tiledata string with the plural
    marker intact ("empty bottle %s%").

FIXED - Magic:
  - Spell power words spoke the legacy letter abbreviation ("IL") for spells
    whose RUNES only carry it. GetPowerWords now uses the spoken word form when
    present and otherwise falls back to the canonical UO mantra ("In Lor"), so
    the player and NPC cast paths read the same words.

[2026-06-14] - NPC casting, creature attacks, packet ordering, view-sync, character creation (Wave 136)
----------------------------------------

FIXED - Magic / NPC AI:
  - NPC casters were exempted from the reagent-availability gate; an NPC with
    an empty pack could never start a cast (every attempt returned failure).
  - NPCSPELL now parses a spell defname/enum name, not only a numeric id;
    numeric spell-type values are range-checked and cast to the enum's ushort
    base before Enum.IsDefined (a numeric NPCSPELL/SPELL value crashed the world
    loader with an ArgumentException).
  - Spell selection no longer re-casts Paralyze on an already-frozen target and
    enforces a per-caster Paralyze cooldown, so a caster stops looping on
    Paralyze (including against a target that breaks free instantly) and unloads
    damage between locks.
  - Self-heal chance is a flat 33% at any wound level, so a critically hurt
    caster spends ~2/3 of its casts on offense instead of turtling.
  - Mana regen uses a flat base amount instead of INT/50; a high-INT caster no
    longer regenerated mana as fast as it spent it and now actually runs dry.

FIXED - Combat / Creatures:
  - Venomous NPCs (Poisoning skill > 0) envenom on a melee hit from the skill
    alone, with no poisoned-weapon tag required.
  - Special movement trail: giant spiders web the ground and fire elementals
    drop fire patches (FIELD_DAMAGE) as they act; driven by body id or the
    WEBTRAIL/FIRETRAIL tag. Fire-elemental body id corrected to 0x000F.

FIXED - Network / packet ordering:
  - The 0x2E worn-item packet no longer sets the 0x8000 hue-flag bit on the
    graphic (that convention belongs to the 0x78 equipment list); a worn item
    with a non-zero hue was sent with an invalid graphic and vanished on the
    client until a resync.
  - Vendor buy/sell lists (0x74/0x9E) and spellbook content (0xD4) are Normal
    priority again. The send queue drains Highest->Low, so a Low member of an
    order-critical sequence flushed after the Normal open-container packet that
    consumes it: vendor rows showed 0gp with no name and spellbooks opened
    empty.

FIXED - View-delta / appearance sync:
  - Dyeing a worn item, and hair/beard dye, now broadcast the recolour; the
    ground-item view-delta never tracks worn items, so the change stayed the old
    colour for everyone until a resync.
  - Criminal/murderer state is part of the view-delta visibility key, so a
    stationary attacker turns grey/red on observers without having to move.

FIXED - Character creation:
  - [STARTSGOLD] bare-amount rows are parsed (the amount is the section key);
    new characters receive one stipend (the start-index row) instead of nothing.
  - Item single-click labels and tooltips fall back to the itemdef NAME= when no
    per-instance name was stamped, so plain spawned/looted items show their name.

[2026-06-13] - Ping fast-path processing (Wave 135)
----------------------------------------

IMPROVED - Network / RTT:
  - 0x73 ping packets now bypass the generic packet-buffer copy path and go
    directly through echo/RTT handling.
  - Added regression coverage for the ping fast path through the real
    NetworkManager parser.

























[2026-06-13] - ClassicUO death scene and corpse binding (Wave 134)
----------------------------------------

FIXED - Death / ClassicUO:
  - Player corpse OWNER/KILLER tags are now written before the
    DeathEngine OnDeath callback, so the GameClient death sequence can
    find the corpse by OWNER_UID and bind to the correct serial.
  - The dying player's own client now receives a 0xAF DeathAnimation
    packet with a non-zero corpse serial. This completes the
    death-screen flow that ClassicUO needs to transition mobile ->
    corpse instead of leaving the live sprite stuck or failing to open
    the "you are dead" scene.
  - Added regression coverage for the DeathEngine owner-tag order and
    the self death-animation packet.

[2026-06-11] - @SpellEffectTick bridge; ship @RegionEnter/@RegionLeave; shared trigger LOCAL pool (Wave 133)
----------------------------------------

ADDED - Scripting engine:
  - Shared trigger LOCAL pool (Source-X CScriptTriggerArgs.m_VarsLocal
    parity): TriggerArgs can now carry one VarMap that every ON=@X
    block in a trigger chain uses as its scope locals — the engine
    seeds values before the fire and reads script writes back after.
    Functions keep their own locals (Source-X call semantics).
  - @SpellEffectTick (SPELLFLAG_TICK) fires from the native poison
    tick: ARGN1 = spell id, ARGN2 = strength on the Sphere memory
    scale, ARGO = a read-only spell-memory shim (BASEID = the spell's
    RUNE_ITEM, MOREY = strength, LINK = poisoner) so the reference
    handler pattern IF (<ARGO.BASEID>==I_RUNE_POISON) works verbatim.
    LOCAL.EFFECT/DELAY/CHARGES/DAMAGETYPE are seeded and read back;
    the script damage applies as-is (no second resist pass), zero
    damage skips the application but keeps the schedule, RETURN 1
    destroys the effect (cures). IsTrigUsed-gated: unhooked shards
    pay a null check per tick.
  - Item @RegionLeave/@RegionEnter wired with Source-X scope — these
    fire on movable multis, not ordinary items: ShipEngine.MoveDelta
    checks the multi anchor's region BEFORE anything moves; on a
    boundary cross the triggers fire on the ship multi with ARGO =
    region and SRC = pilot, and RETURN 1 blocks the whole step.
    Installed only when hooked.

FIXED - Networking (minor):
  - NetState.SendRaw built its buffer one WriteByte per byte; now a
    single block copy (cold path — script SENDPACKET verb).

[2026-06-11] - NPC attack-engagement emote (Wave 132)
----------------------------------------

FIXED - NPC AI / Combat:
  - The "*X is attacking Y!*" / "*X is attacking you!*" emote pair
    (reference Attacker_Add messaging) only existed on the player
    attack path — an NPC engaging a player produced no announcement
    at all, so an aggressor closing in gave no visual warning. NpcAI
    now raises OnNpcAttackNotify the first combat action against a
    new target (past the flee/leash give-up branches, before the
    first swing), and the server broadcasts the same emote pair as
    the player path: observers get the third-person line, the victim
    alone gets the "attacking you" variant, both over the attacker
    in the emote hue. The notify latch re-fires only on target
    switch and is swept with the other per-NPC AI state.

[2026-06-11] - NPC pathfind fail-backoff; main-loop stall diagnostics (Wave 131)
----------------------------------------

FIXED - NPC AI / Performance:
  - A failed combat-chase A* (unreachable target) burns the entire
    node budget before giving up, and the recompute throttle allowed
    a retry every 750ms — each NPC stuck behind an unwalkable area
    stalled the single-threaded apply phase 40-75ms per attempt
    (2000 nodes at ~35us/node), visible as ~160ms ping spikes.
    Failed computes now back off for 5s (successful ones keep the
    750ms cadence) and the node budget drops to 500, bounding the
    worst case to ~17ms. The direct-step fast path above A* still
    runs every action, so the NPC resumes immediately once the
    straight line opens.

ADDED - Diagnostics:
  - [loop_stall] warning: any main-loop iteration over 50ms logs a
    per-segment breakdown (console/actions, network input, periodic
    jobs, network output, server ticks, yield) plus GC gen0/1/2
    collection deltas, rate-limited to one log per 2s. Catches
    latency the tick telemetry cannot see — ticks cover <1% of
    iteration wall time, so tick_stats can read 0.2ms avg while the
    ping echo spikes.

[2026-06-11] - Panel dialog designer with live MUL-art preview (Wave 130)
----------------------------------------

ADDED - Admin panel / Tooling:
  - GumpArtReader decodes gumpidx.mul + gumpart.mul (per-row RLE,
    16-bit 555) to RGBA; a dependency-free PNG encoder serves the art
    over HTTP with caching.
  - Panel endpoints: /gumpart/{id}.png (gump art, outside /api so
    <img> tags work), /api/dialogs (all loaded [DIALOG] names),
    /api/dialog-source?name= (raw section text read from the script
    file, formatting preserved).
  - GumpDesignerPage rebuilt: real gump art on the canvas (RESIZEPIC
    as a true 9-slice, GUMPPIC/BUTTON/CHECKBOX/RADIO images, tiled
    pics), drag to move, property edits and the exported script update
    live. Existing dialogs load from the server and import onto the
    stage; the coordinate walk mirrors the engine (+N/-N/*N, DORIGIN,
    PAGE) and lines with <expressions> are preserved verbatim.
  - The status port also serves /dialogedit — a standalone
    paste-script → live-preview editor that works without the panel
    build.

[2026-06-11] - Resource markers use the worldgem-bit graphic (Wave 129)
----------------------------------------

FIXED - Gathering:
  - Per-tile resource markers (region pool and fallback) were created
    with the 0x1 "nodraw" graphic, which renders nothing even for
    staff with AllShow — veins could not be seen or inspected. Both
    marker systems now use the reference worldgem-bit graphic (0x1EA7)
    named "worldgem bit"; ATTR_INVIS still hides them from players.
    Consumption itself was already pool-limited per tile with a regen
    timer on both paths.

[2026-06-11] - Character creation: outfit, per-skill kits, setup functions (Wave 128)
----------------------------------------

FIXED - Character creation:
  - NEWBIE race variants ("[NEWBIE MALE_DEFAULT ELF]"/GARG — usually
    empty) were keyed by their first token and overwrote the human
    base section, so MALE/FEMALE_DEFAULT resolved to an empty section
    and fresh characters spawned naked. The race suffix now folds into
    the section identity; the default outfit equips again.
  - Each chosen creation skill now grants its [NEWBIE <SKILL>] starter
    kit on top of the default outfit (unknown names no-op).
  - New player characters run the pack setup chain
    f_onchar_create_player → f_onchar_setup_player (Char_Start_Player:
    skillclass and pack-side start configuration) once they exist
    server-side.

[2026-06-11] - Corpse carving uses the chardef RESOURCES list (Wave 127)
----------------------------------------

FIXED - Death:
  - Carving produced hardcoded hides/meat for every corpse. Per the
    reference, parts now come from the victim chardef RESOURCES list
    (with amounts — "19 i_ribs_raw" works): player-corpse parts are
    renamed "<part> of <victim>" and fall to the ground with decay,
    creature parts go into the corpse. Defs without RESOURCES keep the
    legacy random rolls; a corpse can only be carved once.

[2026-06-11] - NPC cast FX, spell-damage feedback, gated per-action triggers (Wave 126)
----------------------------------------

FIXED - Magic / NPC AI:
  - Spell damage applied silently — no damage number, no health-bar
    update — until the victim died; both packets are now broadcast on
    every spell hit (the melee/breath paths already had them).
  - NPC casts had no completion visuals (the bolt/impact effect lives
    in the player client path); a host hook now emits the same effect
    packet for clientless casters, so NPC spells are visible.
  - Per-action NPC triggers (@NPCActWander/Fight/Follow/Cast,
    @NPCLookAtChar/Item) install only when a script hooks them
    (IsTrigUsed gate) — an idle 20-30K NPC world no longer pays a full
    dispatch walk per wander step.
  - Diagnostics: [npc_cast] logs every NPC cast attempt and
    [death_path] names the kill entry point, for the delayed-death
    report.

[2026-06-11] - Staff drag tiledata-locked ground items; stacking and resource names (Wave 125)
----------------------------------------

FIXED - Items:
  - The client refuses to start a drag on tiledata-locked graphics
    (weight 255 — corpses, anvils) unless the world-item packet
    carries the force-movable flag; staff clients now receive it on
    ground items, so a GM can actually pick a corpse up.
  - Stack merging required the scripted CAN_I_PILE flag, which packs
    rarely declare; the reference seeds it from the tiledata stackable
    flag, now used as a fallback — mined ore/ingots/logs merge in the
    pack again.
  - Gathered resources (mining/fishing/lumberjacking) now carry their
    itemdef display name, so single-click and vendor rows name them.

[2026-06-11] - Full hands exclusion; container click shows contents (Wave 124)
----------------------------------------

FIXED - Items:
  - The hand-slot exclusion only covered the shield cases, so a
    one-hand weapon and a two-handed weapon could be held together.
    A true two-handed weapon now bounces whatever the other hand
    holds to the backpack, and any one-hand-layer equip bounces a
    held two-handed weapon (shields still coexist).
  - Containers and corpses append "(N items, N stones)" to their
    single-click label (CONT_ITEMS defmessage).

[2026-06-11] - Attack announcements per reference; death FX reach the dying client (Wave 123)
----------------------------------------

FIXED - Combat / Death:
  - The "is attacking" announcement formatted the victim-facing
    template with the wrong name and broadcast it to everyone. Per the
    reference both lines now render over the ATTACKER: "*X is
    attacking Y!*" in the emote hue to all observers except the
    victim's client, and "*X is attacking you!*" only to the victim.
  - The death particle and sound were delivered broadcast-only and
    never reached the dying client (confirmed by wire capture); they
    are now sent to it directly, with observers still covered by an
    exclude-victim broadcast.

[2026-06-11] - Swing and surround sidestep no longer share a tick (Wave 122)
----------------------------------------

FIXED - NPC AI:
  - When a melee swing actually fired, the NPC could also take its
    surround/flank sidestep in the same AI tick; the walk packet made
    the client cancel the just-started attack animation (hits looked
    like sliding). The sidestep now only happens on recoil ticks where
    no swing fired — the same move-pair conflict class as the pet
    GO/follow bug.

[2026-06-11] - Single-step descent cap raised to climbable parity (Wave 121)
----------------------------------------

FIXED - Movement:
  - The server-only descent cap (MaxDescendZ=11) rejected walking DOWN
    cave-floor ramps that step 24 units in one tile, while the maxZ
    window accepted the matching climb UP — the ramp was one-way and
    every descent rejection rubber-banded the running player back to
    the top (the "mine exit threw me up" report; one session tallied
    33 descent_cap rejects). The client has no descent limit, so for
    client-driven moves the cap can only create desyncs. Raised to 25
    so anything a mover can climb passes back down; kept as an
    extreme-fall guard for chasing NPCs at true cliff faces.

[2026-06-11] - Zero success chance never rolls a success (Wave 120)
----------------------------------------

FIXED - Skills:
  - Skill_CheckSuccess compared "successChance >= rand(0..999)", so a
    ZERO chance still succeeded when the die came up 0 — a 1-in-1000
    fluke per roll. In game that let skill-0 attempts occasionally
    pass impossible difficulties; in the test suite it was the
    long-standing intermittent failure class in the success-curve
    tests (craft fail-loss, stealth — the Wave 111-era test hardening
    only masked the symptom). A non-positive chance now fails
    outright before the die is cast.

[2026-06-11] - Dying in the saddle dismounts before the corpse snapshot (Wave 119)
----------------------------------------

FIXED - Death:
  - The death pipeline never dismounted a mounted victim, so the
    mount-layer item survived into the death state: the client kept
    drawing a mounted body under the dead character, the ghost
    transition did not show and the mount NPC never reappeared.
    DeathEngine now runs a dismount hook before the corpse is made
    (reference Death → Horse_UnMount order): the mount item is
    removed for observers and the mount NPC is placed back into the
    world. Covers every kill path through ProcessDeath.

[2026-06-11] - SPELLFLAG_* constant names resolve in spell flag parsing (Wave 118)
----------------------------------------

FIXED - Magic:
  - SPELLFLAG_* names are engine constants in the reference
    (game_macros.h), not script defnames — script packs typically
    never declare them in a [DEFNAME] block. Spell FLAGS lines using
    those names silently parsed to None, so no spell ever opened a
    target cursor (every cast self-targeted) and field/area/bolt
    behaviors were lost. The flag parser now falls back to the
    built-in constant table (values match the reference one-to-one);
    script-defined defnames still take precedence.

[2026-06-11] - Region EVENTS entries bind REGIONTYPE resource tables (Wave 117)
----------------------------------------

FIXED - Gathering:
  - Area definitions attach their natural-resource tables through the
    region EVENTS list ("EVENTS=r_default,r_default_rock,…"), and the
    reference scans that list by res-type (FindNaturalResource). The
    region loader only stored those names as plain event links, so
    every region had an empty resource-table list and gathering
    (mining / fishing / lumberjacking) fell back to an arbitrary
    loaded table — often an all-nothing dungeon table, which made
    every dig yield nothing and never spawn the resource marker.
    REGIONTYPE names in EVENTS now also populate the region-type list.

[2026-06-11] - Double-click equips first; vendor buy list reverse order (Wave 116)
----------------------------------------

FIXED - Items / Vendors:
  - Reference Cmd_Use_Item parity: an equippable item that is not
    currently equipped is now armed/worn on double-click — from the
    ground or a container — before its use-type behavior runs (weapons
    and tools used to jump straight to the target prompt and never
    reached the hand). Routes through the real pickup + equip paths so
    reach, ownership, @PickUp_*/@EquipTest/@Equip triggers and
    hand-swap rules all apply; the layer's previous occupant bounces
    to the backpack. Spellbooks and ground light sources keep their
    open/toggle behavior.
  - The 0x74 vendor buy list is now sent in REVERSE of the 0x3C
    container order: the client walks the stock container's item list
    from the tail when decorating rows, so same-order packets paired
    every row with another row's price and name (rows showed 0gp and
    blank names on mismatch).

[2026-06-11] - Pet GO order overrides following; dragon-family breath (Wave 115)
----------------------------------------

FIXED - NPC AI:
  - The "all go" pet order ran the follow step and the GO step in the
    same AI tick, so the two moves cancelled out and the pet oscillated
    between its owner and the ordered spot without arriving. An active
    GO order now overrides following entirely; the pet walks to the
    spot and stays there (reference NPCACT_GOTO). Regression test
    added (NpcPetGoOrderTests).
  - The breath attack only fired for the Dragon brain, fire-immune
    monsters or a BREATH.DAM tag — script packs routinely keep
    BRAIN=brain_monster on dragons and relied on the legacy
    body-derived creature type. Dragon-family bodies (dragons, drakes,
    wyvern, serpentine/skeletal, shadow/white wyrm, swamp dragon,
    ancient wyrm) now breathe regardless of the scripted brain.

[2026-06-10] - Shared death-effects pipeline; DeathCorpse, Reveal, SpellEffectAdd/Remove triggers (Wave 114)
----------------------------------------

FIXED - Death:
  - GM kill paths (UID form, target-cursor form) and the guard
    instant-kill path called the corpse/loot engine directly, skipping
    the client-visible death sequence — corpse packet ordering, the
    per-observer 0xAF dispatch and the victim ghost transition — and
    the @Kill/@Death triggers. The visual sequence was extracted from
    the NPC-kill handler into a shared ProcessDeathWithEffects pipeline
    and every non-combat-handler kill path now runs it; self-kills are
    unattributed (no kill credit, no murder mark).

ADDED - Triggers:
  - @DeathCorpse fires on the victim once the corpse exists and loot
    has been transferred (argo = corpse), per the reference order.
  - @Reveal: hidden/invisible removal is centralized in
    Character.ClearHiddenState (attack, speech, detect-hidden, NPC
    detection, reveal spell, stealth-step expiry); the trigger fires
    before the state drops and RETURN 1 keeps concealment. Speech now
    also drops spell invisibility, matching the reference reveal.
  - @SpellEffectAdd / @SpellEffectRemove fire across the timed-effect
    lifecycle (apply, expiry, re-cast refresh, death cleanup);
    ARGN1 = spell id, SRC = caster on add. The transient save-time
    revert/reapply is deliberately silent.
  - @SpellEffectTick is declared but not fired: the engine has no
    periodic spell-effect (tick-flagged) mechanism yet; documented in
    the trigger-coverage guardrail backlog (char backlog now 3).
  - New static hooks added to the test-isolation reset list. Stub
    inventory doc updated with this audit's false-positive list so
    future scans do not re-flag implemented features. Full suite
    green (783).

[2026-06-10] - Dialog layout engine: string LOCALs, function calls, FORINSTANCES, GUMPPIC/TOOLTIP (Wave 113)
----------------------------------------

FIXED - Dialogs:
  - LOCAL assignments in the layout expansion pass coerced every value
    to a number, destroying comma lists and .= string concatenations
    (a list stored as "0" made every downstream loop bound and lookup
    collapse). Values are now stored verbatim; ++/--/.= forms arrive
    pre-rewritten as expressions by the script-key parser and keep
    working, and +=/-= stay numeric.
  - The layout expression resolver had no script-function resolver, so
    [FUNCTION] calls inside layouts (list helpers used as FOR bounds,
    formatters in text args) never resolved and the affected loop
    bodies dropped out of the rendered gump. Function expressions now
    execute through the trigger runner with the dialog character as
    the target object.
  - Bare function tokens resolved to their resource index through the
    plain-defname fallback instead of executing; Function-typed
    defnames are excluded from that fallback so they reach the
    function resolver.

ADDED - Dialogs:
  - FORINSTANCES expansion: runs the body once per live world instance
    of the given itemdef (matching BaseId or the SCRIPTDEF tag) —
    covers the bare counting pattern. FOR-family block matching added
    so a nested FOR* loop's shared ENDFOR terminator cannot close an
    outer loop early.
  - GUMPPIC was only accepted in its single-P spelling; the canonical
    double-P verb is now an alias of the same renderer. TOOLTIP layout
    op added (cliloc tooltip attached to the previous element, new
    GumpBuilder.AddTooltip). Layout verb coverage for the imported
    dialog pack is now complete. Full suite green (783).

[2026-06-10] - Allocation-free decay catch-up; precomputed trigger-function gate (Wave 112)
----------------------------------------

PERFORMANCE - Tick path:
  - The 5-second decay catch-up sweep materialized a full snapshot of the
    world object table (ToArray + LINQ chain) on every pass; on imported
    worlds with 100k+ objects that is a megabyte-scale transient
    allocation and a periodic GC spike. The sweep now collects expired
    ground items directly into a reusable buffer
    (GameWorld.CollectExpiredGroundItems); collection completes before
    any deletion, so the snapshot-free enumeration is safe in the
    serial post-tick phase.
  - Trigger dispatch paid a name concat + lowercase + argument wrapping
    on every char/item trigger fire to probe the f_onchar_*/f_onitem_*
    function fallback even when no such function exists. The set of
    trigger names that have function fallbacks is now precomputed from
    loaded [FUNCTION] defnames (also covers custom TRIGGER-verb names,
    which the old enum scan missed); the fallback step is skipped
    entirely otherwise. If the cache is never built the gate stays open,
    preserving embedded/test behavior.
  - Script resync now rebuilds the used-trigger cache, so [ON=@X] hooks
    and function fallbacks added or removed by reloaded files take
    effect without a restart. Full suite green (783).

[2026-06-10] - Craft work-site proximity matches map statics (Wave 111)
----------------------------------------

FIXED - Crafting:
  - The Wave 107 forge/heat proximity check only saw dynamic forge/fire
    items, so the built-in static forges of town smithies did not count
    as work sites. The check now also scans the map statics around the
    crafter and resolves each static graphic through its itemdef type
    (forge / fire / campfire) — the same graphic→itemdef resolution the
    reference uses for static tiles. Note: the static branch depends on
    loaded MUL data and is exercised in-game; the unit suite covers the
    dynamic-item path (the existing CraftParityTests). Full suite green
    (783).

[2026-06-10] - Spell disturb follows the INTERRUPT curve (Wave 110)
----------------------------------------

FIXED - Magic:
  - The INTERRUPT spell property was read with plain int parsing — the
    pack's decimal curve values ("INTERRUPT=100.0,100.0") failed
    silently and the field was never used anyway: the disturb chance was
    an invented damage/max-hits ratio clamped to 5-95%. Ported the
    reference semantics (OnTakeDamage disturb block): INTERRUPT is a
    per-mille curve across the caster's skill with legacy fixed-point
    values ("100.0" = 1000 = always disturbable, the reference default),
    the damage amount does not factor into the roll, and only player
    casters are disturbed. INTERRUPT=0 spells are uninterruptible. The
    protection-effect dampening is kept as an engine approximation of
    the reference protection-spell cancel.
  - New tests (SpellInterruptTests, 3): zero curve never disturbs, full
    curve disturbs on any damage, NPC casters exempt. Full suite green
    (783).

[2026-06-10] - Multi-stroke crafting + spellbook-in-hand casting (Wave 109)
----------------------------------------

ADDED - Crafting:
  - Crafting now runs as a stroke loop (reference Skill_MakeItem →
    Skill_Stroke) instead of resolving instantly on the gump click: two
    work strokes spaced by the skill's DELAY-curve timeout (one-second
    reference floor), each broadcasting the craft animation and sound;
    the skill roll and resource consumption happen at completion, after
    a CanCraft re-check — walking away from the forge or losing the
    materials mid-craft fails without the roll and without loss. A
    second craft cannot start while one is pending. Both the crafting
    gump and the SKILLMENU MAKEITEM verb route through the same loop,
    driven from the per-client tick pump.

FIXED - Magic:
  - A wielded spellbook counted as a blocking weapon for the
    equipped-cast check, so casting with the book in hand fizzled when
    EQUIPPEDCAST was off. Spellbooks (like wands) no longer block
    casting in either hand.

INTERNAL - Tests:
  - Two spell tests toggled the global spellbook-requirement static,
    which parallel test collections reset mid-run (intermittent
    failures); they now satisfy the requirement with a real spellbook
    item instead. New tests (CraftStrokeTests, 2) cover the stroke
    delay, the busy guard and the walk-away completion failure. Full
    suite green (780) across repeated runs.

[2026-06-10] - [SKILLMENU] selection menus: load, open, select (Wave 108)
----------------------------------------

ADDED - Script menus:
  - [SKILLMENU] sections (reference Cmd_Skill_Menu) are now retained at
    load and reachable at runtime: the new SKILLMENU client verb opens
    the named section as a 0x7C selection menu — first non-ON line is
    the title, each ON=<itemdef> [text] entry shows the itemdef art
    ("<name>"/empty text falls back to the itemdef name), TEST=SKILL
    value lines gate entries on the character's skills (legacy
    fixed-point values), and the remaining lines run as script verbs on
    selection. Menu selection now falls through from console verbs to
    the character's own property/verb surface, so entry bodies like
    POLY work.
  - New entry verbs: SUMMON <chardef> (reference CV_SUMMON — summons the
    chardef as a temporary summoned pet, duration from the Summon
    spell's curve, two-minute fallback) and MAKEITEM <itemdef>
    (reference SKILLMENU MAKEITEM — crafts through the crafting-engine
    recipe loaded from the itemdef's SKILLMAKE/RESOURCES).
  - POLY now resolves chardef defnames and weighted "{ c_a 1 c_b 1 }"
    lists in addition to numeric bodies (the script pack's sm_polymorph
    entries use both forms).
  - Casting Polymorph or Summon Creature opens the pack's
    sm_polymorph/sm_summon menu when one exists (@SkillMenu fires first
    with the menu name and can veto); without a script menu the normal
    cast flow continues unchanged.
  - New tests (SkillMenuTests, 4): menu open + TEST gating, selection
    running POLY, unknown-menu fallthrough, weighted POLY lists. Full
    suite green (778).

[2026-06-10] - Craft parity: work-site proximity, SKILLMAKE tools, fail loss (Wave 107)
----------------------------------------

FIXED - Crafting:
  - Work-site proximity (reference Skill_Blacksmith / Skill_Cooking):
    smithing recipes now require a forge within 2 tiles and cooking a
    heat source within 3 (fire, forge or campfire). Crafting previously
    worked anywhere; the requirement also greys out the crafting gump
    entries when no site is near. (Dynamic forge/fire items only — map
    statics are not type-matched.)
  - SKILLMAKE t_*/i_* entries were parsed and silently discarded; they
    are now presence requirements (reference SkillResourceTest): t_*
    resolves through the TYPEDEF table to an item type that must be
    carried or wielded, i_* to a specific item that must be present.
    Neither is consumed.
  - Fail loss now matches the reference ResourceConsumePart semantics:
    one 0-50% roll applied uniformly to every required resource. The
    old behaviour rolled per resource (50% chance to lose exactly half,
    always at least 1 even for single-unit resources).
  - New tests (CraftParityTests, 4): forge/heat proximity bounds, tool
    presence without consumption, and the fail-loss ceiling. Full suite
    green (774).

[2026-06-10] - Spell cast parity: spellbook, scroll/wand costs, cast-time curve, resist (Wave 106)
----------------------------------------

FIXED - Magic:
  - Spellbook requirement (reference Spell_CanCast): a player casting a
    classic spell (1-64) from memory must now have it in an accessible
    spellbook (equipped hands or backpack; the classic 64-bit More1/More2
    mask). Scroll and wand casts bypass the book. New SpellbookRequired
    config knob (default on) wired next to ReagentsRequired and covered
    by the test-suite static baseline.
  - Scroll casts now pay half mana and roll at half difficulty, and wand
    casts cost no mana (reference parity); scroll casts also skip the
    reagent check. Previously a wand cast still consumed the full mana
    cost and scrolls behaved like memory casts.
  - CAST_TIME is now a curve like EFFECT/DURATION (single value =
    constant, "A,B" interpolates across skill); the invented
    "-skill/250" faster-casting formula is gone — with the script pack's
    constant CAST_TIME values every spell used to get faster with magery
    against the reference.
  - Magic resist now follows the reference roll: a quick MagicResistance
    check whose chance is max(resist/50, resist - ((magery-200)/50 +
    (1 + spell/8)*50)) / 30 — success absorbs a flat 25% of the effect,
    failure absorbs nothing. The previous gradient (circle*80 difficulty,
    0-50% scaling) was an invention.
  - New tests (SpellCastParityTests, 3) pin the resist formula, the
    cast-time curve and the book bit-mask lookup.

INTERNAL - Tests:
  - The Wave 103 reference success curve tops out below certainty
    (~99.6% at the maximum skill-vs-difficulty delta) and keeps a ~0.5%
    success tail at the minimum, which made success- and fizzle-asserting
    tests intermittently flaky. Hardened the margin tests: actors that
    must succeed train past the curve's certainty point or use the GM
    bypass, and the fizzle test's requirement moved past the zero-chance
    tail. Full suite green (770) across repeated runs.

[2026-06-10] - Resource loader: legacy numeric section-id parse (Wave 105)
----------------------------------------

FIXED - Script loading:
  - Numeric section ids ([SKILL n], [SPELL n], [ITEMDEF n], [CHARDEF n],
    [MULTIDEF n]) were always parsed as hex. The reference uses the
    legacy script number rule: "0x.." and leading-zero forms are hex,
    anything else is decimal. Real packs write itemdef/chardef ids with
    a leading zero ("0eed") — unaffected — but skill and spell ids as
    plain decimal, so every [SKILL 10]..[SKILL 57] and [SPELL 10]+
    definition landed on the wrong slot (e.g. the Swordsmanship def
    [SKILL 40] registered as skill 64): names, delays, ADV_RATE curves
    and spell properties above index 9 were silently mismapped or lost.
  - A scan of the mortechUO pack confirms the fix is safe: 32,239
    leading-zero headers keep their hex meaning, 57 [SKILL] + 167
    [SPELL] decimal headers are corrected, and no header uses bare hex
    without a leading zero. Tests that had encoded the hex behaviour
    were updated to decimal ids. Full suite green (767).

[2026-06-10] - Stat-gain parity ([ADVANCE] curves, STAT_* targets) + 0x3A caps (Wave 104)
----------------------------------------

FIXED - Skills:
  - Stat gain was a simplified invention: a flat 5%-per-use chance (or
    BONUS_STATS misread as that chance) picking a weighted stat, with a
    hardcoded skill→stat table as fallback. Ported the reference
    Skill_Experience stat section instead: stats train only while the
    used skill's lock is Up; each stat trains toward the skill def's
    STAT_STR/DEX/INT ceiling; the chance comes from the script [ADVANCE]
    advance-rate curves (uses-per-point, inverse like ADV_RATE) scaled by
    BONUS_* weights and BONUS_STATS percent; polymorphed characters can
    only train INT; one stat gain per skill use. The [ADVANCE] section —
    present in the mortechUO pack but previously skipped as a generic
    sphere section — is now parsed and handed to the skill engine, and
    the test-suite static reset covers the curves.
  - Ported the reference Stat_Decrease for the total stat cap: under the
    cap a gain is simply allowed; over it a DOWN-locked stat (the one the
    skill values least) may erode by 1 (floor 10) to make room, with the
    reference S-curve loss chance.
  - The 0x3A skill-list cap field was hardcoded to 100.0; it now reports
    the per-skill display cap (class caps/overrides).
  - New tests (StatGainParityTests, 5). Full suite green (767).

NOTES - Known gap (next wave):
  - The resource loader parses numeric [SKILL n] section ids as HEX while
    the reference (and the real script pack, [SKILL 0]..[SKILL 57]
    decimal) uses the legacy number rule (decimal unless leading zero) —
    skill defs with index >= 10 currently land on the wrong skill. Fixed
    in its own wave because the same id parse feeds spell defs.

[2026-06-10] - Skill success curve parity + stat-adjusted skill checks (Wave 103)
----------------------------------------

FIXED - Skills:
  - The skill success roll used a tanh-based S-curve with variance 100;
    the reference uses a piecewise halving bell curve with
    SKILL_VARIANCE=150 (chance halves per 15.0 skill points of distance
    from the difficulty midpoint). The tanh shape was much steeper —
    e.g. 15.0 points above the difficulty succeeded ~95% of the time
    instead of the reference 75%. Ported the reference
    Calc_GetSCurve/Calc_GetBellCurve pair exactly and fixed the variance
    constant (the old comment also mis-cited the reference value).
  - Skill checks now use the stat-adjusted skill value (reference
    Skill_GetAdjusted): raw skill + BONUS_STATS percent of the
    BONUS_STR/INT/DEX-weighted stats from the skill def. Previously the
    BONUS_* properties never influenced any check. Scope note: combat
    hit/damage formulas still read the raw skill (their parity is a
    separate combat-engine subject).
  - New tests (SkillSuccessCurveTests, 7) pin the curve points and the
    adjusted-skill formula. Full suite green (762).

[2026-06-10] - Skill gain/atrophy parity: total cap no longer freezes characters (Wave 102)
----------------------------------------

FIXED - Skills:
  - The gain and decay rolls were wrapped in a single gate that required
    a positive difficulty, and the total skill cap zeroes the difficulty
    — so at the cap neither roll ran and a capped character was frozen
    permanently regardless of lock arrows. On top of that the decay
    helper guarded itself with "only at the total cap", which combined
    with the outer gate made skill atrophy dead code on every path.
    Restructured to the reference Skill_Experience shape: the block is
    entered while the trained skill can still rise, the decay roll
    (3:4 of the gain chance) runs independently of the cap and erodes a
    DOWN-locked skill by 0.1, and only the gain roll is gated on
    difficulty — a capped character now retrains by setting arrows just
    like the reference.
  - Removed the invented GAINRADIUS=300 default: the reference treats an
    unset GAINRADIUS as disabled, while the default silently blocked
    low-difficulty gain rolls for every skill above 30.0.
  - New tests (SkillGainParityTests, 4): capped retraining via a
    DOWN-locked skill, frozen-at-cap without one, high-skill gain with no
    gain radius, and locked-skill immutability. Full suite green (755).

[2026-06-10] - Skill definitions: legacy value-curve parsing + ADV_RATE semantics (Wave 101)
----------------------------------------

FIXED - Skills:
  - [SKILL] definition numerics were read with plain int parsing, which
    silently fails on every decimal/multi-value property real script
    packs use (ADV_RATE=2.5,50.0,200.0; DELAY=2.0): the values dropped to
    zero, so ADV_RATE was ignored for every skill and every skill delay
    became instant. Added ValueCurve, a port of the reference curve type
    (credit: Sphere 0.56 CValueCurveDef + ahextoi): legacy fixed-point
    number parse (decimal point concatenates digits, leading zero marks
    hex) and linear interpolation across skill 0-100.0. ADV_RATE, DELAY
    and EFFECT are now curves; the remaining numeric fields use the same
    legacy number parse (this also fixes FLAGS, which the old decimal
    parse misread for leading-zero hex values).
  - ADV_RATE semantics corrected: the curve expresses "skill uses per 0.1
    gain" and the per-mille gain chance is its inverse (100000/value,
    reference GetChancePercent). The previous interpretation treated the
    value as a 0-100 multiplier on a homegrown curve, which made gain
    pacing both wrong and untunable per skill.
  - Skill delays now follow the DELAY curve at the user's skill level
    (reference Skill_GetTimeout); the delayed-skill pipeline passes the
    character's skill value through.
  - New tests (ValueCurveTests, 14) pin the parse rules with the actual
    mortechUO script values and the interpolation/chance math. Full suite
    green (751).

[2026-06-10] - NPCs open unlocked doors on blocked steps (Wave 100)
----------------------------------------

ADDED - NPC movement:
  - Closes the door gap documented in Wave 99: NPC walk validation
    treats a closed door as a hard block, so NPCs could not chase or
    wander through doorways. When a direct step is blocked, the AI now
    checks the blocked tile for a closed, unlocked door item (locked
    doors and portcullises stay barriers when locked) and opens it with
    the reference 50%-per-attempt gate before re-validating the step
    (reference parity: the idle look-at path uses the door, then walks
    through).
  - The state flip lives in a new world-level DoorHelper.TryOpenDoorState
    (same art-offset and DOOR_OPEN tag transition as the player
    double-click path, portcullis offset 2, DISPID-override aware, no
    network side effects); the server wiring broadcasts the open sound
    (0xEA) and the refreshed world item to nearby observers via a new
    OnNpcOpenDoor hook. Doors opened by NPCs close again through the
    normal double-click toggle.
  - New tests (NpcDoorOpeningTests, 5) cover the state flip (closed,
    portcullis, locked, already-open) and the blocked-tile door lookup.
    Full suite green (737).

[2026-06-10] - NPC polish: idle fidget animation + pet follow leash (Wave 99)
----------------------------------------

ADDED - NPC behaviour:
  - Idle NPCs now occasionally (1-in-8 idle ticks) play a fidget
    animation in place instead of taking a wander step, through a new
    OnNpcFidget hook wired to the version-aware, body-translated
    animation broadcast (humanoid fidget/yawn, monster groups 0x11/0x12,
    animal groups 0x09/0x0A, mounted mapping applied). Reference plays
    random idle fidgets; standing NPCs previously never animated at all.
  - Pet follow/guard/return movement now gives up beyond a leash
    distance on the same map (NpcAI.PetFollowMaxDistance, default 36 =
    the reference radar range; 0 disables) and resumes when the owner
    comes back into range. The cross-map catch-up teleport is unchanged.
    The new static is registered in the test-suite static reset.

NOTES - Known gap (documented, not yet implemented):
  - Reference NPCs open adjacent doors while idling (NPC_LookAtItem:
    use the door, then walk through). Our NPC walk validation treats a
    closed door as a hard block, so NPCs cannot chase or wander through
    doorways. Implementing this needs a world-level door toggle (the
    current toggle lives on the client handler surface) - deferred.
  - Full suite green (732).

[2026-06-10] - NPC visibility: untracked-in-range appear + cross-map leave (Wave 98)
----------------------------------------

FIXED - NPC visibility:
  - The object-centric NPC move notification had no branch for a mobile
    that is in range on both ends of the step but not yet tracked by the
    observer (the player path already handled this). An NPC that unhid
    after a scout-hide, or whose initial appear was filtered at spawn
    time, moved invisibly until the observer's next full view refresh.
    NotifyCharMoved now falls through to the appear path in that case
    (hidden/ghost filters still apply), mirroring the player
    enter/leave handler.
  - Cross-map moves (pet follow teleporting to the owner's map) only
    swept sectors on the destination map, so observers left on the old
    map never received the leave-range delete and kept a ghost copy of
    the mobile until their own view refreshed. The sector sweep is now
    per-map: the destination map around the new position plus, for
    cross-map moves, the source map around the old position. This also
    removes the pathological sector rectangle the old min/max span
    produced when the two positions were on unrelated coordinates.
  - New tests (NpcVisibilityNotifyTests, 2) cover the untracked-appear
    branch and the tracked-move 0x77 path. Full suite green (732).

[2026-06-10] - NPC run-bit consistency on path-follow, flee and pet steps (Wave 97)
----------------------------------------

FIXED - NPC movement:
  - The 0x77 direction byte carries the run flag (0x80) that clients use
    to pick the walk vs run animation. Chasing NPCs set it on direct
    steps but lost it on A*-path-follow steps, so a pursuing NPC stepping
    every ~250ms animated as walking — the visible "skating/sliding NPC"
    artifact — and mixed direct/path routes flickered between run and
    walk. Path-follow steps now inherit the caller's run intent.
  - Flee steps now run once there is room and walk while cornered
    (reference parity: the flee path walks at threat distance <= 3 and
    runs beyond), and pet follow/guard/return steps run when the target
    is more than 3 tiles away (reference NPC_Act_Follow run threshold).
  - Full suite green (730); movement + runtime-pressure suites green
    (70).

[2026-06-10] - Spell cast animation: body translation + gesture dispatch (Wave 96)
----------------------------------------

FIXED - Animations:
  - The spell-cast animation hook broadcast a raw legacy 0x6E packet with
    the humanoid cast indices (0x10/0x11) to every observer regardless of
    caster body and client version. Non-humanoid casters (liches, mages
    with monster bodies) therefore played the wrong group on classic
    clients, and KR/Enhanced clients never received the 0xE2 gesture
    variant. The hook now routes through the version-aware animation
    broadcast: the legacy path gets the body-translated group via
    BodyAnimTranslator (monster cast = stomp/pillage, animal cast =
    attack/eat per the Wave 95 table, mounted mapping unchanged) and
    KR/Enhanced observers get 0xE2 with the Spell gesture. Full suite
    green (730).

[2026-06-10] - Body-type animation translation for the legacy 0x6E path (Wave 95)
----------------------------------------

FIXED - Animations:
  - Classic clients read the 0x6E action field as a raw group index into
    the actor body's own anim.mul file, so humanoid action indices sent to
    monster or animal bodies played the wrong group (or none). Added
    BodyAnimTranslator, a port of the reference body-translation table
    (credit: Sphere 0.56 GenerateAnimate): bodies < 200 use the
    high-detail monster set (attacks 4-6, get-hit randomized over
    get-hit/block-right/block-left, cast = stomp/pillage, deaths 2/3),
    bodies 200-399 use the low-detail animal set (attacks 5-6, get-hit 7,
    cast 5/eat, deaths 8/12), bodies >= 400 pass through unchanged.
  - Applied at every emit site where the actor can be non-humanoid: the
    hit-reaction broadcast in the player swing path and the NPC attack
    wiring (both previously sent the humanoid get-hit index 0x14 to any
    body), the NPC swing-action helper (animals previously received the
    monster attack group 4; humanoid check also widened from the two
    human bodies to the full >= 400 range so elf-range bodies animate
    with the humanoid set), the healer heal animation, and the script
    ANIMATE/BOW/SALUTE verbs (mounted mapping preserved, body translation
    added for the unmounted path).
  - New deterministic unit suite (BodyAnimTranslationTests, 7 tests)
    covers the table boundaries and group mappings. Full suite green
    (730).

[2026-06-10] - GameClient decomposition phase 4: IClientContext interface (Wave 94)
----------------------------------------

INTERNAL - GameClient decomposition (plan complete):
  - The union of the nine handler classes' context-shim blocks was derived
    into a narrow internal IClientContext interface (~125 members: core
    state, engine accessors, decomposition components, script services,
    shared mutable state bridges, server-wired callbacks, packet/gump
    helpers, gameplay bridges and targeting helpers). It extends
    ITextConsole because handlers pass their context onward as the script
    console identity (TriggerArgs.ScriptConsole, TryRunFunction,
    TryExecuteCommand). GameClient implements it explicitly in the new
    GameClient.Context partial - pure forwarding, no behaviour - which
    keeps the internal members internal. All nine handlers
    (ViewUpdater, Inventory, ItemUse, WorldFeatures, Targeting, Dialog,
    Skills, Combat, ScriptConsole) and the InfoSkillSink glue type now
    hold IClientContext instead of the concrete GameClient, so the
    compiler enforces that handler code can only reach the declared
    surface; handler constructors became internal (a public constructor
    cannot take an internal parameter type). The field-level shims
    (vitals last-sent, pending-menu state) route through interface
    properties backed by the existing GameClient fields. Full suite green
    (723) + RuntimePerformancePressure 6/6; mojibake scan clean. This
    closes the decomposition plan (phases 1-4); the PacketHelpers/Login/
    Handlers/Chat/Housing partials remain on GameClient by design.

[2026-06-10] - GameClient decomposition phase 3g: script-console handler class (Wave 93)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The script-verb surface of the ScriptConsole partial (~1,300 lines:
    the TryExecuteScriptCommand verb switch covering MESSAGE, SAYUA,
    INPDLG, TRYSRC, TARGETF/TARGET, MENU, DIALOG/SDIALOG, GO/GONAME,
    SERV.*, NEW.*, FILE.*, DB.*/LDB.*, WEBLINK, BUY/BYE and SENDPACKET;
    TryResolveScriptVariable; the FOR* object queries of
    QueryScriptObjects; and their private helpers) moved into
    ClientScriptConsoleHandler using the established shim recipe; bodies
    are byte-identical except the three console-identity passes to
    TryRunFunction/TryExecuteCommand, which become _client. The
    ITextConsole implementation (SysMessage, GetName, GetPrivLevel) and
    the shared combat/status/notoriety helpers that historically lived in
    the same partial (BuildMobileFlags, SetWarMode, FaceTarget, swing
    tables, ComputeNotoriety, SendSkillList, status helpers) stay on
    GameClient - scripts keep receiving the GameClient as their console
    identity and the public delegations preserve the ITextConsole
    interface map. The dialog-depth guard moved into the handler; the
    pending-menu state stays internal on GameClient (the Handlers partial
    consumes the 0x7D menu response); ScriptFile/ScriptDb/ScriptLdb/
    ScriptDatabaseRoot accessors added. Full suite green (723); mojibake
    scan clean.

[2026-06-10] - GameClient decomposition phase 3f: combat handler class (Wave 92)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The Combat partial (~2,020 lines, the hot path: movement validation
    with throttle/credit/queue and the reject taxonomy, speech, the
    attack/swing loop, death and resurrect transitions, spell casting and
    the per-tick client state pump) moved into ClientCombatHandler using
    the established shim recipe; bodies are byte-identical except the
    ITextConsole identity pass on @UserExWalkLimit and the speed-hack
    event raise. The public static movement/speed-hack config surface
    (WalkBufferMax, MoveClock, credit/speed-hack knobs) and the
    OnSpeedHackDetected event stay on GameClient - Program.cs/SphereConfig
    set them and the ResetEngineStatics test discipline keys off them; the
    handler reads them through static shims and fires the event through an
    internal RaiseSpeedHackDetected bridge (C# events are only invocable
    from the declaring class). The movement throttle component and all
    per-session movement state moved into the handler; the vitals
    last-sent fields stay internal on GameClient because the Login partial
    also writes them. Cross-partial helpers promoted private -> internal:
    TryHandleCommandSpeech, SetWarMode, GetSwingAction, GetSwingSound,
    BuildEquipmentList, GetDirectionDelta; engine accessors MoveEng,
    SpeechEng, DeathEng added. Hot-path check: RuntimePerformancePressure
    suite green (6/6); full suite green (723); mojibake scan clean.

[2026-06-10] - GameClient decomposition phase 3f: skills handler class (Wave 91)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The Skills partial (~570 lines: info/active skill flows with target
    cursor, stroke loop and delayed completion, the tracking menu, overhead
    object messages, help request, AOS tooltip assembly and the small
    trade/party/client-version packet handlers) moved into
    ClientSkillsHandler using the established shim recipe; bodies are
    byte-identical except the ITextConsole identity passes (ScriptConsole =
    this, the f_onclient_helppage console argument) and sink construction,
    which become _client. The GameClient property is named SkillUse to
    avoid colliding with the SkillH engine accessor and the Skills
    namespace. The InfoSkillSink glue type stays nested on GameClient -
    the item-use handler and the skill engines construct it as
    GameClient.InfoSkillSink. StableStringHash promoted private ->
    internal for the tooltip hash. Full suite green (723); mojibake scan
    clean.

[2026-06-10] - GameClient decomposition phase 3e: dialog handler class (Wave 90)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The Dialogs partial (~1,740 lines: script [DIALOG] section rendering
    with layout expansion (FOR/WHILE/IF unrolling, DORIGIN coordinate
    cursors), dialog-button script execution, the help menu and the
    page/stuck flows) moved into ClientDialogHandler using the established
    shim recipe; bodies are byte-identical except the ITextConsole identity
    pass to TryRunDialogButton, which becomes _client. The dialog state
    that still lived on the GameClient core moved with it: the
    _dialogSubjectUid subject field (ScriptConsole now reads it through
    Dialogs.DialogSubjectUid), the native-fallback registry plus
    OpenNamedDialog/RegisterNativeDialogFallbacks (registration relocated
    to the handler constructor), and the INPDLG pending-prompt map and
    context counter (the Handlers partial and disconnect cleanup now go
    through Dialogs.PendingInputDlg/NextInputDlgContext). TryFindMenuSection
    and IsPlainDefToken stay reachable for the ScriptConsole partial via
    internal GameClient bridges. Full suite green (723); mojibake scan
    clean.

[2026-06-10] - GameClient decomposition phase 3e: targeting handler class (Wave 89)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The Targeting partial (~710 lines: HandleTargetResponse router covering
    GM cursor flows, script TARGETF dispatch and spell-target completion,
    plus gump response routing, SendGump and SetPendingTarget) moved into
    ClientTargetingHandler using the established shim recipe; bodies are
    byte-identical except the four ITextConsole identity passes
    (ScriptConsole = this, RunFunction/TryRunFunction console argument)
    which become _client. Eight GM target helpers were promoted
    private -> internal (ClearPendingTargetState, ExecuteAreaVerb,
    DuplicateItem, SpawnCageAround, OpenForeignBank, RemoveTargetedObject,
    TryAddAtTarget, ResolvePickedChar) and a Cmds accessor added for the
    command handler. Dialog close-function state stays on GameClient behind
    internal property bridges until the dialog slice moves the fields.
    SendGump/SetPendingTarget remain as GameClient delegations for the many
    cross-partial call sites. Full suite green (723); mojibake scan clean.

[2026-06-10] - GameClient decomposition phase 3d: world-features handler class (Wave 88)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The WorldFeatures partial (~1,780 lines: crafting gump, vendor buy/sell,
    secure trade, rename, guild/house management gumps, doors, potions,
    skill use, 0xBF extended-command dispatch, party commands, context
    menus) moved into ClientWorldFeaturesHandler using the established shim
    recipe; bodies are byte-identical. The static 0xBF dispatch dictionary
    changed element type from Action<GameClient, byte[]> to
    Action<ClientWorldFeaturesHandler, byte[]> so its static lambdas keep
    the verbatim client.X form; the registry-drift guardrail is unchanged.
    Five cross-partial helpers were promoted private -> internal
    (BuildWorldItemPacket, RefreshBackpackContents, BeginInfoSkill,
    BeginActiveSkill, HandleQueryDesignDetails) and three engine accessors
    added (CraftE, GuildM, PartyM). The six partner-side trade callback
    properties stay on GameClient (Program.cs wiring surface), as do
    internal bridges for the test-harness reflection entry points
    (SendContextMenu, HandleContextMenuResponse) and the vendor price
    statics. Full suite green (723); mojibake scan clean.

[2026-06-10] - GameClient decomposition phase 3c: item-use handler class (Wave 87)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The ItemUse partial (~1,890 lines: double-click dispatch, the per-type
    item-use switch, plant/pitcher/ammo helpers, pet speech-command engine,
    vendor buy/sell list construction) moved into ClientItemUseHandler using
    the established shim recipe; bodies are byte-identical. Twenty-six
    cross-partial helpers it needed were promoted private → internal
    (door/potion/book/gump/paperdoll/mount/walk-validator/vendor-price
    helpers and the InfoSkillSink sink type among them); the former partial
    keeps public delegators (HandleDoubleClick, OpenVendorBuy/Sell) plus
    internal bridges for the Combat and WorldFeatures call sites
    (TryHandlePetCommand, ammo helpers, vendor interaction) — the pet-command
    bridge also keeps the test-harness reflection entry point stable. One
    test that reflected the private HandleDyeApply now targets the handler.
    File operations in the conversion tooling now use explicit UTF-8 (see
    the Wave 86 mojibake repair); a post-conversion scan shows zero
    double-encoded sequences.

[2026-06-10] - GameClient decomposition phase 3b: inventory handler class (Wave 86)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The Inventory partial (~820 lines: single click + @Click/@ToolTip
    dispatch, item pickup/drop/equip with drag bookkeeping and trade-window
    routing, profile and status requests) moved into ClientInventoryHandler.
    The method bodies are byte-identical; the handler mirrors the GameClient
    surface it needs through private context shims (character/world/netstate,
    trigger dispatcher, housing/trade/spell engines, Send*/SysMessage/
    notoriety/pack-placement helpers) — that shim block IS the handler's
    dependency list, from which a future ClientContext interface can be
    derived. Cross-partial helpers it needed (GetNotoriety, SendPickupFailed,
    PlaceItemInPack, SendTradeUpdateToBoth, CanSendStatusFor) were promoted
    private → internal; the former partial keeps only delegating members,
    including an internal GetTopContainer bridge still used by ItemUse.

[2026-06-10] - GameClient decomposition phase 3a: view-update handler class (Wave 85)
----------------------------------------

INTERNAL - GameClient decomposition:
  - First real handler-class conversion: the entire ViewUpdate partial
    (view-delta build/apply pipeline, open-door sync, known-char
    notifications — ~480 lines) moved into a standalone ClientViewUpdater
    class that takes the GameClient as its context. The former partial file
    now holds only thin delegating members, so every call site (including
    the multicore tick's parallel BuildViewDelta / serial ApplyViewDelta
    split) is unchanged — the logic moved verbatim.
  - The class boundary is real: the handler can only reach GameClient's
    internal/public surface. The eight Send* packet helpers it needs were
    promoted from private to internal, ClientViewDelta moved from a nested
    GameClient type to namespace level (the server's reusable delta pools
    reference it), and GameClient gained an internal World accessor.
    The conversion recipe is recorded in the decomposition doc as the
    template for the remaining partials.

[2026-06-10] - GameClient decomposition phase 1: state-machine components (Wave 84)
----------------------------------------

INTERNAL - GameClient decomposition:
  - The decomposition plan for the ~16k-line GameClient partial-class cluster
    is documented in docs/GAMECLIENT_DECOMPOSITION_TR.md (phase 1: state
    machines, phase 2: view cache, phase 3: per-partial handler classes over
    a ClientContext), and phase 1 is executed in this wave.
  - Three cohesive state clusters moved out of GameClient fields into
    components, with every reference across the partials renamed — pure
    state relocation, logic untouched:
      * ClientTargetState (GameClient.Targets) — all 28 pending-target-cursor
        fields plus the cursor-active flag; its Clear() is the former
        ClearPendingTargetState body verbatim (notably it still does NOT
        clear the Kill flag, the callback slot or the skill-cancel id,
        matching the original).
      * ClientGumpRegistry (GameClient.Gumps) — active gump set, per-gump
        response callbacks, open script-dialog registry.
      * ClientMovementThrottle (GameClient.Throttle) — walk token bucket
        (seeded with WalkBufferMax at first use, same timing as the former
        field initializer), move-time gate, violation counter, movement
        queue processor.
  - Tests that reached these fields by reflection now use the internal
    component properties directly (InternalsVisibleTo).
  - Phase 2 (same wave): the view-delta bookkeeping (known-char/item/door
    sets, last-known position/item state, tooltip hash cache) moved into
    ClientViewCache (GameClient.View) the same way. Phase 3 — converting
    the partials into handler classes over a ClientContext — is the next,
    per-partial effort and is intentionally left for dedicated sessions.

[2026-06-10] - Character decomposition slice 3: memory subsystem component (Wave 83)
----------------------------------------

INTERNAL - Character decomposition:
  - The memory-item subsystem (EqMemoryObj list, fight/aggressor memories,
    timeout ticks, notoriety notifications — Source-X CChar Memory_*) moved
    into a new CharacterMemoryState component (Character.MemoryState, lazily
    created). All Memory_* methods and the Memories view delegate, so the
    public API, the @MemoryEquip hook, pet-flag handling, the cowardice/fame
    retreat rule and the fight-memory tick cadence are unchanged — the logic
    moved verbatim. Character.cs is down to 4,500 lines (from ~4,950 before
    the decomposition pass).
  - Deliberate completion point for the state-extraction pass: the vitals
    regen + hunger tick block and the core stat fields stay in Character ON
    PURPOSE. That code writes the private stat fields directly and manages
    the dirty-flag discipline by hand (e.g. Resurrect seeds _hits directly);
    forcing it through the public property setters to move it into a
    component would subtly change clamp/dirty semantics, which breaks the
    "logic moves verbatim" guarantee these slices are built on. Any future
    slice of that block needs a dedicated behavioural test net first.

[2026-06-10] - Character decomposition slice 2: poison state component (Wave 82)
----------------------------------------

INTERNAL - Character decomposition:
  - Poison runtime state (level, tick schedule, poisoner attribution) moved
    into a new CharacterPoisonState component (Character.Poison, lazily
    created), following the slice-1 delegation pattern: ApplyPoison,
    CurePoison, ProcessPoisonTick, PoisonLevel and IsPoisoned delegate, so
    the public API, resist math, damage broadcast and kill attribution are
    unchanged — the logic moved verbatim. Character.cs is down to ~4,740
    lines (from ~4,950 before the decomposition started).

[2026-06-10] - Character decomposition slice 1: combat state component (Wave 81)
----------------------------------------

INTERNAL - Character decomposition:
  - First slice of breaking up the Character god class: the per-attacker
    damage log (ATTACKER.*) and the criminal/murder notoriety counters moved
    into a new CharacterCombatState component (Character.CombatState,
    lazily created). Character keeps thin delegating members, so the public
    API, the script property surface (ATTACKER.n.*, KILLS, FORGIVE, ...) and
    all trigger hooks (OnCombatAdd/Delete/End, OnHitIgnored, OnMurderDecay)
    behave identically — the logic moved verbatim. The AttackerRecord struct
    moved from a nested Character type to the namespace level (it had no
    external references to the nested name). Character.cs shrank by ~150
    lines; further slices (stats block, poison state, memories) can follow
    the same delegation pattern.

[2026-06-10] - Combat ability trigger, spawner start/stop triggers, lost-NPC teleport (Wave 80)
----------------------------------------

ENGINE - triggers:
  - @UserSpecialMove fires from the 0xD7 sub 0x19 combat ability request
    (client Send_UseCombatAbility — payload layout verified against the
    ClassicUO writer), N1 = the ability index. Routed ahead of the
    house-design session gate since it shares the 0xD7 opcode.
  - @Start / @Stop fire when the spawner START/STOP verbs toggle spawning
    (Item.OnSpawnStartStop host hook).
  - @NPCLostTeleport is anchored to new engine behaviour: a wild NPC that
    ends up more than three leash radii from home teleports straight home
    instead of walking the whole way back; the trigger fires first and
    RETURN 1 cancels the teleport (the NPC walks as before). Confirmed safe:
    the chase/leash logic runs in the serial ApplyDecision phase, not the
    parallel decision builder.
  - Item @RegionEnter/@RegionLeave remain deferred with a sharpened reason:
    there is no central item-move path to hook (Position is assigned directly
    on hot paths such as ship movement).

[2026-06-10] - Conference chat system (Wave 79)
----------------------------------------

ENGINE - chat:
  - The UO conference chat system is implemented. ChatEngine holds channels
    and membership (one channel per member; ad-hoc channels disappear when
    emptied, the static "General" channel survives). Opening the chat window
    (0xB5) accepts the character's name as the chat handle and sends the
    channel list; the client then auto-joins "General".
  - Incoming 0xB3 chat actions are parsed and handled: talk (0x61, broadcast
    to channel members), join (0x62 — channel name arrives wrapped in quote
    characters, optional password stripped), create channel (0x63), leave
    (0x43). Disconnect drops the membership so channels hold no stale entries.
  - Outgoing 0xB2 chat messages cover the subcommands the client renders:
    create/remove channel, username accepted, add/remove user, clear users,
    joined/left channel, channel message. The byte layout (cmd + 4 reserved
    bytes + null-terminated BE-unicode strings) is verified in tests against
    the exact ClassicUO parser shape. The T2A feature bit that enables the
    client chat button was already sent at login.

[2026-06-09] - Script console compat verbs, drag-release verbs, trigger backlog closure (Wave 78)
----------------------------------------

SCRIPTING - console verbs:
  - DIALOGCLOSE closes the named open script dialog for real: open dialogs are
    tracked server-side (name → gump id), the close sends 0xBF sub 0x04 and
    drops the tracking; ISDIALOGOPEN.* now answers from that registry instead
    of a constant 0. BYE ends the NPC interaction (closes open script dialogs,
    clears the dialog subject). BUY opens the vendor buy list — the dialog
    subject when it is a vendor NPC, otherwise the nearest vendor in range.
  - Character BOUNCE/DROP act on the dragged item (the DRAGGING state set by
    the pickup packet): BOUNCE returns it to the backpack, DROP releases it at
    the character's feet; both cancel the client drag cursor with 0x27 (new
    PacketPickupFailed writer). Item FIXWEIGHT refreshes the client view
    (weight is already computed on demand).

ENGINE - triggers:
  - @HitIgnore is implemented: AttackerRecord gained an Ignored flag, scripts
    control it through ATTACKER.n.IGNORE (read/write), and a hit landed by an
    ignored attacker fires @HitIgnore on the victim (RETURN 1 clears the flag).
  - @Targon_Cancel fires when the player dismisses a target cursor.
  - @ToolTip (char) and @Tooltip (item) fire on single click ahead of @Click,
    IsTrigUsed-gated since single click is a hot path.
  - @UserQuestArrowClick fires from 0xBF sub 0x07 (newly known subcommand).
  - @UserBugReport fires from the 0xF4 crash report.
  - 0xFA (Ultima Store button) and 0xB5 (chat window open) gained packet
    handlers that fire @UserUltimaStoreButton / @UserGlobalChatButton.
  - @UserExWalkLimit fires when the walk token bucket runs dry
    (IsTrigUsed-gated — movement rejects can storm).
  - Still deferred with reasons recorded in the guardrail: UserMailBag /
    UserSpecialMove (no carrier packets in the supported protocol set),
    NPCLostTeleport (leashed NPCs walk home; the AI decision stage is
    parallel), NPCSeeWantItem (covered by @NPCLookAtItem). The sector ambient
    wind sound inventory item was dropped after checking the Source-X
    reference: CSector has no such sound logic — the item was speculative.

[2026-06-09] - Custom house foundation placement and design walk geometry (Wave 77)
----------------------------------------

ENGINE - custom housing:
  - Customizable foundation placement: a house deed carrying TAG.CUSTOMHOUSE
    places the multi as a MultiCustom foundation (PlaceHouse customFoundation
    path) with an empty committed design at revision 1. Unlike regular house
    placement, no real component items are materialized — the client renders
    the foundation multi and the committed design from the 0xD8 stream, so
    nothing double-renders.
  - Committed designs are now server-side walk geometry: WalkCheck virtualizes
    the DESIGN_n tiles of a MultiCustom item the same way it virtualizes
    multi.mul components (surface/bridge/impassable tile flags respected,
    per-tile Z honored for upper stories). The design lookup is served by a
    revision-keyed cache on CustomHousingEngine — a design commit bumps
    DESIGN_REVISION, which invalidates the cache on the next step check. The
    cache is concurrent because the parallel NPC-pathfinding stage consults
    WalkCheck too.

[2026-06-09] - Guild channel delivery, custom house design editor, script verb completeness (Wave 76)
----------------------------------------

ENGINE - speech:
  - Guild/alliance channel chat is now delivered. The channel event carries the
    recipient (it previously invoked with no recipient, so nothing could be
    routed), the host subscribes it and sends 0xAE with talk type 0xD/0xE per
    member, the speaker gets the server echo, and alliance delivery requires a
    mutual (both-sides declared) alliance. Guild/alliance lines are no longer
    spatially broadcast to bystanders near the speaker.

ENGINE - custom housing:
  - The house design editor is implemented end to end. Incoming 0xD7 encoded
    commands now carry their payload (it was previously discarded after the
    subcommand) and are handled: Build, Delete, Stairs, Roof, RoofDelete, Level,
    Clear, Backup, Restore, Sync, Revert, Commit, Close. A per-character design
    session holds the working design; Commit persists it as DESIGN_n tags on the
    multi item (same format the script ADDITEM verb writes) and bumps
    DESIGN_REVISION.
  - Outgoing design packets: 0xD8 design stream (zlib-compressed mode-0 planes,
    byte layout verified against the client parser, large designs split across
    planes), 0xBF 0x1D revision notice, 0xBF 0x20 customization mode on/off.
    The 0xBF 0x1E design query is now a known subcommand answered from the
    committed design. The house sign gump gained a "Customize House" entry for
    owners/co-owners, and a committed design broadcasts the new revision to
    nearby clients so they re-query.
  - @HouseDesignCommit and @HouseDesignExit now fire (Commit/Close paths).

ENGINE - experience:
  - Experience/level runtime: ChangeExperience routes every EXP change (script
    writes included) through @ExpChange (ARGN1 adjustable, RETURN 1 cancels);
    crossing a level threshold fires @ExpLevelChange with the new level. Killing
    an NPC awards the victim's EXP value to the killer — characters without EXP
    grant nothing, so the system stays inert unless definitions/scripts assign
    experience. Level curve is configurable (LevelNextAt step, linear or
    doubling mode).

ENGINE - help menu:
  - The help-menu buttons do real work now: "stuck" teleports to a safe town
    spot and is refused while jailed or in combat; "page" opens a text-entry
    gump routed through the .PAGE command path (staff broadcast + a capped
    recent-page log); "page list" shows recent pages (staff see all, players
    their own).

ENGINE - client packets:
  - 0xA5 web link writer added, exposed as the WEBLINK script verb and a
    "Visit Web Page" button on the guild stone gump when the guild has a web
    URL set.
  - Ground-drop sound is item-aware: gold coins play the amount-scaled coin
    sounds (0x2E4..0x2E6) instead of the generic 0x42.
  - @UserVirtue (0xBF 0x2C) and @UserKRToolbar (0xBF 0x24) now fire.

SCRIPTING - verbs:
  - Item OPEN opens the container on the acting client with script authority;
    DCLICK/USE replay the real client double-click path (doors, potions,
    containers). Character DISMOUNT performs a full dismount through the mount
    engine (client path fires @Dismount and refreshes the rider; flag-clear
    fallback remains for headless runs).
  - Object TRIGGER fires the named trigger through the dispatcher's new by-name
    path, so custom trigger names defined only in EVENTS/TEVENTS work. FIX
    re-seats the object Z on the terrain surface. TAGLIST (character, region,
    room) dumps tags to the invoking console. Region/Room ALLCLIENTS delivers a
    sysmessage or runs a script function for every online client character in
    the area.

DIAGNOSTICS:
  - Unimplemented SERV.* verbs are no longer silent: first use of each verb is
    warn-logged (debug afterwards) and GM-level callers get an in-game notice.
  - FILE.OPEN failures report their reason through a host-wired diagnostic sink
    instead of being swallowed; bare catch blocks in the recording loader,
    host IPC connect retry and stable-pet tag parser were narrowed to the
    expected exception types.

INTERNAL:
  - GameWorld now documents its threading contract (single-writer plain
    collections vs concurrent dirty/region caches; parallel stages must queue
    intents for the serial phase). Stub inventory updated; remaining gaps are
    marked as deliberate deferrals with reasons.

[2026-06-07] - Pet-command speech gating, skin hue preserved across death/resurrect (Wave 75)
----------------------------------------

ENGINE - speech:
  - The pet-command parser no longer treats every two-word line as "<pet name>
    <command>"; it only engages when the line actually starts with a pet command
    verb, so ordinary two-word speech no longer prints the "Sorry" pet-failure
    message when the speaker has no pet.

ENGINE - death / resurrect:
  - Skin hue is preserved across death. The living body's skin hue is captured into
    OSkin before the death spell-cleanup runs, and a dead character logging in keeps
    that hue instead of losing it when switching to the ghost hue. Fixes the
    character staying grey/default-hued after resurrect.

ENGINE - NPC definitions:
  - CHARDEF skill ranges (WRESTLING, TACTICS, MAGERY, etc.) are now parsed and
    rolled onto the character on spawn / applyDefName (CharDefHelper
    ApplyNpcDefinitionSkills, clamped 0..1200). Definition TAG defs are applied to
    the runtime character too (ApplyNpcDefinitionTags), so AI that reads BREATH.*,
    THROWOBJ, THROWDAM, THROWRANGE for dragon/breath/throw special attacks now sees
    the values from the definition (previously the AI read tags that were never set).

ENGINE - combat:
  - Hit-chance gained a stat-based fallback for NPCs whose weapon skill or Tactics
    is 0: the inferred value is clamp(max(Str,Dex)*10, 250..1000). It only applies
    to non-player attackers and weapon skills, so player accuracy is unchanged. This
    stops skill-less NPC definitions from missing almost every swing.

SCRIPTING - definition parsing:
  - Fixed bare Sphere hex parsing in ItemDef and CharDef: values like 0F5E / 013B
    are no longer mistaken for decimal, and the {min max} form is parsed for item
    damage/range. This also closes a path where an item definition could load with
    wrong metadata (e.g. a sword mis-typed so it emitted the bow/arrow shoot sound).

HOST / PANEL / BUILD:
  - Replaced the in-app panel update with a single update.cmd / update.ps1, run
    with the server stopped: git pull --ff-only -> full build.ps1 (panel + Server +
    Host) -> copy the build output into the run (deploy) folder -> relaunch
    SphereNet.Host. Running as a separate process it can replace SphereNet.Host.exe
    too, which the in-app updater could not. The source repo comes from sphere.ini
    AppUpdateRepoDir (or -RepoDir); the deploy folder defaults to the script's own
    folder (or -DeployDir) and the build-output==deploy case skips the copy.
    build.ps1 copies update.* into the build output so a deploy folder self-updates.
  - Removed the in-app UpdateService, POST /api/update/run, GET /api/update/status,
    the Settings "System Update" card and the AutoUpdate startup check.
    AppUpdateRepoDir is now read only by update.ps1; the AutoUpdate key is gone.

ENGINE - sound / visual / effects:
  - Added 0xC0 PacketEffectHued, 0xC7 PacketEffectParticle and 0x23
    PacketDragAnimation outgoing packets.
  - ObjBase.SOUND / EFFECT are no longer stubs: item- and char-based script
    commands now broadcast to nearby clients. Character SOUND / EFFECT route through
    the shared ObjBase path, and FACE now triggers a 0x77 facing broadcast.
  - Pickup and ground-drop emit the 0x23 drag animation, filtered to clients that
    support it (KR / Enhanced / SA).
  - Region change resends GlobalLight, a silent Season and Weather to the client.

ENGINE - combat & movement:
  - NPC walk steps now always face the step direction (surround step, Wander, flee
    and move-away paths previously moved without setting Direction, so the 0x77 move
    mismatched the tile delta and the client snapped the NPC). The surround step is
    also constrained to tiles adjacent to the NPC, so it can no longer jump two
    tiles in one move ("NPC teleport" during combat).
  - A player turning to face a combat target now also updates the attacker's own
    client (0x20 PlayerUpdate + walk-seq reset), mirroring Source-X
    CChar::UpdateMove -> addPlayerView/addPlayerUpdate, so in top-down view the
    avatar visibly faces the target instead of only nearby observers seeing it.

ENGINE - items & visuals:
  - Ground items carry a facing direction: 0x1A and 0xF3 world-item packets send it,
    the per-client known-item cache tracks it, and WorldSaver persists DIR. Dropping
    separate (non-stacking) items on one tile scatters their facing by the tile's
    existing item count instead of every drop landing on Direction=1.
  - A worn item previously known to a client as a ground item is no longer
    0x1D-deleted by the per-tick view delta (the 0x2E worn packet already re-homes
    it) - fixes worn items vanishing until a resync after a GM recolour/property edit.
  - GM x-verbs on item targets (.xcolor / .xid / ...) refresh the item visual for
    all observers; previously only COLOR (via OnVisualUpdate) and character targets
    refreshed.

ENGINE - persistence:
  - Periodic world auto-save: sphere.ini SavePeriod (minutes, 0 = off) now drives a
    save on the main loop. It was read into config but never used; with
    auto-save-on-shutdown disabled, the server never saved on its own.

CONFIG:
  - Notoriety name hues (ColorNoto*): a configured 0 falls back to the built-in
    default instead of rendering the single-click name label black; invis/hidden
    hues keep an explicit 0. sphere.ini docs corrected (hue is hex; 0 = default) and
    the duplicate/incorrectly-"unimplemented" block removed.

LOGGING:
  - Demoted [move_z_delta] (per-step) and [tick_stats] / [gc_stats] (every 30s) from
    INFO to DEBUG so a debug-off console stays quiet; they reappear when packet or
    script debug raises the level. [slow_tick] stays a warning.

[2026-06-06] - Panel update service, smelt/spell item triggers, configurable hues (Wave 72)
----------------------------------------

HOST / PANEL:
  - Added UpdateService and the POST /api/update/run + GET /api/update/status
    endpoints, surfaced as a "System Update / Update from GitHub" card on the
    Settings page (state and last log lines shown live). The update runs git
    status, git pull --ff-only, server save, server stop, build.ps1 -ServerOnly,
    then server start, in that order.
  - build.ps1 gained a -ServerOnly switch that publishes only SphereNet.Server and
    copies the panel static files, leaving the running (and therefore locked)
    SphereNet.Host.exe untouched so the panel can update itself.

TRIGGERS:
  - @Smelt: double-clicking ore now prompts for a forge target and fires @Smelt
    (N1 = ore amount, O1 = forge, S1 = smelt message) before the native smelt;
    RETURN 1 cancels it. On success the ore stack is consumed and an iron ingot
    stack is added to the backpack (or dropped if there is no pack), firing
    @Create on the result.
  - Item-targeted @SpellEffect: Mark, Recall/Gate rune travel, Dispel Field and
    generic TargObj item targets now fire @SpellEffect (O1 = caster, N1 = spell id,
    S1 = spell name); RETURN 1 suppresses the native behaviour. SpellEngine takes
    a TriggerDispatcher, wired from Program.

CONFIG:
  - Notoriety and invisibility hues are configurable via sphere.ini: ColorNotoGood,
    ColorNotoGoodNPC, ColorNotoGuildSame, ColorNotoNeutral, ColorNotoCriminal,
    ColorNotoGuildWar, ColorNotoEvil, ColorNotoInvul, ColorNotoInvulGameMaster,
    ColorNotoDefault, ColorInvisItem, ColorInvis, ColorInvisSpell, ColorHidden.
    NotoToHue now resolves through the loaded NotorietyHueSettings, distinguishing
    NPC innocents (GoodNpc) and GM-level invulnerables (InvulGameMaster). Hue keys
    parse hex; PetsInheritNotoriety and AdvancedLos keys are also read. Hex/decimal
    parsing handled by GetHue / GetIntOrHex helpers.

NPC AI:
  - Consolidated every NPC move/wander/chase/path-step gate into a single
    CanNpcMoveTo check: negative-coordinate guard, tile passability, static-block
    items and occupying characters, all skipped when the NPC can pass walls
    (hidden/invisible staff above the occupied tile do not block).

NETWORK:
  - 0xBE now distinguishes the modern length-prefixed assist/version packet from
    the legacy 5-byte uint form, via TryGetLegacyAssistPacketLength: implausible
    declared lengths followed by a real packet boundary are treated as the legacy
    form so following movement packets are not stranded behind a phantom partial.
  - 0xD9 Hardware Info length corrected to 0x010C (268 bytes total).

SCRIPTING:
  - SRC-aware dialog verb dispatch: UID.<obj>.DIALOG and similar verbs now route
    through _REF_EXEC_AS (srcUid|targetUid|command|args) when SRC is known, so
    client-bound verbs open on SRC's client while the UID object stays the dialog
    subject. HandleDialogCommand / OpenNamedDialog thread the subject through.
  - TriggerRunner stops a trigger body at any following ON= key (not only @-named
    triggers), so ON=*keyword* speech blocks no longer bleed sibling responses
    into one spoken line.

[2026-06-06] - Pickup-source trigger variants (@Pickup_Self / @Pickup_Stack) (Wave 71)
----------------------------------------

TRIGGERS:
  - HandleItemPickup now selects the pickup-source trigger by where the item is
    taken from, via SelectPickupTrigger: Self (dragged off the picker's own
    equipment layers), Stack (a partial amount split out of a larger stack), Pack
    (from inside a container), Ground (loose on the ground). The equip check runs
    before the container check, since equipped items report ContainedIn = the
    wearer and previously fired @Pickup_Pack by mistake. RETURN 1 cancels the
    pickup in every case.

[2026-06-06] - NPC first-sight perception (@NPCSeeNewPlayer) (Wave 70)
----------------------------------------

TRIGGERS:
  - Added per-NPC seen-player memory and wired @NPCSeeNewPlayer.
    Character.SeeNewPlayer records a perceived player and fires the trigger only on
    a first sighting (the per-player timer refreshes on continued sight, so a
    player standing nearby is not re-greeted; it fires again after the player
    leaves for longer than the TTL and returns). NpcAI scans nearby visible players
    each tick, gated on the Character.OnNpcSeeNewPlayer hook (installed only when a
    script hooks the trigger) plus a throttle, so the range scan is skipped when
    unused. O1 = the newly-seen player. Reset in ResetEngineStatics.

[2026-06-06] - Quick-skill check/gain split (@SkillUseQuick) (Wave 69)
----------------------------------------

TRIGGERS:
  - Split SkillEngine.UseQuick's check from its experience gain and wired
    @SkillUseQuick: the trigger now fires (via the IsTrigUsed-gated
    Character.OnSkillUseQuick hook) BEFORE the success check, so a script can
    cancel the quick skill use (RETURN 1 → treated as a failure) before any roll
    or experience gain. N1 = skill, N2 = difficulty. Reset in ResetEngineStatics.

[2026-06-06] - Per-character environment tracking (@EnvironChange) (Wave 68)
----------------------------------------

TRIGGERS:
  - Added per-character perceived-light tracking and wired @EnvironChange.
    Character.UpdateEnvironLight records the current light level and fires
    @EnvironChange (N1 = new light) only when it actually changes; the first call
    just establishes the baseline so entering the world does not spuriously fire.
  - Program drives it from the region-change hook with the surface/dungeon light
    (28 underground, else global light), so crossing a dungeon/surface boundary
    fires the trigger. Routed through a new static Character.OnEnvironChange hook
    reset in ResetEngineStatics. (Global day/night EnvironChange for stationary
    players is a follow-up.)

[2026-06-06] - Call-guards trigger (@CallGuards) (Wave 67)
----------------------------------------

TRIGGERS:
  - Wired @CallGuards. The "guards" keyword handler already existed in
    Program.NpcServices (region-guarded gate, hostile search, redirect of idle
    patrol guards, city-guard summon and optional instant-kill) — only the trigger
    was missing. @CallGuards now fires on the caller for each reported
    criminal/murderer (<argo> = the hostile); a script returning 1 cancels the
    guard response for that hostile.

[2026-06-06] - Item-trigger IsTrigUsed gate and @MemoryEquip (Wave 66)
----------------------------------------

SCRIPTING:
  - Added the item-trigger counterpart of the IsTrigUsed gate:
    TriggerDispatcher.IsItemTriggerUsed, backed by a used-item-trigger set fed from
    RegisterItemEvent, the loaded [ON=@X] resource scan (shared with char triggers)
    and f_onitem_x functions. Covers the cross-fired @itemX mirror.

TRIGGERS:
  - Wired item @MemoryEquip: a memory item equipped on a character (Memory_CreateObj)
    now fires the trigger on that memory item. Memory items are created frequently in
    combat, so the fire is installed only when a script hooks it (the new item gate),
    keeping the path a null check otherwise. Routed through a new static
    Character.OnMemoryEquip hook wired in Program.EngineWiring and reset in
    ResetEngineStatics.

[2026-06-06] - Jail trigger (@Jail) (Wave 65)
----------------------------------------

TRIGGERS:
  - Wired @Jail. The jail system already existed (the GM JAIL command teleports
    the target to the cell, sets the Freeze flag and a JAIL_RELEASE tag, with the
    timed release handled from Program) — only the trigger was missing. @Jail now
    fires on the jailed character with the sentence length (N1 = minutes, 0 =
    indefinite). Routed through a new static Character.OnJailed hook wired in
    Program.EngineWiring and reset in ResetEngineStatics.

[2026-06-06] - Pet desertion trigger (@PetDesert) (Wave 64)
----------------------------------------

TRIGGERS:
  - Wired @PetDesert. The pet loyalty loop already existed (TickPetOwnershipTimers
    decays NpcFood every 60s, NpcAI.TryEatFood restores it from eaten food, and a
    pet goes wild via ClearOwnership at zero) — only the trigger was missing. It
    now fires on the pet when loyalty reaches zero, just before the pet goes wild;
    a script returning 1 cancels the desertion. Routed through a new static
    Character.OnPetDesert hook wired in Program.EngineWiring and reset in
    ResetEngineStatics.

[2026-06-06] - Ship and redeed item triggers (Wave 63)
----------------------------------------

TRIGGERS:
  - Wired item triggers @ShipMove, @ShipStop, @ShipTurn (fired on the ship multi
    from new ShipEngine OnShipMoved/OnShipStopped/OnShipTurned hooks in Move/Stop/
    Face) and @Redeed (fired on the freshly-created deed from a new House.OnRedeed
    static hook in House.Redeed). All routed to FireItemTrigger in
    Program.EngineWiring; House.OnRedeed is reset in ResetEngineStatics.

[2026-06-06] - Global EVENTS / SPEECH config hooks (Wave 62)
----------------------------------------

SCRIPTING / CONFIG:
  - Added sphere.ini global script-hook lists read into SphereConfig: EventsPlayer,
    EventsPet, EventsItem, EventsRegion (global EVENTS applied to every player /
    NPC / item / region) and SpeechSelf, SpeechPet (global SPEECH blocks for
    player and NPC speech). Each value is a comma-separated list of defnames.
  - TriggerDispatcher resolves these into ResourceId lists and runs them in the
    trigger chain: a character trigger now runs the global EVENTSPLAYER/EVENTSPET
    resources after CHARDEF and before the f_onchar_ fallback; item triggers run
    the global EVENTSITEM resources. Speech routes through SpeechSelf (players) and
    SpeechPet (NPCs) before the built-in keyword handling.
  - Lists are populated in Program.EngineWiring from the config defnames;
    empty/unknown entries are skipped, so the path is a no-op when unconfigured.

[2026-06-06] - Movement shove and spell-effect triggers (Wave 61)
----------------------------------------

TRIGGERS:
  - Wired @PersonalSpace (MovementEngine shove point — fired on the mover when it
    pushes past another character's tile) and @EffectAdd (SpellEngine — fired when
    a temporary spell effect is registered for a target). Both route through new
    static Character.On* hooks wired in Program.EngineWiring and reset in
    ResetEngineStatics; @EffectAdd is installed only when a script hooks it
    (IsTrigUsed gate), so applying a buff is a null check otherwise.

[2026-06-06] - Char trigger P0/P1 wiring, IsTrigUsed gate and Source-X parity test harnesses (Wave 60)
----------------------------------------

BUILD / CI:
  - Target framework pinned to net10.0 end to end: the CI and nightly benchmark
    workflows install 10.0.x, a root global.json pins the SDK (10.0.100,
    latestFeature), and the test-output path no longer carries a net9.0 segment.
  - TreatWarningsAsErrors enabled in Directory.Build.props as a warning gate for
    local and CI builds; the one outstanding nullable warning was cleared.

TRIGGERS:
  - Wired char triggers that were defined but never fired: @KarmaChange,
    @FameChange and @MurderMark (DeathEngine notoriety hooks; N1 = delta/new
    count, RETURN 1 cancels or blocks), @SkillChange and @StatChange (SkillEngine
    runtime gain hooks), @CombatAdd / @CombatDelete / @CombatEnd (Character
    attacker-list hooks), @MurderDecay (notoriety-decay hook), @NPCRefuseItem
    (drop-on-NPC accept gate), @NPCSpecialAction (breath/throw cancel gate),
    @NotoSend (ComputeNotoriety colour override), @Eat (food/booze use-item
    gate), @SkillMenu, @SkillWait, @Follow (pet follow/come command),
    @PartyDisband, @SpellSelect and @SpellBook.
  - Value mutations route through new static Character.On* hooks wired in
    Program.EngineWiring; the new hooks are reset in ResetEngineStatics.

SCRIPTING:
  - Added an IsTrigUsed gate (TriggerDispatcher.IsCharTriggerUsed +
    BuildUsedTriggerCache) that scans loaded [ON=@X] blocks, f_onchar_x functions
    and registered handlers, so a hot-path trigger only dispatches when a script
    hooks it. @NotoSend uses it to stay a null check on the per-observer
    ComputeNotoriety path.

ACTIVE SKILL:
  - Corrected the ActiveSkillEngine note: the per-tick @SkillStroke loop is
    implemented in the client (TickPendingSkill) while the engine stays stateless.

TESTS:
  - Added Source-X parity harnesses: vendor 0x3B/0x9F packet roundtrip,
    in-process loopback login (seed, no-crypt, 0x80/0xA0/0x91/0x5D through to
    enter-world), 0xBF extended-command dispatch, a trigger-coverage guardrail
    that locks the not-fired backlog by P0/P1/P2, the active-skill stroke matrix,
    and per-trigger behaviour tests for each wiring above.

DOCS:
  - docs/PARITY.md and docs/TRIGGERS.md updated for the now-covered items and the
    remaining deferred triggers, each noted with the infrastructure it needs.

[2026-06-06] - Animation client-type gating, NPC facing, tooltip and regen parity (Wave 59)
----------------------------------------

ANIMATION:
  - The body-agnostic 0xE2 new-animation packet is now sent only to KR and
    Enhanced clients; Classic/ClassicUO clients receive the legacy 0x6E packet
    even when their version reports High Seas or newer. Selection switched from
    SupportsHighSeas to the parsed client type. Added NetState
    IsKingdomRebornClient / IsEnhancedClient.

NPC AI:
  - NPC attacks no longer emit a synthetic 0x77 MobileMoving on every hit/miss.
    A facing update is broadcast only when the swing actually changes the NPC's
    direction (OnNpcFacingChanged -> BroadcastFacingUpdate), matching Source-X.

TOOLTIP:
  - A 0xD6 full property-list response is no longer followed by a 0xDC revision
    packet (Source-X sends only the requested list). ToolTipMode default is now
    1 (0=off, 1=revision/request, 2=force full) and a ToolTipCache setting was
    added; set TOOLTIPMODE=0 to disable tooltips entirely.

REGEN:
  - HP/stam/mana regeneration intervals derive from the configured REGEN values
    (tenths of a second) instead of hard-coded periods. The HP regen no longer
    grants an extra +1 per tick for STR > 50; it heals 1 per interval while fed.

[2026-06-06] - Client speed mode (0xBF sub 0x26) parity (Wave 58)
----------------------------------------

PROTOCOL / PACKET:
  - Added 0xBF sub 0x26 (PacketSpeedMode) outgoing packet, carrying the client
    movement speed mode byte.

MOVEMENT:
  - SPEEDMODE bit 0 makes a foot character move at the mounted cadence
    (walk/run 200/100ms instead of 400/200ms), matching Source-X Event_Walk.
    Added a speedMode-aware GetMoveDelay overload; the per-step move delay now
    derives from the character's SpeedMode.
  - The speed-hack detector treats a speed-mode character as mounted so the
    faster cadence is not flagged as a violation or counted as a burst.
  - SendSpeedMode pushes the current mode to the active client and resets the
    walk validator (token/throttle state) so the new cadence applies cleanly.

GM / COMMANDS:
  - SPEEDMODE is a GM command (".speedmode <n>"); it sets the property and echoes
    the resulting value.
  - The mode is (re)sent to the owning client on login and whenever SpeedMode
    changes via a command or a .set property edit, so client and server agree on
    cadence without a relog.

[2026-06-05] - Combat attack handshake parity and NPC special-attack facing (Wave 57)
----------------------------------------

PROTOCOL / PACKET:
  - Added 0x2F (combat swing/fight notification: attacker + defender serials)
    and 0xAA (attack target confirmation; serial 0 means refused) outgoing
    packets.

COMBAT:
  - Attack requests now reply with an accept/refuse handshake. A refused attack
    (target 0/self, a hidden staff target, region/PvP block, or an Attack /
    CombatStart trigger returning true) sends 0xAA=0; an accepted attack sends
    0x2F Swing followed by 0xAA carrying the target serial.
  - War mode is entered and the fight target is set only on an accepted attack.
    Previously the request forced war mode (and assigned FightTarget) up front,
    before the rejection checks, so a refused attack still flipped the attacker
    into war mode and left a stale target.

NPC AI:
  - Breath and throw special attacks turn the NPC to face the target and
    broadcast a 0x77 direction update before emitting the effect, so the
    projectile/breath originates from the correct facing instead of the NPC's
    previous direction (FaceAndBroadcastToward).

[2026-06-05] - Turn-in-place movement parity, walk-token config fix, reject diagnostics (Wave 56)
----------------------------------------

MOVEMENT:
  - Turn-in-place parity: a 0x02 walk request whose direction differs from the
    character's current facing is now handled as a pure turn, not a tile step.
    It only updates facing, sends 0x22 MoveAck, and broadcasts 0x77, bypassing
    the throttle, walk-token bucket, collision and Z resolution. Previously such
    a packet advanced the character one tile, so the client's direction-change
    "doublet" packets (~31-47ms apart while running) produced an extra server
    step, a momentary animation hitch, and a sideways snap near stairs/walls.
  - Walk-token bucket is honored as documented and no longer starves movement on
    a low setting: WalkBuffer is the bucket capacity (allowed step burst) and
    WalkRegen is tokens-per-SECOND (a rate, not a per-step tick period). A
    running mover needs ~5 steps/s, so a regen below that drained the bucket and
    rejected legitimate steps as walk_buffer, snapping the client back into a
    repeated stutter. Config restored to working values (75 / 25) and the stale
    "loaded but unused" comments corrected. Validate() now warns when WalkRegen
    or WalkBuffer is set below running speed.
  - Post-reject in-flight steps (seq > 1 arriving after a reject reset
    WalkSequence to 0) are dropped silently — no extra 0x21 reject and no 0x20
    DrawPlayer redraw. Echoing either made the client ClearSteps and snap again,
    cascading into a client resync (the visible walking "teleport"/flash). The
    fixed post-reject absorb window (MoveRejectResyncMs) is disabled accordingly.
  - GM/staff movement is routed through the movement queue instead of an
    immediate step, so the client's transition doublet is paced to the natural
    cadence; the throttle is still skipped for staff, so GM is smoothed but never
    throttle-rejected. Movement speed is unchanged.
  - Queued movement is drained every main-loop iteration (not only on the 50ms
    tick), so the ack/reject flushes in the same iteration as input. The per-step
    _nextMoveTime gate still paces the real drain, so this only cuts latency for
    the client's 5-step budget on fast/stair sections; it does not raise speed.

DIAGNOSTICS:
  - Movement rejections are classified into a normalized cause (throttle,
    walk_buffer, descent_cap, impassable_static, no_surface, z_window,
    diagonal_edge, mob_block, seq_mismatch, queue_full, stale) and accumulated
    into a per-session [move_reject_tally] summary, so the dominant disagreement
    class over a walking session can be aimed instead of guessed. Pure logging.

[2026-06-03] - Packet-era compatibility, version-aware animation, movement & send-priority fixes (Wave 54)
----------------------------------------

PROTOCOL / PACKET:
  - 0x00 Create Character is always 104 bytes. Removed a version-gated
    104/106 length override (the 106-byte form is a distinct opcode, 0xF8);
    treating 0x00 as 106 for modern clients over-read and desynced the stream.
    Removed the now-unused, incorrectly-computed IsClientPost70180 flag.
  - Unregistered modern opcodes (0xF1/F2/F7/F9/FA/FB/D0/DD/E0) are consumed by
    their declared framing and ignored without desync; unknown-opcode logging
    is rate-limited to once per opcode per connection (ShouldLogUnknownOpcode).
  - 0xD7 encoded command (custom-house design editor) decoupled from the 0xBF
    extended-command dispatch: their subcommand IDs overlap (encoded Build 0x06
    vs party 0x06, Roof 0x13 vs context-menu, Revert 0x1A vs stat-lock), so a
    shared table mis-routed house actions into unrelated handlers. Custom-house
    design actions are now safely rejected/ignored (editor not yet implemented).
    Added EncodedCommandRegistry documenting the subcommand set.

ANIMATION:
  - 0xE2 new-animation packet put to use with per-recipient client-version
    selection: High Seas+ clients receive the body-agnostic 0xE2 gesture while
    older clients keep the legacy 0x6E action index. Added NewAnimationGesture
    enum; combat swing/get-hit/miss (player and NPC) route through the shared
    version-aware broadcast.

MOVEMENT:
  - Walking/running on foot no longer drains stamina (Source-X parity: only
    shoving past a mobile costs stamina; the per-step run drain was incorrect).
  - Land-tile walkability matches the client: only impassable WET (water) land
    blocks a walker. Dry impassable slope/mountain terrain is walkable again;
    blocking it desynced from the client and rubber-banded movement beside
    stairs and slopes.
  - Single-step Z descents beyond MaxDescendZ (11) are rejected so a mover stops
    at a cliff edge instead of plunging; ordinary stair/slope steps (<= ~2
    risers) are unaffected. Climbing is unchanged.

NETWORK (send priority):
  - Source-X packet-priority model: outgoing packets are classified
    (Idle/Low/Normal/High/Highest) by PacketPriorityClassifier and NetState
    drains its queues Highest -> Idle each flush. Movement ack/reject
    (0x21/0x22) and auth now lead bulk world/UI traffic, so a running client no
    longer waits on step confirmations and over-predicts.
  - The post-reject resync window no longer echoes a 0x21 per buffered step.
    Echoing one reject per in-flight step made the client resend (rejected
    again) in a tight feedback storm on low-latency links that froze the player
    and read as a teleport. Stale steps inside the window are absorbed; if the
    client still sends seq>1 after the window, the server sends a fresh
    corrective reject + self redraw to break the walk-sequence deadlock.

MOUNT / USE:
  - Double-clicking a mount NPC no longer fails silently when the mount attempt
    is rejected; the player now gets the mount-failed message. The mount sound
    is broadcast only after a successful mount.

TESTS:
  - Added era-boundary (7.0.18-7.0.30 for 0x00/0xF8, 0x08 14/15 grid index),
    unregistered-opcode robustness, custom-house reject, animation
    version-selection, land-block rule, packet-priority, and stale
    walk-sequence recovery tests. Full suite green (583).

[2026-06-01] - Bot combat/magic buff + MageBot; gameplay robustness pass (Wave 53)
----------------------------------------

DIAGNOSTICS:
  - Bot accounts now receive a combat/magic buff on login (Str/Dex/Int 100,
    Wrestling/Tactics/Anatomy/Magery/EvalInt/Healing maxed) so test bots can
    actually deal damage and cast — newbie stats can't out-damage monsters. Bot
    accounts are ephemeral; re-applied each login; gated on IsBotAccountName.
  - Added MageBot (reactive spellcaster: finds the nearest monster and casts an
    offensive spell via the cast→target flow) + wired into BotBehavior/
    CreateRoleBehavior and the --botrunner parser ("mage").

  Findings: across combat, mage and chaos bot runs against the scripted world +
  hostile NPCs — ZERO server exceptions, crashes, wire corruption or bot
  anomalies. The player melee/damage path was read and verified correct
  (unarmed uses Wrestling -> ~85% hit vs an unskilled mob, real Str-scaled damage
  with Tactics/Anatomy bonus). No server-side gameplay bug was found.
  Caveat: reliably making bots KILL mobs is a harness positioning/movement
  problem, not a server one — clustered bots are packed and can't path to a
  target, while spread bots end up isolated from the mob hotspots, so few swings
  land. Tuning bot/mob co-location (or a server-side bot stat boost plus a
  dedicated "skirmish" spawn) is the open item for end-to-end combat-correctness
  bot tests; the code paths themselves are exercised and robust.

[2026-06-01] - Reactive bot infrastructure: per-bot receive loop (Wave 52)
----------------------------------------

DIAGNOSTICS:
  - Each bot now runs a dedicated background receive loop (ReceiveLoopAsync) that
    is the sole socket reader once the bot is playing: it continuously reads
    packets, updates the world model and completes pending actions. Previously
    the only reader was a drain between behavior iterations, and WaitResult-based
    actions stalled ~1 s with a stale world — so combat/skill bots fired blind.
    Now the bot's world model stays current and WaitResult returns promptly, so
    behaviors actually REACT to what they see.
  - Thread-safety for the new cross-thread access: BotWorldModel.SyncRoot guards
    the mobile/item dictionaries (FindNearest/RemoveStale and the receive-loop
    mutations in ProcessIncomingPacket); BotActionApi guards _waitKind/
    _pendingAction with a lock (the TCS already uses RunContinuationsAsynchronously
    so completions never run inline under a lock). Behavior loops no longer drain
    the socket themselves (would be a second reader on the stream).

  Verified: 300 combat bots clustered among 2,000 hostile NPCs now genuinely
  react — pps_in ~1,000-1,700 and recv climbing into the hundreds of thousands
  (vs ~10/bot frozen before), bots attack what they see. Zero server exceptions
  and zero bot anomalies across the run; server tick ~1-1.5 ms. (Newbie bot
  characters don't out-damage 50-HP monsters, so mobs don't die — realistic weak
  combat, not a bug.) This is contained to the bot harness; the server/wire is
  untouched. Confirms the combat code path is robust under reactive load and
  gives a foundation for richer bot gameplay tests.

[2026-06-01] - Bot gameplay fuzzing pass + reactive-behavior groundwork (Wave 51)
----------------------------------------

DIAGNOSTICS:
  - The --botrunner behavior parser accepts the full set: walk, combat, idle,
    smart, full, cluster, skill, loot, vendor, social, chaos.
  - CombatBot reworked to one decision per call (returns each iteration) so the
    runner loop drains incoming between calls, matching the ClusterBot fix.

  Robustness result: 200 chaos bots firing ~24k random gameplay actions
  (attack / skill / double-click / pick-up / target / speech / war-mode, with
  random serials) plus a 300-combat run against a scripted world + 2,000 hostile
  NPCs produced ZERO server exceptions / crashes / wire corruption. The gameplay
  packet handlers are robust against fuzzed and invalid input. One handled edge
  case observed: a bot stepping on a telepad mid-move triggered a script
  teleport, which the server detected and suppressed the MoveAck for (correct).

  Honest limitation found: the bot client has no continuous background socket
  reader. It only reads between behavior iterations, and WaitResult-based actions
  (move/attack) stall ~1 s each waiting for a response that nothing reads during
  the action — so a combat/skill bot's world model never stays current and it
  fires largely blind (cannot reliably see/target real monsters). The
  one-iteration rework helps speak-only bots (ClusterBot) but not WaitResult-
  heavy ones. Genuinely REACTIVE bot gameplay (and outcome-verifying correctness
  tests) needs a per-bot background receive loop + assertions — a real refactor.
  Gameplay correctness is meanwhile covered by the 529 unit tests; bots remain
  best for load/stress (validated) and crash fuzzing (validated, zero crashes).

[2026-06-01] - Configurable world-loop tick interval (Wave 50)
----------------------------------------

CONFIG:
  - The world-loop tick interval is no longer hardcoded to 50 ms. New sphere.ini
    key ServerTickMs (default 50 = 20 ticks/s, clamped to [20,250]) sets it.
    Raising it (e.g. 100 = 10 ticks/s) doubles the per-tick budget for very heavy
    active-AI loads at the cost of coarser world-sim timing — which UO's
    movement (200-400 ms/step) and combat (1-4 s swings) tolerate easily. Network
    I/O is NOT gated by this (it runs every main-loop iteration), so the tick
    rate affects world simulation granularity, not packet latency.
    Verified: ServerTickMs=100 yields ~301 ticks per 30 s window (10 Hz) vs ~601
    at the default 50 ms (20 Hz). Default behavior is unchanged.

[2026-06-01] - Anchored cluster bot + conclusive out-of-process soak (Wave 49)
----------------------------------------

DIAGNOSTICS:
  - ClusterBot now does ONE iteration per call and returns, instead of looping
    forever internally. The runner's behavior loop drains incoming packets
    between calls, so a speak-only bot that never returned never read its socket;
    its inbound backed up, the server saw the connection as hopelessly behind and
    shed everything bound for it. Returning each iteration lets the bot consume
    the broadcast flood it generates. It also anchors each bot to its spawn tile
    (Radius 6) so the fleet stays clustered instead of dispersing.
  - Cluster placement is decided SERVER-side (BotSpawnLocationProvider -> the
    server's BotEngine), so clustering an out-of-process fleet must be configured
    on the server: new --botcity <name> and --botcluster flags pack bot logins
    around a town. (--trustloopback from Wave 48 covers the connection filter.)
  - The runner logs periodic active/pps stats so bot drop-off and real load are
    visible.

  Conclusive soak (server alone in its own process, 1,000 cluster bots from a
  separate --botrunner process, all anchored + speaking in one town):
    - 1,000/1,000 bots stay connected; they actually receive and drain the
      broadcast flood (pps_in a few thousand/s, recv climbing into the millions).
    - Server holds ~5 ms avg tick (p95 ~50 ms), 20 Hz, players=1000, ZERO
      shedding, ZERO backpressure-cap disconnects, ZERO wire corruption; RSS
      ~1.3 GB, ~2 cores.
    - This confirms the earlier IN-PROCESS cluster collapse (1,000 -> ~60, then
      millions shed) was a harness artifact: in-process the bots were CPU-starved
      and couldn't drain, so the server's per-connection buffers backed up. With
      the bots in their own process they drain fine and the server handles 1,000
      clustered, broadcasting clients cleanly.
    - Caveat: broadcast fan-out was moderate (each speech reached a neighbourhood,
      not all 1,000 — spawn radius + anchor drift), so this is a solid sustained
      soak, not the absolute densest worst case. Same machine still shares cores.

[2026-06-01] - Out-of-process bot load runner + soak finding (Wave 48)
----------------------------------------

DIAGNOSTICS:
  - The server exe gains a --botrunner <host> <port> <count> <behavior> [city]
    mode that runs ONLY the bot engine against a separately-running server, so
    the load generator has its own process (own thread pool + GC) and does not
    contend with the server — removing the in-process CPU-contention artifact
    that confounded the cluster measurements.
  - --trustloopback skips the connection rate-limit/accept filter for loopback
    clients (the flood filter otherwise rejects an out-of-process runner opening
    ~1,000 connections from 127.0.0.1; the in-process path bypassed it via
    BotEngine bot-mode, which a separate process doesn't share).

  Soak finding (honest, partly inconclusive):
    - With the server isolated in its own process, 1,000 connected bots sit at
      ~1.2 ms avg tick (p95 ~2 ms), 20 Hz, zero shedding, zero disconnects, ~490
      MB server RSS. This strongly suggests the earlier IN-PROCESS cluster
      "load" (avg climbing to ~37 ms, millions shed) was dominated by harness
      CPU/GC contention — the bots and server fighting for one machine's cores
      and one GC — not genuine server broadcast cost.
    - Caveat: in this run the cluster bots were near-idle (runner used ~2 s CPU
      over the run), i.e. they did NOT reproduce the speech-broadcast flood, so
      the out-of-process broadcast path is not yet conclusively soak-tested. The
      load-generation behavior (keep the fleet clustered and actually sustaining
      the flood) needs another pass before the out-of-process broadcast numbers
      can be trusted. Same machine also still shares cores; true isolation needs
      a second machine.

[2026-06-01] - Fix async-send / interest-management interaction (Wave 47)
----------------------------------------

NETWORK (review fixes for Wave 46):
  - Interest management now sees the async backlog. The chatter-shed check keyed
    only on _sendQueue.Count, but in the async model the queue drains into the
    byte buffer each flush, so a slow client's real backlog lives in
    _outEnd-_outStart with a low queue count — chatter was never shed and the
    client instead hit the hard 512 KB cap and disconnected. Shedding now also
    triggers on byte pressure (outbound backlog > 256 KB), restoring graceful
    degradation in non-blocking mode.
  - The 512 KB cap is now enforced DURING append, before growing the buffer, so a
    huge queue / large gump / burst can't balloon the buffer past the cap in a
    single flush (worst-case memory is now genuinely bounded near the cap).
  - Receive() no longer treats a transient non-blocking WouldBlock/Interrupted as
    a lost connection (returns "no data" instead of disconnecting).
  - Cleaned the NetState nullability warning (byte[]? shared).

  Impact — this fixed more than the review notes: with chatter shed on byte
  pressure, the 1,000-bot one-screen flood now HOLDS all 1,000 clients at 20 Hz
  (~3 ms avg) with ZERO backpressure-cap disconnects, versus the earlier collapse
  to ~60. The earlier "attrition is pure CPU starvation, unchanged" conclusion
  was incomplete: the dominant cause was the un-shed speech flood (the bypassed
  interest management this fixes), not the bots' CPU. Peak RSS ~2.1 GB, zero wire
  corruption, zero final-flush errors. 529 tests pass.

[2026-06-01] - Non-blocking (async) game send (Wave 46)
----------------------------------------

NETWORK:
  - The game stream no longer uses a blocking Socket.Send. A slow client, a full
    OS send buffer, or a packet burst could previously block the flush thread
    that was serving it. The game socket is now set non-blocking on login, and
    FlushOutput accumulates compressed+encrypted bytes into a persistent
    per-connection buffer ([_outStart.._outEnd), stream-ordered) and drains only
    what the socket accepts (Send with out SocketError; WouldBlock leaves the
    remainder for the next flush). The server thread never blocks.
  - Backpressure: a connection buffering more than 512 KB of unsent bytes is
    hopelessly behind and is disconnected (bounds worst-case memory to
    ~maxClients * 512 KB). Clear() now does a best-effort final flush before
    shutdown so a graceful disconnect still delivers queued/pending data.
  - Design note: a synchronous non-blocking + per-tick retry model is used rather
    than SocketAsyncEventArgs/SendAsync — it reaches the same goal (no blocked
    thread) without completion-callback races, in-flight tracking and post-
    completion cleanup, which are the parts most likely to produce wire bugs
    worse than blocking. One active send per connection is inherent (flush is
    serial per connection), packet order is preserved (single ordered buffer),
    and the shared-broadcast/pre-compress path is unchanged. Toggle:
    NetState.NonBlockingGameSend (default on) keeps the old blocking path for A/B
    and rollback.

  Validated: 529 tests; 300-combat correctness run (zero wire corruption, zero
  backpressure trips — healthy connections drain fully each tick, tick
  unchanged); 1,000-bot cluster — zero corruption and zero final-flush errors
  across ~1,900 teardowns, the server never blocks, slow clients are shed cleanly
  by the byte cap instead of a blocking cascade, peak RSS ~2.1 GB (was ~4.6 GB at
  a 2 MB cap). The in-process bot cluster's eventual attrition is unchanged — it
  is the bots being CPU-starved, not the send path — but the failure mode is now
  bounded and non-blocking.

[2026-06-01] - Interest management: shed cosmetic chatter under pressure (Wave 45)
----------------------------------------

NETWORK:
  - When a connection's send queue backs up past a soft cap (1024, well below the
    4096 hard-overflow disconnect), low-priority cosmetic broadcasts to it —
    overhead speech (0xAE/0x1C) and sound (0x54) — are now shed instead of piling
    on toward an overflow disconnect. State-bearing packets (movement, status,
    combat results, corpses, gumps, ...) are never dropped. Classification is by
    opcode in EnqueueShared, so no broadcast call site changed; the shared
    packet's refcount is released for the shed recipient. A [gc_stats] "shed="
    counter reports how many were dropped per window.

  Behaviour / honesty:
    - Inert in normal play: a 300-combat + 2,000-hostile run sheds nothing
      (shed=0; queues never approach the cap) and tick/GC are unchanged — zero
      harm to normal gameplay.
    - Under the 1,000-bot one-screen flood it sheds ~165k chatter packets/window
      and the server logs zero overflows. It does NOT change that scenario's
      eventual bot attrition: that collapse is the in-process bots being
      CPU-starved (they share the machine with the server), not speech volume —
      shedding drops to 0 once the bots idle yet they still disconnect. So the
      mechanism is verified correct and is a standard graceful-degradation layer,
      but its real-world benefit (a genuine remote client draining less) is not
      measurable with the in-process bot harness, and per-client inbound recv
      capacity can't be cleanly measured here for the same reason.

[2026-06-01] - Parallel output flush (Wave 44)
----------------------------------------

PERFORMANCE / NETWORK:
  - ProcessAllOutput flushed every connection serially. Each connection's flush
    touches only its own state (send queue, crypto, batch buffer, socket), so
    once shared broadcast payloads are precomputed they parallelize cleanly.
    Flushing now runs across cores (Parallel.ForEach, capped at ProcessorCount)
    when the active-connection count is above a threshold; below it, the serial
    path is kept to avoid parallel overhead.
  - To make this race-free, a shared broadcast packet's Huffman-compressed
    payload is now computed once in MarkShared (at broadcast time) instead of
    lazily on the first flush; FlushOutput only reads that cache, never writes
    it. The pool return already uses an interlocked refcount.
  - Measured: 300 combat + 2,000 hostile (parallel flush engaged) — p95 tick
    roughly halved (~72 ms -> ~40 ms) and average dropped (~14-21 ms -> ~10 ms)
    versus the serial flush, with zero wire-corruption, zero overflows. The
    1,000-bot dense cluster still holds all 1,000 at 20 Hz with zero overflows;
    its eventual attrition under the extreme flood is client-side (in-process
    bots cannot drain ~1,000 inbound speech packets/s each) and is unchanged by
    server-side flush parallelism, as expected.

[2026-06-01] - Shared path for movement broadcasts (Wave 43)
----------------------------------------

PERFORMANCE / NETWORK:
  - BroadcastMoveNearby now uses the shared-broadcast path too (Wave 42). The
    move packet is identical for every recipient, so it is built/compressed once
    and reused; the per-recipient work that genuinely differs — sending a full
    appearance to a client that doesn't yet know the mover, and updating that
    client's known-position bookkeeping — is preserved exactly (appearance runs
    while collecting recipients, position update runs as the shared packet is
    enqueued).
  - Validated: 529 tests; a 300-combat + 2,000-hostile run (2,000 NPCs moving
    near players, exercising move broadcasts heavily) — 300/300 connected, zero
    malformed/underrun/parse anomalies, zero send-queue overflows, tick within
    the normal combat range.

[2026-05-31] - Shared broadcast packets — raise the dense-crowd wall (Wave 42)
----------------------------------------

PERFORMANCE / NETWORK:
  - A broadcast event has identical bytes for every recipient, yet the old path
    rebuilt and re-Huffman-compressed the packet once per recipient. Broadcasts
    now build the packet ONCE, mark it shared, and enqueue the same buffer onto
    each recipient's queue. The first flush Huffman-compresses it and caches the
    (pre-encryption) bytes; the rest reuse the cache. For no-crypt game streams
    (the common case) the cached bytes are the final wire bytes and are appended
    directly — zero per-recipient compress/build. Crypted connections copy the
    cache into a per-connection scratch and encrypt that, so the shared cache is
    never mutated. PacketBuffer gained a refcount (returned to the pool only when
    the last recipient has flushed; unicast stays refcount 1, unchanged).
    BroadcastMoveNearby (per-recipient appearance side effects) is left on the
    old path for now.

  Validated:
    - 529 tests pass.
    - Correctness (300 combat + 2,000 hostile, no-crypt bots): 300/300 stay
      connected, zero malformed/underrun/parse anomalies, tick unchanged.
    - Dense-crowd wall (1,000 bots clustered in one town, all speaking): vs the
      Wave 41 baseline of ~919 send-queue overflows and a 1,000 -> ~31 cull, the
      server now logs ZERO overflows and holds all 1,000 at 20 Hz (~10 ms avg,
      Gen2 ~0). Residual client attrition under the extreme flood is downstream
      (the in-process bots can't drain ~1,000 inbound speech packets/s each;
      TCP backpressure), not the server send path. README updated.

[2026-05-31] - Cluster bot + measured the dense-crowd broadcast wall (Wave 41)
----------------------------------------

DIAGNOSTICS:
  - Added a Cluster bot behavior + cluster spawn (BotEngine packs the fleet
    around one city centre instead of scattering it across the city box) to
    reproduce the dense single-screen worst case. Use BOT <n> cluster:<city>.
    The bot speaks frequently: speech fans out to every bot in view, scaling
    ~N^2, and (unlike movement, which self-throttles because packed mobiles
    block each other) is not collision limited.

PERFORMANCE (finding):
  - 1,000 clustered, broadcasting bots saturate the per-client send queues. The
    server sheds load via send-queue-overflow disconnects rather than crashing
    or corrupting the wire: ~919 overflows, fleet culled 1,000 -> ~31 survivors,
    peak RSS ~1.8 GB then fell back, recovering to 20 Hz / ~0.4 ms. This is the
    practical ceiling for a single broadcasting screen and the known scaling
    wall. The change that would raise it: broadcast one shared serialized packet
    to all recipients instead of building a fresh packet per recipient. README
    updated with the measured wall.

[2026-05-31] - 1,000-client scaling test
----------------------------------------

PERFORMANCE:
  - Ran 1,000 live TCP bots across three scenarios (loopback). Connect: 1,000/
    1,000 in 25-30 s, zero failures, no disconnects/malformed packets.
      spread + low activity      : ~0.8 ms avg, Gen0 ~1/window, no blocking Gen2,
                                   ~1,750 pps out, 20 Hz
      active combat (+2,000 mob) : ~32-34 ms avg, p95 ~130 ms, p99 ~210-320 ms,
                                   ~19 Hz, ~1,950 pps out
  - Takeaway: cost tracks the active-combatant count, not the client count —
    1,000 spread players are nearly free, 1,000 all fighting at once sit at the
    50 ms budget edge. Forcing all 1,000 into one town did not reproduce the
    dense same-screen O(n^2) broadcast worst case (walker bots disperse and
    idle); that remains the known unmeasured scaling limit. README updated.

[2026-05-31] - Performance verification: combined allocation work (Waves 38-40)
----------------------------------------

PERFORMANCE:
  - Re-measured the combat scenario (2,000 hostile monsters + 300 combat players)
    over seven 30 s windows with all of the allocation work in place. Versus the
    pre-pooling baseline of the same scenario:
      avg tick    21-29 ms  -> 3.6-13.8 ms (climbs to a ~13 ms plateau as the
                               active-combatant set grows; 20 Hz held throughout)
      p95         85-119 ms -> 37-73 ms
      p99         127-167 ms -> 40-110 ms
      Gen0/window 226-255   -> 23-51
      Gen2/window 1-3       -> ~0 (the blocking pauses that drove tick jitter)
      alloc       160-178 MB/s -> 43-86 MB/s
    No exceptions, no disconnects, no malformed packets. README updated.

[2026-05-31] - Allocation-free pathfinder walkability check + benchmark (Wave 40)
----------------------------------------

PERFORMANCE:
  - A deterministic allocation micro-benchmark for FindPath (fixed start/goal on
    open terrain, GC.GetAllocatedBytesForCurrentThread over many iterations)
    revealed that the dominant pathfinding allocation was NOT the A* scratch
    collections pooled in Wave 38 but the per-neighbour walkability check:
    Pathfinder.IsWalkable used the yield-based GetCharsInRange/GetItemsInRange
    range queries, allocating an iterator chain for every explored neighbour.
    On a 120-tile path that was ~486 KB/call.
  - Added GameWorld.IsPathTileBlockedByObject — an allocation-free single-tile
    blocker check that iterates the owning sector's char/item lists by index,
    mirroring the exact filtering of the old range queries. IsWalkable now calls
    it. Measured: ~486 KB/call -> ~1.7 KB/call (the remaining bytes are the
    returned path List, which is the caller's result). The benchmark is kept as
    a regression guard (asserts < 8 KB/call).

[2026-05-31] - Pool outgoing packet buffers (Wave 39)
----------------------------------------

PERFORMANCE / NETWORK:
  - Every outgoing packet allocated a fresh byte[] backing array (PacketBuffer),
    the bulk of a packet's allocation. PacketBuffer now rents its backing array
    from ArrayPool<byte>.Shared and returns it once FlushOutput has consumed the
    bytes. This is safe because every outgoing PacketBuffer is built, queued and
    flushed exactly once — broadcasts build a fresh packet per recipient (they
    pass a PacketWriter, not a shared buffer), so no buffer is ever shared across
    send queues and no reference counting is needed. Buffers are also returned on
    the queue-overflow, not-in-use and disconnect paths so they never leak from
    the pool. Incoming/wrapped buffers (the byte[] constructor) are never pooled.
  - Growth (EnsureCapacity) stays within the pool: rent a larger array, copy,
    return the old one. Rent may hand back an oversized array; all readers use
    the logical Length (never _data.Length), so the slack is never observed.
  - Removes the per-packet byte[] allocation from the send hot path; the benefit
    scales with outbound packet volume (dense clusters / large battles). Verified
    correct under a sustained 300-bot run: 300/300 connected, 0 disconnects, no
    malformed/underrun packets — pooling does not corrupt the wire.

[2026-05-31] - Pool A* scratch collections; GC pressure telemetry (Wave 38)
----------------------------------------

PERFORMANCE:
  - Pathfinder allocated a fresh PriorityQueue, HashSet and two Dictionaries on
    every FindPath call (each growing to thousands of entries, with the
    resize/rehash churn that implies). Under combat — where many NPCs path
    toward targets each tick — this was the dominant per-tick allocation. The
    four scratch collections are now pooled per worker thread ([ThreadStatic],
    cleared per call); FindPath runs inside the parallel NPC-decision phase, so
    the pool must be thread-local. The returned path List is still allocated
    fresh (it is the caller's result).
  - Added [gc_stats] telemetry alongside [tick_stats]: per-window allocation
    rate, Gen0/1/2 collection deltas, GC pause %, and managed heap. (Allocation
    bytes include in-process stress bots, so treat alloc as a relative gauge;
    Gen2 count and pause % are the GC-stall signal.)

  Measured (2,000 hostile monsters + 300 combat players, 30 s windows):
    alloc ~160-178 MB/s -> ~63-103 MB/s; Gen0 ~226-255 -> ~43-91 per window;
    Gen1 ~45-53 -> 2-14; avg tick ~21-29 ms -> ~10-14 ms; p95 ~85-119 ms ->
    ~46-51 ms; p99 ~127-167 ms -> ~83-85 ms. Lower tick time as well as GC,
    since pooling also removes the per-call collection resize/rehash work.

[2026-05-31] - Hostile-monster stress mode + performance characterization (Wave 37)
----------------------------------------

DIAGNOSTICS:
  - The STRESS console command takes an optional third token (STRESS [items]
    [npcs] mob): generated NPCs get a Monster brain and negative karma so they
    read as red (notoriety 6) and drive server-side target acquisition and
    retaliation, turning the stress harness into a combat-load generator. The
    default (no token) keeps the previous inert population; the in-game .STRESS
    path is unchanged.

PERFORMANCE (measured: .NET 10 Release, Server GC, 20 Hz / 50 ms tick, live
TCP bots, real script + MUL install; 30 s windows):
  - 30,000 idle NPCs + 300 roaming players: steady ~3 ms avg tick (6% budget),
    p95 ~7 ms, p99 ~9 ms; full 20 Hz held. Sector sleeping keeps a large world
    nearly free.
  - 2,000 hostile monsters + 300 combat players: ~22 ms rising to ~40 ms avg as
    the active-combatant set grows (p50 ~1 ms, p95 90-130 ms); 20 Hz held early,
    ~16 Hz under sustained load. Cost is dominated by concurrent NPC-AI, not
    player count.
  - 30,000 hostile monsters + 300 players (ceiling): loop saturates
    (~600-800 ms ticks) but degrades gracefully — no crash, no multicore
    fallback. README performance section updated with these figures.

[2026-05-31] - Resolve CHARDEF CAN= flags via script DEFNAME tables (Wave 36)
----------------------------------------

SCRIPT:
  - Symbolic CAN= tokens (MT_RUN, MT_WALK, ...) are now resolved against the
    script's own [DEFNAME can_flags] table at parse time instead of a hardcoded
    map, so a server that redefines these constants is honored. The built-in
    MT_* map remains as a fallback for isolated parsing where no resolver is
    attached. ResourceHolder exposes TryResolveDefNameValue() for numeric
    defname constants and wires CharDef.DefNameResolver on construction.

[2026-05-31] - Fix symbolic CHARDEF CAN= flag parsing (Wave 35)
----------------------------------------

SCRIPT / NPC:
  - CHARDEF CAN= only accepted a numeric mask, so the standard symbolic form
    (CAN=MT_RUN|MT_WALK) failed to parse and left the creature with no movement
    capabilities at all (Can=None) — losing run/fly/swim/passwalls, which the
    pathfinder and tile-entry checks rely on. CAN= now parses the |-separated
    Sphere MT_* movement-type constants (the [DEFNAME can_flags] names) as well
    as a numeric mask. (Regression tests added; 527 passing.)

[2026-05-31] - NPC movement speed: Source-X DEX-scaled cadence (Wave 34)
----------------------------------------

NPC AI:
  - NPC step cadence now follows the Source-X formula (CCharNPCAct move-tick):
    it scales with DEX and the creature's MoveRate instead of a flat rate.
    Running (combat/chase) is ~250ms at high DEX up to ~1.6s at low DEX; walking
    (idle wander) ~1s to ~2.6s; clamped to [100ms, 5s]; pets run a little faster
    (DEX floored to 75). The previous flat 400ms made ordinary mid-DEX creatures
    chase noticeably faster than Source-X. Also fixes the MoveRate sense (higher
    MoveRate now means faster, as in Source-X, not slower).

[2026-05-31] - Cap NPC pathfinding node budget — the real crowd-stall fix (Wave 33)
----------------------------------------

PERFORMANCE:
  - Profiling pinned the crowded-combat tick stall to a single cause: A*
    pathfinding. The Pathfinder's node cap (20000, sized for player
    half-continent .walk) meant an NPC chasing a target it cannot reach through
    a press of bodies explored the full 20000 nodes — ~366ms PER call.
  - FindPath now takes an optional maxNodes; NPC combat-chase passes a far
    smaller budget (2000) — ample to route around local obstacles toward a
    target in sight, while bounding a blocked search to ~20ms. Player .walk
    keeps the full budget.
  - Measured on the pathological 50k-monster cluster around 8 fighting players:
    tick dropped from ~8.8s to ~0.13s typical (max ~0.5s) — the headline
    fix that the earlier budget/scan/throttle waves had been bounding around.
    525 tests pass.

[2026-05-31] - Throttle NPC A* recompute in churning crowds (Wave 32)
----------------------------------------

PERFORMANCE:
  - A* path recompute is now throttled per NPC (at most once per ~750ms). In a
    churning crowd the cached next step is invalidated almost every tick (a
    neighbour moves onto it), which previously forced a fresh A* search every
    tick. Between allowed recomputes the NPC follows its cached path or faces
    the target and holds. A dedicated throttle timestamp survives path-cache
    drops (and is swept for gone NPCs) so the limit actually binds.
  - Helps real crowded combat (e.g. a champion spawn where mobs jostle); the
    pathological 50k-clustered stress remains dominated by other per-NPC
    combat-brain cost that needs profiling to isolate further.

[2026-05-31] - NPC target-scan bounding in same-faction crowds (Wave 31)
----------------------------------------

PERFORMANCE:
  - FindBestTarget now caps the number of attackable candidates it evaluates
    (not just hostile ones). Wave 30's cap only counted positive-motivation
    targets, so a monster surrounded by its own kind still evaluated every
    neighbour before finding the few real enemies — O(local density) per NPC.
    It now considers its nearest handful; a target farther back in a crowd is a
    closer NPC's problem, so combat still engages.
  - GetAttackMotivation does the cheap hostility test before the FindRegion
    lookup, so non-hostile candidates (the bulk of a crowd) skip the region
    query.
  - Note: the residual cost of thousands of monsters densely clustered around
    players is pathfinding/movement in the crowd, which is inherent to that
    (artificial) density and unaffected here; normal play is unaffected.

[2026-05-31] - NPC AI tick-cost bounding under crowded combat (Wave 30)
----------------------------------------

PERFORMANCE:
  - Per-tick NPC budget: a mass sector activation (a player entering a densely
    populated area) could make thousands of NPCs due in one tick and run their
    AI serially, stalling the tick for several seconds. The tick now processes
    at most a bounded number of NPCs and defers the overflow to the next tick.
  - Bounded target acquisition: FindBestTarget previously did a line-of-sight
    raycast (against the real static map — the dominant cost) for every nearby
    mobile, making acquisition O(crowd) per NPC. It now ranks candidates by
    motivation without LOS, caps how many are evaluated, and verifies LOS lazily
    only on the top few — a few raycasts per NPC regardless of crowd size.
  - Net: a pathological stress (50,000 monsters clustered around 8 fighting
    players) dropped from ~8.8s ticks to ~2.2s; normal and realistic loads are
    unaffected. (525 tests pass.)

[2026-05-31] - Headless 'stress' console command (Wave 29)
----------------------------------------

ADMIN / DIAGNOSTICS:
  - Added a `stress [items] [npcs]` console command mirroring the in-game
    .STRESS GM speech command, so the load-test population can be generated
    from a headless server (CI / smoke). Routes to StressTestEngine.QueueGenerate.
  - Smoke-verified on the real script/map data: generating 8,000 NPCs took ~1s
    and the tick loop stayed healthy (avg ~0.6ms, 8,006 chars + 6 players) with
    zero errors. (Note: stress NPCs are neutral load dummies, so combat bots —
    which only engage hostile monsters — do not fight them.)

[2026-05-31] - Headless 'bot' console command (Wave 28)
----------------------------------------

ADMIN / DIAGNOSTICS:
  - Added a `bot <count> [walk|combat|idle|smart]` console command so the bot
    stress/smoke framework can be driven from a headless server (stdin / CI),
    not only via the in-game .BOT GM speech command. Routes to the same spawn
    path. Verified end-to-end: bots auto-create accounts, create characters,
    enter the world, and walk — with the movement validator correctly rejecting
    water/impassable steps and fast-walk against the real map data.

[2026-05-31] - .NET 10 migration, ServerGC, deterministic test suite (Wave 27)
----------------------------------------

RUNTIME / BUILD:
  - All SphereNet projects retargeted net9.0 -> net10.0 (LTS). Full solution
    builds clean; 525 tests pass under net10.
  - ServerGarbageCollection + ConcurrentGarbageCollection enabled on the Host
    entry point (the game Server already had them).

TESTS (flaky CI fix, no tests removed):
  - Root-caused the intermittent CI failures to process-wide mutable static
    hooks left set across tests (ObjBase/Item.ResolveWorld,
    Character.ResolveAccountForChar which drives PrivLevel, VendorEngine.World).
    Under xUnit's non-deterministic order a later test resolved against a stale
    hook (e.g. a player inheriting another test's GM account).
  - Added a BeforeAfterTest attribute that resets those hooks before/after every
    test, plus assembly-wide serialization. The suite is now green across
    repeated runs regardless of order.

[2026-05-31] - D-prefix fix extended to function calls (Wave 26)
----------------------------------------

SCRIPT ENGINE:
  - The D-prefix fix (Wave 24) now also covers D-prefixed FUNCTION calls: a
    [FUNCTION] whose name starts with 'd' (e.g. DamTypesOfString) was losing its
    leading 'd' and never resolving. The D-handler fallback now resolves the
    full token via the function resolver as well as the variable resolver.

TESTS:
  - ExpressionParser regression extended with a D-prefixed function call.

[2026-05-31] - No-space <Rnnn> random expression form (Wave 25)
----------------------------------------

SCRIPT ENGINE:
  - <Rnnn> (no space, e.g. <R999>) is now accepted as the random 0..nnn-1 form,
    matching the spaced <R nnn>. 'R' followed by a digit is random; 'R' followed
    by a letter (REF, REGION, ...) is left alone. Previously the no-space form
    was unresolved and read 0, so IF !(<R999>) was always true (the rare branch
    fired every time).

TESTS:
  - ExpressionParser regression test for the no-space <R5> form (spaced <R 5>
    still works).

[2026-05-31] - Bare <dispid>/<dex> expression resolution fix (Wave 24)
----------------------------------------

SCRIPT ENGINE:
  - The "D force-decimal" prefix was wrongly swallowing bare self-properties
    that start with 'd' (DISPID, DEX, DIR, ...): it stripped the leading 'd'
    and resolved the remainder (<dispid> -> <ispid> -> 0). It now tries the
    'd'-stripped remainder first (preserving the legitimate <DHITS>/<DLOCAL.HEX>
    decimal form) and, only if that is unresolved, resolves the full token as a
    property.
  - Added an Item <DISPID>/<ID> read property (reflects the DISPID override,
    defname-or-hex form like BASEID). There was no read before, so
    IF (<dispid> == i_xxx) weapon-type bonus dispatch and furniture open/close
    (DISPID toggle) did not work.

TESTS:
  - ExpressionParser regression test that <dispid>/<DEX> are not mangled while
    the <DHITS> decimal form is preserved (524 passing).

[2026-05-31] - Script intrinsics RESOURCETYPE / RESOURCEINDEX (Wave 23)
----------------------------------------

SCRIPT:
  - <RESOURCETYPE defname> and <RESOURCEINDEX defname> resolve a defname to its
    resource type code and index. The type uses the Source-X RES_* numbering
    (CharDef=6, ItemDef=14, Spawn=37, ...) so that
    <RESOURCETYPE <entry>> == <def.res_chardef> compares correctly. The worldgen
    spawner's NPC/item discrimination and staff restype menus rely on this.
    Resolved via ResolveDefName; an unknown defname returns 0 (RES_UNKNOWN).

[2026-05-31] - Region periodic triggers @RegPeriodic / @CliPeriodic (Wave 22)
----------------------------------------

REGION TRIGGERS:
  - @CliPeriodic fires periodically for every online player on their current
    region (SRC = the player). @RegPeriodic fires once per tick for each region
    holding at least one player (SRC = a representative player), mirroring
    Source-X CSector's iRegionPeriodic environ tick; uninhabited regions never
    tick. Region ambient sound/music scripts (ON=@RegPeriodic SRC.SOUND,
    ON=@CliPeriodic SRC.MUSIC) now run. Reuses the existing FireRegionEvents
    mechanism (same path as Enter/Leave/Step).

[2026-05-31] - NPC speech broadcast (SAY/SAYU/EMOTE) (Wave 21)
----------------------------------------

NPC SPEECH:
  - SAY / SAYU / SAYUC / EMOTE now BROADCAST as overhead speech to nearby
    clients instead of a private system message to the source (SAY = ASCII
    0x1C, SAYU/SAYUC = Unicode 0xAE, EMOTE = emote type). An optional
    @color,font,mode prefix is parsed for the hue. NPC dialog/bark lines were
    previously invisible because they only went to the source as a system
    message.

[2026-05-31] - NPC AI morale/kiting and vendor restock unification (Wave 20)
----------------------------------------

NPC AI:
  - Morale/fear (Source-X NPC_GetAttackContinueMotivation): the decision to
    flee is now based on RELATIVE strength instead of a fixed "HP < half"
    threshold — STR difference + relative HP% difference. A strong, healthy
    NPC presses a weak target even below half HP; a weak/hurt NPC flees an
    overwhelming one. Applied as a penalty only.
  - Ranged kiting (Source-X NPC_FightArchery): when a target closes inside a
    ranged NPC's minimum range, the NPC backs off (~50%) to reopen the gap
    instead of standing locked in melee unable to fire.
  - NpcAIFlags bit values aligned to the NPC_AI_* defines (0x20 gap kept), so
    a raw script NPC.AI integer maps to the correct bits in future.

VENDOR:
  - GM VENDORINV restock now writes to the virtual stock container (LAYER 26)
    that the buy gump reads from, not the regular backpack — so GM-defined
    stock both displays and stays excluded from the world save.
  - Restocked items stamp PRICE from the itemdef VALUE; the two restock paths
    (template SELL= and GM VENDORINV) now share the same virtual stock model.

TESTS:
  - Unit test for the morale penalty (weak flees / strong presses); vendor
    trade tests moved to a serial collection to avoid a shared-static race
    (523 passing).

[2026-05-29] - Vendor virtual stock and lazy NPC loot materialisation (Wave 19)
----------------------------------------

VENDOR STOCK (VIRTUAL):
  - The vendor stock container (LAYER 26/27) and its items are no longer
    written to the world save; they are rebuilt from the SELL template
    (VENDOR_SELL_LIST tag) when a player opens the vendor. This removes
    thousands of transient stock records from saves with many vendors.
  - Buying decrements the stock amount; a depleted entry is removed, and the
    container is repopulated from the template on the next open once empty.
  - Buy now validates the stock serial against the vendor's own stock
    container, preventing a crafted packet from "buying" an arbitrary world
    item by serial.
  - Stock price is stamped from the itemdef VALUE; an item with no PRICE tag
    now uses the same fallback price as the display instead of 0 (the server
    side previously rejected such items and broke the purchase).

NPC LOOT (LAZY):
  - NPC plain loot (non-wearable, non-ITEMNEWBIE chardef ITEM= entries) is no
    longer materialised onto the living NPC at spawn; it is rolled straight
    into the corpse at death (Source-X CTRIG_CreateLoot parity). Living NPCs
    carry no transient loot items in the world save.
  - Wearable gear (weapons/armour) is still equipped at spawn (combat /
    appearance) and reaches the corpse via the normal unequip path.

TESTS:
  - Regression tests for vendor virtual stock: decrement, depletion removal,
    non-stock serial rejection, over-stock amount rejection, price fallback
    (522 passing).

[2026-05-29] - NPC AI: Source-X parity fixes and smarter behaviors (Wave 18)
----------------------------------------

NPC AI - CORRECTNESS / PARITY:
  - Flee logic: a fearful NPC (negative motivation) now routes to ActFlee
    instead of silently dropping its target.
  - Multicore ApplyDecision is sorted by NpcUid before applying, removing the
    non-deterministic completion-order apply and matching single-thread behavior.
  - _losFailCounts leak: separate purge pass for NPCs that never built a path
    cache entry.
  - Multicore reschedule now checks IsDead (symmetry with single-thread), so a
    dead NPC is not re-queued into the timer wheel.
  - Self-kill (reactive-armor reflect): killer==victim is attributed as null
    (no Kill trigger on self).
  - Beneficial-spell targeting fix: ChooseBestSpell now returns the cast target,
    so heal/cure go to the right recipient. Previously every spell was cast on
    the enemy, so an NPC "self-heal" actually healed its enemy.

NPC AI - SMARTER BEHAVIORS:
  - Spell combo chain: Paralyze -> Explosion -> EnergyBolt -> Poison while the
    target is locked down (COMBO_STEP/COMBO_TARGET state).
  - Ally support: caster NPCs heal/cure a wounded nearby ally, not just self.
  - Hidden-target pursuit: when a target hides, the NPC moves to its last known
    spot and tries Reveal instead of giving up immediately.
  - ReacquireDelay: target-less NPCs throttle the full-range scan (reset on being
    attacked, so retaliation is never delayed).
  - Pet mode revert: after an attack target dies, the pet returns to its previous
    order (Guard/Follow) instead of trailing the master.
  - Threat targeting (Source-X NPC_AI_THREAT): NPCs prefer whoever has dealt
    them the most damage, driven by the attacker list (the previously-unused
    NpcAIFlags.Threat is now honored).
  - FightMode bias (FIGHTMODE tag): weakest / strongest / evil target preference.
  - Dynamic tick: badly-hurt NPCs act slightly slower.
  - Backoff: timid animals back off from the nearest threat to a safe distance.
  - DetectHidden: guards / high-INT NPCs periodically reveal hidden players.
  - Stuck -> Teleport: a stuck caster blinks toward the target instead of giving
    up.
  - Forced target: bard Provocation (PROVOKED_TARGET) and scripted ConstantFocus
    (CONSTANT_FOCUS) override normal target selection.
  - Scout retreat: creatures with Hiding vanish mid-flee to break pursuit.
  - Looting (Source-X NPC_AI_LOOTING): looter NPCs scavenge items from nearby
    corpses into their pack (LOOTING tag or global flag).
  - IntFood (Source-X NPC_Act_Food): hungry NPCs eat food from their pack or
    graze (INTFOOD tag or global flag).
  - Spellbook spell loading (Source-X NPC_AddSpellsFromBook): a caster NPC with
    no scripted spell list derives one from a carried/equipped magery spellbook.
    (Wand-holding NPCs already cast reagent-free via the wand path.)
  - Hireling wage (Source-X NPC_OnHirePay): a hired NPC (HIRE_WAGE tag) is paid
    from the master's bank box on a timer and deserts when it can't be paid;
    hirelings are exempt from hunger-based loyalty loss.

DOCS:
  - docs/ restructured: added ARCHITECTURE, STAFF_COMMANDS, TRIGGERS and a docs
    index; ADMIN_SECURITY folded into DEPLOY; stale internal notes removed.
  - README rewritten in English with README-TR.md (Turkish).

[2026-05-29] - Gameplay bug fixes and Source-X parity completion (Wave 17)
----------------------------------------

SCRIPT ENGINE:
  - Numeric brace-range {n m}: random-range and weighted {a w b w} selection in
    the expression parser; CharDef stat lines now roll between min/max.
  - FOR argument forms: FOR x (1..x), FOR min max, FOR var max, FOR var min max,
    with inclusive ranges, count-down, and a named loop variable.
  - MAX/MIN intrinsics, the @ power operator, and the numeric 3-way
    QVAL(v1,v2,lt,eq,gt) form.
  - ItemDef TDATA1/TDATA3 defname capture with lazy resolution.

COMBAT:
  - Era-0 hit-chance now factors in target DEX and a ranged-weapon adjustment.
  - Era-aware damage bonus is percentage-based; tactics/anatomy excluded in era-0.
  - Damage min>max guard prevents a Random.Next exception.
  - Weapon parry with a shield or a one/two-handed weapon.
  - Reactive-armor reflected damage is now attributed to the attacker.

MAGIC:
  - Spell-success scale fix: SKILLREQ is checked at the correct scale (high-skill
    casters no longer fizzle constantly).
  - Implemented CreateFood and DispelField; MindBlast now uses the INT-difference
    formula; EvalInt scales spell damage.
  - Cast-recovery cooldown plus a light skill-based faster-casting effect.
  - Field spells form a multi-tile wall and deal periodic tick damage to
    stationary targets.
  - Incognito disguises the real name and restores it on expiry.
  - Recall/Gate are blocked while frozen/jailed and against blocked destination
    tiles.
  - Magic-resist circle aligned with GetCircle().

SKILLS:
  - GMs no longer accrue skill/stat gains.
  - Taming difficulty rescaled to 0-100, so high-HP creatures are tameable.
  - Stealing fails without a backpack, preventing item loss and a free criminal
    flag.
  - Crafting: exceptional durability bonus, resource hue transfer, and randomized
    failure resource loss.
  - RemoveTrap springs the trap on failure; bandages gain an anatomy bonus and
    produce bloody bandages; Veterinary routes through Healing.
  - SkillEngine uses Random.Shared for multicore thread safety.
  - Gathering no longer yields more than a depleted node holds.

TRIGGERS:
  - @ReceiveItem / @NPCAcceptItem fire when giving an item to an NPC.
  - @Criminal (cancellable), @SeeCrime (nearby NPCs), @SpellInterrupt, @Hunger.

ITEMS:
  - Item CONT= / Z= / LAYER= setters for script-driven container moves.
  - DUPE verb; character CURE / REVEAL / ATTACK verbs; character ARMOR/AR read.
  - Crops/foliage harvesting, seed planting, and empty-pitcher filling from water.
  - Light sources: in-container ignite block and charge consumption; switch
    graphic swap; food/drink single-unit consumption.

WORLD:
  - Timed jail auto-release (DateTime-based, reboot-safe) plus expired-jail
    cleanup on login.
  - Party: proper disband notification and honoured invite-accept result.
    Guild: leave privilege check, candidate->master promotion, multi-guild block.
  - Housing: transfer cap, footprint passability, and IDOC lockdown/secure item
    preservation.
  - Zero-value creatures grant no fame/gold, preventing farming.

CHARACTER:
  - Poison ticks broadcast health/damage packets and attribute the kill.
  - HP regen halts while poisoned; starvation drains stamina/hits; FOOD is
    clamped; resurrect leaves at least 1 HP; pet control-slot summation and
    loyalty warnings.
  - Movement shove now costs stamina exactly once (fixing the double-deduction /
    false-block bug).

TESTS:
  - Regression tests for brace-range, MAX/MIN/@, and QVAL (515 passing).
